# Copy to deploy/.env.deploy.local (gitignored). Never commit real values.

# Cloudflare Pages (website + mobile)
CLOUDFLARE_API_TOKEN=

# Production SSH (admin deploy + optional mobile CORS patch)
DEPLOY_SSH_HOST=
DEPLOY_SSH_USER=
DEPLOY_SSH_KEY_PATH=
DEPLOY_SSH_PORT=22
