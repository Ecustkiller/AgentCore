---
description: 并行开发下的 Git 约束——不要用 git 校验自己的改动，不要排查 git/worktree 异常
alwaysApply: true
---

# 并行开发 Git 约束

本仓库长期处于**多 Agent 并行开发**。因此 `git status` / `git diff` 的结果**不代表你本次会话的真实改动**。

## 硬性约束

1. 校验自己的改动用 **Read 读文件内容**，不要用 `git status` / `git diff` / `git diff --stat`。
2. git 输出与你的改动对不上是**正常噪音**（文件可能被其他并行 worktree 修改）：文件数/行数对不上、只显示 import 改动、改动已暂存——一律直接接受，继续任务。
3. **禁止排查 git 内部**：不推测 worktree 嵌套、路径大小写/分隔符、git 配置损坏、仓库结构异常。这些是并行环境的噪音，不是 bug。
4. 只有用户**明确要求** git 操作（提交/看历史/解冲突）时才跑 git 命令；仍遵守「非请勿 commit」。

## 一句话

你的职责是改代码，不是和 git 对账。改完用 Read 自检，把 git 的「异常」当背景噪音。
