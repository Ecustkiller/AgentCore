---
description: AI 开发工作流——角色分工、决策权限、设计纪律、编辑安全
alwaysApply: true
---

# 开发工作流

项目采用「AI 全程开发、人做决策」模式。多 Agent 并行开发（不同模块可同时推进）。

## 角色与决策权限

| 角色 | 职责 |
|---|---|
| 人（产品负责人） | 需求定义、优先级、方案抉择、验收 |
| AI（开发者） | 方案设计、编码、测试、自检修复、文档同步 |

| AI 自主决定 | AI 提案 → 人确认 | 人直接决定 |
|---|---|---|
| 实现细节、Bug 修复（不改契约）、测试/重构 | 架构选型、新依赖、设计文档修改 | 功能范围、需求变更、发布决策 |

遇到需人决策的节点，立即暂停并清晰描述问题和备选方案。

## 设计纪律

- 正向推导：**用户需求 → 产品设计 → 技术实现**。禁止用「技术方便」反推需求。
- 动手前先答三问：**谁用 / 解决什么真实问题 / 产品上如何呈现**。
- 出现「为复用旧实现而裁剪需求」「实现冒充需求」→ 立即停，回到三问或提报决策。
- **开发期无真实数据**：项目当前没有任何真实运行数据（真实用户 / 对话 / 指标 / eval 跑分 / 质量分都不存在）。一旦要基于「真实运行数据」讨论或优化质量 → **先声明「目前无真实数据，以下为假设」再展开**；宜改以 `conformance/vectors/`、`evals/` 合成样本为依据，别把不存在的真实数据当成既有事实。

## 补丁绊线

修复满足任一条件，**先停**、提根因重设计提案：
1. 需新增兜底/对账/自愈/特例类逻辑；2. 同一根因改 ≥2 层；3. 同一接缝第 3 次打补丁。

## 开发准则

- 生产级代码：完整错误处理、类型标注、合理日志
- 优先写接口定义（Protocol / ABC / Pydantic），再实现
- 模块间通过抽象接口解耦；共享类型放 `shared/` 或 `core/types/`
- 核心逻辑必须有单元测试
- 子代理（Task tool）必须指定 `composer-2.5` 模型

## 文件编辑安全

- **禁止用脚本批量改源码**（PowerShell/Bash 文本替换易静默损坏文件）
- 用 StrReplace + `replace_all` 逐文件改，每步可验证
- 改完用 **Read 读文件**自检（不应出现大量删除/清空）

文档治理规范见 `doc-governance.mdc`。
