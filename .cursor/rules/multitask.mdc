---
description: Multitask 模式下主 Agent 与子代理的分工、委派规范、模型约束
alwaysApply: true
---

# Multitask 委派规范

产品 CEO 路由权威 → [编排器 · 信息判据](/docs/03-AI核心/编排器与CEO主Agent.md#信息判据)。本文只管 Cursor 主 Agent / Task。

## 模型约束

子代理（Task tool）**必须**指定 `model: "cursor-grok-4.6-xhigh"`，无例外；`resume` 与同任务线再起同样每次显式传。工具说明（resume 勿传 model）与实测不符：不传会静默回落主模型。本规则即用户指定。hook 会兜底，显式传仍是第一道防线。

## 信息判据

主 Agent 窗跟会话走（进窗后每轮都在）；子代理窗跟这次 Task 走。收益只认墙钟（弱依赖并行）和注意力（容量 / 不许见面 / 不把大段过程留在主窗）。人数不是理由。

| 顺序 | 否 → |
|---|---|
| 切出去判断还成立吗？ | 自己做 |
| 换墙钟还是注意力？ | 都没有 → 自己做 |
| 目标·约束·验收 + 审查仍小于收益？ | 自己做 |

自己做（切不出去或编码≈做完）：调查与修复强耦合；纯讨论；文档治理；单文件小改（约 ≤30 行）。软禁：未定案 / 整锅写不清单一目标+边界+验收 / 把想清楚交给子代理。定案摘要后可对清晰块委派。窄例外：只读探事实。

## 分工

主 Agent 做决策，子代理自主实施。prompt 只写决策层：目标 + 约束 + 验收。简单 <50 / 中等 <200 / 复杂 <500 token。禁止贴代码、逐条改法、Step 1/2/3。

## 委派

探索条数自主。能切开的独立切片必须并行（同一消息多 Task）；单实施须写明切不出去的原因。完成后主审查。

子代理：执行层自解；方案层立即停下。禁嵌套 Task；禁子代理选架构。

## 探索与真跑

探索结论照信，禁重探已覆盖面。承重结论（驱动改动或转述给人）先点开它引的行。真跑 / 端到端 / 已验证不照信——核原始证据（配方 → `verify-scope.mdc`）。禁止跨端文案编出行号；禁止把跑错场景报成通过。

## 讨论 + 审计分相

讨论默认审计 **HEAD 与 docs**，不是生产窗。宽窗日志审计仅当用户明说巡检 / reviews：主只对齐；审计派只读子代理；禁重探同一证据面。用户贴单条 trace/cid → 主按 `conversation-logs.mdc` 自跑（不套分相）。

## 禁止项

- 禁主 Agent 代写实现：prompt 不贴代码、不写逐步改法
- 禁未定案/整锅委派；禁把可拆无交叉块捆成单 Task
- **禁用 AwaitShell 等子代理**：完成通知只在回合干净结束后投递且可能滞后，中途崩溃则永不投递。启动后并行干活或直接结束回合；等不到通知时读 `agent-transcripts/{会话id}/subagents/*.jsonl`
