---
description: Multitask 模式下主 Agent 与子代理的分工、委派规范、模型约束
alwaysApply: true
---

# Multitask 委派规范

## 模型约束

子代理（Task tool）**必须**指定 `model: "cursor-grok-4.5-high-fast"`（Cursor Grok 4.5 high fast）。无例外。

**续跑 / 再起同一子代理时同样强制指定**：无论是 `resume` 续跑，还是同一任务线后续再起新 Task，每次调用都显式传同一 `model`，禁止漏写、禁止换模型。

## 核心理念

**主 Agent 做决策，子代理自主实施。**

| 主 Agent (高级模型) | 子代理 (Grok 4.5 high fast) |
|---|---|
| 理解需求、做方案决策 | 自行读代码、定位文件、实施改动 |
| 给出目标和约束 | 自主探索实现路径 |
| 审查结果、决定后续 | 回报结果或遇到方案层问题时停下 |

**禁止**：主 Agent 在 prompt 中写完整实现草稿、逐步 StrReplace 指令、大段代码块。子代理有能力自己读代码和决定改法。

## 委派 prompt 规范

prompt 只写**决策层信息**，不写实施细节：

- **目标**：一句话说清要达到什么效果
- **决策/约束**（如有）：架构决策、接口契约、不可变更的边界
- **验收**（如有）：怎么算完成（跑什么测试、检查什么行为）

**禁止出现**：完整代码块、逐条 StrReplace 指令、逐文件改法说明。如果你发现自己在写「Step 1 / Step 2 / Step 3」或粘贴代码 → 停下，删掉，只留目标和约束。

### 按复杂度分级

| 复杂度 | prompt 内容 | 示例 |
|---|---|---|
| 简单 | 目标一句话（<50 token） | 「把 X 组件的 margin-top 从 8px 改成 12px」 |
| 中等 | 目标 + 涉及模块 + 关键约束（<200 token） | 「删除三档路由，合并为两档。涉及 prompt.py 和两份设计文档。保留现有测试覆盖」 |
| 复杂 | 目标 + 架构决策 + 边界约束 + 验收标准（<500 token） | 不写改法，只写「为什么这样做」和「不能动什么」 |

## 主 Agent 自做（不委派）

满足任一条件，主 Agent 直接做，不启动子代理：

- 单文件 ≤30 行改动
- 调查与修复强耦合（需边读边判断边改）
- 文档治理（规则文件、设计文档更新）
- 纯讨论/方案对齐（不产出代码）

## 探索信任原则

探索子代理回报结果后，主 Agent **直接基于结论决策**：

- **禁止**再 Read/Grep 探索子代理已覆盖的同一批文件
- 只在子代理明确报告「某部分未覆盖」或结论存疑时才补充探索
- 探索与实现可合并到同一子代理（让子代理先探索再实施）

## 并行委派

默认**单子代理**完成整个任务。只在以下条件满足时才并行：

- 前端 + 后端**各自独立**的大块工作
- 用户一次提了**多个明确无关**的任务
- 工作量大到单子代理耗时过长，且文件无交叉

### 约束

- 同时在飞子代理 ≤ 4 个
- 并行子代理的 prompt 须标明文件互不交叉
- 全部完成后，主 Agent 统一审查
- **要立即消费报告的委派优先前台跑**（不设 `run_in_background`；并行 = 同一消息发多个 Task）——结果同回合内联返回，不走不可靠的通知信道。后台仅用于 fire-and-forget，或 Multitask 模式强制后台时（此时按禁止项的转录自救路径收报告）

## 子代理自主度

| 情况 | 子代理行为 |
|---|---|
| 执行层问题（路径、import、lint、测试修复） | 自行解决，不用回报 |
| 需要多改一个文件或小范围调整方案 | 自行决定并执行，完成后说明 |
| 方案层问题（方案不可行、需改接口契约、架构决策） | **立即停下回报**，不自行决策 |

## 禁止项

- **禁止子代理嵌套子代理**：层级只允许 主Agent → 子代理
- **禁止子代理做架构/方案选择**：遇岔路口回报主 Agent
- **禁止主 Agent 代写实现**：prompt 中不贴代码块、不写逐步改法
- **禁止用 AwaitShell 等子代理**：完成通知只在回合**干净结束**后投递，且可能成批滞后数分钟以上；AwaitShell 阻塞期间、工具调用间隙、被中止的回合一律收不到，子代理中途崩溃（账单/认证错误）则永远不发通知（多场会话实证：干等失联、要用户手动解围）。启动后并行干活或直接结束回合；等不到通知或用户说已完成时，直接读 `agent-transcripts/{会话id}/subagents/*.jsonl` 收报告
