---
description: Multitask 模式下主 Agent 与子代理的分工、委派规范、模型约束
alwaysApply: true
---

# Multitask 委派规范

## 模型约束

子代理（Task tool）**必须**指定 `model: "composer-2.5"`。无例外。

## 角色分工

| 主 Agent (高级模型) | 子代理 (composer-2.5) |
|---|---|
| 理解需求、拆解任务、设计方案 | 按指令执行具体编码/测试 |
| 读代码、定位文件、确定改法 | StrReplace / Write / Shell 操作 |
| 审查子代理结果、决定后续 | 回报结果或异常 |
| 做方案选择 | **不做方案选择** |

一句话：**主 Agent 规划，子代理执行。**

## 委派前必做

下发任务前，主 Agent 须根据复杂度完成相应规划：

- **简单任务**：定位到模块 + 描述目标 + 给出参考模式
- **中等任务**：定位到具体文件 + 描述改法 + 列出涉及的接口/类型
- **复杂任务**：定位到具体文件和函数 + 逐步改法 + 验收标准 + 边界约束

## 委派模板

每次下发子代理任务，prompt 须包含：

1. **目标**：一句话说清要做什么
2. **涉及文件**：列出需要读/改的文件路径
3. **具体做法**：怎么改（越具体越好，composer-2.5 不擅长自主探索）
4. **验收标准**：怎么算完成（跑什么测试、检查什么行为）
5. **禁止项**（如有）：不要动哪些文件、不要改哪些接口

## 子代理自主度 — 三档

| 情况 | 子代理行为 |
|---|---|
| 琐碎障碍（路径拼写、import 缺失、lint 报错） | 自行修复，不用回报 |
| 执行层问题（测试挂了、需要多改一个文件） | 尝试修一轮；修不好就停下回报失败原因 |
| 方案层问题（方案不可行、需改接口契约、涉及架构决策） | **立即停下回报**，不自行决策 |

## 禁止项

- **禁止子代理嵌套子代理**：层级只允许 主Agent → 子代理，到此为止
- **禁止子代理做方案选择**：遇到"选 A 还是选 B"的岔路口，一律回报主 Agent
- **禁止不带规划就委派**：不允许把用户原始需求直接转发给子代理
