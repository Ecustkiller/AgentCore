---
description: AgentCore 项目术语表——讨论产品、架构时确保术语一致
alwaysApply: false
---

# 术语表

**三层分界（不可违反）**：

- **Agent** = 有独立 LLM 推理的参与者，占轮次
- **Skill** = Agent 的内在能力（触发条件/指导/约束/输出格式），绑定到 Agent，Prompt 组装时注入；非运行参与者、不占轮次
- **Tool** = 确定性原子能力，给参数固定执行返回结果，不调 AI

Agent 与 Tool 概念不相交。不承认「会思考的工具」中间形态。
Skill 是可绑定、可市场化的独立实体（经绑定对 Agent 生效），不是某个 Agent 版本的内联配置。
