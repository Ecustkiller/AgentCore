---
description: 硬/软拦截纪律——禁默认加闸；误伤优先；提拦截须走人确认
alwaysApply: true
---

# 拦截纪律（硬闸 · 软闸）

产品 AI「不听话」时，**禁止默认提案**加硬拒 / 软警告 / 扫自由文。拦截极易误伤；软闸也不免费（累计只读软提醒曾 A/B 净负已撤）。

现状与否决见 [编排器 · 失败与否决](/docs/03-AI核心/编排器与CEO主Agent.md)；安全熔断诚实边界见 [安全权限与治理](/docs/05-平台与运维/安全权限与治理.md)。

## 阶梯（必须按序；禁止跳级）

1. **提示词 / Skill / 结构字段**（playbook、`deliverable`；✅ S3 已删 `completion_criteria` kind，见 [编排器 · S3](/docs/03-AI核心/编排器与CEO主Agent.md)）
2. **观测 + 人审**（日志、审批卡、`escalate`）——先看见再谈拦
3. **一次性软提示**（可忽略、不累计、不改成功路径、不扫用户长文猜意图）。✅ `consumer_deps` 漏边软警告已进 CEO 可见委派结果 → [编排器](/docs/03-AI核心/编排器与CEO主Agent.md)
4. **硬拒**——仅同时满足：条件**结构化可证明**；误伤面可枚举且可接受；不拦会破契约 / 安全底线 / 明确能力缺失

## 硬闸白名单（仅此类可提案，仍须人确认）

- **结构契约**：schema / form / 写盘 scope、合同字段（✅ S3 已删 criteria kind 互斥；禁借机加替代启发式）
- **能力缺失**：无执行环境却要外环验证 / 长驻就绪等（表述跟工具/契约，**不**跟已删 kind）
- **灾难安全**：熔断级毁灭性动作、敏感读；文案须诚实「启发式兜底，并非完整拦截」。✅ 方案 C：`host_shell` fuse 已覆盖的毁灭形 → breaker 直接 `DENY`（禁再提 B 可批可跑 / D 仅改文案当终案）→ [安全权限 · 熔断](/docs/05-平台与运维/安全权限与治理.md)

## 默认否决（提了也先当否决项写明）

- 用硬/软闸「优化模型服从度」「防吹牛」「质量启发式」
- **意图分类器**：扫用户原文 / task·角色自由文猜意图再分叉（短允许表点名刷新除外，已定案）。✅ 已删角色名正则静默改写 deliverable → [编排器](/docs/03-AI核心/编排器与CEO主Agent.md)
- 累计计数软提醒、与硬闸同条件「先软再硬」叠床架屋（成篇审计等**已有意递进**除外，禁止新开同类）
- 扩大收口/书目/完成话术正则面冒充「近零误报」；✅ 已收窄撤回姿势 A 的 20260803 扩面（复核/审查/验收通过、已修复、可玩等）——见 [执行引擎 · 可用性诚实性](/docs/03-AI核心/执行引擎架构设计.md)；禁借「漏拦」再扩面
- ✅ S3 已落地：禁边删 `completion_criteria` kind 边加新启发式完成硬闸；禁把「验码绿」等再伪装成新 kind / 领域 kind 扩表（S2a/b）

## 提建议时的强制格式

若仍要提拦截：单列 **误伤面**、**为何阶梯 1–2 不够**、**软是否可能净负**；标「需人确认」。未写这三项 = 无效提案。与 `dev-process.mdc` 补丁绊线对齐：拦截类补丁触绊线 → 停、提根因重设计，不直接落地。
