---
description: 改产品 AI 提示词——内容闸（可复现；挡强吸引子；不服务更好）；宪法非法例；身份≠产品事实；入场闸只选一层；禁字面测试；瘦身不换闸
alwaysApply: true
---

# 提示词改动

产品 AI 提示词（CEO 核 / 基座 / 队员身份 / 辩论 / Skill / 工具 schema / 事实层 / 场面门 / 引擎当场文案 / 官方出厂模板）**先读再改**：[上下文工程 · 提示词设计原则](/docs/03-AI核心/上下文工程.md#提示词设计原则)。内容闸、入场闸、一层一所有者、否决表权威在原则文，此处不重抄。不覆盖用户规则 / 用户上架正文。

- 加一行先过[内容闸](/docs/03-AI核心/上下文工程.md#内容闸可复现不服务更好)（产品合同或强吸引子原则两问都不成立 → 不写），再选一个落点 → 原则文[新事故入场闸](/docs/03-AI核心/上下文工程.md#新事故入场闸)；加禁止句先定层再过[补集测试](/docs/03-AI核心/上下文工程.md#禁止句何时才该出现)。审议已有一句是否留在常驻 → 同一闸方向相反：先过内容闸，再过「每回合都要用的身份？」；产品事实即使在 `<身份>` 也出核。
- 一层一所有者：同一条纪律在提示串里只出现一次。标签 = 决策时刻（身份 / 输入 / 工作权威 / 诚实 / 输出），权威在原则文。基座不写 `<身份>`，只写 CEO 与 worker 同真的句子；工种不对称进角色 `<身份>` 或节点合同。HOW 默认在 `consult` 正文；写参当轮必见的合同在该工具 description / 参数（现：`delegate` 编制、`ask_user` 填卡）；when-to-use 在工具 description；目录行只写这是什么；闸在代码不进提示词。`HOW→consult(name)` 的 name 必须能拉到手册（系统 Skill，或 host / browser 面）；禁止空转自指；consult 赶不上这一次填参 → 上收进按钮，不留会被跳过的 skill。
- 禁止项：质量菜谱（文风课 / 设计系统 / 论证强度，正向也不写出厂；挡预训练默认脸的原则除外） / 补集禁止 / 判例入常驻 / 层放错 / 产品事实冒充身份 / 重复抄写 / 瘦身换闸 / 测试钉散文 → 判据在原则文[否决](/docs/03-AI核心/上下文工程.md#否决)。
- 加闸 → `intercept-discipline.mdc`，禁止用拦截补短提示。
- 注入字面出门过 [现行信息](/docs/01-产品/现行信息.md)：不改变以后行动的句子不留；废名连删；测试删字面断言，不改成缺席坟碑。禁止靠点名废字段「禁复活」。
- 测试钉键/层/标签，不钉现行教学句。一条契约一个测试所有者；影子因同一旧词失败 → 删影子，不要跟着改字。禁止为改措辞再加「新句必须出现」。细则 → 原则文[测试守卫原则](/docs/03-AI核心/上下文工程.md#测试守卫原则不守卫字面)。

体积：`tests/test_ceo_core_ratchet.py`（基座+核）、`tests/test_tool_schema_size_ratchet.py`（工具面）默认只降不升；`test_prompt.py` 守结构 / 分层唯一性，不守教学字面、不写废名坟碑。