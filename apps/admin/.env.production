# 生产构建烘焙的后端地址。Vite 在 production 构建期把 import.meta.env.VITE_API_URL
# 静态替换进产物。admin 打**自己域**下的 /api（cookie 隔离，见 deploy/nginx/office-admin.conf
# 顶部注释），刻意不与桌面/web 客户端共用产品域；部署脚本可经
# AGENTCORE_OFFICE_API_URL / VITE_API_URL 覆盖。非密钥、可入仓。
VITE_API_URL=https://office.fashitianxia.xyz/api
