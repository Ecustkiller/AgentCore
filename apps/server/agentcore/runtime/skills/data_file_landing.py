"""Skill body: data_file_landing."""

from __future__ import annotations

_DATA_FILE_LANDING = """\
<data_file_landing>
## 数据文件 → 可打开产物

用户丢账单 / 报表 / 导出记录 / 凭证等数据文件（PDF、CSV、xlsx、HTML 表、文本导出）+ 一句话，\
要一份能直接打开的整理产物时走本页：【单 worker 线性】一人做完。【禁止】套多角色 DAG、为拆表另开流水线。

【有执行】对照 `<workspace_context>`。`code_execute=已装配`：CEO 派 `form=files`，\
`artifacts` 写死产物路径，task 点源文件。
六步（按序；禁止跳到「对着抽出文本手抄」）：
1. **看原件**：读工作区原文件。PDF/Office 预解析 `.md` / `file_read` 抽出文本只是线索，\
【不是】表结构真源。xlsx/csv 须 `code_execute` 按相对路径读，不要指望 `file_read` 抽全表。\
PDF 在执行环境用已预装库自己抽：有框线格子表用抽表专用库（按页抽文本会拆行）；\
文本流排版以文本层为真值，抽表空表≠失败。【禁止】对着预解析残文硬凑。\
已预装库以本回合 `code_execute` 工具描述为准，勿另猜库名。
2. **认形态**：原件真实结构（列对齐、分页页眉、合并格、分隔符、编码）。\
预解析把一行拆成多行、页眉混进数据、列对不齐 → 换抽取（执行环境重抽；文本流改抽文本层），\
【禁止】顺着预解析拆乱文本继续。
3. **换抽取 + 写一次性变换脚本**：按认清的形态抽，再按用户口径变换（分表 / 分栏 / 汇总）。\
脚本落盘后用 `code_execute` 跑。【禁止】让模型逐行手抄 CSV / 单元格进 `file_write`。
4. **执行**：跑通；抽取仍乱 → 改脚本再跑，不要退回手抄。
5. **不变量校验**（不过闸不交付）：分类笔数之和 = 源记录总数；有金额则核对合计；抽样几行对照原件。\
用脚本算，不要心算凑。失败则修变换再跑；【禁止】改口径数字去凑、禁止无校验交差。
6. **产出可打开文件**：真目标后缀写入工作区（有执行且 `.xlsx=可产` → 交真 \
`.xlsx`）。handoff 指向该路径，用户能直接打开 / 预览。【禁止】只交脚本或说明冒充已可打开的表。

【口径】用户只给粗分法时，可自决更合理的细类（例如把退款从收支里拆出），把规则写进产物汇总页。\
末尾给改口入口（再说一声即可重跑脚本）。【禁止】拿交付当人质——先交文件，再谈口径。

【无执行】`code_execute=未装配`：本回合的完整交付不是表格，而是三件套——凑齐即正常完成，不是缺口。\
CEO 派 `form=files`，`artifacts` 写死下面两份路径，task 点源文件：
1. **原件结构报告**（落盘）：只写本回合已经给出的事实。表格类附件用结构面（列名、总行数、每列推断类型、样例行）；\
PDF/抽出文本只写抽出里能看见的形态（分页、拆行、列对不齐也照实写）。【禁止】凭空补列、补行、补金额。
2. **待跑变换脚本**（落盘）：按认清的形态写好一次性脚本，读工作区原件、按用户口径变换、做不变量校验；本回合不跑。\
【禁止】把抽出文本或样例逐行手抄进 csv/xlsx 交差。
3. **一句人话下一步**（正文与 handoff.next_steps）：运算环境暂时不可用，稍后再试即可。\
【禁止】让用户绑本机文件夹、改工作区形态、导入到云、连接 Git、在本机终端跑脚本。
`.xlsx=不可产` 时不要改手抄 csv 顶替表格。【禁止】谎称已生成 / 已校验。
</data_file_landing>"""
