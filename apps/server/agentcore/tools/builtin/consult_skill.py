"""consult_skill — removed in 步 1 (按需三合一).

Use :class:`~agentcore.tools.builtin.consult.ConsultTool` (``consult``) instead.
This module is kept only so stale imports fail loudly with a clear pointer.
"""

from __future__ import annotations

raise ImportError(
    "consult_skill was merged into consult (步 1 · 按需三合一). "
    "Import agentcore.tools.builtin.consult.ConsultTool instead."
)
