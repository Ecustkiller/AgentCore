"""DEPRECATED: DeferredWorkspace removed in Folder refactor.

Per-conversation scratch space replaces the lazy-promote pattern.
This file is retained as a tombstone; imports from here will fail loudly.
"""

raise ImportError("DeferredWorkspace was removed in the Folder refactor — use build_workspace directly")
