# 国内本地 electron-builder 构建加速（CI 走 GitHub Actions 直连，无需此配置）
electron_mirror=https://npmmirror.com/mirrors/electron/
electron_builder_binaries_mirror=https://npmmirror.com/mirrors/electron-builder-binaries/

# 未签名 MVP 构建：PowerShell 会话另设 $env:CSC_IDENTITY_AUTO_DISCOVERY="false"
# 可避免 winCodeSign 下载超时（仅影响本地打包，CI 无需）
