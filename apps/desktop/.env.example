# 桌面端连接的后端地址（与后端端口一致，默认 8000）。
# 这是开发期默认值；生产构建走 .env.production（已烘焙 https://app.fashitianxia.xyz/api），
# 无需在此改。本机临时覆盖用 .env.local / .env.production.local（均被 gitignore）。
VITE_API_URL=http://localhost:8000

# 开发期自动登录（可选，仅 DEV 构建生效，生产构建会被静态剔除）。
# 配好后启动桌面端会走真实 /auth/login 流程自动登录，免去每次手输；
# 不绕过后端认证，只是自动化一次正常登录。
# 用法：先在后端执行 `uv run python scripts/seed_dev_user.py` 创建同名用户，
# 再把真实值写进 apps/desktop/.env.local（已被 gitignore，勿提交）。
# VITE_DEV_USERNAME=dev
# VITE_DEV_PASSWORD=devpassword

# 本地引擎（sidecar）（双模式工作区 / 远期规划 §一.1）。
# 开关已移到应用内：设置 → 模型配置 →「本地引擎」（默认关，持久化在 localStorage）。开启后，
# 绑定了「本机存在的本地文件夹」的对话，回合改由本机 `python -m agentcore.sidecar` 跑（引擎在
# 本地、文件/代码直接碰真实盘），而非云端遥控桌面；裸聊 / 云端文件夹 / 带附件的回合仍走云链路。
# 主进程默认用 apps/server/.venv 的 python 拉起 sidecar；若服务端目录不在 ../server，
# 用 AGENTCORE_SERVER_DIR（主进程环境变量，非 VITE_）指定，或用 AGENTCORE_SIDECAR_CMD 覆写命令。

# AgentTown 独立客户端路径（可选）。开发期默认找 apps/town/Builds/Windows/AgentTown.exe
#（先跑仓库根目录 pnpm town:build）；找不到时可设 AGENTTOWN_PATH 指向 exe 或所在目录。
# AGENTTOWN_PATH=
