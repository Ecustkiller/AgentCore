# 生产构建烘焙的后端地址（P2-6）。Vite 在 production 构建期把 import.meta.env.VITE_API_URL
# 静态替换进 renderer 产物，故打包出的安装包默认连生产 API（经 Nginx 反代 /api/ → :8000，
# 见 docs/05-平台与运维/部署与运维.md §二）。非密钥、可入仓。
# 本机临时覆盖用 apps/desktop/.env.production.local（被 .gitignore 忽略）。
VITE_API_URL=https://app.fashitianxia.xyz/api
