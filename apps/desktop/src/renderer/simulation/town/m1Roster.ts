/** @deprecated Use `townRoster` — kept for M1 call sites during M2 migration. */
export {
  TOWN_AGENT_HOME as M1_AGENT_HOME,
  TOWN_AGENT_IDS as M1_AGENT_IDS,
  TOWN_AGENT_NAMES as M1_AGENT_NAMES,
  TOWN_SPAWN_OFFSET as M1_SPAWN_OFFSET,
  type TownAgentId as M1AgentId,
} from "./townRoster";
