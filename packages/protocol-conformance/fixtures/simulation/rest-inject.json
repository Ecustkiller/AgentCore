{
  "name": "simulation-rest-inject",
  "description": "REST shape: POST /v1/simulation/runs/{id}/inject → InjectSimulationEventResponse (202). The event is queued for the next tick.",
  "endpoint": "POST /v1/simulation/runs/{run_id}/inject",
  "status": 202,
  "response": {
    "run_id": "run-conformance",
    "event_id": "wx-inject-1",
    "event_type": "announcement",
    "title": "镇长通告",
    "queued_for_tick": 4
  }
}
