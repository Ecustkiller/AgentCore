# 前端技术选型与 UI 设计方案

> **状态**：已确定

---

## 一、技术栈

| 类别 | 选择 | 版本 | 理由 |
|------|------|------|------|
| 构建工具 | electron-vite | latest | HMR 3-5x 快于 Webpack，2026 年 Electron 项目事实标准 |
| 打包分发 | electron-builder | latest | 支持 NSIS/DMG/AppImage，自动更新成熟 |
| UI 框架 | React | 19.x | 生态最大，与 AG-UI 语义对齐 |
| 状态管理 | **Zustand** | 5.x | 1.2KB，零样板，selector 精确订阅 |
| 服务端状态 | ⏳ TanStack Query | 5.x | 缓存、重试、乐观更新（**未引入**，走 Zustand + fetch） |
| 样式 | Tailwind CSS | 4.x | 零运行时、AI 友好、开发速度快 |
| 图标 | lucide-react | latest | 轻量 SVG 图标，与 Tailwind 自写组件配合 |
| UI 组件库 | ⏳ shadcn/ui | — | Radix + Tailwind，源码可控（**未引入**；当前为 Tailwind 自写组件） |
| 流程图 | @xyflow/react (React Flow) | 12.x | 任务依赖图可视化，7M 周下载 |
| 自动布局 | ELK.js | latest | 支持层次/树形布局，配合 React Flow |
| IPC 通信 | 类型安全 contextBridge | - | 共享类型定义，编译期校验 |
| 测试 | Vitest + Playwright | 3.x | 单元 + E2E |
| 代码质量 | Biome | 1.x | lint + format，比 ESLint+Prettier 快 30x |

---

## 二、状态管理策略

### 统一 Zustand

所有客户端状态统一使用 Zustand 管理。通过正确的 selector 模式实现精确订阅。

实际 Store 划分与类型以 [`核心接口定义.md` §7.1](/docs/02-架构/核心接口定义.md) 为准（`useConversationStore` / `useExecutionStore` / `useGraphStore` / `useUIStore` / `useSidebarStore` / `useFoldersStore` / `useUserStore`）。⏳ 服务端数据（会话历史、模板列表等）**未引入 TanStack Query**，走 Zustand store + `services/api.ts` 直接 fetch。

→ 见代码：`apps/desktop/src/renderer/stores/`

### 强制规则

> 所有从 store 读取状态的组件必须使用精确 selector，禁止 `useXxxStore()` 不带参数直接获取整个 store（否则任意状态变更都会触发该组件重渲染）。

---

## 三、流式输出性能优化

> **实现现状**：`OutputBuffer` / `useAgentsStore` **未实现**；流式文本走 `useConversationStore.appendToLastMessage`（→ 见代码 `apps/desktop/src/renderer/stores/conversation.ts`、`services/streamConversation.ts`）。下表优化策略为设计目标，虚拟滚动等尚未落地。

### 问题

每个 Agent 的 LLM 流式输出约 30 token/s，4 Agent 并行 = 120 次状态更新/s。
性能瓶颈不在状态管理库，而在 DOM 渲染。

### 优化策略

| 优化 | 解决什么 | 实现 |
|------|----------|------|
| Token 批量更新 | 减少 state 更新频率 | requestAnimationFrame 批量刷新 |
| 虚拟滚动 | 长输出不卡 | 只渲染可见区域 DOM 节点 |
| 进度条用 CSS | 60fps 动画 | `transition` 而非逐帧重渲染 |
| 主面板单焦点 | 只渲染当前 Agent 输出 | 其他 Agent 存 store 不渲染 |

```typescript
// ⏳ 设计目标：token 批量更新（未实现）
// 当前实现：content_delta / run_output_delta 直接 appendToLastMessage
```

---

## 四、IPC 通信方案

### 核心原则

- 所有 IPC 通道和消息类型定义在 `shared/ipc-contract.ts`
- preload 和 renderer 引用同一套类型
- 编译期发现类型错误

### 契约与封装

IPC 调用/事件契约（invoke 请求-响应、main→renderer 事件推送）与 preload 的类型安全封装已落地：

→ 见代码：`apps/desktop/src/shared/ipc-contract.ts`（契约类型）、`apps/desktop/src/preload/index.ts`（contextBridge 封装）、`apps/desktop/src/main/fs-service.ts`（主进程 fs 服务）

---

## 五、窗口布局设计 ✅ 已确定

> 参考 IDE 式侧栏布局。核心原则：**全局侧栏 + 页面级自定义布局**。

### 全局骨架、侧栏与页面布局

→ 见代码 `apps/desktop/src/renderer/components/layout/AppShell.tsx`、`components/sidebar/`、`router.tsx`、`pages/`、`components/layout/SidePanel.tsx`

### 对话列表设计

**关键决策**：侧栏只列最近若干对话（当前对话恒置顶）+「查看全部对话」入口；完整列表、文件夹分组与筛选收敛到对话管理页 `/conversations`（分组 = 用户文件夹 + 未分组，按最后活动时间降序）。**已否决·拖拽移动对话到文件夹**：移动 = 切工作区目录（可能云↔本地翻转），与「开始后锁定」冲突。

→ 见代码 `components/sidebar/RecentConversations.tsx`、`pages/ConversationsPage.tsx`、`ConversationItem.tsx` 等；IA 见 [`前端UX设计.md` §一](/docs/04-前端/前端UX设计.md)。

### 对话页侧面板（SidePanel）已落地

右侧收敛为**单一 `SidePanel`**（扁平 tab 栏：固定「工作区」home tab + 按需 run-detail tab；run tab 动态开关、多开对比、只存引用指针、单一数据源）。→ 见 [`前端UX设计.md` §十](/docs/04-前端/前端UX设计.md)（SidePanel 真相源）；信息层级 Layer 0–4 见 [`前端UX设计.md` §一](/docs/04-前端/前端UX设计.md)。

### 页面宽度规范

→ 见 `.cursor/rules/desktop-layout.mdc`（`PageContainer` content 896px / canvas 1200px 两档）；对话页 / 文件页自有分栏布局，不走此组件。

### 设计原则

| 原则 | 说明 |
|------|------|
| 全局只管壳 | AppShell 不预设右面板，页面自行决定布局 |
| 对话页突出差异 | 多 Agent 时助手消息内嵌协作图（状态条 + DAG），展示 Agent 协作信息 |
| 简单任务零噪音 | 单 Agent 时不出图，体验同 ChatGPT |
| 桌面优先 | 不做移动端适配，少量 breakpoint 用于窗口极窄场景 |
| 页面自治 | 各页面/插件自定义内部布局，互不干扰 |

---

## 六、项目结构

> 目录结构以 [`项目结构.md` §三](/docs/02-架构/项目结构.md) 为唯一真相源（与实际代码对齐，含 `main/` `preload/` `shared/` `renderer/`）。本节只保留前端特有的组织判据；Agent 状态面板与任务流程图详见 [`前端UX设计.md`](/docs/04-前端/前端UX设计.md)。

**目录组织原则**（`components` / `pages` / `stores` / `services` / `lib` 五层的职责与判据）以 §9.1 组件组织原则为唯一真相源——按职责分层（非功能域），`components/` 默认扁平、不预先按功能域切深目录。页面级布局见 §五（`PageContainer` 宽度档位 + 各页 `pages/`）。

---

## 七、桌面端架构 ✅ 已确定

> 桌面端是唯一产品客户端（无独立 Web 端）。定位：**常驻本地、会协作的 AI 团队**——后台干活、碰本地文件、需要时敲你一下。

### 7.1 桌面价值主张（vs Web/PWA）

| 能力 | 网页天花板 | 桌面解锁 |
|------|-----------|---------|
| 异步任务 + 审批门禁 | 关标签即断 | 后台常驻 + 原生通知 + 审批闭环 |
| 全局热键召唤 | 埋在标签页里 | Spotlight 式悬浮窗 |
| 文件工作区 | 只能上传 | 本地文件直连 / Agent 读写磁盘 |
| MCP 工具层 | 浏览器跑不了 stdio | 本地 MCP 运行时（常驻进程池） |

被否决：PWA（无常驻/通知/文件）、全原生 UI（贵且漂移）、Tauri（团队无 Rust，WKWebView 有差异，备选）。

### 7.2 主进程架构

> **实现现状**：主进程当前创建窗口 + `registerAppProtocol()`（prod 经 `app://agentcore` 自定义协议加载 renderer，给其正式 origin 以受控 CORS/cookie，见下表）+ `registerFsIpc()`（→ 见代码 `apps/desktop/src/main/index.ts`、`main/fs-service.ts`）。`connection-manager`、`mcp-pool`、`local-store` **均未实现**。

```
主进程 (Node.js)
├─ ⏳ connection-manager  ← 独立 attention 轮询 + 通知（未实现）
├─ ⏳ mcp-pool            ← MCP server 常驻进程池（未实现）
├─ fs-service          ← 本地文件 IPC 服务（已落地）
└─ ⏳ local-store         ← sql.js，对话/消息读缓存（未实现）
renderer (Vite + React Router，本地)
└─ 桌面业务组件 + IPC ↔ 主进程；SSE 直连后端
         │ API (HTTPS)
         ▼
云端 api-server（LLM 推理 · Agent 运行时 · 数据持久化）
```

| 决策 | 结论 | 理由 |
|------|------|------|
| renderer 加载 | 本地（prod 走 custom protocol `app://`） | 给本地 renderer 正式 origin，CORS/CSP/cookie 可控 |
| SSE 连接 | renderer 直连后端（非 IPC 转发） | 长连接高吞吐，IPC 序列化开销不可接受 |
| 本地数据层 | ⏳ sql.js（读缓存优先，非离线写） | **未实现**；当前会话数据由后端 API 提供 |
| 组件策略 | 桌面端独立演进（非共享 packages 业务组件） | 桌面优先 = 自由演进 > 两端一致 |

### 7.3 安全基线

- `contextIsolation: true` + `sandbox: false`（当前）+ renderer 关 `nodeIntegration`
- 原生能力只经 preload 白名单 IPC 暴露
- httpOnly cookie 持久 session（Chromium OS DPAPI/Keychain 加密），不额外上 safeStorage

> 注：原设计目标 `sandbox: true`；当前 `webPreferences.sandbox` 为 `false`（→ 见代码 `main/index.ts`）。

### 7.4 OS 深度集成

- **资源管理器右键菜单**：安装期注册 shell verb，`--analyze <path>` 唤起，授权目录后 Agent 可读
- **文件关联双击**：electron-builder `fileAssociations`，`.md/.txt/.json/.csv/.yaml` 等
- **全局热键**：Spotlight 式召唤悬浮窗
- **Taskbar/Dock badge**：未读通知计数
- **深链**：`openDeepLink(target)` 统一入口，本地 renderer 已加载走 React Router，否则 `loadURL`

### 7.5 通知双通路

| 通路 | 覆盖场景 | 机制 |
|------|---------|------|
| 方向 A：SSE 桥接 | 桌面正持有 SSE 流 | `approval_required` / `checkpoint_required` / `message_end` 等事件桥接到原生通知 |
| 方向 B：Attention 轮询 | 未持有 SSE 流的盲区 | `GET /v1/attention`（45s 间隔），补 durable paused turns + pending approvals |

A+B 共用去重集（稳定 id），避免同一待办双弹。

### 7.6 自动更新

- 静默下载 + 用户决定安装时机
- 检查频率：启动 + 每 4h + `powerMonitor 'resume'`
- 远程熔断：后端 `updates_enabled = false` 才暂停（fail-open）
- 双通道：beta / stable；灰度分桶 `stagingPercentage`
- 平台：Windows + macOS（Win 优先），Linux 附带

### 7.7 关窗 = 隐藏到托盘

attention 轮询 + SSE 挂在渲染进程，`close` 默认 `preventDefault + hide`（仅 `before-quit` 放行销毁）。可配置：托盘开关「关闭时最小化到托盘」。

---

## 八、本地文件系统设计 ✅ 已确定

> 把本机磁盘文件提升为 **Agent 可直接读写的一等对象**。核心原则：**本地 = 唯一真身，云端不持久化文件字节**。

### 8.1 隐私边界

| 边界 | 含义 | 策略 |
|------|------|------|
| **at-rest** | 不把文件字节/正文存进服务端 | 默认守住；用户显式「备份/分享」为知情例外 |
| **in-transit** | Agent 读文件时正文临时发给 LLM | 接受（同 Cursor 把代码发给模型） |

铁律：Agent 要理解文件 → 正文必须进 prompt；at-rest 默认不外发。

### 8.2 Client-Tool 桥架构

> **实现现状**：`client_tool_requested` SSE 桥、IPC 回传 `client-tool-result` **均未实现**（→ 见代码 `services/streamConversation.ts` 事件类型预留）。

云端运行时 + 本地文件桥（与 Cursor / Claude Desktop 同构）：

```
云端 executor 命中 client-bound 工具（文件读写）
  → emit SSE client_tool_requested{op, params}
  → Redis BLPOP 阻塞（keyed by conv_id + tool_call_id）
[SSE] → Electron 渲染层 → IPC → 主进程 fs-service 在授权根内执行
  → POST /v1/conversations/{id}/client-tool-result 回传
  → resolve → 续跑 ReAct
```

设计选择：
- **原地阻塞而非 durable 快照**：文件读写秒级；桌面须在线才能执行——断连即 cancel、本轮干净失败
- **不落 Postgres**：client_tool 请求是临时态，持久化会把路径写库违 at-rest，只用 Redis
- **安全护栏**：桌面 fs 桥的授权根 allowlist + realpath 边界校验，云端视为不可信请求方

### 8.3 对话产物落点

由「本轮有无可写本地根」一个开关决定：

| 运行语境 | 落点 |
|---------|------|
| 桌面 + 有可写根 | 只挂本地写工具，产出写本地原件 |
| 桌面 + 无根（随手新对话） | 懒创建：首次写文件时在默认根创建目录，之后同上 |
| 无桌面（headless / 自动化） | 禁产：不挂写工具；要产物须声明显式出口（webhook 等） |

### 8.4 文件树设计原则

> 树/预览/增删改已抽到 source-agnostic 共用组件（§8.7 `FileSource`）。

- **一棵本地主树**：以本地授权目录为根，非"本地/云端两个平级源"
- 无全局树 store——各目录节点各自懒读子项并 watch，互不串扰
- 选中/改名/新建态由组件局部持有、单向下传
- fs 操作在 IPC 边界返回判别结果（`{ ok, reason }`）而非抛异常
- `fs.watch` 跟随展开状态，磁盘变更即刷新

### 8.5 本地目录授权模型

- 授权 = 持久契约（非会话参数）——per-target 显式授权
- 作用域变更对用户与 Agent 同等显式
- 兜底只追加、不替换、留痕迹
- 绝对路径不出本机（错误语言按受众分层）

### 8.6 项目身份模型（dotfile）

> **实现现状**：`.agentcore/project.json` dotfile **未实现**。

**本地文件夹 = 项目**：文件夹根下 `.agentcore/` dotfile 记录云端项目编号（类比 `.git/`），桌面打开即认、换机即认、零绑定仪式。

```
<项目文件夹>/
  .agentcore/
    project.json     # { schemaVersion, id, name, createdAt }
    rules/*.md       # 文件夹级规则
    memory/*.md      # 文件夹级记忆
```

**决策：云端权威 + dotfile 记号**（否决本地发号）。云端继续发编号、管对话，dotfile 只负责"记号"——保留手机/网页/平板等无本地文件夹设备的接入能力（纯本地身份会从结构上焊死手机端）。

**dotfile 自带身份**：映射跟文件夹走，同步即带、打开即认；解析方向：打开路径 → 读 dotfile → 云端号。

**复制文件夹（同号）**：学 Git 对 `cp .git/` 的态度——允许两副本指同一云端项目，并发写云端时复用三方冲突检测（冲突保留两份、绝不自动覆盖）；要独立副本须显式"另存为新项目"。

### 8.7 `FileSource` 抽象（对话工作区面板 + @ 提及共用一套）✅ 已落地

对话工作区面板（服务端·REST）与本地授权根（IPC）本质都是「一棵带预览 + 增删改的文件树」，抽 **`FileSource` 接口**收敛为一套。

→ 见代码 `lib/fileSource.ts`、`services/sources/`、`components/files/`、`components/workspace/`、`pages/FilesPage.tsx`

- **核心 + 能力位**：核心 `listDir/read/createFile/mkdir/move/delete`（寻址一律**源内相对 POSIX**，不暴露绝对路径）；可选 `listTree?`（能廉价全枚举的源急加载）、`listFileIndex?`（喂 @ 索引）、`writeBytes?/download?/watch?`。**能力差异不靠分支硬塞**，由 source 声明能力位 `caps{watch,transfer,edit,snapshots,handoff}`，共用 UI 据此决定挂哪些面（有 `watch` 才实时、有 `transfer` 才显上传/下载、有 `snapshots/handoff` 才挂叠加层）。
- **文件夹即工作区·两视角互跳**：文件视角 `/files` 与对话视角 `/conversations` 互跳，枚举/选源同一套接缝（`/v1/workspaces` 与 `/conversations` 文件夹列同源），「有哪些项目」单一真相、不双份记账。→ 见 [`双模式工作区.md` §八](/docs/02-架构/双模式工作区.md)。
- **云/本地选实现，UI 不变**——`createWorkspaceSource` 据对话绑定（模式条）在云 REST / 本地 IPC 间选实现。为何本地走 IPC 而非 REST：`LocalWorkspace` 的 channel 是 per-turn SSE 挂起原语（绑在对话 run 的 sink 上）；故 `location=="local"` 选 `LocalRootSource`(IPC) 直读其 `root_id`，`"cloud"` 才走 REST（后端契约见 [`双模式工作区.md` §九](/docs/02-架构/双模式工作区.md)）。
- **拖拽 MIME 统一**为一套，载荷带 `sourceId` 防跨源误投。
- **@ 提及多源索引**：聚合多个 `FileSource` 的 `listFileIndex` 输出（本地根 + 云端工作区）；切会话失效索引，下次 @ 重建。→ 见代码 `lib/fileIndex.ts`、`components/chat/MessageInput.tsx`。

### 8.8 文档编辑：源无关编辑契约 + 本地 CAS 写盘 ✅ 已落地（P1）

把 AI 产物从「只读预览」升级为**可编辑文档**：工作区面板里 `.md/.markdown` 在 edit-capable 源（当前本地根）上挂 CodeMirror 源码编辑器（渲染层架构见 §9.10），其余文件维持只读预览。读写经 `FileSource` 编辑契约，宿主不分支本地/云端。

- **源无关编辑契约**（gated by `caps.edit`）：`readForEdit(path)→{text,version,encoding,eol}` 读完整正文 + 版本基线；`writeText(path,{content,encoding,eol,baseline})→` 判别式结果（`conflict` 携当前版本供「仍然覆盖」再写）。宿主 `MarkdownFileEditor` 只认这层、不按源分支；`baseline` 由各源映射到 mtime（本地）/ etag（云端 P4 ⏳）。→ 见代码 `lib/fileSource.ts`、`services/sources/localRootSource.ts`。
- **编辑读走独立通道**（`fs:readTextFile`，非预览 `fs:readFile`）：预览有 256KB 截断且判别图片/二进制——**截断正文一旦保存会丢尾**，故编辑必须独立拿**完整**正文 + 基线 `mtimeMs`/编码/换行。
- **本地 CAS 写盘**（`fs:writeFile`）：写前 `stat` 比对 `baselineMtimeMs`，不符即 `conflict`（带磁盘 `diskMtimeMs`，**绝不盲覆盖**）；落盘走「临时文件 + rename」原子写，授权根 realpath 边界沿用 §8.2；换行按 `eol` 还原，**GBK 等非 UTF-8 只读不回写**（`unsupported`，避免编码往返损坏）。→ 见代码 `shared/ipc-contract.ts`（`Fs{TextFile,WriteInput,WriteResult}`）、`main/fs-service.ts`。
- **为何 CAS 而非盲写**：本地文件可能被外部编辑器 / Agent 并发改，mtime 基线让「磁盘已变」可检测、交用户裁决，与云端三方冲突检测同源（云端条件写属 P4 ⏳）。

> **被否决 · 编辑复用预览 `readFile`**：预览截断 + 图片判别使正文不全，保存即丢尾——故编辑另开 `readTextFile` 全量通道。

---

## 九、前端运行时架构模式 ✅ 已确定

> 渲染层的组件组织、状态归属和数据流决策。

### 9.1 组件组织原则

按职责分层，非按功能域：

→ 见代码 `apps/desktop/src/renderer/components/`、`pages/`、`stores/`、`services/`、`lib/`

`components/` 默认扁平、仅当功能多文件协作内聚成体系才开子目录。刻意不预先按功能域切深目录——浅层 + 少数子目录比深路径更易定位。

### 9.2 Store 关键拆分

> **实现现状**：全部已落地。`useExecutionStore` 按 **messageId** 隔离执行槽（§9.3）✅；`message.runs` 持久化 + 刷新回放 ✅；`useSidePanelStore`（统一侧面板：详情 run-detail tab + 工作区，仅存引用）✅。

→ 见代码 `apps/desktop/src/renderer/stores/`

**run 树独立成 store** 是核心决策：执行态（编排步骤 + 委派子 Agent 统一为一种 run）与消息态正交，分开既防 chat reducer 膨胀、又让 run 按 messageId 隔离。

### 9.3 按 messageId 隔离执行态 ✅ 已落地

**核心决策**：执行态键控从 `conversationId`（每对话单槽）改为 `messageId`（每回答一槽）——否则同对话连发多回合会节点串台，且历史回合无图可回看。实时与回放走**同一 fold**（`projectExecution`），SSE 据回合 `conversationId` 解析 live assistant 消息 id 路由 run/tool 帧；`message_end` 持久化回合 journal（后端落 `turn_journal`，读取投影为 `runs` 载荷），刷新经 `hydrateFromJournal` 幂等回放。

**⏳ 已知限制**：历史执行槽内存回收未做（`byId` 随浏览深度累积，开发期可接受）。

→ 见代码 `stores/execution.ts`、`services/streamConversation.ts`

### 9.4 单一数据源、两层投影 ✅ 已落地

**核心决策**：inline 信号层（状态轨 + 协作图）与详情面板层同投影同一消息执行槽；面板 tab 只存 `{messageId, runId}` 引用。**节点高亮一面一源**（派生自面板当前激活 run-detail tab，全屏 overlay 用自身 `selectedRunId`）。**否决**跨 store 反向 `selectRun` 对账（补丁绊线）。

布局与交互决策见 [`前端UX设计.md` §一/§十](/docs/04-前端/前端UX设计.md)。→ 见代码 `stores/sidePanel.ts`、`stores/execution.ts`

### 9.5 事件归属显式化

**核心决策**：回合归属在 SSE 入口一次性确定——dispatch context 携带 `conversationId`，chat 类写会话切片、run/tool 类解析 live 消息 id 写执行槽；无「可路由事件白名单」需维护。

→ 见代码 `services/streamConversation.ts`

### 9.6 按 conversationId 隔离会话运行时（后台 turn 存活）✅ 已落地

**核心决策**：会话运行时按 `conversationId` 分片（非单一全局态）——切走对话不打断后台 turn，切回即见。闲切片（无生成中且无挂起审批）释放、忙片保活；所有写入按 turn **自身的 `conversationId`** 路由。

**被否决（旧行为）**：`switchConversation` 切走即 `abort`——与「后台干活」心智冲突。

与 §9.3 正交：本节按会话保活 turn；§9.3 按 messageId 隔离协作图。

→ 见代码 `stores/conversation.ts`、`stores/approval.ts`

### 9.7 消息载入模型：最新窗口 + 双向无限滚动 + 命中定位 ✅ 已落地

**核心决策**：会话 `messages` 是一段**连续游标窗口**（非全量历史）——打开载入最新一窗，上/下滚扩展，搜索命中走 `load-around` 必达。live 回合只能追加在真实队尾（HEAD 不变量）。

→ 见代码 `services/messages.ts`、`lib/useChatScroll.ts`、`stores/conversation.ts`

### 9.8 全局搜索与命令面板（Tier 1 关键词检索 + Tier 2 命令导航）✅ 已落地

**核心决策**：`Ctrl/Cmd+K` 命令面板 = Tier 1 实体搜索（后端 `GET /v1/search`，ILIKE 子串、owner-scoped、中文友好）+ Tier 2 命令导航（前端静态注册表 `lib/paletteCommands.ts`，本地过滤零往返）。全局快捷键单一真相源 `lib/shortcuts.ts`。

**暗色模式 ✅**：`useUIStore.theme` 持久化，DOM 应用收口 `lib/theme.ts`；入口 = 命令面板「切换主题」+ 外观设置页（浅色/深色/跟随系统）。UX 见 [`前端UX设计.md` §十四](/docs/04-前端/前端UX设计.md)。

→ 见代码 `services/search.ts`、`lib/paletteCommands.ts`、`lib/theme.ts`、`components/layout/CommandPalette.tsx`

### 9.9 错误处理与提示分诊（统一错误契约 → 分场景承载）✅ 已落地

**核心决策**：后端 `{ error: { code, message } }` 为唯一真相源；前端 `lib/errors.ts` 单点翻译（`describeError` + `errorActionForCode`），REST 与 SSE 共用。按 code 分诊（`QUOTA_EXCEEDED`/`LLM_KEY_REQUIRED` 不给重试；401 静默）。承载面：回合内联错误卡 / REST toast / 路由兜底 / 会话级全屏重试。

配额/限流拒绝的前端呈现见 [`前端成本呈现.md` §7.6](/docs/04-前端/前端成本呈现.md)。

→ 见代码 `lib/errors.ts`、`lib/toast.tsx`、`services/api.ts`、`services/streamConversation.ts`

### 9.10 Markdown 文档编辑器（CodeMirror 源码 + 内联实时预览 + AI 改写）✅ 已落地

编辑 AI 散文产物（报告/方案/笔记 `.md`）的渲染层内核。**选 CodeMirror 6 源码编辑器、否决 TipTap/WYSIWYG**——核心是 **markdown round-trip 有损**：富文本模型须 `md→doc→md` 往返，公式 `$…$`、mermaid/markmap 图、列表/换行规范化全是坑（重排整文 → 噪音 diff，撞快照/三方冲突检测）。CodeMirror 路线**文本进文本出、字节忠实**（人存的字节即 AI 读的字节），所见即所得是**「视图」而非「模型」**——故有手感又零 round-trip 债。

编辑契约与写盘见 §8.8；宿主挂在工作区面板 `FileBrowser`（SidePanel，UX 见 [`前端UX设计.md` §九](/docs/04-前端/前端UX设计.md)）。→ 见代码 `components/markdown/`、`components/files/MarkdownFileEditor.tsx`

> **被否决 · TipTap / 整页 contenteditable WYSIWYG**：round-trip 有损、math/diagram/clean-diff 全坑、与「文本即典范」冲突；IME 友好度亦劣于 CodeMirror。

---

## 十、设计系统 ✅ 已确定

> **实现现状**：视觉令牌定义在桌面端本地 `apps/desktop/src/renderer/styles/globals.css`（**非**共享包 `theme.css`）。部分内容（跨端共享 `theme.css`、全端令牌统一）为远期目标。

### 10.1 令牌唯一源

桌面端当前以 `globals.css` 为唯一令牌源。跨端共享 `theme.css` 为远期目标，改令牌改 `globals.css`。

### 10.2 色彩令牌设计

统一 **OKLCH** 调色，**hue 255 蓝为品牌色**：`primary` / `ring` / `info` 走品牌蓝（主 CTA、活跃、进行中），中性表面带极淡同色相冷调，整体归为一个色族。状态色为一等令牌：`success`（绿，成功/已完成）、`warning`（琥珀，警告/检查点）、`info`（蓝，信息）、`destructive`（红，错误/停止）；`accent` 是中性 hover 表面，**不表达状态**。侧栏并入标准 `sidebar` 令牌族（含 `sidebar-primary` / `sidebar-ring`），保留 `--sidebar-gradient` 竖向渐变（`bg-sidebar` 叠加渐变 = 极淡蓝调浅底）。

令牌清单、语义、「执行状态 → 令牌」映射与禁用项（禁硬编码调色板/hex、禁 `hsl(var(--…))` 包裹、禁拿 `accent` 冒充成功色）统一由规则约束 → 见 `.cursor/rules/color-tokens.mdc`；令牌值见 `apps/desktop/src/renderer/styles/globals.css`（`:root` / `.dark`）。

### 10.3 中性色冷调

中性色（背景/表面/边框/文本）在 `globals.css` 的 `:root` / `.dark` 直接带极淡 hue 255 冷调，与品牌蓝同色相，整屏归为一个色族。桌面端是唯一客户端（见 §七），无需 `.theme-desktop` 之类 per-端 scope 隔离——直接着色即可。

### 10.4 暗色模式 ✅ 已落地

Light-first；`.dark` 令牌覆盖块与 `@custom-variant dark` 已配置。主题切换经 `lib/theme.ts` 挂 root `.dark` 类，入口见 §9.8（命令面板 + 外观设置页）。

### 10.5 组件复用边界

| 层 | 策略 |
|---|---|
| **令牌** | 全端共享（已统一） |
| **原语**（button/input/dialog） | 共享包已有，桌面端不强迁，新组件上机会性收敛 |
| **域卡片** | 不设共享包。等有真实跨端消费者再从桌面端提取 |

---

## 十一、桌面端插件架构 ✅ 已确定

> **实现现状**：插件注册表、`getPluginRoutes()`、`getPluginNavItems()`、插件 IPC 前缀 **均未实现**（→ 见代码 `main/index.ts` 仅窗口 + fs IPC）。以下为架构设计目标。

### 11.1 插件分类

| 类型 | 特征 | 示例 |
|------|------|------|
| 垂直工具 | 有独立页面、可能有本地存储 | 法规库、表格 |
| 基础设施 | 有独立生命周期、push 事件 | 文件系统、MCP |
| 轻量工具 | 仅主进程 I/O + 简单页面 | 脱敏、日程 |

### 11.2 六贡献点（类比后端 Vertical）

| 贡献点 | 消费方 |
|--------|--------|
| 路由注册 | router（经 `getPluginRoutes()` 注入） |
| 侧栏导航 | app-shell（经 `getPluginNavItems()` 注入） |
| IPC Handlers | 主进程注册表（自动加 `app:{id}:` 前缀） |
| 生命周期 | 应用启动/退出时调用 |
| 推送事件 | 主进程 → renderer 单向推送 |
| 跨插件服务 | 其他插件经 `getPluginService()` 调用 |

### 11.3 IPC 类型安全

三层结构建立编译时契约：

```
plugins/{name}/ipc-schema.ts   ← 单一真相源
        ↓
plugins/{name}/main.ts         ← TypedHandlers<Schema> 约束实现
        ↓
preload/index.ts               ← IpcBridge<Schema> + AssertIpcBridge 断言
```

新插件必须先写 schema 再写 handler。

### 11.4 错误隔离

每个插件路由自动包裹 ErrorBoundary（注册表注入，插件开发者无感知）。单插件崩溃仅影响其自身路由区域，不触发全局错误。

### 11.5 设计决策

| 决策 | 理由 |
|------|------|
| 显式注册（非约定扫描） | 当前体量下显式 import 更可控、可 tree-shake |
| IPC Schema 渐进迁移 | 避免大爆炸重写 preload |
| ErrorBoundary 注入在 registry | 零额外代码 |
| 路由嵌套在统一工具布局下 | 侧栏/顶栏/面包屑一致 |

---

## 十二、SSE 事件消费规范

→ 见代码 `apps/desktop/src/renderer/services/streamConversation.ts` + [`执行引擎架构设计.md` §十二](/docs/03-AI核心/执行引擎架构设计.md)（SSE 事件定义与映射）

### 设计约束

- **单一入口**：所有 SSE 事件经 `services/streamConversation.ts` 统一分发，不在组件中直接监听
- **Store 最小更新**：每条事件只更新必需的 Store 字段，不做全量替换
- ⏳ **对账兜底**：`reconcileAllRuns()` **未实现**
- **乐观更新**：发送消息时立即在 conversation store 创建 user message 条目，无需等后端确认

---

## 十三、待定

| 议题 | 说明 |
|------|------|
| 本地 MCP 打包 | dev 模式 vs 生产包内置策略 |
| 代码签名 | Win11 右键菜单 / SmartScreen 证书方案 |
