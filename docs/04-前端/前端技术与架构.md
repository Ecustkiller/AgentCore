---
status: landed
code: apps/desktop/src/renderer/,apps/mobile/src/
related:
  - docs/04-前端/前端UX设计.md
  - docs/02-架构/双模式工作区.md
skip_if:
  - 只改后端 API
  - 只改 UX 布局（读前端 UX）
---

# 前端技术与架构

> 布局/协作图/检查点 UX → [前端 UX](/docs/04-前端/前端UX设计.md)；工作区 → [双模式工作区](/docs/02-架构/双模式工作区.md)。

## 一、技术栈

| 类别 | 选择 | 约束 |
|------|------|------|
| 构建 / 打包 | electron-vite + electron-builder | — |
| UI / 状态 | React 19 · **Zustand 5** · ✅ TanStack Query | 服务端列表走 Query；流式态走 Zustand |
| 样式 / 图标 | Tailwind 4 · lucide-react | ⏳ shadcn **未引入**（自写组件） |
| 流程图 | @xyflow/react 12 + ELK.js | — |
| IPC | 类型安全 contextBridge | 按域拆 contract |
| 测试 / 质量 | Vitest + Playwright · Biome | — |

## 二、状态与流式

- Store 按关注点分片（→ `apps/desktop/src/renderer/stores/`）；类型约束 → [核心接口定义 §七](/docs/02-架构/核心接口定义.md)。
- **强制**：读 store 必须精确 selector，禁止无参 `useXxxStore()`。
- 流式：`appendToLastMessage`；**rAF 合批 ✅**（`contentBuffer` / `execFrameBuffer`）；虚拟滚动 ⏳。

## 三、IPC

按域拆 contract：`ipc` / `window` / `updater` / `log` / `sidecar` / `agenttown` / `outbox`。preload 与 renderer 同类型。→ 见代码 `apps/desktop/src/shared/*-contract.ts`。本地引擎 → [双模式工作区](/docs/02-架构/双模式工作区.md)。

## 四、壳与 AuthGate

全局骨架 → [前端 UX](/docs/04-前端/前端UX设计.md)；宽度 → `desktop-layout.mdc`。

`AuthGate` 三态：`loading` / `unavailable` / `unauthenticated`；仅 `authenticated` 进路由。认证前用 `PreAuthShell`（壳外）。`__WEB_PREVIEW__` 仅 `dev:web`/shoot 旁路。N4-A：会话断网软提示留壳；冷启动无缓存仍墙；发送硬禁用。→ `components/auth/AuthGate.tsx`。

## 五、客户端架构（桌面 + 手机）

**双端独立**：零共享业务实现；共享 = 协议类型 + golden + design token（§十二、`cross-platform-frontend.mdc`）。

**手机 = 桌面 − Electron/OS 能力**（黑名单减法）：保留云端聊天/协作/审批/成本/IM；减去本地文件、stdio MCP、托盘、热键、静默更新、sidecar。**否决** RN、纯 PWA。传输 = web SPA → Capacitor；Android WebView origin `https://localhost` 须进 CORS；无 Firebase 不得 `PushNotifications.register()`。→ `apps/mobile/`。

**桌面价值**（vs Web）：后台常驻 + 通知、热键、本地文件、本地 MCP。**否决** PWA / 全原生 / Tauri（备选）。

**主进程**：renderer 本地加载；SSE 直连后端或经 sidecar `/v1/inference`（不经 IPC）。✅ local-store 只读离线；✅ outbox 回写器（主进程耐久，renderer 退出 HTTP 回写）；✅ sidecar recovery/attach。⏳ `connection-manager` / `mcp-pool`。边界 → [双模式 · Sidecar / 契约 α](/docs/02-架构/双模式工作区.md)。

**跨端策略**：**否决**共享实现核、一棵树降级。红线 = fold 必过 `pnpm conformance`；契约只走后端单一源。域卡片各端重建。

## 六、本地文件

权威 → [双模式工作区](/docs/02-架构/双模式工作区.md)。桌面 `FileSource` 统一云 REST / 本地 IPC。**引用即驻留**：附加文件复制进 `attachments/`，绝对路径不下发 renderer。→ `main/fs/stageAttachment.ts`。

## 七、运行时架构（关键决策）

| # | 决策 | 否决 / 约束 |
|---|---|---|
| 9.1 | `components/` 按职责扁平，不预先按域深切 | — |
| 9.2–9.3 | 执行态按 **messageId** 隔离；`projectExecution` live=回放同 fold | 历史槽内存回收 ⏳ |
| 9.4 | inline 与详情同投影一槽；高亮一面一源（SidePanel **焦点面板** / `sidePanelFocusTabId`） | **否决**跨 store `selectRun` 对账 |
| 9.5 | 回合归属在 SSE 入口一次确定 | 无事件白名单 |
| 9.6 | 会话运行时按 `conversationId` 分片；切走不 abort | **否决**切走即 abort |
| 9.6.1 | `turnPhase`：`idle→…→stopping→terminal`；phase 门禁 + 事件门 | **否决**仅靠 AbortSignal；迟到事件仍 fold |
| 9.7 | 消息 = 连续游标窗口 + 双向无限滚 + load-around | live 只能追加队尾 |
| 9.8 | Cmd+K = Tier1 搜索 + Tier2 命令；快捷键 SSOT `shortcuts.ts` | — |
| 9.9 | 错误码单一目录（后端 `error_codes` ↔ 前端镜像） | 不可重试码策略驱动分诊 |
| 9.10 | Markdown = **CodeMirror 6** 源码编辑 | **否决** TipTap/WYSIWYG（round-trip 有损） |
| 9.11 | UI 持久化收口 `uiStorage`；业务禁直碰 localStorage | 一次性控件不持久；preview 切内存后端 |
| 9.12 | ✅ **统一浏览器（BrowserSession）**：右坞单一壳（页签/地址栏/人机共控）；**桌面** Local（CDP）\| Sandbox（云端）；**Web / 无 Electron → 仅 Sandbox**；多 `session_id`；产物完整预览并入同壳（桌面 `workspace://` + BrowserPanel；Web 下载 / 系统浏览器；导航策略见 `main/browser/navigation.ts`）。**壳内页签**：可拖排序（落盘顺序）+ 条可横滚/溢出指示；「在系统浏览器打开」：http(s)/mailto → `openExternal`（`isSafeExternalUrl` 白名单不放宽）；桌面 `workspace://` → 与文件树同源「快照/检出 → 临时目录 → `openPath`」（禁把自定义协议交给 `openExternal`；本地静态服为后续 B）；其余 scheme 灰态说明 + 页签/地址栏右键。**Local Attachment**：主进程一等附着态（`activePageId` + generation）；`hide`=脱离保活（清 active、bump generation，与 `show` 串行）；仅 `BrowserPanel` 可 `show`；关坞/关浏览器 tab/切对话在改 UI 态前显式 detach；Bridge 建页默认隐藏，不得因「曾 active」复活可见；关页/删对话才 destroy。**挂回**：`show` 带回宿主真实 URL——地址栏跟 WebContents；仅宿主空白且 store 有 URL 才冷 `navigate`（禁止旧 store URL 打回保活页）。**页签冷恢复（P1）**：每对话页签列表（url/title/激活/顺序）经 `uiStorage` `browserTabs` 落盘；进入/激活对话时再 hydrate 该对话（禁启动批量建 WebContents）；不恢复 cookie/localStorage / 不做 persist partition；关页/删对话清记录。**Local 对话硬隔离**：外网与 workspace 两类 Electron partition **均**按 `conversation_id` 切开（同对话多 tab 共享 Context；项目共享登录 ⏳）；Bridge / UI IPC 强制传 cid，缺字段 fail-closed；删对话清 `browserSessions` + 关该 cid 全部 Local 页（与 server `registry.close` 双关幂等）。**Local web `window.open`**：按 disposition 分流——登录类 popup → 同 conversation partition 独立子窗（保 opener / 同 session）；普通 `target=_blank` → 同壳新页签；危险 scheme 仍 deny（→ 见代码: `apps/desktop/src/main/browser/navigation.ts`）。Sandbox 弹窗附着 ⏳。**AI 打开本会话 HTML（定案）**：桌面 Local Bridge 上 `browser_navigate` 支持会话 workspace 相对路径 → 与用户「完整预览」同源（`workspace://` + 该对话 workspace partition）；打开后可继续 click/snapshot 等同页交互；公网仍走该对话 web partition；**不**放开任意 `file://`。无桌面预览（Sandbox / 未装配）→ 诚实降级（引导用户点产物完整预览 / 标明不可测），禁止假成功。**绑页 / hydrate**：`tool_use_end.display` 带 `session_id`+`host_kind` 推送 upsert；Local hydrate = sidecar Registry（`listBrowserSessions` RPC），云仍走 `GET …/browser/sessions`；空 Local list 不抹 upsert 页 / hydrate 代际防过期 merge | **否决**双产品入口（旧 L2/L3 / 平行「预览」tab）；**否决**双 Registry；**否决** WebSocket 直播；用户可随时接管（废止 turn 中拒接管）；**否决** AI 与用户预览两套打开能力；**否决** Bridge list IPC 双真相 / 只 openTab 薄补丁；**否决**「一律 flatten 为同壳 tab」冒充 OAuth（须同 partition 子窗保 opener） |
| 9.13 | GraphScene IR → 布局 → `projectTurnGraph` 单一核 | **否决** LOD 薄版先行、四处修 bug；产品 UX → [协作图 UX](/docs/04-前端/协作图与双视图UX.md) |

→ 见代码 `stores/`、`services/sse/`、`components/graph/scene.ts`、`BrowserPanel`（完整预览 · `workspace://`）。

## 八、设计系统

权威 → `color-tokens.mdc`；token → `@agentcore/design-tokens`；分层 → [UI-Pattern 索引](/docs/04-前端/UI-Pattern索引.md)。**手机仅浅色**。域卡片不设共享包。

## 九、插件架构 ⏳

前端插件化贡献点**均未实现**；后端亦仅 feature-flag。→ `main/index.ts`。

## 十、SSE 与协议一致性

入口：`services/sse/dispatch.ts`（单一分发）；传输/重连在 `streamConversation.ts`。事件语义 → [SSE 事件与录制回放](/docs/03-AI核心/SSE事件与录制回放.md)。

**约束**：发送即有流（POST 恒 SSE；`turn_queued` / `turn_queue_cancelled` / `turn_steer_accepted` EPHEMERAL）；✅ `delivery` 必填 + 空闲默认 Steer / 生成中默认 Queue / 显式插队 Steer（见 [运行时三模型 · 同对话再发](/docs/03-AI核心/运行时三模型与挂起.md#同对话再发steer--queue)）；Store 最小更新；乐观发消息；⏳ `reconcileAllRuns` 未实现。

**打开对话水合**：消息窗与 `loadRecovery` 并行；**UI ready** = 消息窗 adopt（+ recovery await）之后，attach/settle **后台**跑（本地 sidecar live 不挡揭开）。在线 SWR：有 opened 本地快照则先揭开再 reconcile。切会话保留最近 5 个 idle slice（busy 不踢）以暖开；**切会话 ≠ 卸 sidecar 观察泵**（hydrate 不传页级 AbortSignal；显式 detach 另传）。本地事实（sidecarLive / unsynced）→ 投影 + attach，**不调** `resolveSidecarRoot`、不走云 attach；无本地事实 → 云 `attachOnOpen`。live attach：journal 回放段经 `: attach-caught-up` 边界一次折叠（避免已完成 worker 再走 running→completed），再 live tail；sidecar 快照同步 fold。契约 α：刷新双面恢复、resume 中勿只信云薄壳；hydrate **不**放宽、**不**透传 unsynced journal 充云投影。→ 代码 `ConversationPage` / `sliceLru` / `streamConversation.attachConversation`；权威 [双模式 · 契约 α](/docs/02-架构/双模式工作区.md)、[运行时三模型](/docs/03-AI核心/运行时三模型与挂起.md)。

**conformance**：`fold(events)→ProjectedTurn` 纯函数；后端 oracle 裁判；`pnpm conformance`。向量 = 手写 builder + 录制裁切并存。纪律 → `protocol-conformance.mdc`。

**离线预览**：`#/preview` 经真实 dispatch 回放；**否决** Storybook / 手写 mock。e2e：Playwright + webapp 壳 + 向量 SSE；**否决** `route.fulfill`。→ `frontend-preview.mdc`。

## 十一、待定

本地 MCP 打包策略；代码签名（详细提案不在公开仓 / 维护者本地）。
