# 项目结构

> **状态**：已确定

---

## 一、总览

Monorepo 布局，前后端统一管理，通过 OpenAPI 自动类型同步。

```
AgentCore/
├── docs/                          # 设计文档
│
├── apps/                          # 应用层
│   ├── server/                    # Python 后端（FastAPI）
│   └── desktop/                   # Electron + React 前端
│
├── packages/                      # 共享工具/脚本
│   └── scripts/
│       └── generate-types.ts      # OpenAPI → TS 类型生成
│
├── deploy/                        # 部署配置
│   ├── docker-compose.yml
│   ├── Dockerfile.server
│   ├── nginx/
│   └── scripts/
│
├── .github/workflows/             # CI/CD
│   ├── ci.yml
│   └── deploy.yml
│
└── README.md
```

---

## 二、后端结构（apps/server）

```
apps/server/
├── pyproject.toml
├── alembic.ini
├── agentcore/
│   ├── __init__.py
│   ├── main.py                    # FastAPI app 入口
│   ├── config.py                  # 环境配置加载
│   │
│   ├── api/                       # HTTP 入口层
│   │   ├── __init__.py
│   │   ├── routes/
│   │   │   ├── conversations.py   # 对话 CRUD + 消息发送
│   │   │   ├── auth.py            # 注册/登录/令牌刷新
│   │   │   ├── tools.py           # 工具列表、审批响应
│   │   │   └── system.py          # 健康检查
│   │   ├── schemas.py
│   │   ├── dependencies.py
│   │   └── sse.py                 # SSE StreamingResponse 封装
│   │
│   ├── middleware/                 # 平台中间件
│   │   ├── __init__.py
│   │   ├── auth.py                # JWT Cookie 认证
│   │   └── rate_limit.py          # 请求限流
│   │
│   ├── conversation/              # 对话管理
│   │   ├── __init__.py
│   │   ├── service.py             # ConversationService（stream_chat 入口）
│   │   └── history.py             # 历史重建与回放
│   │
│   ├── runtime/                   # 执行核心 + 编排（§12-18 统一模型）
│   │   ├── __init__.py
│   │   ├── pipeline.py            # ChatPipeline（Prepare → Plan → Execute → Finalize）
│   │   ├── planner.py             # 编排器：用户请求 → 结构化协作计划（失败回退 single_agent）
│   │   ├── plan.py                # 计划数据模型（OrchestratorPlan）+ 容错 JSON 解析
│   │   ├── engine.py              # ReAct 主循环（流式、工具调用、并行执行）
│   │   ├── runs.py                # 多 Agent DAG 调度（并行/依赖/取消/检查点/汇总）
│   │   ├── workspace.py           # TaskWorkspace（Agent 间共享工作区）
│   │   ├── interactions.py        # 交互原语注册表（检查点 审批/调整/停止）
│   │   ├── events.py              # SSE 事件类型定义 + EventSink
│   │   ├── prompt.py              # system prompt 组装
│   │   ├── loop_controller.py     # 收敛治理（熔断/早停/反思注入）—— 待实现（空壳）
│   │   ├── journal.py             # Turn Journal（append-only 事实流）—— 待实现（空壳）
│   │   ├── resolve.py             # AgentResolver（prompt/模型/工具集解析）—— 待实现（空壳）
│   │   └── suspend.py             # 暂停/恢复 + 检查点持久化 —— 待实现（空壳）
│   │
│   │   # 注：编排已并入 runtime（不再有独立 orchestrator/ 目录）
│   │
│   ├── llm/                       # LLM 抽象层
│   │   ├── __init__.py
│   │   ├── protocol.py            # LLMProvider Protocol + 数据类型
│   │   ├── deepseek.py            # DeepSeek V4 实现
│   │   └── config.py              # 模型配置与 ModelTier 映射（fast/strong）
│   │
│   ├── tools/                     # 工具系统
│   │   ├── __init__.py
│   │   ├── protocol.py            # Tool Protocol + ToolBinding + 审批三态
│   │   ├── registry.py            # ToolRegistry（注册与查询）
│   │   ├── builtin/               # 内置工具实现
│   │   │   ├── __init__.py
│   │   │   ├── web/               # 联网：web_search(SearXNG) + read_url + _net 出网韧性
│   │   │   ├── file_ops.py
│   │   │   └── code_execute.py    # 代码执行（调用 sandbox）
│   │   └── sandbox/               # 沙箱子系统
│   │       ├── __init__.py
│   │       ├── protocol.py        # SandboxProvider Protocol
│   │       └── subprocess.py      # SubprocessSandbox 实现（MVP）
│   │
│   ├── memory/                    # 记忆系统：会话内历史（见 conversation/history.py）+ 长期记忆文件
│   │   ├── __init__.py
│   │   ├── conversation_title.py   # 会话标题生成 TitleGenerator + LLMTitleGenerator（会话摘要层已移除，见 Agent记忆与知识系统.md §1.3）
│   │   ├── user_memory.py         # 长期记忆：LLM 产出 ops（LLMMemoryExtractor）+ 确定性套用到 markdown（MarkdownMemoryApplier）
│   │   ├── store.py               # MemoryStore 协议 + FileMemoryStore（MVP：每用户一个 markdown 落在 data_dir/memory/）
│   │   └── maintenance.py         # maintain_user_memory：会话末 load→extract→apply→save 编排，全程兜底
│   │
│   ├── db/                        # 数据库层
│   │   ├── __init__.py
│   │   ├── base.py                # Engine / async_session_factory
│   │   ├── models.py              # SQLAlchemy ORM 模型
│   │   ├── repositories.py        # 数据访问层（Repository 模式）
│   │   └── migrations/            # Alembic 数据库迁移
│   │
│   └── core/                      # 共享基础
│       ├── __init__.py
│       ├── types.py               # 共享枚举、基础类型
│       ├── errors.py              # 异常定义
│       └── logging.py             # 日志配置
│
└── tests/                        # 当前仅 conftest.py；以下为规划测试目录（待补）
    ├── conftest.py
    ├── test_runtime/              # 执行核心 + 编排测试
    ├── test_tools/
    └── test_api/
```

### 模块职责边界

| 模块 | 职责 | 依赖 | 不依赖 |
|------|------|------|--------|
| `api/` | HTTP 入口，参数验证，SSE 响应 | schemas, conversation, middleware | runtime, llm, tools |
| `middleware/` | JWT 认证、限流 | db, core | 业务模块 |
| `conversation/` | 对话生命周期管理，stream_chat 入口 | runtime, db, memory | api |
| `runtime/` | 编排（计划生成）、ReAct 循环、多 Agent DAG 调度、交互原语 | llm, tools, core | conversation, api |
| `llm/` | LLM API 调用封装 | core | 所有其他模块 |
| `tools/` | 工具注册、执行、审批 | sandbox, core | 所有其他模块 |
| `memory/` | 长期记忆维护（ops 提取/套用 + 文件存储）+ 会话标题接口 | llm, core, config | db, runtime |
| `db/` | 数据持久化 | core | 所有其他模块 |
| `core/` | 共享类型和工具 | 无 | 所有其他模块 |

### 调用链路

```
POST /v1/conversations/{id}/messages
  → api.routes.conversations.send_message()
  → conversation.service.stream_chat()                # 入口：持久化用户消息、载入历史
      → conversation.history.load_history()           # 会话内历史（最近 N 条）
      → runtime.pipeline.run_chat_pipeline()          # ChatPipeline
          │
          ├─ [Prepare]
          │   → memory.store.FileMemoryStore.load()      # 载入用户长期记忆
          │   → runtime.prompt.assemble_system_prompt()   # 记忆包成 <rules> 软注入（单/多 Agent 共用基座）
          │   → 注册内置工具（web_search / read_url / file_ops / code_execute）
          │
          ├─ [Plan]
          │   → runtime.planner.make_plan()           # 用户请求 → 结构化计划；失败回退 single_agent
          │   → runtime.plan.parse_plan()             # 容错 JSON 解析
          │
          ├─ [Execute]
          │   · single_agent → runtime.engine.react_loop()
          │   · multi_agent  → runtime.runs.run_multi_agent()    # DAG 调度
          │       → runtime.engine.react_loop()        # 每个 step 一次 ReAct
          │       → llm.deepseek.stream()              # 调用 LLM（流式）
          │       → tools.registry.execute()           # 执行工具（并行，上限 5）
          │       → runtime.workspace                  # 上游产出写入 / 下游读取
          │       → runtime.interactions.wait()         # 检查点挂起，等用户决策
          │       → runtime.events.emit()               # SSE 推送（content / run_* / approval）
          │
          └─ [Finalize]                                # 在 conversation.service 内，pipeline 结束后
              → db.repositories.save()                 # 持久化助手消息
              → memory.conversation_title 生成标题       # 仅首轮；title_generated SSE 即时刷新侧栏
              → memory.maintenance.maintain_user_memory()  # 每轮 extract→apply→save，无变化则跳过写
  → SSE StreamingResponse → Client

> 注：loop_controller / journal / resolve / suspend 为规划中模块（当前空壳）；
> 检查点、prompt/模型/工具解析的职责暂内联在 runs.py / pipeline.py，后续抽取到对应模块。
```

---

## 三、前端结构（apps/desktop）

```
apps/desktop/
├── package.json
├── electron.vite.config.ts
├── tsconfig.json
├── biome.json                     # 代码质量（替代 ESLint + Prettier）
├── postcss.config.js              # Tailwind / PostCSS
├── vitest.config.ts               # 单元测试配置
│
└── src/
    ├── main/                      # Electron 主进程
    │   ├── index.ts               # 应用入口、窗口与 IPC 注册
    │   └── fs-service.ts          # 本地文件系统服务（FS IPC handler 实现）
    │
    ├── preload/                   # 预加载脚本（安全桥梁）
    │   └── index.ts               # contextBridge 暴露（含 window.fsApi）
    │
    ├── shared/                    # 主/preload/renderer 三端共享
    │   └── ipc-contract.ts        # 本地文件系统 IPC 契约（单一真相源）
    │
    └── renderer/                  # React 渲染层
        ├── index.html
        ├── main.tsx               # React 入口
        ├── App.tsx                # RouterProvider
        ├── router.tsx             # 路由表（HashRouter）：AppShell + 页面
        ├── env.d.ts
        │
        ├── components/            # UI 组件（按业务域分目录）
        │   ├── layout/            # AppShell（侧栏 + Outlet）, TitleBar
        │   ├── chat/              # ChatView, MessageList, MessageBubble, MessageInput,
        │   │                      #   TaskCard（任务卡片）, CheckpointCard（检查点）
        │   ├── graph/             # GraphView, GraphOverlay（全屏覆盖）, AgentNode,
        │   │                      #   StepEdge, NodeDetail
        │   ├── files/             # FileTree, FileTreeNode, FilePreview
        │   └── sidebar/           # Sidebar, ConversationList, ConversationItem, UserMenu
        │
        ├── pages/                 # 页面级布局（路由目标）
        │   ├── ConversationPage.tsx   # 对话页（聊天主区 + 详情）
        │   ├── MessagesPage.tsx
        │   ├── FilesPage.tsx
        │   ├── ToolboxPage.tsx
        │   ├── ExplorePage.tsx
        │   ├── MorePage.tsx           # 设置容器（嵌套子路由）
        │   └── more/                  # GeneralSettings / AppearanceSettings /
        │                              #   ShortcutsSettings / AboutSettings
        │
        ├── stores/                # Zustand 状态管理
        │   ├── conversation.ts        # 对话 + 消息状态
        │   ├── execution.ts           # 执行状态（Agent、步骤、进度）
        │   ├── graph.ts               # 图视图状态（节点、边）
        │   ├── ui.ts                  # UI 状态（视图模式、主题）
        │   ├── user.ts                # 用户信息
        │   ├── files.ts               # 文件树状态
        │   ├── sidebar.ts             # 侧栏状态
        │   └── __tests__/             # conversation / execution / sidebar 单测
        │
        ├── services/              # 外部交互
        │   ├── api.ts                 # REST 客户端（fetch wrapper）
        │   ├── streamConversation.ts  # SSE 流式对话（连接 + 事件分发）
        │   └── resolveCheckpoint.ts   # 检查点决策回传
        │
        ├── lib/                   # 工具库
        │   ├── elk-layout.ts          # ELK.js 图布局
        │   └── format.ts              # 格式化工具
        │
        ├── types/                 # 类型定义
        │   └── events.ts              # SSE 事件类型（api.generated.ts 待生成）
        │
        └── styles/
            └── globals.css        # Tailwind 入口 + CSS 变量
```

### 前端组织原则

1. **页面级布局（pages/ + router）**：全局只有 AppShell（侧栏 + Outlet），各页面自定义布局（对齐 [前端UX架构设计.md](前端UX架构设计.md)）
2. **components/ 按业务域分目录**：chat / graph / files / sidebar / layout，组件就近组织
3. **stores/ 集中管理**：多页面共享状态（如 execution 被 chat 与 graph 同时消费）；单测就近放 `stores/__tests__/`
4. **shared/ipc-contract.ts**：主/preload/renderer 三端共享的 IPC 契约单一真相源（本地文件系统）
5. **types/ 自动生成（待接入）**：`api.generated.ts` 规划由后端 OpenAPI schema 生成，不手动编辑
6. **shadcn/ui（待建）**：基础组件计划从 shadcn/ui 添加（`components/ui/`），当前直接用 Tailwind

---

## 四、部署结构（deploy/）

```
deploy/
├── docker-compose.yml             # 服务编排（server + postgres + redis）
├── docker-compose.dev.yml         # 开发环境覆盖
├── Dockerfile.server              # 后端镜像
├── nginx/
│   └── agentcore.conf             # Nginx 反代配置
└── scripts/
    ├── deploy-server.sh           # 服务器部署脚本
    └── init-db.sh                 # 数据库初始化
```

---

## 五、包管理与工具链

| 角色 | 工具 | 说明 |
|------|------|------|
| Python 包管理 | uv | 最快的 Python 包管理器，替代 pip/poetry |
| Node 包管理 | pnpm | 磁盘效率最高，monorepo 友好 |
| 代码格式化/检查 | Biome (前端)、Ruff (后端) | 取代 ESLint+Prettier 和 Black+isort |
| 测试 | Vitest (前端)、pytest (后端) | |
| E2E 测试 | Playwright | |
| 数据库迁移 | Alembic | |
| 类型生成 | openapi-typescript | OpenAPI schema → TypeScript types |

---

## 六、开发命令

```bash
# 后端
cd apps/server
uv run uvicorn agentcore.main:app --reload      # 开发服务器
uv run pytest                                    # 运行测试
uv run ruff check .                              # lint

# 前端
cd apps/desktop
pnpm dev                                         # electron-vite 开发模式
pnpm test                                        # Vitest
pnpm build                                       # 构建

# 类型同步
cd packages/scripts
pnpm generate-types                              # OpenAPI → TypeScript
```
