# 协作优先重设计提案（阶段 0：任务原型与理想协作剧本）🗂️

> **状态**：🗂️ **阶段 0 已定稿**（2026-07-14 产品负责人对 §七 十项决策点全部按建议拍板，v2 剧本一并定稿）→ 阶段 1 基线已记录 → 阶段 2 规划层已收口（形状均分 0.46→0.83+）→ **阶段 3 进行中：第 1 件组队门槛闸门 ✅、第 2 件返工环三件套 ✅（smoke r2 通过）、第 3 件用户确认卡代码已落地（B1/B2 + 方案挑选卡/风险确认卡，离线自检闭环，待毕业考真跑）**。v2 修订来源：主 Agent 自审 + 产品视角/技术可行性双路子代理审计（原型 5→7、每原型收敛一条硬增益、人介入收敛为单点主拍板、冲突清单 7→15 条、评测主指标改为质量对比）。
>
> **日期**：2026-07-14
>
> **触发**：产品负责人实测发现研究类请求 100% 坍缩进 `research_report` playbook（trace `6d89c7d8…`，形状永远是 并行调研→提纲→撰稿→审校），进而暴露更大的问题——协作机制普遍休眠，运行时常态是「派单 → 各自独工 → 汇总」。决定不打补丁，做协作层重设计。

---

## 〇、已拍板前提（2026-07-14 产品负责人）

> **协作是第一性、默认多 Agent。**协作不是可选优化，是产品的存在价值本身——如果大多数任务都路由到单 worker，产品与「单 Agent + 好看的可视化」无异。

本提案所有设计从这条前提正向推导。它推翻/动摇现有文档中的若干已落地决策，见 §一.3 冲突清单。

---

## 一、背景与证据

### 1.1 机制不缺，缺的是「发生」

协作机制在设计文档里相当完备（DAG 委派、嵌套 lead、便签墙、`escalate` 三档、`handoff`、波边界 `replan`、辩论主持人、检查点），但全量开发日志统计（119 次 `delegate`，含合成/评测流量，**无真实用户数据**，仅作方向参考）显示：

| 机制 | 触发量 | 解读 |
|---|---|---|
| `delegate` 派团 | 119 次（46 次单 worker） | 派单是常态 |
| playbook | 8 次，全部 `research_report` | 另两个模板 0 次；模板坍缩 |
| `replan` 波边界纠偏 | 1 次 | 「边干边调整计划」不发生 |
| worker `escalate` | 7 次 | 「干活中喊人」不发生 |
| 便签墙贴 / 读 | 32 / 43 次 | 零星，未成日常 |
| 辩论 | 39 场 | **唯一真正跑起来的可见协作** |

### 1.2 三层根因

1. **规划层：团队形状不来自任务结构。**「4 家公司 × 4 维度」的任务天然形状是按公司分组、组内拆维度、尾部横向对比；提示词却把模型漏斗进通用流水线模板（skill 散文 +「是就直接套」姿态 + schema 广告位三处合力）。换个主题形状一模一样。
2. **执行层：协作被设计成「自觉行为」，而模型不自觉。**便签、升级、返工全是 opt-in、靠散文期望快模型主动触发。项目已有实证：委派软护栏 A/B 测过「被模型无视且净负」后移除。**反例是辩论**——它的互动由 Moderator 确定性循环结构驱动，不靠自觉，也是唯一跑起来的协作。这是「协作长在结构里」路线可行的内部证明。
3. **度量层：没有「协作质量」的定义与评测。**好协作长什么样无可检验标准，优化全靠体感，陷入打补丁循环。

### 1.3 与现有已落地决策的冲突清单

**A 组 · 方向级冲突（P0 前提直接推翻/收窄的既有决策）**

| 现有决策（出处） | 本提案立场 |
|---|---|
| 「默认不拆、单 coherent worker 优先」（编排器 §团队形态判据） | **推翻**：改为「实质任务默认组队，按档位定队形」（§二 P0） |
| 「聊天优先 + 按需编排」（编排器） | **保留**：闲聊 / 单点事实 / 追问仍直答，协作优先只作用于实质任务（✅ 已拍板） |
| playbook「是就直接套」铺开姿态（2026-06-30） | **推翻**：模板降级为「形状词汇表」的教学示例，不再作为一键成品（✅ 已拍板，阶段 2 实施） |
| `finalize` 单 worker 直出（编排器） | **收窄**：仅留给机械单步小活（改一句话、转格式）（✅ 已拍板） |
| `complexity_hint=light` 档（编排器 §2.5） | 保留，对应机械小活档；注意 light 隐含裁掉协作设施，组队默认后其自动推断触发面收窄 |
| `coordination` 缺省 `none`（协作模式 §便签墙） | 档位联动重设：中档以上共享口径/接口/约束时默认 `wall`（§六 阶段 3 定） |
| MAST 参考「默认单 Agent」原则（多Agent系统难点参考 §一/§七） | **立场重写**：承认协调税，但产品的回答是「让协作真产生质量增益 + 分档控税 + 评测证明」，而非退回单 Agent（阶段 2 在该文档加产品立场注记） |

**B 组 · 结构级张力（技术审计查漏所得；剧本已按现实改写，标「阶段 3」的需结构解法）**

| # | 现有机制事实 | 对剧本的影响 |
|---|---|---|
| B1 | **协调态 × 检查点**：≥2 worker 默认协调时 `checkpoint_after` 不挂 durable 用户卡、只发 `BOUNDARY_YIELD` 事件 | ✅ 阶段 3 第 3 件已解：**含把关节点的批不进协调、强制经典阻塞路径**（durable 提纲卡全套机制现成；协调收益留给无把关批）。协调态 checkpoint 分叉保留为防御（replan 中途加把关节点时注入文案改为强制转 ask_user） |
| B2 | **开工卡跳过/叠卡规则**：同回合已确认阻塞 `ask_user` 则跳过 `team_preview`；未确认时先后两张卡 | ✅ 阶段 3 第 3 件最小重理：批含把关节点 → 开工卡**计划半边让位**提纲卡（能力授权半边照旧，安全刚需）；主拍板四选一通则进 CEO 提示词层。已确认 ask→跳开工卡的既有规则对新卡（挑选/风险确认同为 checkpoint_resolved）自动生效 |
| B3 | **协调预算（8 次注入）+ CEO 回合数成本纪律** | 中途互动变多（便签/升级/局部辩论/续派环）会烧预算，耗尽后事件降级为模板——✅ 阶段 3 第 2 件已改随批规模 `min(16, max(8, 节点+4))` |
| B4 | **嵌套 lead 子树阻塞语义（不变量 B）**：组内阻塞 escalate 直挂用户，不挂 CEO | 原型 1 重档的组内升级路由按此写实，剧本不假设「组内升级到 CEO」 |
| B5 | **便签墙可见域 = 单扇出批**：顶层 `seed_notes` 不下推 lead 子墙 | 重档分组的口径播种由各 lead 在子批自播（写进 lead 的 task），不假设树级一面墙 |
| B6 | **BYOK 全链路单模型** | 审查者/互评者/辩手的「独立性」= 角色 + 上下文隔离，**非模型多样性**；剧本按此设预期（多模型辩手在远期） |
| B7 | **契约门自动返工（1 次机械门）≠ 独立审查环** | 两概念分开：前者是落盘对账，后者是本提案要新建的质量环 |
| B8 | **认知分工「交付物结构归专家 worker」** | 不冲突但须划界：CEO 用形状词汇组的是**团队拓扑**，不是交付物的章节蓝图 |

**硬上限速查**（剧本形状须服从）：嵌套深度 ≤2；顶层单次 ≤10 任务；单 lead 扇出 ≤4；树级并发 8；同人唤回 ≤3 次/作者链；协调注入预算 8。

---

## 二、设计原则（提案，待批）

- **P0 协作优先（已拍板）**：实质任务默认组队。组队门槛线草案：任务具有**可分解结构**（多对象 / 多角度 / 多阶段 / 多部件）**或需要把关的质量面**（成篇、构建、决策、既有材料的审查）即组队；纯机械单步（转格式、改一句话）与闲聊/单点事实留单 worker / 直答。✅ 已拍板：连接词取「或」（产品审计的「且」方案否决——协作第一性优先，控税交给三档形状的轻档）
- **P1 结构优先规划**：团队形状从任务的自然结构推导（并列对象？流水线阶段？对抗视角？发散方向？部件拼装？既有材料多透镜？），规划层学的是**形状词汇表 + 组合规则**（§五），不是成品模板。自检判据：换个主题，形状还一模一样吗？
- **P2 协作长在结构里**：重要的互动（对齐、审查、返工、冲突消解）必须是计划里的**节点、边、共享面**，不是提示词散文里的期望。辩论是既有证明案例。
- **P3 协调税分档支付**：每个原型给轻/中/重三档形状（§三通则），规划层按任务规模选档。协作优先 ≠ 到处开满配——税要花在该原型的硬增益上。
- **P4 协作质量可评测**：**主指标 = 相对单 Agent 的交付质量（同任务单 Agent 基线盲评对比）+ 形状匹配度**；互动发生率只作辅指标且**只认「该发生处的发生」**——否则评测会奖励协作剧场（详见 §六 阶段 1）。

---

## 三、剧本通则（v2 新增，适用于全部原型）

1. **一条硬增益**：每原型只钉一条硬增益作为组队正当性来源（并行提速、便签互贴、人设发散等一律标次要，不得作为评测优化目标）。
2. **每任务恰好一个主拍板**：开工确认 / 提纲 / 挑选 / 风险确认 **四选一**；其余人介入降为例外升级（只有影响范围/预算/不可逆时才 `ask_user`）。「中途重大动态默认问用户」类设计一律删除——团队内消化。卡片合并/跳过规则阶段 3 随 B1/B2 重理。
3. **三档形状（降档条件）**：每原型写明轻/中/重三档与选档判据；默认中档，轻档保底、重档需任务规模或用户点名支撑。
4. **失败退路**：审查/验证/修订环到限（≤2 轮）仍不达标 → 不空转、不假装收工，向用户交代缺口与建议（对齐既有 4a 完工判定）。
5. **多轮接续**：剧本不止首轮——用户回来改需求/改稿时，默认带现场续派回原作者（同人续派机制已就绪），不重开全新团队。
6. **评测断言**：每原型的「理想形状 + 该发生的互动」即阶段 1 合成场景的断言来源，写剧本时即写断言。

---

## 四、七个任务原型的理想协作剧本

> 模板：例 → 触发特征 → **硬增益**（组队正当性）→ 形状三档 → 中途互动 → 主拍板 → 收尾 → 现状差距。（✅ v2 剧本随 §七 十项决策于 2026-07-14 一并定稿）

### 原型 1：多对象研究对比（宽度型）

- **例**：「分析 OpenAI / Google / Anthropic / Meta」「对比 5 款车 / 3 个城市」。触发特征：多个并列实体 × 多个维度，要求横向可比。
- **硬增益**：**口径一致性**——对比报告最大质量杀手是各实体口径漂移（统计时间、货币单位、来源可信度），用共享面播种统一口径 + 汇总前对账治它；单 Agent 长对比同样会前后不一。次要增益：并行提速、每实体不挤上下文。
- **形状三档**：轻 = 2~3 实体浅比，单 worker 直接写。**中（默认）= 每实体一名调研员（扁平）+ 横向对比员（`depends_on` 全部）**，`coordination=wall` + CEO `seed_notes` 播种口径。重 = 实体多且用户点名深覆盖时，每实体一个 lead（内拆维度 ≤4）+ 对比员；注意扁平「4 实体×4 维度」=16 节点超顶层上限 10，深覆盖**必须**走分组，且各 lead 在子批自播口径（B5）、组内升级直挂用户（B4）。
- **中途互动**：口径级决定贴 `decision` 便签；数据缺口 `escalate kind=dep` → 波边界 `replan(add)` 补定向调研。~~重大动态默认 ask_user~~（v2 删除：易假紧急、烦用户；仅当影响范围/预算时才升级）。
- **主拍板**：开工确认（实体清单、维度、口径预设）。
- **收尾**：CEO 对账口径一致性与实体覆盖均衡（4b），综述差异亮点，指引看对比表。
- **现状差距**：`research_report` playbook 抢走这类任务（形状错）；wall 缺省关；分组形状不被自发使用。

### 原型 2：深度成篇写作（深度型）

- **例**：「写一篇 XX 论文 / 深度报告 / 长文」（单主题深挖）。触发特征：单对象、成篇、证据密集。
- **硬增益**：**独立审查环**——审查者 ≠ 作者（自审是结构性盲区）+ 结构化问题清单 + 有界修订（≤2 轮）。「结构跟着证据走 + 提纲把关」是既有产品级增益，保留。次要增益：多角度并行调研（真增益来自强制不同信息源/问题清单，不是人多）。
- **形状三档**（2026-07-14 拍板改**审计后派**：独立审计不再预排在首张 DAG，改为成品落盘后 CEO 单独后派——冷眼进场独立性最彻底（B6 下独立性=上下文隔离）、审的对象=用户将拿到的落盘成品、环每步都是显式委派可计数可拦截；**系统只软提醒、绝不代派**）：轻 = 撰稿落盘 + 后派 1 审计员（短文，环 ≤1 轮）。**中（默认）= 角度调研×N → 提纲（主拍板）→ 撰稿（成品落盘）→ 后派独立审计（冷启动读成品全文，产出结构化问题清单）→ 修订环**（续派唤回原作者修订 → 审计员复核，≤2 轮）。重 = 审计升 2-3 透镜分工（事实核查/逻辑/受众），问题清单按风险分级合并。
- **中途互动**：调研员关键发现贴便签（次要，不作质量证明）；审校与作者真冲突 → CEO 仲裁（罕见才有价值）。
- **主拍板**：提纲检查点。~~已知结构缺口（B1）~~ ✅ 阶段 3 第 3 件已解：含把关节点的批强制走经典阻塞路径，durable 提纲卡（continue/adjust/stop + 批注引导下游）如期弹出。
- **约束写实**：修订环由 CEO 多轮 `delegate` 驱动（初派 + 1~2 次续派，0 次 replan 也可）；作者链唤回上限 3；审查独立性 BYOK 下仅角色级（B6）。
- **收尾**：CEO 报告审校发现了什么、修掉了什么（让用户看见质量环真跑过），交付成品。
- **现状差距**：现有骨架方向正确，但审校是终点不是环；CEO 不自发用续派做修订环。

### 原型 3：构建类交付（接口协作型）

- **例**：「做个网站 / 小工具 / 脚本」。触发特征：产出可运行物。
- **硬增益**：**真跑通**——独立验证者 + `code_verified` 客观验收（协作里少数可证伪的增益）。次要增益：模块并行、接口契约对齐（中大活才真）。
- **形状三档**：**轻（默认小活）= 实现者 + 独立验证者双人**（✅ 双人底线已拍板）。中 = 按活的自然模块拆分 + `wall` 接口契约广播 + 独立集成验证。重 = 接口定稿节点/lead + 前端 ∥ 后端 ∥ 测试。**前后端拆分是重档特例，不是默认队形**（产品面向大众，默认英雄场景不锁死在开发者任务上）。
- **中途互动**：接口/字段决定贴 `decision` 便签；测试挂 → 定向续派原作者修复（≤2 轮）；缺依赖 `escalate kind=dep`。
- **主拍板**：开工卡（含能力授权——安全刚需，保留；不再叠第三张风格问卷）。
- **收尾**：CEO 汇报「跑通了什么、怎么验证的」，附启动方式。
- **现状差距**：`build_feature` playbook 0 次使用；验证者独立无 enforce（criteria 未强制验证 run ≠ 实现 run）。

### 原型 4：开放决策权衡（对抗型）

- **例**：「两个 offer 选哪个」「该不该 all-in 某方向」。触发特征：无标准答案、多立场张力、用户要「想清楚」。
- **硬增益**：**结构驱动的真对抗**（Moderator 循环压力测试论点）——现状最成熟，也是「协作长在结构里」的内部证明。
- **形状**：沿用辩论编排（多立场辩手 → 逐轮交锋 → 修订对射 → 收敛 → 简报 + 用户拍板）。
- **补强**：① 拍板后衔接执行团队（转原型 2/3/7）——**缓做**（✅ 已拍板；连续性缺口而非协作形状缺口）；② **局部辩论 = 冲突消解的备用词汇**（其他原型中两 worker 结论真冲突时 CEO 可开小型辩论），**禁止**「默认冲突即开辩」——成本炸弹 + 稀释辩论招牌。
- **主拍板**：终局拍板（辩论已有）。
- **现状差距**：小。多模型辩手在远期（对抗独立性同为角色级，B6）。

### 原型 5：创意多方案（发散挑选型）

- **例**：「起名 / slogan / 海报方向 / 文案风格」。触发特征：审美主观、答案发散、用户要可挑选的差异化选项。
- **硬增益**：**人挑选**——多风格方案墙 + 用户挑选 + 中选作者带现场续派深化；「领导者做选择」是本原型的产品核。次要增益：多人设并行发散（同底座模型有风格坍缩风险，靠人设差异化播种约束 + 评测盯方案区分度）；交叉互评**降为轻量结构**（结构化短评、只挑受众错配/硬伤，防互相吹捧——不作质量证明）。
- **形状三档**：轻 = 3 方案并行、无互评、直接挑。**中（默认）= N 风格并行（N≤5）→ 轻量交叉互评波 → 方案墙挑选（主拍板）→ 中选深化**。重 = 挑选后多轮深化迭代。注意 N 风格 + N 互评双波节点数逼近顶层上限 10，N≥6 必分批。
- **主拍板**：挑选。✅ 阶段 3 第 3 件已落地「方案挑选卡」：`ask_user` 带 `card="proposal_pick"`（复用 choice 单选 + recommended + selected 回流，恰 1 问、选项 2–6；不新增交互 kind，移动端自然降级为普通选择卡）。
- **收尾**：CEO 报告各方案定位与互评共识，深化后交付。
- **现状差距**：互评波无结构承载；挑选卡不存在；挑选→深化靠 CEO 自觉拼第二轮。

### 原型 6：审查诊断（评估型）【v2 新增】

- **例**：「帮我看看这份合同有什么坑」「审下我的简历 / 方案 / 对外回复」。触发特征：用户带来**既有材料**，要「找坑、分级风险、可行动建议」，不是再写一篇。
- **硬增益**：**独立多透镜诊断**——条款/事实/风险/受众等 N 个透镜并行审 + 风险分级汇总；自审盲区是单 Agent 的结构性缺陷。法律垂直（`legal_answer_brief` 对方律师作战室）已是雏形，本原型把它抽象成平台通用形状——平台与垂直不再脱节。
- **形状三档**：轻 = 单审查员 + 红队复核双人。**中（默认）= N 透镜并行诊断 → 风险分级汇总员**。重 = + 对方视角红队（对抗压测，衔接局部辩论词汇）。
- **中途互动**：透镜间对同一条款/段落的发现贴便签防重复；发现按严重度分级汇总，**不中途打断用户**。
- **主拍板**：风险确认卡（哪些要改 / 要深究）→ 用户勾选后可衔接定向修订（转原型 2 修订环）。✅ 阶段 3 第 3 件已落地：`ask_user` 带 `card="risk_ack"`（choice 多选、选项 1–10、label 以严重度开头约定）。
- **收尾**：分级风险清单 + 每条的证据位置与建议动作。
- **现状差距**：无平台级形状（法律是 opt-in 垂直特例）；~~风险确认卡交互不存在~~（已落地，见上）。

### 原型 7：组合方案规划（拼装一致性型）【v2 新增】

- **例**：「自媒体启动方案」「旅行计划 / 婚礼筹备 / 开业筹备」。触发特征：多部件交付（定位/选题/变现；行程/住宿/预算）且**部件间必须互相一致**，用户要可执行方案不是散文。
- **硬增益**：**约束共享面 + 部件一致性对账**——预算上限/日期/人设红线播种为共享约束（wall + seed），尾部一致性对账节点专查部件接缝（行程超预算、选题与定位打架）；这正是单 Agent 长方案最容易前后打架的地方。次要增益：部件并行提速。
- **形状三档**：轻 = 单 worker 写全案 + 一名对账审查。**中（默认）= 约束播种 → 部件并行起草 → 一致性对账员 → 定向修订（≤2 轮）→ 定稿**。重 = 部件 lead 分组（大型筹备）。
- **中途互动**：部件间共享决定贴 `decision` 便签（「总预算 1.2 万、日均 xx」）；对账员发现冲突 → 定向续派修订。
- **主拍板**：约束确认（开工卡）**或**方案墙挑选，**二选一不叠加**——双检查点会把旅行计划做成项目管理软件。
- **收尾**：CEO 报告部件间一致性对账结果 + 方案总览。
- **现状差距**：这类请求现被漏斗进 `research_report` 或平铺扇出；一致性对账无结构承载。与原型 3 同族（部件 + 接缝 + 集成验证），验收从「真跑通」换成「语义一致性」。

### 次优先任务家族（不进主集，词汇可组合表达）

- **批量内容生产线**（一周 7 条脚本 / 系列邮件）= 口径定稿 + 并行产稿 + 抽检审校；**学习辅导/备考**（30 天计划）= 摸底 → 计划 → 练习反馈环；**数据分析** = 角度扇出 + 流水线。阶段 2 规划层用组合规则覆盖，暂不单列原型。（✅ 已认可）

---

## 五、形状词汇表（从剧本提炼，阶段 2 规划层的新教材骨架）

> 规划提示词教的是**词汇 + 组合规则**，CEO 按任务结构组合，替代成品模板。

| 词汇 | 语义 | 用于原型 |
|---|---|---|
| 并列对象分组 | 每对象一员（重档升 lead 内拆维度），尾部挂横向汇总节点 | 1 |
| 角度扇出 | N 个角度并行调研/产出，汇入下游 | 1、2、6 |
| 证据驱动流水线 | 调研 → 结构定稿（主拍板）→ 产出 | 2 |
| 独立审查 | 审查者 ≠ 作者；**成篇/构建类默认后派**：成品落盘后 CEO 单独派冷启动审计员读成品全文，产出结构化问题清单（默认 1 员，重档 2-3 透镜） | 2、3、6 |
| 有界返工环 | 审查发现 → 原作者带现场续派修订 → 复核，≤2 轮，到限交代缺口；**跨委派轮次的结构环**（派审计→清单→续派原作者→复核，每步显式委派） | 2、3、7 |
| 契约共享面 | wall + seed_notes 播种口径/接口/约束，decision 便签广播 | 1、3、7 |
| 独立多透镜诊断 | 对既有材料 N 透镜并审 → 风险分级汇总 → 风险确认卡（`ask_user card="risk_ack"` 多选勾选）→ 定向修订 | 6 |
| 部件一致性对账 | 并行部件起草 → 对账节点专查接缝 → 定向修订 | 7 |
| 对抗辩论 | Moderator 驱动交锋；顶层入口 / **局部备用**（真冲突才开） | 4、冲突消解 |
| 发散挑选 | N 风格并行 →（轻量互评）→ 方案挑选卡（`ask_user card="proposal_pick"` 单选）→ 中选深化 | 5 |

**组合规则示例**：多对象 + 成篇 → 分组 × 流水线；构建 + 并行模块 → 契约共享面 + 独立验证；审查发现要改 → 接有界返工环；任何原型中结论真冲突 → 局部辩论。

---

## 六、阶段规划

| 阶段 | 内容 | 出口 |
|---|---|---|
| **0（本文档）** | 对齐「好协作长什么样」：七剧本 + 通则 + 原则 + 冲突清单 | 产品负责人批改定稿 |
| **1 评测基线** | 七剧本做成合成 eval 场景（复用 `evals/` + `evals/mast.py`）。**主指标**：① 相对单 Agent 交付质量（同任务单 Agent 基线，盲评对比，BYOK 同模型公平）② 形状匹配度（规划 DAG vs 剧本预期档位）。**辅指标**：互动发生率（只认该发生处）、返工有效性（审查问题被修复率）、MAST 失败分布、协调税率（成本/延迟倍数 vs 单 Agent）。跑现状记录基线 | 差距量化报告 |
| **2 规划层重设计** | 结构优先规划提示词 + 形状词汇表替代成品模板；playbook 处置（降级为词汇表教学示例，✅ 已拍板）；组队门槛线落地（P0）；MAST 参考文档加产品立场注记 | 七场景形状匹配度达标 |
| **3 执行层结构补齐与默认值重校** | **结构缺口**（技术审计确认需新建）：有界返工环（计数 + 强制复核）、交叉互评波、方案挑选卡、风险确认卡、**协调态用户检查点等价物（B1，必解）**、审查者≠作者 enforce；**默认值联动**：档位 ↔ wall / 独立审查 / 检查点 / 协调预算；卡片合并/跳过重理（B2） | 主指标不降、互动发生率 / 返工有效性达标，协调税可控 |

**风险与对冲**：① 快模型规划复杂形状不稳 → 词汇表降低自由度成本，评测盯形状匹配度；② 成本/延迟上升 → 三档形状 + 协调税率持续记录；③ 「为协作而协作」恶化质量 → **主指标就是质量对比**，互动指标只认该发生处；④ 同模型角色独立性有限（B6）→ 靠结构化问题清单/透镜分工压榨角色级独立性，多模型留远期。

### 阶段 1 基线 v0（2026-07-14 · 改造前底片）

套件：`evals/cases/collab_shapes/`（七场景）+ `shape_score.py`（SSE `run_plan` 取团队图打分）；迭代基建后补 `--plan-only`（真实规划路径、worker 零执行，七场景 ×3 采样 ≈13 分钟 / $0.03）+ `--samples N`。

改造前基线：**形状匹配均分 0.46、L0 通过 4/7**。失败三类：**该组队不组队**（P1/P6/P7——P0 门槛线缺位的直接证据）、**组队但形状缩水**（P5 单 worker 包办 4 风格）、**形状对但交付翻车**（P2 修订环预算窒息只交过程汇报；P3 验证者无执行工具、成品只落工作区）。原型 4 辩论满分＝现状唯一成熟协作管线。**对比臂（团队 vs 单体盲评，n=2/臂）**：P1 并列对象扇出全面占优（质量胜 + 成本 0.58× + 更快——上限存在的证明）；P2/P7 输给单体（成本 4×），输因全在阶段 3 清单（返工环预算、超编防线、交付形态）。警示：浅 rubric 下 solo 也能及格，**协作增益必须靠对比臂量化**。

### 阶段 2 规划层收口（2026-07-14 ✅）

落地（提示词/描述三面）：CEO prompt 植入 P0 门槛线（可分解**或**质量面 → 默认组队；探路硬上限 2 次）、`team_orchestration_advanced` 重写为形状词汇表 + 组合规则 + 三档、delegate 撤 playbook 广告位。经 4 轮 plan-only 迭代收敛至均值 0.83（P1/P3/P4/P7 稳定 1.0；逐轮记录见 git 历史）。**可复用经验**：机械化触发规则（「≥2 并列实体＝每实体一员」「点名 N 风格＝每方案一员」）对 flash 模型比原则性表述有效一个量级。残余失败＝**随机 solo 塌缩**（~15% 采样「认规则 → 自我说服『我自己做更快』→ 违规」），提示词层已到边际 → 结构闸门进阶段 3。

### 阶段 3 第 1 件：组队门槛软闸门 ✅（2026-07-14）

引擎层 captain-only、每 run 至多一次（`engine.team_gate_nudge`）：调查工具累计 ≥2 次仍欲继续、或早期轮次无工具长正文欲直答 → 丢弃草稿注入门槛复核提醒。**软闸门**：给归类理由仍可自主直答；已 delegate 永不触发；worker 不受影响。验证（plan-only ×3）：均值 0.83→**0.91**，决策级 solo 塌缩 ~15%→0/21，P5 首次稳定 1.0。遗留观察：空参数 delegate 重试率（flash 已知毛病，真实运行轮次充足可自愈）、P6 轻档偶发 1 人队。→ 见代码 `engine/governance.py`（`team_gate_*`）、`tests/test_team_gate.py`。

### 阶段 3 第 2 件：返工环三件套 ✅（2026-07-14 · smoke r2 通过）

摸底根因（机制事实 + 推断）：① 协调态 `ALL_COMPLETED` 注入不携带成品——CEO 凭 80 字/人完成简讯写收尾 → 交过程汇报（接缝缺陷）；② 审校→修订→复核环零结构承载——CEO 惯用「同张 DAG 预排修订编辑」偷懒形（改稿者≠原作者、改完无复核）。拍板：**独立审计改后派**（成品落盘后 CEO 单独派冷启动审计，替代预排审校；理由见原型 2 注）；**系统只提示、绝不代派**。落地三件：ALL_COMPLETED 注入携带成品 + 固定审计提醒（`coordination/inject.py`）、审计软闸门（`engine.audit_gate_nudge`：首个实质批完成后欲收尾时注入，轻批 / 已二次委派不触发）、协调预算随批规模 `min(16, max(8, 节点+4))`。smoke 两轮（小改：all_completed 也注入 audit_gate、captain max_rounds 16→24）后**通过**：返工环真实发生（写作→独立审校→逐条修订）、CEO 自然收口交终稿而非进度汇报。留观察：审计软闸门真跑触发样本仍 0（逻辑靠单测背书）、worker 检索空转烧预算。逐轮 smoke 记录见 git 历史；质量毕业考（对比臂 vs 底片）留至第 3 件完成后。

### 阶段 3 第 3 件：用户确认卡（2026-07-14 代码已落地，**待毕业考真跑**）

对应 §六 阶段 3 清单的「方案挑选卡、风险确认卡、协调态用户检查点等价物（B1 必解）、卡片合并/跳过重理（B2）」四项，一批落地。核心设计取舍：

| # | 决策 | 理由 |
|---|---|---|
| 1 | **B1 取「含把关点的批强制经典阻塞」**（另一方向「协调态 durable 检查点」否决） | 带用户把关点的计划本质是「到点必停」的强顺序流程；经典路径 durable 提纲卡（continue/adjust/stop + 批注引导下游 + resume 续跑）全套已验证。协调态做 durable 挂起须从后台调度器里拆回合收口（挂起由 CEO 主线程发起才安全），高风险新机制换边际收益不划算。协调收益（活跃仲裁、审计闸门）留给无把关批 |
| 2 | **两张新卡复用 ask_user，不新增交互 kind**：`ask_user` 加 `card` 参数（`proposal_pick` / `risk_ack`），结构校验后把 card 写进卡片 payload 的 `intent`（扩为四值） | 新增 durable 交互 kind 要动 13+ 处契约（Suspension/事件/resume/recovery/sidecar/conformance/双端），且移动端未知 kind 会静默错 UI；ask_user 的 choice 单选/多选、recommended、selected 一等回流恰好覆盖两卡机制面。决策 #9「复用 checkpoint 卡机制心智」的字面落法。移动端零改动自然降级为普通选择卡 |
| 3 | **B2 最小重理**：批含把关节点 → 开工卡「计划预览」半边让位（主拍板=提纲卡）；「能力授权」半边照旧 | 授权是安全刚需不算主拍板；四选一通则的其余部分进 CEO 提示词（核心提示词「主拍板纪律」+ ask_user_midtask 两卡教学 + delegate_checkpoint/编排技能注记） |

改动清单 → 见代码：服务端 `ask_user/card.py`（card 校验）+ `tool.py`/`schema.py`（card 参数）+ `kickoff/gate.py`（开工卡计划半边让位）+ `coordination/`（含把关批入口跳过 + 防御转 ask_user）；桌面 `ask/ProposalPickBody.tsx`（方案墙单选）/ `RiskAckBody.tsx`（严重度勾选清单，label `[高]/[中]/[低]` 前缀出徽章）；conformance 新增 `proposal_pick_checkpoint` / `risk_ack_checkpoint` 两向量；移动端零改动自然降级为普通选择卡。验证：两卡渲染走 `#/preview` + shoot 离线自检闭环（交互态卡片 plan-only 覆盖不到）；B1/B2 走单测矩阵；全链路真跑并入毕业考（P2 对比臂含提纲把关路径）。

---

## 七、决策点汇总（✅ 2026-07-14 全部按建议拍板）

| # | 决策点 | 拍板结论 |
|---|---|---|
| 1 | 组队门槛线连接词：「可分解 **或** 质量面」vs 产品审计建议「**且**」 | 保留「或」，控税交给轻档（协作第一性已拍板） |
| 2 | 「聊天优先」保留范围：闲聊/单点事实/追问直答不变 | 保留 |
| 3 | playbook 处置：降级为词汇表教学示例（撤广告位）vs 彻底删除 | 降级，阶段 2 实施 |
| 4 | 构建小活「实现 + 独立验证」双人底线（成本 ↑ 换真跑通） | 接受双人底线 |
| 5 | 新增原型 6 审查诊断、原型 7 组合方案规划（v2 已写入） | 确认新增（双审计高置信度支持） |
| 6 | 主拍板通则：每任务恰好一张用户卡（四选一），其余降例外 | 按此定 |
| 7 | B1 产品要求确认：「主拍板必须是用户卡」（协调态检查点结构冲突的解法方向阶段 3 定，先确认要求本身） | 确认该要求 |
| 8 | 评测主指标 = 相对单 Agent 交付质量 + 形状匹配度；互动率降辅指标 | 按此定（防协作剧场） |
| 9 | 原型 5 方案挑选卡、原型 6 风险确认卡 两个新交互进阶段 3 | 纳入（复用 checkpoint 卡机制心智） |
| 10 | 原型 4「拍板后衔接执行」 | 缓——先做形状与结构缺口 |
