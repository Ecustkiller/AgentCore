# 远期规划（发布后演进方向）🗂️

> **定位**：收口两类「内测之后」的工作——§三 放量 / 正式发布前 backlog + §一/§二 发布后远期演进方向（**未必落地**）。发布前检查维度模板见 [`部署与运维.md` §7.8](/docs/05-平台与运维/部署与运维.md)。
>
> **治理**：本目录仅 `06-规划`；🗂️ = 远期想法、未承诺；每项标注**触发条件**（出现什么驱动力才启动）。任一项决策通过、开始落地后迁入对应正式文档（`01`–`05`），本条目退役。

---

## 一、架构演进（本地引擎已落地，剩离线 LLM + 跨进程两条远期触发项）

> **已落地（as-built 见 [`../02-架构/双模式工作区.md §十`](/docs/02-架构/双模式工作区.md)）**：sidecar 本地引擎——桌面 spawn `python -m agentcore.sidecar` 跑**同一 `run_chat_pipeline`**、stdio JSON-RPC、工作区直连本地盘；LLM 经云代理权威计费（Slice 4a，平台 key 不下放，岔路 B）+ 回合云端回写（`local-turns` 幂等）+ 结构化挂起本机帧（durable pause/resume）+ 内置 Python 打包（方案 B）+ `Journal` 端口 / 执行级事实。绑本机本地文件夹的对话走 sidecar，开关「本地引擎」已**默认开**、可关闭（启动失败自动降级回云端兜底 + 首次走 sidecar 前探活预检、偏好三态存储）。
>
> **驱动力（已识别）**：同一用户兼做「改本地真实仓」与「泛知识工作」，本地交互不能长期二等（「云端引擎遥控桌面」单通道在速度 / 并行 / 离线均受限）；beta 维持「默认云 + 打开文件夹经 `WorkspaceChannel` 实时改本地真文件」作兜底。
>
> **来源**：原 `02-架构/技术架构与基础设施.md §六`（曾名《架构演进提案》）。其中「客户端工具执行 + 本地优先 folder」（引擎在云、文件 op 遥控桌面）与「引擎本体搬到本地」（sidecar）均已落地、迁入 [`双模式工作区.md`](/docs/02-架构/双模式工作区.md)（§四 / §十）；其余借鉴项（统一 Run / WaveScheduler / `delegate` / Turn Journal / Interaction 挂起）归 [`执行引擎架构设计.md §八`](/docs/03-AI核心/执行引擎架构设计.md)。本节仅余下面两条**未落地、待触发**的演进。

### 1.1 完全离线（本地推理 + 本地消息库）

现 sidecar 是**混合态**：引擎在本地，但推理 / 持久化 / 计费仍借道云（见 [双模式工作区 §10.2/10.3](/docs/02-架构/双模式工作区.md)）。完全离线 = 把这几条也换本地实现，让整轮零云依赖：

- **本地 `InferenceGateway`**：接本地模型（Ollama / llama.cpp / LM Studio 等），不走 `/v1/inference` 云代理（复用 `LLMProvider` 抽象 + 工厂，见 §2.2）。
- **本地消息库**：sidecar 本机 SQLite 落消息，替掉云端 `local-turns` 回写（`ConversationStore` 端口本地实现）。
- **计费解耦**：离线 = 用户自有算力、不计费（本地 = free，`BillingSink` 空实现）。
- **价值**：真离线 / 隐私（数据不出机器）/ 零推理成本。
- **代价 / 就绪度**：[执行引擎 §8.6](/docs/03-AI核心/执行引擎架构设计.md) 的 10 宿主端口当前仅 3 个 Protocol 化（`EventSink` / `ClientRequestBridge` / `Journal`），其余 7 个仍具体实现——`ports.py` 明示「无第二实现时 Protocol 化 = 过早抽象」，故**按纪律留缝、不预抽**；本地模型质量 ≪ DeepSeek V4，大众定位下属 niche。
- **绑定项·JSON-RPC 云侧统一**：sidecar 桌面↔本地通道已是 JSON-RPC；本项可顺带把**云侧** SSE + HTTP（[执行引擎 §二](/docs/03-AI核心/执行引擎架构设计.md)）也统一为 JSON-RPC 运行时协议（云 / 桌面 / UI 一套代码、pause/resume/交互回值天然统一）。**但 SSE 事件契约已稳定对齐、无独立收益时重构纯亏**，故与离线绑定、离线不做则不动（**非 MCP**——MCP 仍用于 Agent↔外部工具）。
- **启动触发**：出现明确的隐私 / 企业自托管 / 真离线诉求。

### 1.2 跨进程子树分发（C2）

worker 委派现为**进程内** asyncio 子 task（`DelegationTransport` 端口仍具体实现）；跨进程 = 把委派子树分发到别的进程 / 机器（Redis durable queue + 跨进程挂起 BLPOP）。

- **价值**：多机扩容、本地内环 + 云端并行团队混合执行。
- **就绪度**：[`双模式工作区.md §五`](/docs/02-架构/双模式工作区.md) 多 worker 的锁 / 审批门 / 通道 / handoff 现为进程内态；限流已留 `RateLimiter` protocol 缝；挂起跨进程 Redis 见 [执行引擎 §暂停与恢复](/docs/03-AI核心/执行引擎架构设计.md)。
- **启动触发**：单机并行触顶 / 多副本部署 / 本地↔云混合执行成为真实诉求。

---

## 二、平台能力演进（抽象已预留、第二实现待启）

> 共性：抽象接口（Protocol + 工厂）或机制扩展点在 MVP 即预留、首实现 / 首模式满足发布；第二实现 / 强制模式属**发布后**增强，出现明确驱动力才启动——与 §一 Sidecar 同范式（「抽象先行、第二实现留缝」的能力域版）。

### 2.1 真实沙箱（SandboxProvider 第二实现）

代码执行默认走受限 `SubprocessSandbox`；**gVisor 第二实现已落地**（`GVISOR_ENABLED` 默认关）。余下是**公测生产化**与更强隔离（Firecracker / NATS worker）。

- **价值**：不可信代码安全执行、多租户隔离。
- **启动触发（已定）**：云端将面向**公众多租** → 沙箱硬化为**公测上线前必做**（见 [安全 §五](/docs/05-平台与运维/安全权限与治理.md)）；另含放开云端代码执行 / 代码执行成高频能力。

**目标架构细节**：

| 方案 | 结论 |
|---|---|
| gVisor（用户态内核拦截） | ✅ 已落地、默认关；公测前须生产化（含 `/workspace` overlay） |
| 独立小虚拟机（Firecracker） | 换带 KVM 机器后平滑升级 |
| 独立 Worker + NATS | API 永不接触 Docker；代码执行经 NATS request 转发到 worker 容器 |

gVisor 执行模型锁死项：默认禁止联网、只读根文件系统、内存/CPU/pids 上限、非 root。资源限额默认：256MB 内存 / 1 CPU / 128 pids / 默认禁网 / 并发 2。**公测生产化缺口**：给 `/workspace` 上 overlay（ro 下层 + tmpfs 上层），兑现「只读根 + tmpfs 工作区」，消除 cwd 在 ro 挂载上相对写静默失败。

→ 见 [`../05-平台与运维/安全权限与治理.md` §五](/docs/05-平台与运维/安全权限与治理.md)

### 2.2 多 LLM Provider（LLMProvider 第二实现）

默认 `DeepSeekProvider`（Flash + Pro）+ **已落地的 `ProviderRouter`**（按 `provider/model` 前缀分发）；其余模型家族（海外 / 本地 / 其他厂商）经 `OpenAICompatibleProvider` 接入，`llm/factory.build_router` 据已配厂商 key 组装注册表。

- **价值**：出海与多模型选择、客户指定模型、对冲单一供应商的可用性 / 成本风险。
- **代价**：各家 API 差异（工具调用 / 思考模式 / 流式增量）需在 provider 层抹平；定价表与计费每接一家加一份。
- **就绪度**：✅ 多 provider **路由已落地**——`ProviderRouter` + `OpenAICompatibleProvider`，`build_router` 据已配厂商 key 组装（Kimi / 智谱 / 豆包 前缀，配 key 即启用；空 key 退化为「只有 DeepSeek」零行为变化），已作「真·多模型辩手」执行支点（as-built 见 [`辩论编排设计.md §7.5`](/docs/03-AI核心/辩论编排设计.md)，各厂商接入事实 / model 串约定见 [`平台LLM接入.md`](/docs/05-平台与运维/平台LLM接入.md)）。BYOK「一轮一网关」凭据传播原则已定（[执行引擎 §8.7](/docs/03-AI核心/执行引擎架构设计.md)）。**仍属远期**：本地推理网关（Ollama / llama.cpp）、**模型目录 + 各家定价表 + 前端选模型 + 计费归因**（每接一家加一份）、跨家族质量档对齐。
- **被否决**：① OpenRouter 聚合网关 → 暂不（先连国内 Kimi / 智谱 / 豆包）；但「通用 OpenAI 兼容 provider + 路由器」地基对 OpenRouter 同样适用、将来可低成本接。② 逐家官方 SDK（Anthropic / Google 非 OpenAI 格式、多把 key）→ 否；统一走「通用 OpenAI 兼容 provider」一套。
- **启动触发**：出海需要海外模型 / 客户要求指定模型 / 对冲单一供应商风险。

### 2.3 治理强制检查点层（结构化挂起的强制模式）

> **来源**：原 07 草稿《结构化挂起后端落地设计》§4.7「治理强制注入层」**已退役删除**；该蓝图 2a/2b 全落地迁入正式文档后，唯一延后项迁出至此。

结构化挂起底座（`WaveScheduler.on_checkpoint` + `InteractionRegistry` 的 `plan_review` kind + 回合级 `paused_turns` 落盘 + `POST /resume`）已全落地，**首模式 = CEO 自声明** `checkpoint_after`（CEO 自愿在高危 step 后标记，用户 continue/adjust/stop，软挂起 timeout=continue 放行，已满足发布）；**第二模式 = 治理策略强制**：检查点由策略强制注入（非 CEO 自愿声明）、声明即必停（不可被 CEO / 用户跳过）、挂起点可审计留痕、超时取 `halt`（而非软挂起的 `continue`）。

- **价值**：合规 / 企业治理场景的「保证闸门」——某些高危 / 不可逆步骤必须由人确认，不依赖 CEO 自觉；审计留痕满足审查要求。
- **代价**：调度器 / 计费 / 事件层需区分「自声明 vs 强制」两类挂起；timeout 语义按类分流（软 = continue、强制 = halt）；强制注入规则的策略面与配置。
- **就绪度**：机制底座 + 首模式（CEO 自声明）已全落地（见 [`../03-AI核心/执行引擎架构设计.md` §暂停与恢复](/docs/03-AI核心/执行引擎架构设计.md)）；仅缺「强制注入 + 必停 + halt 超时 + 审计」这层策略。
- **启动触发**：出现明确的合规 / 企业治理需求（某类客户或审查要求强制人工闸门）。

### 2.4 评估体系：真实轨迹回流 + L2/L3 深化（数据 / 上线依赖）

> **机器已落地、缺真实数据**：评测新体系骨架（L0 契约硬门 + L1 rubric 裁判主轴 + milestone 协作 + baseline 门 + kappa 校准回路）已落地为现状，见 [`../02-架构/后端架构.md §五`](/docs/02-架构/后端架构.md)。本条只收口**依赖真实数据 / 上线**、开发期做不了的深化（四层里 L0/L1 已落地在跑，跑在现有合成 core + milestone 用例上）。

- **真实轨迹回流（现在可起步、缺数据 + 缺人工标注）**：从 `logs/dev.jsonl`(事件) + Postgres(消息正文) 按 conversation / trace **join** 挖 dogfood 真实任务 → 按 intent 聚类成分桶集（QA / 检索 / 写作 / 工具改文件 / 多步研究 / 团队任务…）；每个 bug / 复盘缺陷 → 一条回归用例。**人工 gold-set**：从这批真实轨迹抽 100–200 条标注，既作裁判 kappa 校准基准、又作 L1 高价值种子。
  - 价值：用例脱离「全合成且极小」的 smoke test；裁判校准门（kappa≥0.6）拿到可信基准 → baseline 硬门禁才敢上线。
  - 就绪度：校准 / milestone 机器 ✅；缺真实轨迹 join 脚本 + 人工标注（实现现实见 [`conversation-logs.mdc`](/.cursor/rules/conversation-logs.mdc)：正文在 Postgres、事件在 `logs/dev.jsonl`，须两边 join）。
  - **启动触发**：现在就可起（开发期 dogfood 数据已有），人工标注是唯一人力前置。
- **协作质量 · MAST「真数」闸门（多 Agent「学·度量」的在线半，机制全落地、唯缺真数 + API key）**：多 Agent 协作的「学·度量」离线/在线半已全就位——`turn_metrics` 四列在线看板（[`管理员后台.md` §四·协作质量 · MAST 看板](/docs/05-平台与运维/管理员后台.md)）+ 离线 `log_stats` + MAST 评测集（`evals/mast.py` 14 类 + `cases/mast/` 多 Agent 用例）已接入 `evals-nightly.yml`；**唯缺真模型回合跑出各 MAST 类 pass_rate / 首计划存活率**。
  - 两个前置：① **配 `EVAL_DEEPSEEK_API_KEY`（尚缺，唯一硬阻塞）**——建议指向低额度账号，未配则夜跑自动跳过真跑、仅留硬门禁；② 裁判 gold-set `labels.json` **已备 30 条**（AI 起草 / 人拍板，覆盖长度·自信·指令遵循·幻觉·过度拒答等裁判偏差探针，`--calibrate` 即用），**建议扩到 ≥100** 并复核边界条目（与上「真实轨迹回流」的人工 gold-set 互补：一为裁判校准探针、一为真任务种子）。
  - **产出后做什么**：用四指标 + MAST 看板**验证「拆 / 通 / 合 / 变」已默认铺开的协作姿态是否真减返工**——拆 lead B（[`编排器 §2.4`](/docs/03-AI核心/编排器与CEO主Agent.md)）/ playbook（[`编排器 §一`](/docs/03-AI核心/编排器与CEO主Agent.md)）/ 通便签墙 + 变 worker 拉取（[`Agent协作模式 §二`](/docs/03-AI核心/Agent协作模式.md)）。「首计划存活率」直接量「太依赖第一次编排」、也是 **lead B 值不值得铺开**的主证据；不达预期则回调提示词姿态（全是可退提示词、不动机器）。
  - **启动触发**：配上 `EVAL_DEEPSEEK_API_KEY` 即可起跑（gold-set 扩样为质量前置、非硬阻塞）。与下「提示词优化（数据驱动）」条共用「真跑评测」主线。
  - **开发期无真实数据**：当前无任何真实运行 / eval 跑分；一切「减没减返工 / 推多狠合适」均为**假设**，须等评测真跑出数才能验证——别把不存在的数据当既有事实。
- **提示词优化（数据驱动 · gated on 本节真跑出数）**：注入侧 prompt 架构已成熟克制，高杠杆不在再加架构、而在「先有量化方向盘、再精修少数高错率行为」——变体 A/B 注入 + 绝对分裁判 + 路由探针 + anti-slop linter 的**确定性 plumbing 均已落地**（见 [`../02-架构/后端架构.md §五`](/docs/02-架构/后端架构.md)），唯出数挂本节真跑主线（③ 的 seed 标签另需人工评审）。拿到数据后的 open 项：**③** 路由高错边界加 few-shot（比加规则更适合细腻判断，可顺带替换冗长规则瘦身）；**④** 用违规率判 `<output_style>` 该不该瘦；**②** 按角色 right-size 已落地，后续切口仍接此数据验证。**启动触发**：同上条。来源：原 `提示词优化方向.md`（🗂️ 已退役删除）。
- **L2 体验 / 信任 + 协作质量 nightly 深化（P1）**：可读性 / 诚实度 / 首字延迟 / 该追问是否追问 + 团队 milestone / 协作分；切片分析（按 intent / 质量档 / 路径 + 置信区间 + 分布）。信息性 → 逐步可门禁。**启动触发**：P0 稳定 + nightly 跑顺后。
- **L3 在线闭环（P2 · 上线后）**：上线后在线采样真实流量评分 → 自动聚类失败模式（LangSmith Insights 式）→ 低分轨迹自动进黄金集；评估 → 路由反馈（**gated 于路由引擎**落地）。**启动触发**：上线有真实流量；路由引擎落地。
- **不提前造飞轮**：AgentCore 未上线，故「生产轨迹飞轮」分两阶段（现在挖 dogfood / 上线后在线），不提前造在线采样基建（那是另一种「实现冒充需求」）。
- **来源**：原 07 草稿《评测体系重设计》的未落地远期部分（轨迹回流 + P1/P2）；已落地 P0 现状已迁入 [`../02-架构/后端架构.md §五`](/docs/02-架构/后端架构.md)，草稿退役删除（正文见 git 历史）。

### 2.5 交付前核验·重层审查（独立审查 worker，高风险触发）

> **来源**：原 07《交付前核验设计》（2026-06 实测复盘：宣传官网 / 建设工程法律实务会话）。轻层 `finish_guard`（造引用 + 代码围栏结构完整性）已落地、迁入 [`../03-AI核心/执行引擎架构设计.md` §四·交付前核验](/docs/03-AI核心/执行引擎架构设计.md)（→ 代码 `runtime/verify.py`）；本条收口**未落地的重层**，原草稿退役删除。

ReAct 唯一终止信号是模型自报 `done`——团队有验收结构、缺验收行为（典型事故：CEO 直答引用越界、worker 自报 done 无人回读产物）。轻层是「产出者机械自检」（纯代码、近零误报，已落地）；**重层 = 独立审查 worker**，仅对高风险交付启动（要跑 / 要重算 / 换眼睛找漏 / 回源对照），机械轻层够不着的语义/逻辑缺陷靠它兜。

> **协作生命周期定位（合·独立验证回合）**：在「拆 / 通 / 合 / 变 / 学」五环里这就是**「合」环的独立验证回合**——合环已落地的 **4a 完工判定 + 4b 语义边界对账是 CEO 可退提示词**（见 [`编排器 §收尾即验收`](/docs/03-AI核心/编排器与CEO主Agent.md)），本条重层是合环唯一「**新机制 + 每高风险回合真成本**」项。**2026-06-30 人明确暂不建**——4a 已 inline 覆盖；走「先开 §2.4 度量数据闸门、用数据证明 CEO 自检确实漏了『自己批自己』，再建」，与下方「挖坑探针缺陷率≈0」证据一致。

- **已定策略**：① 交付前核验是团队分内职责，非用户额外期望；② 责任留 CEO、动作下放团队（CEO 读短结论，不亲自当 QA）；③ 信号决定论触发——轻层宽触发、重层严触发（可运行物 / 关键数字 / 论证型成篇交付），品味走 `ask_user` 不进审查。
- **未落地硬点**：重层审查波次（`WaveScheduler` 自动追加审查尾节点）/ 分诊员（代码扫不出机械信号时廉价 LLM 判轻·重·品味）/ 核验呈现（审查节点可见、发现修正如实说、通过安静）/ 并行扇出拓扑（多 worker 并行时逐个核 vs 核拼装整体）/ `contract` 自动注入（轻层已决定论、合同规格校验仍靠 CEO `delegate` 时显式声明）。
- **价值**：高风险交付（可运行代码 / 带关键数字或法条 / 论证成篇报告）的独立复核——机械轻层结构上看不出、只有「真跑一遍 / 回源对照」才拦得住的一类。**法律垂直 GLAW 红队闸门 + 可辩护玻璃箱过程明确依赖此层**（见 §4.5 法律垂直）。
- **代价**：每次高风险交付多一轮独立 worker = 成本/延迟翻倍；触发判定与「谁修发现」（回炉原 worker vs CEO 补）的机制面。
- **就绪度 / 证据**：轻层 ✅；重层**有证据地暂缓**——2026-06 两轮「挖坑探针」（生成走真实模型、过轻层后用确定性地面真值判对错）显示**可判任务上缺陷率≈0**：算术 / 单一答案 25/25、代码必须真跑 20/20，故现在为此烧贵的重层不划算。探针已接 nightly 评测当回归绊线（`evals/cases/probe/` + `scripts/probe_code_execution.py`，软门禁 + 作业摘要）。残留风险只在「大而模糊」的语义交付（长报告埋一个错结论），那类确定性判不了、靠裁判或真实用户数据。
- **启动触发**：nightly 探针通过率抬头（缺陷率显著）/ 内测真实交付出现语义错成痛点 / 法律垂直启动需 GLAW 重层闸门。

### 2.6 角色记忆（跨回合复利的「学」环大赌注）

> **来源**：原 07《多 Agent 协作优化方向》§学·角色记忆（草稿退役删除，正文见 git 历史）。多 Agent「学」环两半——度量见 §2.4（在线看板 + 真数闸门），记忆即本条。

今天团队**跑完即忘**（`agent_id == run_id`，见 [`编排器 §六·Agent 实体化`](/docs/03-AI核心/编排器与CEO主Agent.md)）——协作不随时间复利，每回合从零组队、专才积累的项目约定 / 已验证来源全丢。把记忆扩成按 workspace / 项目作用域的「角色记忆」，让专才跨回合积经验。

- **价值**：跨回合 / 跨会话复利——专才记住项目约定、已验证来源、踩过的坑，少重复犯错。
- **代价 / 冲突**：直接冲突「**不做团队选择器、每回合按任务重组团队**」的现有赌注（见 [`编排器 §六`](/docs/03-AI核心/编排器与CEO主Agent.md)）——故是五环里**最大、最后下的赌注**。
- **最小下注版**：`revise` / `RunSession` 记忆按**角色 + workspace 持久化** + 给 worker 注入「项目约定」记忆；与 [`Agent记忆与知识系统.md`](/docs/03-AI核心/Agent记忆与知识系统.md) 终态、§三 backlog「持久 Agent 实体 / 记忆管理 UI」、§一 Agent 实体化 Phase 2 同一条线。
- **就绪度 / 前置**：**先用 §2.4 度量证明「一次性团队」在哪些活上吃亏**（跨回合该记没记住而返工），再下这一注——否则是「技术方便反推需求」。
- **启动触发**：§2.4 数据显示一次性团队在重复性 / 长项目任务上明显吃亏；或 Agent 实体化 Phase 2 启动。

### 2.7 文件夹级共享工作区 — ✅ 已收口为「项目即工作区」

结论已迁入 [`../02-架构/双模式工作区.md` §六](../02-架构/双模式工作区.md)：Folder 必绑 local/cloud，项目内会话共享 `folder:<id>`，裸聊仍 `conv:<id>`。详见该文档；本条不再保留远期方案细节。

### 2.8 Agent 行为审计合规深化（Phase 3 · 企业 / 合规需求验证驱动）

> **来源**：原 `多Agent协作审计功能.md` + `因果图可视化规划.md`（均已退役）——Phase 1–2 后端与桌面因果图 UI 已全落地并沉淀 [`安全权限与治理.md §八`](/docs/05-平台与运维/安全权限与治理.md) · [前端 UX §五·§十](/docs/04-前端/前端UX设计.md)。本条收口**未落地的合规与深化尾巴**，各按触发条件独立立项。

- **因果图可视化 UI · Phase 1–2 ✅**：Run 详情「数据从哪来」+ GraphView inject 聚焦高亮 → [前端 UX §五·§十](/docs/04-前端/前端UX设计.md) · [安全 §八](/docs/05-平台与运维/安全权限与治理.md)。

- **只读工具审计**（feature flag `audit_read_tools`，默认关）：当前只记副作用工具，只读工具（读文件 / 搜索）不入审计。**触发**：出现「读了什么也要留痕」的合规诉求。
- **可配告警阈值**：现 admin 看板超阈值高亮为硬编码「非零即警」；升级为可配阈值 + 设置项（不建规则 CRUD / 通知渠道——无真实数据、阈值只能是假设、通知渠道无需求验证）。**触发**：有真实运行数据可校准阈值。
- **合规审计 + 导出**：WORM 或签名链、法定格式导出（含 `causal_graph`）。**触发**：企业客户合规需求验证。
- **分级保留 / 冷归档**：JSONL 按月分片 → 对象存储；ClickHouse / BigQuery 有真实体量再议。**触发**：审计体量或留存合规要求超出主库 PG 90 天 TTL。
- **跨用户匿名聚合 + 异常检测**：内容哈希 + 采样。**触发**：滥用检测 / 合规分析成真实诉求（与 [`安全权限与治理.md §九·滥用防护`](/docs/05-平台与运维/安全权限与治理.md) 协同，审计是检测数据源）。
- **手机端审计视图**：RunDetailPanel 已有（任务/上下文/工具/输出/关系），有意省略诊断/审计；因果层与「数据从哪来」未做（边列表形态）。**触发**：手机审计层立项（对齐「手机 = 桌面 − 能力层」）。

---

## 三、放量 / 正式发布前 backlog（非内测阻塞）

> 内测可跑、放量 / 正式发布前补齐的长期项（原「就绪路线图 §二」迁入）。均为**已设计未落地**或**部分落地**，细节以各正式文档为准；与 §一/§二「发布后可选演进」不同，这些是「发布前应补、但不阻内测」。

- 自动更新的**灰度 / 双通道**（electron-updater 客户端 ✅ 已落地、远程熔断 `DESKTOP_UPDATES_ENABLED` / `GET /updates/policy` ✅ fail-open，见 [`../04-前端/前端技术与架构.md` §7.6](/docs/04-前端/前端技术与架构.md)；剩 `stagingPercentage` 灰度 / beta·stable 双通道依赖下面「特性开关架构」一项，构建/发布链路见 [`../05-平台与运维/部署与运维.md` §7.6](/docs/05-平台与运维/部署与运维.md)）
- 特性开关架构（[`../05-平台与运维/部署与运维.md` §7.9](/docs/05-平台与运维/部署与运维.md) 已设计、代码未落地）
- Redis 分布式状态（多副本前置）——限流已留 `RateLimiter` protocol 抽象作为注入缝
- pgvector / 文档树记忆（[`../03-AI核心/Agent记忆与知识系统.md`](/docs/03-AI核心/Agent记忆与知识系统.md) 终态）
- 语义搜索 **Tier 3**（⏳ backlog）：Tier 1 关键词全局搜索 + Tier 2 命令导航 **均已落地**（`CommandPalette`，见 [`../04-前端/前端技术与架构.md` §9.8](/docs/04-前端/前端技术与架构.md)）；Tier 3 = 在其上叠加 pgvector 语义检索（**仅此 Tier 未落地**）。与 Agent 侧 `code_search` 工具（tree-sitter + BM25，见 [记忆 §5.6](/docs/03-AI核心/Agent记忆与知识系统.md)）为**两层**：前者是 UI 全局搜、后者是 ReAct 按需调用的工作区检索工具
- 工程门禁（[`../05-平台与运维/部署与运维.md` §九](/docs/05-平台与运维/部署与运维.md)）：类型债棘轮 + 跨端契约 `gen:types` + god-file 棘轮 + **conformance 向量棘轮**（golden 导出口已建，从真实 turn pipeline / `turn_journal` 抓运行序列补向量仍待建；见 [`前端技术与架构.md` §十二](/docs/04-前端/前端技术与架构.md)）（部分未接 CI）
- 集中日志聚合 / 漂移哨兵（日志轮转 + structlog + `/readyz` 巡检均已落地，仅聚合与哨兵未接）
- 长文分段生成（案例1 #4，能力/质量项，**`file_append` 已落地**）：超长单文档仍倾向「分段写作 + 大纲优先」；`file_append` 工具 + 系统 Skill `long_form_writing` 已接入 worker 工具集与 CEO 能力目录。引擎级流式落盘仍不做（后 C1 边际收益小、破坏「工具原子执行+审批」模型、且不降单次延迟）
- MCP Client / A2A 协议（MCP 瘦接入细节 → [工具与能力系统](/docs/03-AI核心/工具与能力系统.md) §协议与安全）、持久 Agent 实体、记忆管理 UI
- 监控栈（Prometheus）、异地备份副本、E2E 聊天集成测试
- 内测全员群的**内测后归宿**：转放量 / 正式发布时该群保留为社区 / 拆主题多群 / 关停，靠 `chats.auto_join` 开关 + 特性开关控制（决策上下文见 [`../05-平台与运维/消息IM.md` §七](/docs/05-平台与运维/消息IM.md)）
- **终端后续演进**（M1–M3 均 ✅ 已落地，as-built 见 [双模式工作区 §四](/docs/02-架构/双模式工作区.md) / [安全 §三](/docs/05-平台与运维/安全权限与治理.md) / [前端 UX §十](/docs/04-前端/前端UX设计.md)）：① 进程退出 / 输出命中模式 → **主动通知 AI**（对齐 VS Code backgroundNotifications，替代下回合 `read` 才得知）；② AI 向用户终端写入（`send_to_terminal` 类，高危，需独立安全评审）；③ 云容器交互 shell / 云 dev server 端口预览（重投入另立提案）；④ M1/M2 观测滚屏由纯文本升级 xterm 只读渲染（ANSI 颜色）。**触发**：终端真实使用反馈

### 3.1 桌面端代码签名（Win + macOS）

内测档位刻意**不做** Developer ID / notarize / Win Authenticode（见 [`部署与运维.md` §7.6](/docs/05-平台与运维/部署与运维.md)）。放量 / 正式 GA 前补齐：

- **价值**：macOS 首次打开无 Gatekeeper 警告、自动更新 `quitAndInstall` 不再二次拦；Windows 减轻 SmartScreen / 右键菜单「未知发布者」摩擦。
- **macOS 就绪度**：`electron-builder.yml` `mac` 段未配 `identity`、`hardenedRuntime`、`entitlements` / `entitlementsInherit`、`notarize`；CI 无 Apple 证书 / API Key secrets。
- **Windows 就绪度**：[`前端技术与架构.md` §十三](/docs/04-前端/前端技术与架构.md) 仅有议题占位，证书方案未定。
- **启动触发**：对外放量或 App Store 外正式分发前；与 Win 签名一并规划落地。
- **来源**：原 07 草稿《Mac 桌面端部署设计》§五–§六（P0/P1 已迁入 [`部署与运维.md` §7.6](/docs/05-平台与运维/部署与运维.md)，草稿已删；正文见 git 历史）。

**发布前运维 checklist**（自 [`部署与运维.md` §7.8](/docs/05-平台与运维/部署与运维.md) 活清单迁入）：

| 项 | 缺口 |
|---|---|
| 推送 workflow | 5 个 workflow 需提交推送到 `master` 方在 GitHub 生效 |
| 关键密钥 + CI secrets | `PLATFORM_API_KEY`（生产）、`EVAL_DEEPSEEK_API_KEY`（CI 真跑）、ACR×3 + SSH×3~4、服务器一次性前置 |
| 定时单元上线 | `agentcore-backup.timer` + `agentcore-healthcheck.timer`、`ALERT_WEBHOOK_URL`、灾备演练 |
| 跨源 cookie 端到端 | `app://agentcore` + CORS + `SameSite=None` 就位，待真实后端全流程验证 |
| 官网下载 CTA | 待桌面端新版首发后 `releases/latest` 才解析 |

### 3.2 手机端原生壳上架（Play / App Store）

`apps/mobile` 的 **web 客户端与 Capacitor 基建已落地**（见 [`前端技术与架构.md` §七](/docs/04-前端/前端技术与架构.md)、[`项目结构.md` §四](/docs/02-架构/项目结构.md)）；web SPA 已部署 Cloudflare Pages（`m.fashitianxia.xyz`，→ [`部署与运维.md`](/docs/05-平台与运维/部署与运维.md)）。内测可跑 web 版；**原生壳上架与推送真机闭环**放量前补齐：

- **价值**：应用商店分发、后台 FCM/APNs 推送（pause 通知 deep-link）、Keychain / Keystore 安全存储。
- **就绪度**：Android 工程已 scaffold ✅；Bearer 鉴权 + `/v1/devices` + `@capacitor/push-notifications` 已接 ✅（→ [`认证与会话.md` §十](/docs/05-平台与运维/认证与会话.md)）；iOS 工程（需 macOS `npx cap add ios` / `cap sync`，仓库尚无 `apps/mobile/ios/`）⏳；release `VITE_API_URL`、Android keystore、商店元数据、无 `release-mobile` workflow（对比桌面 [`release-desktop.yml`](/.github/workflows/release-desktop.yml)）⏳；`google-services.json` + 真机 token 注册 + pause 通知 E2E ⏳；Keychain / Keystore 适配器已注入、待模拟器 / 真机验证 ⏳。
- **启动触发**：决定上架 Play / App Store，或内测需原生推送 / 安全存储时。
- **来源**：原 `手机端上架余项.md`（原 `手机端落地设计.md` 核心决策已迁入 [`前端技术与架构.md`](/docs/04-前端/前端技术与架构.md) §七 / §十二 与 [`项目结构.md`](/docs/02-架构/项目结构.md) §三–§四，草稿已退役）。

### 3.3 审计系列待办（结案后汇拢的 deferred / 独立项）

> **来源**：项目审计系列（首轮 + 9 专项，均已结案）退役时把仍 OPEN 的 deferred / 拆出独立项汇拢于此（持久安全不变量沉淀进 [`../05-平台与运维/安全权限与治理.md` §十](/docs/05-平台与运维/安全权限与治理.md)，逐轮结论正文见 git 历史退役的 `项目审计.md` §十一之五）。均**今日不可利用 / 非阻塞**，按触发条件各自立项。质量 / 评估线（真模型 L2 出数 / gold 人核 / L3 轨迹回流）已并入 §2.4、真沙箱（承 SEC-005）已并入 §2.1，下表不重列。

**安全纵深**：本组 deferred 已全部闭环，无 OPEN 项。

> connect-src 收紧 ✅ 已闭环（2026-07-01）：桌面 CSP `connect-src` 由宽策略（`'self' https: http: ws: wss:`）收窄为 `'self' <后端源> <同源 ws/wss>`（`desktop/main/index.ts` 的 `CONNECT_SRC`，按构建期 `API_ORIGIN` 推导，源不可解析时失败放开退回宽策略，不把自己锁在 API 门外）。排查证实后端 URL 是**构建期**烘焙（渲染层 `services/api.ts::BASE_URL` 与 CSP `__API_BASE_URL__` 同出一个 `VITE_API_URL`）、渲染层全部 fetch/SSE 同源、无 WebSocket / 无跨源 fetch，故收紧对真实流量**零误伤**；它把 connect-src 变成 `script-src 'self'` 背后的外泄墙（未来即便出 DOM-XSS 也无处 POST/开 socket）。
> SEC-007 pinned-IP transport ✅ 已闭环（2026-07-01）：`core/net.py::PinnedIPTransport` 解析一次 → 对已校验 IP 钉死建连、保 Host/SNI/证书校验，封 DNS-rebinding TOCTOU；read_url + favicon 已接，PoC + 回归见 `tests/test_net_pinned_transport.py`，不变量沉淀进 [`安全权限与治理.md` §十](/docs/05-平台与运维/安全权限与治理.md)。

**工程 / 可维护性**：

| 项 | 内容 | 触发 |
|---|---|---|
| AUD-010 whiteboard `engine.ts` 拆分 | 部分推进（2026-07-01）：①纯几何变换数学抽 `transform.ts`（+11 单测）②键盘快捷键策略抽 `keymap.ts`（+11 单测）③指针手势测试网补齐（engine.test 12→22，含多选 scale）④残余拖拽手势数学抽 `gestures.ts`（marquee/create 橡皮筋 + arrow 45°·+10 单测）；engine 1650→1424。**余有状态交互 FSM（PointerController）仍是内聚核**，待白板收口再拆 | 白板功能 WIP 收口后（避免动在飞的 WIP） |

**成本 / 性能**（已落地的 PERF-001 / PERF-003 / COST-001 见代码 + git 历史退役的 `项目审计.md` §十一之五·成本性能，下为余 deferred）：

| 项 | 内容 | 触发 |
|---|---|---|
| COST-004 提示预算软闸 | 现「仅观测」（埋 `cost.prompt_assembled`）；A 级「软闸 · 仅裁易变尾 order≥800」待真实负载数据再开 | 有真实负载 / 用量数据 |
| 余 8 条 P3 | token 防御纵深 / 回合尾延迟（标题 / 追问 / 快照阻塞流关闭）等 | 待真 profiler（慢查询日志 / py-spy 火焰图 / 用量报表）量化再排序 |

**白板纵深**（承 AI 协作白板专项，读图实跑 WB-002 = 配 `VISION_API_KEY` 后真跑 `board_read`）：

| 项 | 内容 | 触发 |
|---|---|---|
| WB-002 读图端到端实跑 | 人选视觉模型 + 配 `VISION_API_KEY` 后真跑 `board_read` | 选定视觉模型 + 配 key |
| WB-003 M3 v1 干净接缝 | 待后端文件信号补齐 | 后端文件信号就绪 |
| WB-004 实时协作 | 多人同画布，远期留缝不预建 | 多人协作成真实诉求 |
| WB-005 引擎状态机测试姿态 | ✅ 已覆盖（2026-07-01）：`engine.test.ts` 22 集成测试（命令 API + 9 类指针手势：resize/marquee/move/arrow+绑定/freedraw/eraser/pan/alt-拖复制/多选 scale）+ `keymap.test.ts` 11，引擎类由零直接测试转有回归网 | — |
| WB-006 多选 scale 手柄绘制却不可点 | ✅ 已修（2026-07-01）：渲染器按 `selectionHandlesScreen` 画多选 union 手柄，但 `engine.handleAt` 只 hit-test 单选（`size!==1` 直接 return null）→ 手柄可见不可点、scale 分支成死代码。归一 handleAt 到同一 `selectionHandlesScreen`（单一事实源），顺带修单选旋转手柄 hit-test 漂移 | — |
| WB-007 rotation 已铺管线·零产出 | 📌 reserved（2026-07-01 排查）：`rotation` 被 render / hit-test / persist 全链认，但**无任何 producer**——无旋转工具·手势·手柄、无工具栏/样式/菜单动作、AI `board_ops` 闭合动词集也不含（仅手改持久化 JSON 可置）。不画假控件故非 bug，属前瞻预留能力；已在 `types.ts` rotation 注记标 reserved，MVP 未暴露 | 补旋转手柄 + Pointer `rotate` 态（± board_op），随交互 FSM/PointerController 收口 |

**前端接缝（白板外·承 FE 排查第一批 + 第二批 2026-07-01）**：

| 项 | 内容 | 触发 |
|---|---|---|
| FE-004 质量档 UI（已永久移除） | 质量档机制已由用户统一 model（BYOK）替代；设置页 / `ModeSelector` / 词表已删。→ 见 [`前端UX设计 §十三`](/docs/04-前端/前端UX设计.md)、[`编排器 §2.1`](/docs/03-AI核心/编排器与CEO主Agent.md) | — |
| 移动端会话分享 parity | 桌面分享全链（建/列/撤 + 公开 SSR 页）已落；`apps/mobile` 无 `/shares` 客户端（非退役、无文档否决，属 mobile lite 减法未定） | 人定：要移动端分享时对齐 |
| 记忆动态 feed（load_map 已备） | `db/repositories/memory_updates.py::load_map` 注「预留跨会话记忆动态 feed」但仓库零调用 | 建「记忆动态」feed UI 时复用 |

> **开发期无真实数据**（`dev-process`）：成本 / 性能与质量项均**不凭猜优化**，待真实 profiler / 跑分出数再排序。

---

## 四、产品形态演进

> 发布后才考虑的产品形态方向在此累积，按 §一/§二 格式（价值 / 代价 / 就绪度 / 启动触发）。

### 4.1 人 + AI 混合群（消息页差异化形态）

消息页 IM 群聊里 `@` 唤起 agent → 接 CEO 编排，让 AI 作为群成员参与人类群聊（区别于对话页的纯 AI 团队群）。是消息页相对对话页的差异化卖点；数据层 `chat_messages.sender_type=agent` 已为此预留。

- **价值**：把 AI 团队能力带进人类社交 / 协作群——群里 `@` 某 agent 即可让它响应、查资料、执行任务，人与 AI 同会话协作。
- **代价**：要打通两套当前解耦的栈——IM（`messaging` service / `chat_messages` / firehose）↔ AI 执行引擎（CEO 编排 / `RuntimeEngine` / 事件流）。需处理：群消息识别 `@agent` → 转一次编排 run → agent 输出回写为 `chat_message`（`sender_type=agent`）→ 计费 / 限流归属 → 权限（谁能 `@`、agent 可见多少群历史）。
- **就绪度**：数据层 `sender_type=agent` 已留；IM transport（全员群 / 成员 / 实时 fan-out）与 CEO 编排引擎均已落地；**缺两栈之间的桥**（IM 消息 ↔ 编排 run）。
- **启动触发**：内测群验证人群聊价值后出现「群里直接调 AI」的明确诉求；或需把消息页做成区别于对话页的协作场景。

> **来源**：原 [`../05-平台与运维/消息IM.md`](/docs/05-平台与运维/消息IM.md) §七 P2 ⏳ 项，按文档治理迁入。

### 4.2 桌面端 OS 深度集成（macOS 优先）

TitleBar Mac 化（`titleBarStyle: hidden`、交通灯、`macTitleBarInsetClass`）✅ 已落地；余下原生感增强未做：

- **范围**：macOS 原生菜单栏、Dock badge；与 [`前端技术与架构.md`](/docs/04-前端/前端技术与架构.md) §7.3–7.7 同类远期项（全局热键 / Spotlight 召唤、右键「分析」、文件关联、taskbar badge）一并评估。
- **价值**：更像本地 Mac 应用，降低「Electron 壳」感知。
- **就绪度**：主进程窗口与更新 ✅；菜单栏 / Dock badge / 其余 OS 钩子未接。
- **启动触发**：Mac 内测反馈「不像本地应用」成高频诉求时。
- **来源**：原 07 草稿《Mac 桌面端部署设计》§五 P1+ 项（草稿已删；正文见 git 历史）。

### 4.3 「谁更聪明」多模型辩论宣传视频（真录）

主题「各大模型谁更有智商」天然是一场辩论——真跑一场跨厂商多模型辩论并录制出片，等于给辩论功能做一次真实压测。**区别于** [`apps/promo` 脚本化 Remotion 品牌片](/apps/promo/README.md)（主题「协作智能」、纯复刻渲染）：本片主题「谁更聪明」、真实对辩、横屏。

- **价值**：用最有说服力的真实对辩（豆包turbo × DeepSeek 等各自独立驱动）展示「真·多模型辩论」差异点，兼作辩论功能的真实压测。
- **就绪度**：真·多模型辩手能力**已落地**（[`辩论编排设计.md §7.5`](/docs/03-AI核心/辩论编排设计.md)，⏳ 端到端真跑待验）；**分镜未设计**；是否并入 `apps/promo` 或走 `#/preview` fixture 回放录制**未定**（前端录制路径见 [`frontend-preview.mdc`](/.cursor/rules/frontend-preview.mdc)）。
- **配套辩论形态演进**：方舟开通 GLM / DeepSeek 凑 **3+ 方圆桌**；打分 / 排名 / 锦标赛形态（模型目录 + 定价 + 选模型 + 计费归因见 §2.2）。
- **被否决**：竖屏社媒形态 → 否（用户选横屏品牌片形态）。
- **启动触发**：需要一支「谁更聪明」内容片；或 3+ 方圆桌 / 锦标赛成为真实产品诉求。

### 4.4 对话内协作深化（决策聚合驾驶舱 / 轮次摘要卡）

聊天页从「单对话消息流」向「可俯瞰、可回顾的协作空间」深化：把散落各回合的待决策项聚合成「待决事项」驾驶舱、长对话自动插入轮次摘要卡。

- **价值**：多 Agent 长任务里，检查点 / 发问 / 计划复核 / 队员升级散落各回合易漏；聚合成一份「待你拍板」清单（可跨回合乃至跨对话）＝老板一眼看全待办。轮次摘要卡降低长对话回溯成本。
- **代价 / 就绪度**：决策卡片体系（`CheckpointCard` / `PlanReviewCard` / `EscalationCard`）与跨对话感知（对话列表状态点 + 完成通知，[`前端UX设计 §一`](/docs/04-前端/前端UX设计.md)）已落地、聚合视图可复用其数据源；缺跨回合/跨对话聚合面板 IA 与「轮次摘要」的生成时机 / 触发规则。
- **被否决**：**Agent 产出「选中评注」（Figma / Notion 式文本选区批注 + 评论线程）**——属文档协作产品能力，偏离「多 Agent 工作台（团队替你跑任务）」核心心智，当前阶段性价比低，不做。
- **启动触发**：多 Agent 长任务成主流用法、用户反馈「待办 / 决策点难追踪」；或长对话回溯成高频痛点。
- **来源**：原 06 提案《聊天页面体验优化》方向 6（P2），按文档治理迁入（原文已退役，见规划索引）。

### 4.5 法律垂直（首个行业垂直 · hero 已落地、库接入等 open）

> **来源**：原 `法律垂直场景设计.md`（🗂️ 已退役，2026-06「先做法律」讨论 + 同场行业调研；M1–M3 as-built 与行业调研正文见 git 历史 + 代码）。是 [产品定位 §三「行业垂直深度」壁垒](/docs/01-产品/产品定位与品牌.md) 的首个落地实例。

**为什么是法律**：法律推理**天生对抗**，恰落在 AgentCore 唯一差异点（真多 Agent 团队 + 辩论互审）上；中国法律 AI 几乎全是「单模型 + RAG」、引用可靠性公认未解（Stanford RegLab：专业 RAG 仍 17%–34% 幻觉），多 Agent 对抗核验是结构性更优解且尚无人占据。故 hero 选**「对方律师作战室」**（把立场 / 草稿扔进去 → 一队 AI 对方律师先把你打一遍、预演每条抗辩 → 你的队加固），文书类型只是产物、退居次要——只有真正独立的对抗团队做得了，单模型 incumbents 复制不了。

**已落地（M1–M3 ✅，opt-in `legal_vertical_enabled` 默认关，纯资产层不改内核）**：hero「对方律师作战室」`legal_answer_brief`（答辩状）+ 第二支「三方视角案情研判」`legal_case_analysis`（接案评估 / 诉讼策略）——`runtime/legal_skills.py` + `tests/test_legal_skills.py` + conformance 三向量（`conformance/vectors/legal.py`）进 CI；来源卡编号台账方案①（终稿已核验法条 `[n]` 连来源卡、`[待核验]` 不编角标、`finish_guard` 造引用闸门）机制现状见 [`核心接口定义.md`](/docs/02-架构/核心接口定义.md)。反幻觉四件套（检索接地 → 核验 / 红队 → 人审闸门 → 审计留痕）为「真交付律师」硬底线。

**Open backlog**：

| 项 | 内容 | 触发条件 |
|---|---|---|
| 库接入（本地检索 Tool） | 内部 `Tool` Protocol 实现「本地检索 Tool」（`statute_search` / `case_search`），grounding 改「本地优先 → web 退避 → `[待核验]`」。**根因解**：M2 实测核验 churn（~40 搜 / 12 轮被引擎强制收口）根因 = 无权威本地源，提示词补丁对轻档无效（触补丁绊线，不再加提示词）。纯资产层、gating 仅 `legal_vertical_enabled` 时挂给起草 / 核验 worker、客户案情不外发、库 / 案情隔离。含**方案②**（citation schema 加法条原生字段 + dedup 改法条号 + 前端 `SourceCard`「现行有效」徽标，协议先行、与本项合流）。**属新基建、越界即触补丁绊线，待确认后再写码** | 用户法规 + 案例库数据导入就绪；v0 收口后启动评审 |
| 第二 skill M2 真跑 | `legal_case_analysis`（M1 骨架 + M3 fixture 已落地）缺真跑验证：仿 `scripts/probe_legal_war_room.py` 拿真实案例 A/B 两场景端到端（debate 原被告 → 法官 worker → 核验 → 收口落盘《接案研判.md》/《诉讼策略.md》），验三方独立 + 法官按举证责任研判 + 反幻觉（胜负定性为「倾向性研判，非判决预测」） | 排期 + 配 `EVAL_DEEPSEEK_API_KEY` |
| 待议 | ① 领域 Skill 载体迁市场 Skill 的时序（待该基建落地）② 落地表面：桌面对话 vs Word / WPS 插件（Spellbook 启示：律师在 Word 写字）③ 商业化档位：个人律师 / 小微 vs 企业法务 ④ B 多模型法律辩论纳入 v0 的时序（已部分落地）⑤ 案例能力时序：v0「用户提供 + 核验」→ 库导入接地 | 逐项按需立项 |

**依赖**：GLAW 红队闸门 + 可辩护玻璃箱过程依赖 §2.5 交付前核验·重层（法律垂直启动是其启动触发之一）。守设计纪律：一旦发现为法律改编排器 / loop / 调度，即停、提根因重设计（能力包抽象不够，而非该改内核）。
