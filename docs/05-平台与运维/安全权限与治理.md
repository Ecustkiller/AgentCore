---
status: landed
code: apps/server/agentcore/
related:
  - docs/05-平台与运维/认证与会话.md
  - docs/05-平台与运维/管理员后台.md
skip_if:
  - 只改登录流程（读认证与会话）
---

# 安全、权限与治理

> **状态**：工具审批三态、沙箱方案、凭据加密、治理边界 ✅ 已确定；Agent 行为审计 Phase 1 ✅ 已落地（§八）；权限模型 / 滥用防护 / 合规细节 ⏳ 待定（§九）。**安全审计系列已坐实的不变量见 §十。**

---

## 一、数据安全

**用户间隔离** ✅ 已落地：→ 见 [`认证与会话.md` §八](/docs/05-平台与运维/认证与会话.md)（IDOR 全库权威）。

Agent 间隔离、项目间隔离、传输/静态加密、数据驻留与生命周期 → ⏳ 见 §九。

### 对话导出与公开分享（导出对话 / 分享对话）✅ 已落地

> 把对话「带出产品」的两条路径，安全要点全在「代码看不出来的取舍」上。→ 见代码 `api/routes/conversations/crud.py`（export）、`api/routes/sharing.py`、`conversation/export.py`、`conversation/sharing.py`、`db/models/conversations.py:ConversationShare`。

**导出（私有下载）**：所有者把整段对话存成 Markdown（仅问答正文，给人读/粘贴）或 JSON（全保真，可再导入）下载。读**整段**转录（非滚动窗口，避免漏）、owner-scoped（非属主 404，IDOR）；花费等台账字段不进导出（在成本台账，不在消息体）。下载文件名由服务端净化并带 RFC 5987 `filename*`（承载中文标题），经 CORS `expose_headers` 放行供前端读取。

**分享（公开只读链接）**——决策与约束：

| 决策 | 取舍 |
|---|---|
| **冻结快照**而非实时只读 | 链接 = 创建那刻的内容拷贝（`conversation_shares.snapshot`）；之后编辑 / 新回合都不影响已有链接（所见即所享），杜绝「未来内容意外泄露」，也免去公开面实时鉴权 |
| **仅问答正文**（user/assistant 文本） | 快照只取对话正文，**剔除** reasoning / 成本 / 工具内部数据 / 引用等敏感或冗余面——公开面最小化暴露 |
| **服务端渲染 HTML**（非前端构建） | 公开页 `GET /shared/{token}` 直出自包含 HTML，无需装应用即可看；Markdown 经 `markdown-it-py`（`html=False` + 拒危险链接 scheme）渲染——未受信正文当数据、绝不当可执行 HTML / 活链接，杜绝 XSS。页面带 `noindex` |
| **不泄露存在性** | 未知 / 已撤销 / 格式非法 token 一律返回极简 404 页（不 500，也不区分「不存在」与「已撤销」） |
| **可随时撤销** | 软撤销（`revoked_at`）：撤销后公开页立即 404；创建 / 列出 / 撤销管理端点均 owner-scoped |
| **级联撤销** | 删除对话、或注销账户（匿名化）时连带撤销其全部分享链接——数据卫生与隐私一致性（→ `ConversationShareRepository.revoke_all_for_*`，调用点见 `conversations/crud.py` 删除、`auth.py` 注销） |
| 过期策略 | ✅ 新建默认 **30 天** TTL（`expires_in_days` 可选 7/30/`null` 永不过期）；到期公开页 404。仍可显式撤销 + 级联 |

> 这是统一 `share` 插件（[`后端架构.md` §二·平台业务](/docs/02-架构/后端架构.md)：文件 + 对话 + `share_token` + 过期）的**首个落地切片**，现以对话专用实现内建于 conversation 模块；统一插件仍 ⏳。

## 三、工具审批（Human-in-the-Loop） ✅ 已确定

### 核心问题

旧模型（布尔开关 `approval_required`）只有「每次都问 / 从不问」两档。批量场景崩坏：一次团队委派产出 76 个本地文件 = 76 次审批弹窗。审批疲劳的终点是用户无脑点允许——门还在，防护已经没了。

### 三态策略

审批级别是**工具的固有属性**（声明在工具定义处），判据 = 可逆性 × 副作用范围：

| 级别 | 语义 | 判据 | 代表工具 |
|------|------|------|---------|
| `never` | 静默执行 | 只读 / 纯计算 / 可信沙箱 | 检索、计算器、数据库查询 |
| `grantable` | 首次审批，可「本轮内都允许」 | 有副作用但局部可控，典型为批量发生 | 写本地文件、**Git 写入**（`add`/`commit`/`branch`/`checkout`）、执行代码、HTTP 请求 |
| `always` | 每次都审批，不提供放行 | 不可逆外部副作用 | 发送邮件、支付、删除外部数据 |

`always` 不提供放行是刻意的安全边界：「我允许写文件」不能被语境拉伸成「我允许发邮件」。

> **实现现状**：✅ CEO 聊天路径已落地——`ApprovalGate.authorize()` 对 `GRANTABLE` 工具挂起，SSE `approval_required` + resolve 端点回填；「本轮内都允许」已实现。委派 worker 本地模式经同款门。**不答语义（提问确认交互统一重构 D2）**：默认**无限期等待**（`approval_timeout_seconds` 默认 None，运维可设上限），卡片明示「等你拍板 · 不限时」——废除旧 300s 静默 auto-DENY；服务重启等失效场景由 `interaction_orphaned` 事实翻「已失效」灰态（lease 恢复重驱后发新卡），绝不静默拒绝。审批 required/resolved/orphaned 全量入 turn_journal（刷新/重启可恢复，pending 集 = journal fold），机制见 [执行引擎 §8.2](/docs/03-AI核心/执行引擎架构设计.md)。

→ 见代码 `runtime/approvals.py`、`api/routes/conversations/`、`tools/builtin.approval_class_tool_names()`（文件改动类 + `git` 口径）

**`git` 写入子命令**：`git` 工具 `schema.approval=NEVER`（使 CEO 注册表自动收录只读子集），但 `tool_call_requires_approval()` 对 `add`/`commit`/`branch`/`checkout` 升格为与 `file_write` 相同的 `ApprovalGate`。「本轮内允许所有文件改动」批量授权**同时覆盖** `git` 写入；代码执行类（`code_execute` / `test_run`）**可本轮放行**（对标 Cursor；`per_call_tool_names()` 现为空），仍**不被**文件类批量授权搭乘。

### 「本轮内都允许」的四条边界

| # | 边界 | 理由 |
|---|------|------|
| 1 | **turn 级**，非会话级/永久 | 每条用户消息是一个新的意图边界，上轮「整理文件」的授权不延伸到下轮「清理磁盘」 |
| 2 | **按「类操作」放行**，非全部工具 | 授权语义是「这类操作我信任」、不是「随便干」：默认粒度是【同名工具】（含 `code_execute` / `test_run` 的「本轮内都允许」）；文件改动另给一档【文件改动类】（`file_write`/`file_append`/`str_replace`/`file_delete`/`file_move`/`git` 写入子集一并放行，口径 = `approval_class_tool_names()`）。执行类**不被文件档覆盖**，但可对本工具名本轮放行（对标 Cursor；PI-004「注入搭便车」接受为本地可信用户的同款取舍） |
| 3 | **整棵 DAG/委派树共享** | 子 Agent 也执行相同操作，只对主 Agent 生效则形同虚设 |
| 4 | **新轮清场 + TTL 兜底** | 下一条消息永远从干净状态开始 |

### 交互设计

- 审批卡按钮：**允许一次 · 本轮内都允许 · 拒绝**；文件改动类工具（写/改/删/移/**git 写入**）多一档 **本轮内允许所有文件改动**（一次覆盖整类含 `git` 写入；代码执行类不含在内，但 `code_execute` / `test_run` 自身有「本轮内都允许」）；`always` 级只渲染两按钮（无放行选项）
- **执行确认面（对标 Cursor · 单一聊天内确认）**：
  - **`workspaceOp('execute')` / AI `code_execute`**：仅聊天审批卡——`workspace_op_required` 只在后端 `ApprovalGate` 放行后触发；主进程**不再**叠 OS「即将运行 python」框。
  - **用户直触 bash**（代码块「在终端运行」/ ClientTools「运行」）：聊天内 `RunConfirmPrompt` DecisionCard（取消 | 运行 | **本会话都允许**）；经 `rendererConfirmed` + `fs:grantSessionRun` 跳过 native。未带确认的旧 IPC 入参仍走 native 兜底。
  - **`openPath`**：仍白名单 + 单次 native 确认；不引入永久跨天 allowlist。
- `code_execute` 审批卡信息面 ✅：可本轮放行后，优化方向仍是**提高单次审批的信号质量 + 削减不必要弹窗**。① 卡片标题用模型自述的可选 `purpose` 参数（意图声明），并列展示由代码文本启发式派生的风险标签（联网/写文件/装依赖/子进程；只展示命中项、不做「安全」承诺——自述与派生分开，防模型话术掩盖代码行为）；② 参数预览默认 600 字符截断防 SSE 事件膨胀，唯 `code_execute.code` 放宽至 20K——代码本身是审查面，不能让用户批准看不到的代码（仍截断时卡片明示；`file_write` 主信号是路径，维持 600）；③ 工具描述引导「抓网页/调公开 API 优先 `read_url`/`web_search`」，削减纯网络读取走 `code_execute` 的弹窗（此类弹窗本不该出现）。根因解（本地沙箱默认禁网+工作区限定、越界才弹升级审批）→ 见 §五 演进方向
- 并行审批自动批量放行：多张待批卡，点一张放行、其余【同 scope】立即放行——「本轮内都允许」扫同名工具，「本轮内允许所有文件改动」扫整个文件改动类。**权威实现在后端门**：`ApprovalGate` 在放行落定时按 `list_pending` 回溯放行该 scope 所有【已挂起】审批（`_sweep_pending_tools`），关掉「兄弟卡 `approval_required` 还在 SSE 途中、点击时尚未进前端 store」的竞态；前端 `decideApproval` 的乐观兄弟放行只作即时反馈，与后端幂等共存（重复 resolve 对已结项是 no-op）。sibling 清扫的 journal 语义有 conformance 向量（`approval_sibling_sweep`）钉住。
- **委派级授权（团队开工一次拍板）✅**：CEO `delegate` 的团队若可能用到 GRANTABLE 工具，**worker 起跑前**挂起一张 `delegation_authorization` 卡（列 worker 预览 + 待授权工具清单），三选一：`grant_delegation`（整棵委派树内这些工具免逐次弹窗）/ `per_call`（保持逐次审批）/ `deny`（团队不启动）。与「本轮内都允许」正交：前者按**委派树**一次拍板，后者按**类操作**逐次放行。同 `execution_id` 复用已有 grant 不重复发卡。桌面 / 手机同卡（D4 补齐）。→ 见代码 `runtime/approvals.py::request_delegation_authorization`、`tools/builtin/delegate/drive.py`。
- 工具审批拒绝回填 ✅ 已实现：被拒调用向模型回注 `not authorized` 令其改道。超时语义已变更（提问确认交互统一重构 D2）：默认无限期等待、无自动 deny；运维可配 `approval_timeout_seconds` 上限，届期才视为 `deny` 回填

### 被否决的方案

- **永久 allowlist**：消费级跨天对话里永久放行 = 失去感知（本会话 / 本轮放行 ≠ 永久）
- **按 Agent 放行**：用户难以理解「放行的是谁」，DAG 对用户半透明
- **审批队列批量勾选**：UI 复杂，不解决后续调用还会弹的根本问题

### 演进路线

根答案是**可回滚性决定是否需要审批**。路线：本地写入前自动备份 → 一键还原 → 写文件从 `grantable` 降为 `never`（零审批、可撤销），审批只剩不可逆动作。三态模型为此预留平滑通道——只改工具声明级别，交互不动。

---

## 五、代码执行沙箱 ✅ 已确定

> AI 的「跑代码」工具（`code_execute`）的隔离边界设计。

> **MVP**：受限 subprocess + `SandboxProvider` 抽象。gVisor 第二实现已落地（默认关）；生产化 / Firecracker / NATS → 见 [`06-规划/远期规划.md` §2.1](/docs/06-规划/远期规划.md)。

### 5.1 为何需要独立沙箱

API 容器内子进程不构成安全边界——资源爆炸拖垮全站、子进程可达内部 DB、黑名单模型不可靠。

**威胁模型前提（产品决策）**：云端 server **将面向公众多租**（现内测、公测后开放注册）→ 不可信多租成立。故 `test_run` / 沙箱硬化（真隔离、越权拦截、资源/网络限额、工作区写模型兑现）列为**公测上线前必做**，不是「仅自用则可选项」。

### 5.4 默认关闭治理

> **实现现状 ✅**：云端 `location=server` 工作区默认**不注册**代码执行类工具（`code_execute` / `test_run`——`test_run` 跑测试即执行任意工程代码，执行力等价，同经 `code_execution_enabled_for` 一个闸；`CODE_EXECUTE_CLOUD_ENABLED=false`）；本地 / sidecar（`location=local`）保留 + 审批门。独立沙箱 worker ⏳ 远期。

**目标设计 ⏳（未落地）**：`code_sandbox_backend`（默认 `off`）与 `http_request_enabled`（默认 `false`）——关闭时工具不进入工具列表。

### 5.x SandboxProvider Seam

→ 见 [`核心接口定义.md` §三](/docs/02-架构/核心接口定义.md)（`SandboxProvider` 协议、`code_execute` 只依赖协议、失败语义映射为 `ExecutionResult` 不抛异常打断 ReAct 循环）。

**第二实现 ✅（默认关）**：`GVisorSandbox`（`tools/sandbox/gvisor.py`，`GVISOR_ENABLED`）已落地；工厂选择 `local`/`sidecar` → `SubprocessSandbox`（可信本机，**不**强制 gVisor），`server` + gVisor 门控 → `GVisorSandbox`；Firecracker + NATS worker 再后（见 [远期规划 §2.1](/docs/06-规划/远期规划.md)）。云端模型：**禁网**（`network_mode` 字段保留、当前不生效，网络恒 `--network=none`）、**只读根 + tmpfs 工作区**——`/workspace` 只读挂载项目、`/scratch` 可写临时区是设计取舍（非 bug）；本地 Subprocess 可持久写、gVisor 只临时写，属有意的模式差异。**已知限制**：cwd 落在 `/workspace`(ro) 时相对路径写会静默失败；**公测生产化**须给 `/workspace` 上 overlay（ro 下层 + tmpfs 上层）兑现「tmpfs 工作区」。**被否决**：Phase 2 主路径走 E2B/Modal 托管沙箱（与自建/BYOK 方向冲突，可作远期企业选项）。

---

## 七、凭据加密与密钥管理 ✅ 已确定

> **实现现状**：✅ 已落地。加密模块 `security.KeyEncryptor`（AES-256-GCM）；写入面 `llm/key_service.py`，读取面 `llm/resolve.py`（fail-safe，永不抛）。

用户敏感凭据加密入库，明文永不落库、永不返回前端。算法细节（wire format、回显掩码）与 BYOK 管理面交叉引用 [`成本配额与计费.md` §〇·五](/docs/05-平台与运维/成本配额与计费.md)。

### 失败行为（fail-closed / fail-safe 二分）

| 场景 | 行为 | 理由 |
|---|---|---|
| 凭据为必选（`billing_mode=byok`） | 生产启动即抛 `RuntimeError`（fail-closed）✅ | 拒绝带病启动 |
| 凭据为可选（`billing_mode=platform`） | 静默不可用（fail-safe）✅ | 平台兜底仍可工作 |

---

## 八、治理模块边界 ✅ 已确定

### 应用边界原则

「一受众一应用」：面向不同受众的系统（终端用户产品、管理后台、营销站）独立为单独应用，各自独立部署、隔离网络与主题，不混在同一路由组。

### 管理后台模块定位

→ 详见 [`管理员后台.md`](/docs/05-平台与运维/管理员后台.md)（独立 `apps/admin`、P0 契约、鉴权姿态）。

例外规则：域 service 无对应写方法的治理专用字段（如 `role`/`status`），由管理模块端到端拥有；一旦域将该写操作正式暴露为 service 方法，立即改为委托。

### 审计日志

管理操作写入 **append-only 审计表**，确保所有治理行为（下架、角色变更、停用）可追溯。

### Agent 行为审计（✅ Phase 1–2 已落地）

**为什么单独一层**：多 Agent 协作引入四类单 Agent 不存在、现有基础设施未直接覆盖的追溯难题——**决策归因**（文件谁写 / 谁触发，跨 CEO delegate / 上游 pass_through / 便签 decision）、**并行因果**（同波 worker 的输入依赖谁的产出）、**跨 Agent 数据流**（pass_through 注入 / 便签广播 / 文件指针的敏感流转）、**权限传播**（`can_delegate` 嵌套 / `tools` 收窄 / 审批 DAG 的**生效**权限）。**不重复建设边界**：Turn Journal 仍是回放 / UI 单一事实源，审计表是从 journal + 运行时钩子**投影**的查询视图（不替代，见 [执行引擎 §8.3](/docs/03-AI核心/执行引擎架构设计.md)）；`cost_events` 仍是钱的单一源（审计只引 `run_id`/`message_id`、不复制 token）；`admin_audit_logs` 只管人工治理操作（与 Agent 自动行为审计分表、分受众）。

**采集与 schema**：append-only `agent_audit_events` 专表，`AuditRecorder` **先 journal 后审计**偏序采集，写库走 **telemetry 独立池**（与正文写 primary 池分离，重度并行下永不抢正文连接，见 [成本配额 §三](/docs/05-平台与运维/成本配额与计费.md)）+ **回合内 `create_task` 异步投递 + 回合末 `flush`**（不阻塞热路径、失败不阻断回合、降级计数入 `turn_metrics.audit_drops`）；仅 delegate 回合写审计（单 Agent 纯聊天回合不写）。记录编排决策 + 副作用工具 + 审批 + Agent 间通信（流向元数据；便签 ≤200 字全文为唯一例外）+ 状态 / 失败 + 每 run 权限生效快照 + checkpoint 挂起 / 恢复 + 重试 `attempt` 序号 + 权限明细（`permission.tool_disabled` / `permission.write_conflict` / `approval.swept`）；不记只读工具与流式 delta。schema 取舍：去 `actor_run_id`（由 `actor_kind`+`run_id` 推导）、`seq` per-recorder 独立编号 + `(turn_id, seq)` **唯一约束**（幂等去重，容 at-least-once 重投不生重复行）、`target_ref` 存工作区相对路径。

**因果 · 查询 · 留存**：审计 API 可选返回 `causal_graph`——后端按 `depends_on`/`parent_run_id` **运行时重建** parent / depends_on / inject 三类边（`include_causal` 开关，`build_causal_graph` + 单测已就绪）。inject 上游取自 `run_context` wire 的 `blocks[].source_run_id`（`projector._context_inject_source_run_ids`）；修复前写入的历史行不回填，须新跑多 Agent 回合才有 inject 边。**桌面 run 详情 ✅**：多 Agent 回合经 `useTurnAudit` 请求 `include_causal`，「关系」区段内「数据从哪来」子块展示当前 run 的 inject 入边列表（默认折叠、可下钻上游 run）。**协作图 Phase 2 ✅**：GraphView 在 `litRunId` 聚焦时高亮 inject 邻域 + 补洞虚线；工具栏 toggle「始终显示审计数据流」。**⏳ 手机因果层未落地** → [`远期规划 §2.8`](/docs/06-规划/远期规划.md)。三入口：终端用户单回合 `GET /conversations/{id}/messages/{mid}/audit`、按文件路径反查 `GET /conversations/{id}/audit/file?path=`（均 owner-scoped）、运营 `GET /admin/audit/summary`（失败 / 审批超时 / 采集降级聚合，超阈值高亮）；GraphView 节点审计角标 → drill-in run 详情「活动记录」。留存 90 天 TTL sweep（`audit_retention_days` + 日级 sweeper）+ 删对话级联删审计行；审计走 REST 拉取面、不进 SSE conformance fold（`#/preview` 空态兜底）。→ 见代码：`runtime/audit/`（recorder 采集链 / projector 投影 / causal 重建），桌面 `hooks/useTurnAudit.ts` · `sections/RunCausalInject.tsx` · `GraphView.tsx`。

**关键取舍（被否决方案存档）**：① 专表 append-only 而非扩 `turn_journal`（否则污染 fold 回放契约、含 delta 破坏「投影非第二事实源」）；② 独立钩子先 journal 后审计偏序，而非塞 `EventSink.emit` 热路径（阻塞 SSE）；写库改 telemetry 独立池 + 回合内异步投递 + 回合末 flush（不抢正文连接、不阻塞热路径），仍 **best-effort**（未落盘持久化解耦队列——那会「崩溃丢未 flush 事件」，但审计定位不需合规级不丢；对比计费 `proxy_spend` 因涉钱才用 at-least-once 落盘队列，见 [成本配额 §三](/docs/05-平台与运维/成本配额与计费.md)）；③ 数据流只记流向元数据而非全文（避免第三副本、违反最小化）；④ 权限记每 run 生效快照而非仅审批（合规需证明「授权了什么」而非仅「出了什么错」）；⑤ 桌面因果 UI 仅画 inject、否决全量虚线叠加 / 迷你 DAG / ELK 为 inject 重布局 / audit 替换 handoff（→ [前端 UX §五](/docs/04-前端/前端UX设计.md)）。其余议题（受众 / 粒度 / 因果模型 / 查询面 / 留存分级）逐项论证见 git 历史。

**边界约束**：写冲突审计只覆盖实际走 `WriteCoordinator.claim()` 的路径（当前 `file_write`/`file_append`）；`str_replace`/`file_delete`/`file_move` 未走 claim——是否扩大 claim 覆盖属**行为变更、另立议题**，不混入审计。

**未落地尾巴**（只读工具审计 flag / 可配告警阈值 / 合规 WORM·签名·导出含 `causal_graph` / 分级保留 / 跨用户匿名聚合 / 手机端审计视图）→ 见 [`06-规划/远期规划.md` §2.8](/docs/06-规划/远期规划.md)。

### 内容审核策略

| 策略 | 描述 | 取舍 |
|---|---|---|
| **B（事后治理，首选）** | 保留创作者自助发布，管理侧抽查下架 | 零侵入发布链路、最快见效；代价是违规内容有窗口期 |
| A（前置过审，保留） | `draft→pending_review→published`，强制审批 | 零窗口期但侵入发布链路，违规压力上升时启用 |
---

## 九、待定

| 议题 | 说明 |
|------|------|
| 权限模型 | RBAC/ABAC 粒度、默认最小权限 vs 便利 |
| 数据与合规 | LLM 数据处理边界、私有部署、GDPR/数安法优先级 |
| 传输与静态加密 | TLS、静态数据加密策略、密钥轮换 |
| 审计与追溯 | ✅ Phase 1–2 后端已落地：决策 / 副作用 / 审批 / 通信 / 状态 / 权限快照 / **因果图 API** / 文件反查 / 权限明细 / TTL sweep + 用户 + admin 查询 API + 删对话级联删（→ §八·Agent 行为审计）。**桌面因果图 UI Phase 1–2 ✅**（run 详情 inject 列表 + GraphView 聚焦高亮）。未落地尾巴（合规导出 / 只读工具 / 手机端审计）→ 见 [`远期规划 §2.8`](/docs/06-规划/远期规划.md) |
| 滥用防护 | Prompt 注入、Agent 越权、资源滥用、信息泄露；机制优先级 |
| 沙箱上线 | `code_execute` 启用时机、是否独立执行机 |

---

## 十、安全审计已坐实的不变量（审计系列结论沉淀）

> **来源**：项目审计安全系列（第二轮安全 / 第三轮提示注入 / 第四轮前端 XSS / 第五轮 IPC 权限面 + 动态红队，均已结案退役）的**持久结论**沉淀于此——逐轮发现 / 修复 / 验证全程见 git 历史（退役的 `项目审计.md` §十一之五 + 各专项稿）。下表是**已坐实的安全姿态（现状，非待办）**；仍 OPEN 的纵深 / 独立项见 [`06-规划/远期规划.md` §3.3](/docs/06-规划/远期规划.md)。**整体：零未决 P0/P1。**

| 面 | 已坐实不变量（现状） | 锚 |
|---|---|---|
| 凭据 / 密钥 | BYOK key AES-256-GCM 落库、永不明文、响应仅掩码末 4、key 只进 provider `Authorization` 头不进任何 logger；prod 启动守卫拒占位 / 畸形密钥 | §七 · `security/keys.py`、`llm/resolve.py` |
| 多租隔离（IDOR） | 路由边界 scoped owner 检查（非属主 404）**+ 仓储层 owner 作用域结构默认**（SEC-002：conversation/board/folder 的 `get_by_id`/mutate `user_id` 必填，跨 owner 仅经显式 `*_unscoped()` 供可信内部/admin，边界测试守）；14 个 admin 端点全 `AdminUser` 门控 | §一 · [`认证与会话.md` §八](/docs/05-平台与运维/认证与会话.md) · `tests/integration/test_repo_owner_scope.py` |
| 注入 | SQL 全参数化、路径穿越单守卫 `resolve_safe_path`（祖先链 + realpath + 跳 symlink）、命令全 argv 无 shell；git URL scheme 白名单 | `tools/sandbox/`、`workspace/` |
| SSRF | 单一守卫 `core/net.py` `classify_url`：拒非 http(s) / 保留域 / 私网 / 回环 / 链路本地（169.254 云元数据）/ 组播；read_url + favicon + git clone 全过、重定向逐跳重校验**＋ read_url / favicon 再经 `PinnedIPTransport` 把已校验 IP 钉死建连（保 Host/SNI/证书校验），封 DNS-rebinding TOCTOU（SEC-007：pre-flight 与 httpx 连接时两次解析之间不留重绑窗口）**。**决策（2026-07-03·保持严格）**：不为开发/特殊部署放宽此守卫——开发机代理 fake-ip 模式（DNS 把域名应答成 `198.18.0.0/15` 保留段、被正确拦截、非生产缺陷）靠关代理 fake-ip / 改 redir-host 解决（`scripts/probe_egress.py` 诊断 + [本地开发.md](/docs/02-架构/本地开发.md) 排障），不加 dev-only 放行开关 | `core/net.py` · `tests/test_net_pinned_transport.py` |
| 限频 | auth per-IP 固定窗 + 账号锁定；XFF 取右起第 N 跳（防伪造绕限频） | `middleware/rate_limit.py` |
| CSRF | 仅无 access cookie 时才豁免 bearer（堵「cookie + 伪造 bearer」绕 CSRF） | `middleware/csrf.py` |
| 提示注入（AI） | 共享 base 提示 `<untrusted_content>` 把工具 / 网页 / 文件 / 记忆 / 跨 Agent 文本框定为「数据非指令」；`code_execute` 可本轮放行（对标 Cursor；PI-004 注入搭便车接受为本地可信用户取舍）+ 聊天审批卡唯一人门（不再叠 execute native）；`read_url` 新域名 + 长查询观测；记忆固化候选「指令样式」过滤；渲染侧 markdown 图降级点击外链（断零点击外泄信标） | `runtime/resolve/prompt.py`、`runtime/approvals.py`、`desktop/main/fs/execGate.ts` |
| 前端 XSS / Electron | 三端 CSP `script-src 'self'`（无 unsafe-eval）+ 桌面 `img-src 'self' data: <后端源>`（网络层拦渲染期信标，覆盖现有 / 未来图表引擎）**+ 桌面 `connect-src 'self' <后端源> <同源 ws/wss>`（收窄出网到唯一构建期后端源，作 `script-src` 背后的外泄墙；源不可解析则失败放开）**；renderer `sandbox:true` + will-navigate 守卫 + openExternal scheme 白名单（`isSafeExternalUrl`） | `desktop/main/index.ts`、`shared/safe-url.ts` |
| IPC 权限面 | 文件 op 根显式授权 + `resolveLexical`/`realInside` 双守卫（无穿越）；execute 仅聊天审批；bash 用户直触走聊天 RunConfirm（native 仅兜底）；`openPath` 危险类型仍单次确认 | `desktop/main/fs/execGate.ts` |
| 沙箱 / 代码执行 | cloud 代码执行类（`code_execute` / `test_run`）默认关；开 cloud 但无 `code_execute_cloud_unsafe_ack` 即拒启动（不再静默 RCE） | §五 · `tools/sandbox/subprocess.py` |
| 依赖 / 供应链 | 全 monorepo 依赖漏洞归零（JS dev 工具链 + Python 升级）；`--prod` 扫描本就 0 | `pnpm audit` / `pip-audit` |

> 上表是「**为什么我们现在是安全的**」可辩护证据链的浓缩；逐条根因 / PoC / 回归测试见 git 中退役的专项稿（`项目审计-安全专项.md` 等）。§九「待定」的「滥用防护 / 权限模型」仍为开放设计议题，与上表「已坐实」不冲突；Agent 行为审计 Phase 1 见 §八。
