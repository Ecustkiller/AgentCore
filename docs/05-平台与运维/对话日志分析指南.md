---
status: reference
code: apps/server/agentcore/observability/,apps/server/scripts/log_stats.py,apps/server/scripts/log_timeline.py
related:
  - docs/02-架构/后端架构.md
  - .cursor/rules/conversation-logs.mdc
skip_if:
  - 不涉及产品 AI 日志排查 / trace 分析
---

# 对话日志分析指南

> **Why**：产品 AI 回合的结构化事件在 `logs/dev.jsonl`（JSONL），完整正文在 Postgres——查失败回合靠 **trace_id 全链路**，不是搜 Cursor 转录。
>
> **How（操作步骤权威）**：用户给出 32-hex / UUID / 排查包时 → **先读** [`.cursor/rules/conversation-logs.mdc`](/.cursor/rules/conversation-logs.mdc)。本文不重复 CLI / 手工查询长文。
>
> **默认证据面**：`log_timeline` 人读与 `--json` = **`decision_spine`**；全量事件流用 `--raw`。现码 `spine_events` = **turn_spine**（会话 turn 目录），不是产品面。
>
> **排查包（P1）**：`--pack <dir> --trace <tid>` 写出可投递目录——必有 `decision_spine.json`（契约 v0）+ `timeline.jsonl` + `meta.json`（含 `schema_version` / traffic / jsonl 断档 / 导出时间）；有 join 时附 `turn_metrics.json` / `messages.preview.json`；显式 `--full` 才带 `messages.json`（无 LLM body）。另一会话只读包内文件即可复盘。
>
> **Dogfood 入槽（P2）**：出包后仓库根 `python evals/dogfood/fill_from_pack.py --pack <dir>` 写入空 `pending_label`（只填真实 id；三维分可选人工）。最短路径 → [`evals/dogfood/README.md`](/evals/dogfood/README.md)。

## 接缝（日志 ↔ DB）

| | 产品 AI 日志 | Cursor IDE 会话 |
|---|---|---|
| 位置 | `logs/dev.jsonl`（仓库根；20MB 轮转） | `agent-transcripts/`（与产品无关） |
| ID | `trace_id` = **32-hex 无连字符**；`conversation_id` = UUID | 文件名 = 带连字符 UUID |

- **全链路键**：`trace_id`（同一次用户交互，含进程内委派）；会话 → `conversation_id`；单次回合运行 → `attempt_id`；持久回合 → `message_id`（≡ journal `turn_id`）。
- **`chat.turn_start.via`**：执行路径 `sidecar|cloud`，与工作区 `location`（local/server）正交；decision_spine head 会抽取。
- **DB join**：`messages` / `cost_events` / `run_sessions` 带 `trace_id`（assistant / 落账有值；user / handoff 常为 `NULL`）。键集合权威 → [后端架构 · Context 注入](/docs/02-架构/后端架构.md)。
- **不落完整正文**；`preview` / `reply_preview` ~200 字；`LOG_LLM_BODIES` 开时有截断脱敏 `llm.request`/`response`。
- **模块入口**：事件注册表 `observability/catalog.py`（`sync_log_event_registry.py` 生成）；查询层 `observability/query/`；CLI `log_timeline.py` / `log_stats.py`。新增埋点写 `logger.info("component.action", …)` 后 sync——**勿手改**下方事件表。

## 事件词表

<!-- BEGIN AUTO:log-event-catalog -->

> 全量 **1015** 个在用事件登记于 `apps/server/agentcore/observability/catalog.py`（权威源，勿手改）；下表只列 **61** 个带说明/字段富化的重点事件。更新：
> `cd apps/server && uv run python scripts/sync_log_event_registry.py && uv run python scripts/gen_log_event_docs.py`

| 事件 | 说明 | 关键字段 |
|---|---|---|
| `approval.sandbox_auto_pass` | — | `tool`:str |
| `approval.timeout` | — | `tool`:str |
| `auth.login_failed` | 敏感操作审计：登录失败（password/unknown/locked/mfa/role；无明文凭据） | `method`:str, `platform`:str, `reason`:str, `subject`:str, `user_id`:str |
| `auth.mfa_enrolled` | 敏感操作审计：Admin MFA 绑定确认成功 | `user_id`:str |
| `auth.mfa_recovery_used` | 敏感操作审计：Admin MFA 恢复码成功消费 | `user_id`:str |
| `billing.background_platform_auth_fallback` | 后台 chrome 平台 key 被上游 auth 拒绝后一次回落用户 BYOK | — |
| `chat.regenerate_rejected` | regenerate 早退拒绝（会话不存在 / 目标非用户消息或已删除）；排前端传错 id | `conversation_id`:str, `found_role`:str, `message_id`:str, `reason`:str, `user_id`:str |
| `chat.resume_complete` | 暂停恢复回合收尾（终态带协作计数；STOP 终结不带） | `boundary_yields`:int, `delegated`:bool, `duration_ms`:int, `escalations`:int, `finish_reason`:str, `revises`:int, `scope_signals`:int |
| `chat.turn_complete` | 回合收尾（含 Phase-0 延迟：prepare/assemble/ttft_*；model/credential_source） | `assemble_ms`:int, `boundary_yields`:int, `credential_source`:str, `delegated`:bool, `duration_ms`:int, `escalations`:int, `finish_reason`:str, `input_tokens`:int, `model`:str, `output_tokens`:int, `prepare_ms`:int, `provider_id`:str, `reply_preview`:str, `revises`:int, `rounds`:int, `scope_signals`:int, `ttft_content_ms`:int, `ttft_reasoning_ms`:int, `workers`:int |
| `chat.turn_start` | 回合起点（preview/chars/history） | `chars`:int, `history`:int, `location`:str, `preview`:str, `via`:str |
| `compaction.done` | 长对话压缩成功（folded/kept/summary_chars） | `conversation_id`:str, `folded`:int, `kept`:int, `summary_chars`:int, `trigger_input_tokens`:int |
| `compaction.failed` | 长对话压缩失败（顶层异常；不推水位） | `conversation_id`:str, `error`:str |
| `compaction.schedule_failed` | 压缩调度 due 判定异常 | `conversation_id`:str, `error`:str |
| `compaction.timeout` | 长对话压缩 LLM 超时（空摘要；不推水位） | `conversation_id`:str |
| `cost.ledger_write_failed` | — | `error`:str |
| `cost.prompt_assembled` | 系统提示装配观测（段 chars + assembly_hash；零行为副作用） | `assembly_hash`:str, `over_soft_cap`:bool, `scope`:str, `sections`:dict, `soft_cap`:int, `total_chars`:int |
| `cost.recorded` | 回合落账成功（含 by_role 角色拆解） | `by_role`:dict, `models`:list, `runs`:int, `total_nano`:int, `total_usd`:float |
| `delegate.completed` | 委派批次完成（escalations/scope） | `escalations`:int, `scope`:int, `scope_ratio`:float |
| `delegate.completion_criteria_hoisted` | 历史兼容（S3 前）：criteria hoist；现行不发 | — |
| `delegate.completion_criteria_ignored` | S3：CEO 误传已删 completion_criteria 时打点（忽略字段，非硬闸） | — |
| `delegate.completion_criteria_unmet` | 历史兼容（S3 前）：按 kind 硬判未满足；现行不发 | — |
| `delegate.continuation_ok` | — | `run_id`:str |
| `delegate.continuation_rejected` | — | `cause`:str, `reason`:str, `run_id`:str |
| `delegate.run_redirect_hot` | redirect 热修续派（revise 重算桶，与 continuation_ok 同义） | `cancelled_run_id`:str, `continuation_run_id`:str, `execution_id`:str, `recall_count`:int |
| `delegate.started` | 编排委派开始（agents/plan/waves） | `agents`:list, `call`:str, `nodes`:int, `parallel`:int, `plan`:list, `waves`:list |
| `delegate.yielded` | 委派中途让出（replan 边界） | `reason`:str |
| `engine.ceiling_finalize` | 收敛治理：硬顶强制收尾（reason=max_rounds 轮预算耗尽 / token_budget） | `reason`:str, `rounds`:int, `thrashing`:bool, `token_budget`:int, `tokens`:int |
| `engine.loop_finalize` | 收敛治理：强制收尾 | — |
| `engine.loop_nudge` | 收敛治理：循环提醒 | — |
| `event_sink.close` | EventSink 真 close（开→关仅一条）；was_detached 区分先前仅断线 vs 仍附着收口 | `conversation_id`:str, `message_id`:str, `reason`:str, `was_detached`:bool |
| `event_sink.detach` | SSE 消费者 detach（断线/排队无 waiter 等）；already_detached 区分幂等再 detach | `already_detached`:bool, `conversation_id`:str, `message_id`:str, `reason`:str |
| `http.unhandled_error` | HTTP 层未捕获异常 | `error`:str, `method`:str, `path`:str |
| `llm.call` | 单次 LLM 调用（latency/tokens/cost_nano） | `cost_nano`:int, `finish_reason`:str, `input_tokens`:int, `latency_ms`:int, `model`:str, `output_tokens`:int, `reasoning_tokens`:int, `scenario`:str, `stream`:bool |
| `llm.call_failed` | LLM 调用失败（model/credential_source；可取则带 provider_id） | `credential_source`:str, `error`:str, `model`:str, `provider_id`:str, `scenario`:str |
| `llm.request` | LLM prompt 截断脱敏（需 LOG_LLM_BODIES） | `model`:str, `scenario`:str |
| `llm.response` | LLM 回复截断脱敏（需 LOG_LLM_BODIES） | `model`:str, `scenario`:str |
| `llm.stream_stalled` | LLM 流式空闲超时（model/credential_source；可取则带 provider_id） | `committed`:bool, `credential_source`:str, `model`:str, `provider_id`:str, `scenario`:str |
| `llm_provider.deleted` | 敏感操作审计：BYOK 服务商（含密钥）删除 | `provider_id`:str, `user_id`:str |
| `llm_provider.key_updated` | 敏感操作审计：BYOK API Key 轮换保存（无明文） | `provider_id`:str, `user_id`:str |
| `pipeline.error` | 回合管线未捕获异常 | `error`:str |
| `rate_limit.redis_fail_open` | Redis 限流请求中途失败 → fail-open 放行本请求（可告警；与 construct 期 security.rate_limit_redis_fallback 对偶） | `count`:int, `error`:str, `prefix`:str |
| `react.round_end` | ReAct 轮结束（reasoning_tokens/tools） | `done`:bool, `input_tokens`:int, `output_tokens`:int, `reasoning_tokens`:int, `round`:int, `tools`:int |
| `react.round_start` | — | `round`:int |
| `roster.session_evicted` | — | `bytes`:int, `max_bytes`:int, `n_sessions`:int, `reason`:str, `run_id`:str, `total_bytes`:int |
| `run.captain_failed` | — | `error`:str |
| `run.failed` | — | `error`:str |
| `session_roster.wired` | — | `conversation_id`:str, `loader`:bool, `persist`:bool |
| `sidecar.turn_cancel_requested` | 桌面 cancel RPC 到达 sidecar（solo blocking 无 coordination.user_stop_* 时的指纹） | `coordination_cascaded`:bool, `reason`:str, `task_cancelled`:bool, `turn_id`:str |
| `sidecar.turn_cancelled` | 本地回合 CancelledError salvage；reason=cancelled_without_rpc 表示非 RPC cancel | `reason`:str, `salvaged`:bool, `turn_id`:str |
| `tool.args_parse_failed` | — | `args_preview`:str, `msg`:str, `parse_class`:str, `pos`:int |
| `tool.args_salvaged` | handoff 参数 JSON 窄 salvage 成功（裸字符串字段 / 截断闭合） | `args_preview`:str |
| `tool.execute_end` | 工具执行结束（status/duration_ms；error 时带 reason） | `duration_ms`:int, `index_status`:str, `reason`:str, `status`:str, `tool`:str |
| `tool.execute_start` | — | `tool`:str |
| `tool.web_search` | — | `hosts`:list, `query`:str |
| `worker.escalate` | worker 升级求决策 | `assumption`:str, `blocking`:bool, `kind`:str, `question`:str |
| `worker.handoff` | worker 交接（chars=summary 长；body_chars=交付正文长） | `body_chars`:int, `chars`:int, `has_motion_card`:bool, `has_summary`:bool, `run_id`:str |
| `workspace.index_build_complete` | 后台代码索引 ensure 完成（duration_ms；可取则带 generation/truncated/files） | `duration_ms`:int, `files`:int, `force`:bool, `generation`:int, `truncated`:bool, `updated`:bool |
| `workspace.index_build_start` | 后台代码索引 ensure 开始（IndexMaintainer） | `force`:bool |
| `workspace.index_failed` | 后台代码索引 ensure 失败（带 error/duration_ms） | `duration_ms`:int, `error`:str, `force`:bool |
| `workspace.index_skip_channel_busy` | Local channel 仍忙，跳过本轮索引并 coalesce 重试 | `force`:bool, `inflight`:int, `wait_ms`:int |
| `workspace.snapshot_failed` | — | `error`:str |

<!-- END AUTO:log-event-catalog -->

## 委派验收事件（S3 后）

✅ S3 已删 `completion_criteria` kind。统一说法：

| 事件 / 字段 | 现行？ | 语义 |
|---|---|---|
| `delegate.acceptance_resolved` | ✅ 现行 | kickoff 仍打；`criteria`/`source` 现为 `null`（无 kind binding） |
| `delegate.completion_criteria_ignored` | ✅ 现行（观测） | CEO 误传已删字段时打点；忽略字段，非硬闸 |
| soft overlays | ✅ 现行 | TS 落盘验证提醒、import 图闭合自动扫等仍为 soft note：不挡批次，经 `delivery_status`（`severity=warning` → `state=notes`） |
| deliverable / contract 缺口 | ✅ 现行 | 仍走 `delivery_status` + CEO 复盘；错收工接盘靠提示词 + soft + 人审 |
| `delegate.completion_criteria_unmet` / `…_hoisted` | ❌ 历史兼容 | **不再**由引擎按 kind 触发；catalog / spine 仅读旧 JSONL；勿当现行契约 |

历史日志里可能仍见 `completion_criteria_unmet` / 非空 `criteria`——那是 S3 前回合。

## Phase-0 字段（代码看不出来）

`chat.turn_complete` 的 `prepare_ms` / `assemble_ms` / `ttft_reasoning_ms` / `ttft_content_ms`：**缺路径为 `null`，勿当 0**。TTFT 只记 captain 首 stream；非 `llm.call.latency_ms`、非整回合 `duration_ms`。详释与排查配方 → `conversation-logs.mdc`。

## Token 两口径（decision_spine vs turn_metrics）

复盘时 **`llm.*_tokens` ≠ `tail` / `turn_metrics` tokens 不一定是 bug**：

| 口径 | 来源 | 含义 |
|---|---|---|
| **llm**（spine） | 同 `trace_id` 全部 `llm.call` 合计 | **全 trace**（含 pause 前 / team_preview 前） |
| **tail / turn_metrics** | 收口折账（`turn_metrics` 优先，否则 JSONL close） | **本段结算**；`kind=resume` / `chat.resume_complete` 通常只有 resume 段 usage |

spine JSON：`llm.token_scope=full_trace`；`tail.token_scope=settlement` 或 `settlement_segment`（resume）；`health.token_accounting` 有短说明。人读 timeline 在两数不一致时会打 `Token口径:` 行。**勿把差值当计费漂移去改账**——先看是否 resume。

## 改编排 / 引用闸前对照 dogfood

改路由、交付验收或引用闸前，用维护者 dogfood 金标槽位做前后观察（窄维：routing / deliverable / citation；不进 PR）。出包 → `fill_from_pack` 入槽、证据三档 → [`evals/dogfood/README.md`](/evals/dogfood/README.md)。CLI 细节仍以本文开头指针与 `conversation-logs.mdc` 为准，不在此重复。
