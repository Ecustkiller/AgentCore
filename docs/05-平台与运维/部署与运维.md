---
status: reference
code: deploy/
related:
  - docs/02-架构/技术架构与基础设施.md
  - .github/workflows/ci.yml
skip_if:
  - 只改应用代码不涉及部署
---

# 部署与运维

> **状态**：已确定（复用现有阿里云服务器，Docker Compose 单机部署）
>
> CI 现状：**GHA 无配额、`ci.yml` 实际不运行**（push/PR job 0 step 秒失败）；门禁以本地跑为准（§九为设计目标，以实现为准）。触发保留不动，配额恢复即自动复活。

---

## 一、服务器环境

### 域名拓扑

| 域名 | 路由方式 | 用途 |
|------|---------|------|
| `app.fashitianxia.xyz` | A 记录 → 服务器 | 应用 API（Nginx 反代后端 + MinIO）+ **web 客户端 SPA 根路径**（`apps/desktop` → `dist-web`，同源托管，见 §二） |
| `m.fashitianxia.xyz` | Cloudflare Pages | 手机网页版（`apps/mobile` SPA）；DNS 须**显式** CNAME → `agentcore-mobile.pages.dev`，压过 `*` Tunnel 兜底 |
| `fashitianxia.xyz` | Cloudflare Pages | 官网（`apps/website`） |
| `relay.fashitianxia.xyz` | Cloudflare Tunnel | PChat 公网中转 |
| `office.fashitianxia.xyz` | Cloudflare Tunnel → Nginx `:8090` | 管理后台（`apps/admin` SPA，`deploy-admin.yml`） |
| `*.fashitianxia.xyz` | Cloudflare Tunnel → Nginx | 兜底 |

> 硬件规格、公网 IP、SSH 密钥路径等运维快照**不入 docs**（随实例漂移）；需要时查运维 runbook 或云控制台。

### 预装软件（决策）

Docker + Nginx + certbot（自动续签）+ ufw（22/80/443）+ fail2ban。Docker 端口绑定 `127.0.0.1`，不对外暴露。

---

## 二、网络与域名

### Cloudflare Tunnel

Tunnel 承载 `office` / `relay` / 通配符兜底；apex 官网已迁出隧道（改走 Cloudflare Pages）。配置细节不入 docs（运维 runbook）。

### Nginx 反代决策

| 决策 | 内容 |
|---|---|
| `/api/` | 反代后端 API，**关闭缓冲**以支持 SSE 流式 |
| `/storage/` | 反代 MinIO，**Host 头改写**为 `minio:9000` 以匹配 SigV4 签名 |
| 安全头 | HSTS / nosniff / X-Frame-Options DENY |
| 上传上限 | `client_max_body_size 100m` |

具体 server 块见生产机 `/etc/nginx/sites-enabled/`（运维注记，不入 docs）。

### 官网（Cloudflare Pages）

营销官网部署在 **Cloudflare Pages**（Next.js 静态导出），与后端解耦、零服务器占用。

| 项目 | 值 |
|------|------|
| 项目名 | `agentcore-website` |
| 源码 | `apps/website`（Next.js 静态导出，`output: "export"`） |
| 构建 | `next build --webpack`（**不可用默认 Turbopack**：Next 16 下 Turbopack 不产出 `out/`，会部署成空站） |
| 部署 | **Cursor Agent 本地脚本** + wrangler direct-upload（✅ `pnpm deploy:pages`） |
| 凭据 | `deploy/.env.deploy.local`（或 `apps/website/.env.deploy.local` 仅 CF token） |

**部署决策**（2026-06）：不再用 GitHub Actions `push` / `workflow_run` 自动部署官网。

| 方案 | 结论 |
|---|---|
| Cloudflare Pages **Git OAuth 集成** | **否决**——现有项目是 direct-upload 类型，改接 Git 须重建项目并迁移 apex 域名 |
| GitHub Actions + wrangler | **否决**——私有仓 Actions 分钟配额易耗尽；配额用尽时 job **0 step 秒失败**，push 后无法可靠上线 |
| Cursor 代部署（本地 build + wrangler） | **采用**——与 direct-upload 模型一致；token 本地一次配置；用户说「部署官网」时 Agent 执行并 curl 验收 |

✅ 已落地（官网 + mobile + admin）：`pnpm deploy:pages`、`.cursor/rules/cursor-deploy.mdc`、GHA push 触发已关闭。

→ 见代码 `apps/website/package.json`、`apps/website/scripts/deploy.mjs`、`.cursor/rules/cursor-deploy.mdc`

### 手机网页版（Cloudflare Pages）

手机端 web SPA（`apps/mobile`，Bearer 鉴权）部署在 **Cloudflare Pages**，与 API 解耦。

| 项目 | 值 |
|------|------|
| 项目名 | `agentcore-mobile` |
| 自定义域 | `m.fashitianxia.xyz` |
| 源码 | `apps/mobile`（Vite SPA，`dist/`） |
| 构建 | `VITE_API_URL=https://app.fashitianxia.xyz/api pnpm --filter agentcore-mobile build` |
| 部署 | **Cursor Agent 本地脚本** + wrangler direct-upload（✅ `pnpm deploy:pages`） |
| 后端 | 生产 `CORS_ALLOW_ORIGINS` 须含 `https://m.fashitianxia.xyz` |

→ 见代码 `apps/mobile/package.json`、`apps/mobile/scripts/deploy-pages.mjs`

### web 客户端（同源 · 服务器 Nginx）

主力 web 客户端（`apps/desktop` 渲染层跑在浏览器，形态 A：web = 「云工作区」一等入口）**同源**部署在 **app. 根路径**：页面在 `https://app.fashitianxia.xyz/`、API 在 `https://app.fashitianxia.xyz/api/`，由**同一台 Nginx** 托管 → 第一方 cookie、**零 CORS**，免 DNS / Tunnel 改动。选同源而非 Pages 子域的理由：cookie 鉴权同源最稳（彻底免疫跨站 cookie 收紧），且 API 已在该 Nginx 上，加一段 `location /` 即可。

| 项目 | 值 |
|------|------|
| 源码 | `apps/desktop`（Vite 渲染层 webapp 入口 → `dist-web/`） |
| 构建 | `pnpm --filter agentcore-desktop build:webapp`（`.env.production` 已钉 `VITE_API_URL=https://app.fashitianxia.xyz/api`，同源） |
| 静态目录 | `/opt/agentcore/web/dist`（产物 `index.webapp.html` 发布为 `index.html`；哈希路由 `createHashRouter` 无需服务端重写） |
| 部署 | **Cursor Agent 本地脚本**（✅ `pnpm -C apps/desktop deploy:web`）：build → 发布 index.html → scp → 解包 + `nginx -t && reload`（**仅同步文件，不改 server 块**） |
| Nginx | 一次性把 `root` + `location /` + `/assets`（SPA `try_files … /index.html`）合入 **app. `:443` 块**，**替换**原有兜底 `location /`（→`:8000`，曾出 `/docs`、`/openapi.json`，故二者公网不再出）；**勿动** `/api`、`/api/ws`、`/storage`、`/internal`、`/_market`——参考 `deploy/nginx/app-web.conf` |
| 后端 | 同源 → **无需 CORS / DNS 改动** |

**首次上线清单**（一次性）：
1. 生产机 app. `:443` 块按 `deploy/nginx/app-web.conf` 把兜底 `location /`（→`:8000`）**替换**为 `root /opt/agentcore/web/dist` + `location /`（保留 `/api`、`/api/ws`、`/storage`、`/internal`、`/_market`；改前自动备份 `.bak.web-*`）。
2. `pnpm -C apps/desktop deploy:web`（建目录 + 发文件 + reload）。
3. 验收：`curl.exe https://app.fashitianxia.xyz/` → 200 出 SPA；浏览器登录发一条消息走通（同源 cookie + 云 SSE 流式）。

**状态**：✅ 已上线（2026-06-30）——`https://app.fashitianxia.xyz/` 出 SPA（200）、`/api/version` 同源 200、主 JS 资源 200（`immutable` 缓存）。代价：`/docs`、`/openapi.json` 公网不再出（落 SPA 外壳，FastAPI 文档本不宜公开）。

→ 见代码 `apps/desktop/scripts/deploy-web.mjs`、`apps/desktop/package.json`、`deploy/nginx/app-web.conf`

### 管理后台（Tunnel + Nginx）

运营控制台（`apps/admin`，Cookie 鉴权）静态文件部署在**生产机**，由 Nginx 本机 `:8090` 提供，经 **Cloudflare Tunnel** 暴露为 `office.fashitianxia.xyz`。

| 项目 | 值 |
|------|------|
| 静态目录 | `/opt/agentcore/admin/dist` |
| Nginx | `deploy/nginx/office-admin.conf` → `sites-enabled/office-admin` |
| 构建 | `VITE_API_URL=https://app.fashitianxia.xyz/api` + `openapi-typescript` |
| 部署 | **Cursor Agent 本地 SSH 脚本**（✅ `pnpm deploy:production`） |
| 后端 | `CORS_ALLOW_ORIGINS` 须含 `https://office.fashitianxia.xyz`；首次 `create_admin.py` |

凭据：`deploy/.env.deploy.local` 中 `DEPLOY_SSH_*`（私钥路径，不入仓）。

→ 见代码 `apps/admin/package.json`、`apps/admin/scripts/deploy-production.mjs`、`deploy/scripts/admin-remote-install.sh`

---

## 三、容器编排

> **实现现状**：目标生产部署运行于外部阿里云服务器 `/opt/agentcore/`。仓库 deploy 资产（dev/server/app compose、Dockerfile、`deploy-server.sh`、`production.env.example` 等）→ 见代码 `deploy/`。发布自动化：日常走 Cursor 本地脚本（`pnpm deploy:backend <sha>`，§三 CI/CD）；GHA `build-backend.yml` / `deploy-backend.yml` 仅 `workflow_dispatch` 紧急备用（push / tag 自动触发已弃用）。Nginx / Cloudflare 配置片段已入仓（`deploy/nginx/`、`deploy/cloudflare-access-office.md`；全量生产 Nginx 以机上为准）。仍 ⏳：worker 容器。
>
> **⚠️ 现状落差**：脚本链（`pnpm deploy:backend <sha>` → `remote-build-deploy.mjs` → `finish-server.sh`）**代码就位但尚未接管生产**。当前生产栈为**线下手动**维护——活栈跑在生产机本地、未入仓的 `deploy_f6d1637/` 快照（自带 compose 与 `config/production.env`），由生产机本地脚本一次性引导、稳态重发靠手动重拉镜像（属运维 runbook，不抄入 docs）；`.last-deployed-sha` 当前陈旧、非真相源。消除此落差（生产回归 `prod-*` / `deploy-server.sh` 自动化）为运维待办。

### 镜像仓库

阿里云 ACR 个人版，命名空间 `agentcore1`；公网/VPC 端点见 `deploy/config/production.env.example`。

### Docker Compose 文件

仓库内部署资产（dev/server/app 三 compose + `apps/server/Dockerfile` + `deploy-server.sh` + `production.env.example` + `searxng/settings.yml` + `init-db.sh`）→ 见代码 `deploy/`。仍 ⏳ 未纳入：

| 文件 | 用途 | 状态 |
|------|------|--------|
| worker 容器（api/worker 全量编排） | 独立 worker 入口；当前进程内协程，无 | ⏳ 暂缓 |
| 沙箱叠加 compose | 代码执行沙箱升级 | ⏳ 未纳入 |

> SearXNG 引擎集面向大陆收敛（`keep_only`: baidu/360search/sogou/quark），配置见 `deploy/searxng/settings.yml`，dev 与 prod 共用；应用经 `SEARXNG_URL`（默认 `http://127.0.0.1:18888`）连接。

### 当前容器清单

目标态容器以 `deploy/docker-compose.server.yml` + `deploy/docker-compose.app.yml` 为真相源；生产机遗留组件取舍见 §六。官网走 Cloudflare Pages（§二）。

### 部署目录结构

生产机 `$AGENTCORE_HOME`（`/opt/agentcore/`）布局由 `deploy/scripts/deploy-server.sh` 约定：根 `.env`（ACR 凭据 + 镜像标签，**不含应用密钥**）、`.last-deployed-sha`、`config/.env`（应用密钥，见 §8.2）、`config/searxng/`、`data/`（pgdata / redisdata / miniodata / … 运行时卷）、`backups/`、`repo/`（git clone，部署脚本同步）。→ 见代码 `deploy/scripts/deploy-server.sh`。**（⚠️ 落差：当前活栈实际跑在 `repo/deploy_f6d1637/` 快照、非此蓝图布局；见 §三。）**

### CI/CD 流程

> **2026-06 决策**：**部署/发版**全部切出 GHA push 触发，改 **Cursor Agent 本地脚本**——私有仓 Actions 配额/账单不可靠（用尽时 job 0 step 秒失败）。GHA workflow **保留 `workflow_dispatch` 紧急备用**；`prod-*` / `desktop-v*` tag **仅作发布点标记**，不再触发自动部署。CI 门禁（`ci.yml`）触发保留 push/PR，但**当前 GHA 无配额、实际不运行**（job 0 step 秒失败）——发布前本地跑全套门禁为准，配额恢复后 push/PR 自动复活。

`remote-build-deploy.mjs` **现行时序**（生产机）：`git checkout` → `docker buildx build` → 推 ACR(VPC) → `finish-server.sh`（迁移前快照 → `alembic upgrade` → compose up → `/readyz`）。

### GHA → Cursor 本地替代（✅ 2026-06）

| 流程 | 旧（GHA push） | 新（日常） | workflow |
|------|---------------|-----------|----------|
| 官网 | `deploy-website.yml` | `pnpm -C apps/website deploy:pages` | dispatch only |
| 手机 | `deploy-mobile-web.yml` | `pnpm -C apps/mobile deploy:pages` | dispatch only |
| 管理后台 | `deploy-admin.yml` | `pnpm -C apps/admin deploy:production` | dispatch only |
| web 客户端 | `deploy-web.yml` | `pnpm -C apps/desktop deploy:web` | dispatch only |
| 后端构建+部署 | `build-backend` + `deploy-backend` | `pnpm deploy:backend <sha>` | dispatch only |
| 桌面发版 | `release-desktop.yml` | Win 本地 `release:win`；Mac = GHA `Release Desktop`（仅 mac） | dispatch only（仅 mac） |
| CI | `ci.yml` | **GHA 无配额→实际不跑**；发布前 `pnpm release:gate` 为准（触发保留，配额回自动复活） | push/PR |
| evals 夜跑 | `evals-nightly.yml` | 保留（花 API key，非部署） | schedule+dispatch |

→ Agent 细则：`.cursor/rules/cursor-deploy.mdc`。凭据：`deploy/.env.deploy.local`（CF + SSH）；ACR 在生产机 `/opt/agentcore/.env`。

**发布动作**（现行）：push `master` → 上表「新」列逐项执行 → 验收 → `git tag prod-vN` / `desktop-v*` **仅标记**（不触发 GHA）。回退：对旧 SHA 重跑 `pnpm deploy:backend <sha>`。

---

## 四、Systemd 与定时任务

生产机运行 Docker / Nginx / Cloudflare Tunnel / certbot 等标准服务；旧版 lawcore 遗留的 `agentcore-api`/`agentcore-web` systemd 单元与无效 cron 待清理。→ 运维 runbook，不入 docs 清单。

---

## 五、资源现状

> 内存 / 磁盘占用是随数据增长漂移的运行时快照，不入文档。需要现状直接在服务器跑 `docker stats --no-stream`（各容器内存）与 `df -h` / `du -sh /opt/agentcore/data/*`（磁盘）。组件取舍（Neo4j / ES 可移除以释放内存）见 §六。

---

## 六、组件取舍与容量

Neo4j + Elasticsearch（lawcore 法律垂直遗留）可移除释放内存；www 容器已由 Cloudflare Pages 取代；**SearXNG 保留**作 Agent 网络搜索。

### ⏳ 开放容量问题

- **是否升级服务器规格**：多 Agent 并发 + 沙箱升级可能需要更多资源；放量前按实际负载评估（当前 `ecs.e-c1m4.large` 2C8G 共享型够 MVP）。

---

## 七、发布策略 ✅ 已确定

> **实现现状**：`GET /livez` / `GET /readyz` / `GET /version` 已实装。部署/发版走 Cursor 本地脚本（§三）；GHA 仅 workflow_dispatch 备用（CI 触发保留但无配额、实际不跑）。`/readyz` 巡检：`deploy/scripts/healthcheck.sh` + systemd timer。

### 7.1 版本钉定

**多轨道 semver**（开发期）：API（`pyproject.toml`）、Desktop（`package.json` + `desktop-v*` tag）、Web 客户端（mobile/admin build 注入 `__APP_VERSION__` + git SHA）**独立 bump**（`pnpm bump-version <track> patch|minor|major|x.y.z`）；About / 系统页双行展示「客户端版本」与「API 版本」。所有客户端请求带 `X-Client-Platform` + `X-Client-Version`（→ `scripts/client-build-info.mjs`、各端 `lib/clientBuildInfo.ts`）。

部署一律按 **git SHA**，不按浮动 `latest`。应用镜像构建期注入 `GIT_SHA` / `BUILT_AT`，运行时经 `GET /version` 暴露（端点已实装；启动入口在环境未注入时自 git 工作树补 SHA、按启动时刻补构建时间，发布流水线经环境变量注入则优先）。部署脚本传 `latest` 时，会先解析为当前 `master` 的具体 SHA 再 pin——容器跑的永远是确定 SHA（可追溯、可秒回）。

### 7.2 发布点（Release Point）

`master` 是 WIP 快照主干，**HEAD ≠ 可上线点**。用 `prod-*` tag 标记发布点（语义：「这个 SHA 是要上线的版本」）。部署以发布点为准，而非 `origin/master`——tag **仅作标记**、不触发自动部署（`deploy-backend.yml` 仅 `workflow_dispatch` 备用，§三 CI/CD 决策；生产当前尚未走脚本自动化，见 §三落差；漂移哨兵仍 ⏳）。

### 7.3 漂移检测

Admin 系统页 **发布漂移** 卡片（✅）：客户端拉 GitHub Latest（`AgentCore-releases`）与官网 `/api/desktop-release`，对比下载页是否滞后；控制台 SHA vs API SHA 标注「异轨/同部署」。

CI 每日自动比对线上 `/version` 与最新 `prod-*` tag（⏳ 目标；MVP 未接入）：

- prod 落后 tag → 「发了版没部署」（真漂移，告警）
- `master` 超前 tag → 「攒了未发布的 WIP」（正常，不告警）

桌面端同理：比对 `package.json` version 与公开发布仓的 `releases/latest` tag（⏳ 未接入）。

### 7.4 Deploy ≠ Release

构建/部署之上还有一层「部署 ≠ 发布」解耦——后端功能可先暗发到生产，由配置/特性开关翻开才对用户可见。支持灰度 + 最低客户端版本门控（让后端先于桌面发版，仅追上的客户端点亮）。

#### 跨轨道发布协同（2026-06 决策）

**不强制**后端与桌面（或各客户端）同一天、同 SHA 锁步发布——§7.1 多轨道 semver 的设计意图即**独立节奏 + 按需协同**。`prod-*` 与 `desktop-v*` **不绑定**；Admin 漂移卡片标注「异轨/同部署」是观测，不是告警。

| 变更类型 | 是否要快跟 | 说明 |
|---------|-----------|------|
| 纯后端（客户端无感） | 否 | skills / evals / prompt 等；桌面不必跟 |
| **协议 / 事件契约** | **是（天级窗口）** | 动了 `packages/contract-types`、protocol-conformance 向量、各端 fold → 后端 + mobile/admin **同日** deploy；桌面 **7 天内** patch 发版 |
| 纯客户端 UI | 否 | 主题 / 布局；后端不必跟 |

**判据**：本次 PR 是否改协议 fold 或事件类型？是 → 走「协议变更」行；否 → 各轨道独立发。

**协议变更轻量流程**（人工 checklist，非自动化门禁）：

1. `pnpm conformance` 全绿后再 prod deploy
2. 同日：`prod-*` + mobile/admin Pages deploy
3. **7 日内**：`desktop-v<patch>` + 官网 `FALLBACK_VERSION` + `deploy:pages`
4. 旧桌面用户：对话通常仍可用（后端向后兼容发事件），新 UI 等 auto-update；不应依赖「必须同天发桌面」来保活

**否决**：`prod-vN` 与 `desktop-vX.Y.Z` 强制同 tag / 同 SHA 发布——发布成本与回滚独立性不划算。

⏳ 远期：`min_client_version` 特性开关接线后，协议变更类功能可后端先上、仅新版本客户端点亮（§7.9），进一步降低人工记「7 天内补桌面」的负担。

### 7.5 Monorepo 选择性构建

`push master` **不自动构建/部署**（2026-06 切 Cursor 本地，§三）。Agent 执行 `pnpm deploy:backend <sha>` 在生产机构建 api 推 ACR。⏳ 多服务 retag 优化未纳入。

### 7.6 桌面端构建与双仓发布

> **实现现状**：`.github/workflows/release-desktop.yml` 仅 `workflow_dispatch`（`platform=mac`）→ 预建 draft → **macOS arm64** 构建 → electron-builder 上传公开发布仓 `Lawofall/AgentCore-releases`。**Windows 不走 GHA**（为省额度；本地 `release:win` / 分步 `--win`）。`desktop-v*` tag **仅作发布点标记**，不触发构建。内测档位：**未签名**；Mac **arm64 only**（Apple Silicon，Intel / Universal 不在支持范围）。正式签名 + 公证 → [`../06-规划/远期规划.md` §3.1](/docs/06-规划/远期规划.md)。

| 环节 | 机制 |
|---|---|
| 构建触发 | Win = 本地；Mac = Actions **workflow_dispatch**（仅 `mac`）。`desktop-v<ver>` tag 仅标记，不触发 |
| 产物 | Win 本地 / Mac GHA → 公开发布仓草稿 release（人工转正） |
| 灰度 | `latest.yml` 的 `stagingPercentage` 分桶 |
| 熔断 | 后端 `DESKTOP_UPDATES_ENABLED=false` |

**预建草稿 release**：安装包发**公开**发布仓，源码仓保持私有。**现行 Win = Mac 模型**（`release:win`：`--publish never` → 断言本地 exe/blockmap/latest.yml → `gh release upload --clobber` → 解析 `gh release view` 资产列表，缺一非 0）。预建 draft 仍保留（与 Mac GHA 汇入同一 tag）。勿再把 electron-builder `--publish always` 当唯一上传路径（#6676 竞态：常只传上 `.blockmap`、丢掉 `.exe` + `latest.yml`）。

**内置 Python 运行时**：CI 在 electron-builder 前先跑 `pnpm bundle:sidecar`（`scripts/bundle-sidecar.mjs`），产物落 `resources/sidecar/` 经 `extraResources` 拷入安装包。方案 B 本身（内置 CPython 的动机、不可交叉编译故按 OS 打包）见 [`../02-架构/双模式工作区.md` §10.5](/docs/02-架构/双模式工作区.md)。

**原生模块（node-pty，终端 M3）**：`asarUnpack` 解包 `node_modules/node-pty/**`（`.node` / conpty DLL 在 asar 内 dlopen 失败）；`npmRebuild: false` + `nodeGypRebuild: false`——官方 prebuilds 走 N-API、Electron ABI 直接可加载，强制 rebuild 反而在缺 Spectre 缓解库的 Win 机器上失败。postinstall 的 `scripts/rebuild-native.mjs` 同理：检测到官方 prebuild 即跳过 rebuild。新增原生依赖时按此模式对齐。

**单版本 draft 资产**（Win + Mac 齐全再 publish；缺一平台不要转正——**开发期例外见 checklist 步 4**）：

| 平台 | 文件 | 用途 |
|---|---|---|
| Windows | `AgentCore-<ver>-win-x64.exe` + `.blockmap` + `latest.yml` | 安装 + 自动更新 |
| macOS | `AgentCore-<ver>-mac-arm64.dmg` | 内测首装 |
| macOS | `AgentCore-<ver>-mac-arm64.zip` + `.blockmap` + `latest-mac.yml` | 自动更新 |

**macOS 内测安装**（测试者）：下载 dmg → 拖入「应用程序」→ 首次打开须 **右键 → 打开**（或终端 `xattr -cr /Applications/AgentCore.app`）。自动更新经 zip + `latest-mac.yml`；未签名环境下 `quitAndInstall` 后可能需再次右键打开。

**发布 checklist**：

1. bump `apps/desktop/package.json` version
2. `git tag desktop-v<x.y.z> && git push origin desktop-v<x.y.z>`
3. 构建上传：
   - **Windows（本地）**：`pnpm -C apps/desktop release:win`（`--publish never` + `gh release upload --clobber` + 资产断言；`GH_TOKEN` / `gh auth token`）
   - **macOS（GHA）**：`gh workflow run "Release Desktop"`（或 Actions 手动；仅 mac）→ 产物进同一 draft
4. draft 含上表全部 7 个资产 → 可选真机 QA（见下）→ Publish。⚠️ **开发期例外（无真实用户）**：可仅 win 转正（`gh release edit v<ver> --draft=false --latest`），mac 就绪后由 GHA 或 `gh release upload v<ver> …mac… --clobber` 补进同版本；站点缺 mac 资产会自动隐藏 mac 按钮、win 自更新照常，`FALLBACK_VERSION` 暂留上一含 mac 的版本。正式上线后恢复「齐全再转正」（2026-07 v0.3.7 即 win-only 转正）
5. 刷新官网下载页：`node scripts/bump-version.mjs desktop <ver>` 已同步 `FALLBACK_VERSION` → Agent 执行 `pnpm -C apps/website deploy:pages`（✅ §二）

Mac 补发 / 重跑：`gh workflow run "Release Desktop"`（或 Actions → Release Desktop → Run；**仅 mac**，勿用 GHA 打 Win）。

| 失败 | 处理 |
|---|---|
| Mac job 红 | 修 sidecar / 构建 → `gh workflow run "Release Desktop"` 重跑 |
| Win 本地失败 | 本地修后重跑 `release:win` / 分步 `--win` |
| 已 publish 坏包 | 删发布仓错误资产或发 hotfix 版本 |

**真机 QA**（Apple Silicon，Rosetta 不算；本地文件夹 E2E 仅真机可验）：DMG 安装可启动 → 登录 / SSE 流式 → 绑定本地文件夹（sidecar 内置 Python）→ 本地回合文件读写 → 自动更新能发现新版本。CI 内 `bundle:sidecar` 末尾 `import agentcore.sidecar.server` 冒烟必须通过。

→ 见代码 [`.github/workflows/release-desktop.yml`](/.github/workflows/release-desktop.yml)、[`apps/desktop/electron-builder.yml`](/apps/desktop/electron-builder.yml)、[`apps/desktop/package.json`](/apps/desktop/package.json)（`build:mac`）、[`apps/desktop/scripts/bundle-sidecar.mjs`](/apps/desktop/scripts/bundle-sidecar.mjs)、[`apps/desktop/src/main/updater.ts`](/apps/desktop/src/main/updater.ts)

客户端自动更新行为（检查频率 / fail-open / 双通道）见 [`../04-前端/前端技术与架构.md` §7.6](/docs/04-前端/前端技术与架构.md)。生产 API 指向 `https://app.fashitianxia.xyz/api`。

### 7.7 备份与恢复策略

**只备不可重建的主数据**——PostgreSQL + 对象存储（工作区文件）。Redis 是缓存丢了自愈。

| 原则 | 说明 |
|---|---|
| 迁移前快照 | 每次 `alembic upgrade` 前自动 `pg_dump`，失败即终止迁移 |
| 破坏性干净恢复 | 回灌前 `DROP SCHEMA public CASCADE`（非空库直接灌会主键冲突） |
| 停应用再恢复 | 避免并发写入；恢复后不自动起应用（需重新 deploy 对齐版本） |
| 异地副本 | 默认只有本地——防不了整机损坏，放量前必须开异地 |
| **灾备演练** | 备份未验证过恢复 = 没有备份。放量前必做一次完整恢复演练 |

**脚本已入仓** → 见 `deploy/scripts/backup.sh`（全量备份 + 轮转）/ `restore.sh`（破坏性干净恢复：完整性校验 → 确认门 → 停 api → 回灌 → 不自动起）/ `deploy/systemd/agentcore-backup.*`（每日定时 oneshot）。**当前仅覆盖 PostgreSQL**（DATA_DIR 文件 / 对象存储需另行文件级备份）；异地副本与灾备演练仍为运维动作（参数/排程见脚本与 runbook）。

### 7.8 上线检查清单结构

**Cursor Agent 全端发布**（push `master` 后按序执行；细节与踩坑 → `.cursor/rules/cursor-deploy.mdc`）：

1. `git push origin master`
2. 后端：GHA `build-backend.yml` 绿 → `git tag prod-vN && git push origin prod-vN`；GHA 红（账单）→ `node deploy/scripts/remote-build-deploy.mjs <sha>`
3. 并行：`pnpm -C apps/website deploy:pages` / `apps/mobile` / `apps/admin deploy:production`
4. 验收：`GET https://app.fashitianxia.xyz/api/version` 的 `git_sha`、三静态域 HTTP 200

Go/No-Go 清单按六维度组织（清空未完成项后仅保留检查维度模板）：

| 维度 | 示例检查项 |
|---|---|
| **CSRF** | `CSRF_ENABLED=true`；Cookie 客户端（admin/桌面）登录后带 `X-CSRF-Token` |
| **限流** | `RATE_LIMIT_BACKEND=redis`（多 worker）；`TRUST_PROXY=true`（反代后真实 IP）；`/readyz` 在 redis 模式下返回 `redis: true` |
| **Admin 网络** | `office.*` 已挂 Cloudflare Access（见 `deploy/cloudflare-access-office.md`） |
| **稳定** | 冷启动可跑通、`/readyz` 中 `database` + `redis`（若启用）均为 true、回滚演练通过 |
| **可观测** | 巡检 cron 已接、漂移哨兵在跑（哨兵当前 ⏳ 未接，见 §7.3——上线前须补） |
| **关键路径** | 注册→登录→对话→委派→文件端到端手测通过 |
| **容量** | 放量前轻量压测、降级预案就绪 |
| **数据** | 完整备份已配且实跑过灾备演练、异地副本已开 |

未完成项迁入 [`06-规划/远期规划.md` §三](/docs/06-规划/远期规划.md)。

### 7.9 特性开关架构（⏳ 目标设计 · 未实装）

特性开关是 §7.4「Deploy ≠ Release」的落地机制——后端代码先暗发到生产，由配置翻开才对用户可见。**实现现状**：本节为已确认目标设计，服务端三门求值 / 灰度分桶 / `min_client_version` 接线均 ⏳ 未建（现仅有 `desktop_updates_enabled` 熔断与 `config/features.py` 编译期垂直开关；`api/routes/system.py` 注释预留接缝）。

**服务端求值**：灰度/版本逻辑只在后端运行，客户端与后端代码路径都只读「已解析的布尔」。理由：求值逻辑不泄漏到端、各调用方判定一致、单一真相源。

**三道门顺序求值**：每个 flag 按以下顺序判定，任一道关闭即最终为 `false`：

| 门 | 含义 |
|---|---|
| `enabled` | 总开关（关 = 永远关） |
| `min_client_version` | 仅 ≥ 该版本的客户端可见（跨列车安全阀） |
| `rollout` | 灰度百分比（0–100），按稳定 key 分桶 |

**跨列车安全阀**：`min_client_version` 让需要新客户端支持的后端功能先进生产，仅追上版本的客户端点亮——将「后端先上、桌面后放量」的跨列车顺序约束变成开关里的硬门，而非靠人工记顺序。

**分桶策略**：优先用户 `user_id`（同一用户跨设备判定一致），未登录回落到设备 `client_id`（UUID 持久化）。同一 key 永远落同一桶（不抖动），按 flag 加盐避免不同 flag 命中同一批人。登录成功后需重拉开关以纠正分桶 key 切换。

**fail-safe vs fail-open 对偶**——刻意相反的失败行为：

| 机制 | 失败兜底 | 为什么 |
|---|---|---|
| 特性开关 | **fail-safe（默认关）** | 未发布的功能要保守，不能因后端抖动误开半成品 |
| 桌面自动更新 | **fail-open（默认开）** | 已发布的安全网络要保活，可远程熔断 |

**演进预留**：v1 配置驱动（翻转需重启），运行时即时翻转（Redis/DB override）为 v2 注入缝。

---

## 八、配置分层与种子数据 ✅ 已确定

### 8.1 启动依赖层级

| 层级 | 服务/配置 | 失败行为 |
|---|---|---|
| L0 硬依赖 | PostgreSQL, Redis | 启动失败 |
| L0 生产硬约束 | `JWT_SECRET_KEY`(非默认/非空)；`billing_mode=byok` ⇒ `ENCRYPTION_KEY`(64 hex，非空且合法)；`COOKIE_SAMESITE=none` ⇒ `COOKIE_SECURE=true`（桌面端 `app://` 跨站 cookie 前提） | `RuntimeError`（fail-closed，启动即拒） |
| L1 软依赖 | NATS, MinIO, 搜索引擎 | 警告后继续，相关功能降级 |
| L2 运行时 | LLM_API_KEY, Embedding | API 可运行，调用时失败或跳过 |

**设计原则**：关键安全配置走 fail-closed（缺失即拒绝启动），功能性配置走 fail-safe（降级但不崩溃）。

### 8.2 环境变量分层

两个 `.env` 文件明确分工：

| 文件 | 内容 | 消费方 |
|---|---|---|
| 根 `.env` | 镜像仓库凭据、镜像标签、依赖密码、内存调优 | compose `--env-file` + 部署脚本 |
| `config/.env` | 应用运行配置（JWT/`ENCRYPTION_KEY`/LLM Key/CORS 等） | api 容器 `env_file` |

DB/Redis/NATS 等连接串在 `config/.env` 仅占位，由 compose 的 `environment` 覆盖为服务名。

### 8.3 种子数据与确定性 UUID

使用 **UUID v5** 确定性生成种子实体 ID：`uuid5(NAMESPACE, slug)` → 同一 slug 永远同一 ID → seed 脚本幂等安全。→ 见 seed 脚本。

### 8.4 种子安全开关

| 开关 | 默认 | 理由 |
|---|---|---|
| `SEED_CLEANUP` | **关** | 破坏性。自动部署若带它，回退到旧 SHA 会跑旧种子集、连带删掉新 SHA 才加的实体——回退应只回退代码、不静默删数据 |
| `SEED_DEMO` | 跟随 dev/prod | 演示数据不应进生产；本地保留以便联调 |

### 8.5 平台 LLM 上游（runbook）

平台推理（`billing_mode=platform`）当前经**本地 `codex_chat_proxy`（`:9090`）** 转发到 ChatGPT Codex 后端——架构、凭证、启动、token 刷新与故障排查以 [`平台LLM接入.md`](/docs/05-平台与运维/平台LLM接入.md) 为权威 runbook。（历史：曾用 Sub2API 网关，因 K-12 账号限制弃用；`SUB2API_ADMIN_URL` 仅保留作 `platform` 模式 503 时的账号诊断探测。）

#### 切换平台模型

1. 编辑应用 `.env`（本地 `apps/server/.env`；生产 `deploy/config/production.env` 或 api 容器 `config/.env`）中的 `PLATFORM_MODEL`（如 `gpt-5` ↔ `gpt-5.5`）。
2. **重启后端**（本地 dev：停掉 `uv run python -m agentcore` 再启；生产：`finish-server.sh` 或 compose 重拉 api）。
3. 验收：新回合聊天输入区模型徽章、`GET /v1/llm/key/status` 的 `default_model` / `platform_model`、SSE `run_completed.model`、计费 `cost_events.model` 均显示新 id。

#### 更换上游地址或 API Key

1. 改 `PLATFORM_BASE_URL`（须含 `/v1` 后缀，例 `http://localhost:9090/v1`）和/或 `PLATFORM_API_KEY`。
2. 重启后端（同上）。
3. 验收：发一条云端回合或走 sidecar 本地引擎，确认推理成功且无 401。

#### 平台上游不可用时的排查

| 步骤 | 操作 |
|---|---|
| 1 | 确认本地 `codex_chat_proxy` 进程存活（默认 `:9090`）；proxy 502 / upstream_error 多为 Codex token 过期 |
| 2 | 检查 Codex access_token 是否过期（`config/codex-credentials.json` 的 JWT `exp`；过期/风控致上游 401/403），刷新后重启 proxy |
| 3 | 用 curl 直连 OpenAI 兼容端点：`POST {PLATFORM_BASE_URL}/chat/completions` + `Authorization: Bearer {PLATFORM_API_KEY}` |
| 4 | 查 AgentCore `logs/dev.jsonl`（或生产等价日志）关键字 `inference.proxy_upstream_error`、`llm.` 上游错误 |
| 5 | sidecar 回合额外确认 `inferenceToken.ts` 的 `baseUrl` 命中 `/v1/inference/v1` 代理 |

→ 架构与凭证、token 刷新、故障排查详见 [`平台LLM接入.md`](/docs/05-平台与运维/平台LLM接入.md)。

---

## 九、工程门禁与结构治理 ✅ 已确定

> 用 CI 不变量替代人工纪律，防止多 Agent 并行开发下行为回归/类型债/结构腐化悄悄累积。

### 核心哲学：棘轮（Ratchet）

基线 = 当前现状，落地当天全绿，只拦新增腐化。修好真问题就收紧基线（只降不升），放宽必须在 PR diff 里显形并给理由。

### 三道门禁

| 层 | 拦什么 | 策略 |
|---|---|---|
| **行为回归** | 后端 lint + 测试，前端类型检查 | 合并前 CI 硬阻塞 |
| **类型债** | tsc strict / mypy strict | 棘轮：逐文件计数基线，不允许新增错误。棘轮终局 = 修到零 → 升为硬门禁 |
| **结构治理** | god-file 膨胀、模块漏文档 | 行数棘轮（超标存量登记天花板）+ 模块文档守卫 + 清单守卫 |

### 跨端契约门禁

> **实现现状**：✅ 已写入 `ci.yml` 的 `contracts` job（"Contracts + conformance (gen + drift gate)"）：`node scripts/gen-types.mjs`（重生成 REST + SSE 契约产物）+ `agentcore.conformance.export`（重生成一致性夹具）+ `git diff --exit-code`——committed 产物与真相源漂移即红。**但 GHA 无配额、CI 实际不跑 → 发布前本地手动跑这三步核对 0 diff。**

后端改 schema / SSE 事件必须重新生成前端类型并提交，`git diff --exit-code` 拦截 generated 静默漂移（目标）。覆盖两条契约：

| 契约 | 真相源 | 生成产物 |
|---|---|---|
| REST | OpenAPI schema | `openapi.ts` |
| SSE | 后端 EventType + payload dataclass | `sse-events.ts` |

`task gen:types` 一次重生成两者。根除「后端发了新事件/字段、前端 union 或 interface 漏抄、功能静默装死」。

### 拆分实操经验

- **facade + 子包**：按子域拆，`__init__` 全量 re-export 保持公共 API 不变 → 调用方零改动
- **mixin vs 协作者类**：跨切面、读写宿主多字段的状态逻辑用 mixin——经 MRO 保留 `self._x()` 调用、零调用点改动
- **棘轮行数口径**：脚本内自产自校，CI 与基线自洽；勿用不同工具对账（口径不同）
- **陷阱**：移动挂着门禁的文件要同步硬编码路径，否则门禁静默失效

---

## Cursor 代部署操作手册

**2026-06 决策**：所有**部署/发版**不走 GitHub Actions push 触发（私有仓 Actions 配额/账单不可靠）。GHA workflow 仅 `workflow_dispatch` 紧急备用。→ [`部署与运维.md` §三](/docs/05-平台与运维/部署与运维.md)

凭据：`deploy/.env.deploy.local`。**禁止**在 chat 索要/回显 token / SSH 私钥 / `RELEASE_GH_TOKEN`。

### 全端发布顺序

| 步 | 动作 | 命令 |
|---|------|------|
| 1 | 推送 `master` | `git push origin master` |
| 2 | 后端（构建+部署一体） | `pnpm deploy:backend <short-sha>` |
| 3 | 官网 | `pnpm -C apps/website deploy:pages` |
| 4 | 手机 | `pnpm -C apps/mobile deploy:pages` |
| 5 | 管理后台 | `pnpm -C apps/admin deploy:production` |
| 6 | web 客户端 | `pnpm -C apps/desktop deploy:web` |
| 7 | 验收 | 见下表 |
| 8 | 打发布点 tag（**不触发 GHA**） | `git tag prod-vN && git push origin prod-vN` |

> web 客户端 `deploy:web` **仅同步静态文件 + reload**；一次性 Nginx 接线（app. `:443` 兜底 `location /` 替换为静态 SPA，保留 `/api`、`/storage` 等）已于 2026-06-30 完成 → 参考 `deploy/nginx/app-web.conf`、docs §二。

协议变更类另需桌面发版 → 见 §桌面发版。

**桌面版本号已 bump 时**：须先完成 §桌面发版（Publish `vX.Y.Z`），再跑步 3 官网。官网构建会拉 GitHub `releases/latest` 写入下载页；若在 Publish 前部署，会把旧版 bake 进静态页。运行时 `/api/desktop-release`（Pages Function）可纠偏，但 Function 必须随 `deploy:pages` 上传（wrangler cwd = `apps/website`，才能看到 `functions/`）。

#### 版本漂移防护（后端先行 + 自动门禁）

**铁律：新增/改接口 → 后端必须先部署（步 2）。** 后端对旧前端向后兼容，早部署安全；反过来「前端先上」会调到后端还没有的接口 → 404（记忆·主题 曾因此在生产显示「不可用」）。**绝不从比线上后端新的 commit 单发前端**（哪怕自以为是「纯 UI」——里面可能夹着需新后端的功能）。

三个前端 deploy 脚本（`deploy:web` / mobile `deploy:pages` / admin `deploy:production`）在**构建前**都跑 `assertBackendContractSatisfied`（`deploy/scripts/load-deploy-env.mjs`）：比对本次 `git HEAD` 与线上 `GET /api/version` 的 `git_sha`，仅当**后端已含本次 commit**（HEAD 是后端 sha 的祖先）才放行；前端更新则**直接拦截退出**。确需强发（已核实无新接口）加 `--force` 或 `DEPLOY_SKIP_CONTRACT_GATE=1`。探针失败/sha 未知时**放行并告警**（不因抖动阻断）。

### 验收

| 目标 | 检查 |
|------|------|
| API | `curl.exe https://app.fashitianxia.xyz/api/version` → `git_sha` |
| web 客户端 | `curl.exe https://app.fashitianxia.xyz/` → 200 出 SPA（同源 cookie + 云 SSE） |
| 官网 / 手机 / admin | HTTP 200（见 §二 URL） |
| 官网桌面版 | `curl.exe https://fashitianxia.xyz/api/desktop-release` → JSON `version` 与 Latest 一致（非 404） |

Windows 用 **`curl.exe`**（PowerShell `curl` = `Invoke-WebRequest`）。

### 后端（生产机构建）

```bash
pnpm deploy:backend <short-sha>      # = remote-build-deploy.mjs
pnpm deploy:backend:status           # 查 buildx 是否仍在跑
```

SSH 到生产机 → `git checkout` → `docker buildx build`（默认清华 PyPI）→ 推 ACR(VPC) → `finish-server.sh`。

| 坑 | 对策 |
|---|---|
| 本地 Windows `docker build` | 加 `--build-arg PYPI_INDEX=https://pypi.org/simple/`；日常仍应在生产机构建 |
| `uv sync` 15–30min+ | 首次无缓存正常，勿杀 |
| 仅重部署、镜像已在 ACR | SSH：`bash deploy/scripts/finish-server.sh <sha>` |

**禁止**依赖 `build-backend.yml` / `deploy-backend.yml` 的 push 触发。

### 桌面发版

```bash
pnpm bump-version desktop patch
git commit && git push && git tag desktop-vX.Y.Z && git push origin desktop-vX.Y.Z
# tag 不触发 GHA
# Windows（本地）：
$env:GH_TOKEN = (gh auth token)                         # gh 登录 Lawofall 且对 releases 仓有写权
pnpm -C apps/desktop release:win                        # --publish never → gh upload --clobber → 资产断言
# macOS（GHA 仅 mac；勿用 GHA 打 Win）：
gh workflow run "Release Desktop"
# 上传目标 = Lawofall/AgentCore-releases 的 vX.Y.Z draft（win/mac 汇入同一 draft）
# draft 齐 7 资产（win+mac）→ Publish → bump-version desktop 已同步 FALLBACK → deploy:pages
```

**⚠️ 竞态坑（electron-builder#6676）**：旧路径 `--publish always` 会并行 create release，常只传上 `.blockmap` 而**丢掉 `.exe` / `latest.yml`**。**现行 `release:win` 已对齐 Mac**（never + `gh upload` + `gh release view` 断言），勿再手搓 `--publish always` 当唯一上传路径。

**开发期例外（单平台转正）**：无真实用户时可仅 win 转正（`gh release edit vX.Y.Z --repo Lawofall/AgentCore-releases --draft=false --latest`）；mac 就绪后由 GHA `Release Desktop` 或 `gh release upload` 补进**同版本**。站点缺 mac 资产会自动隐藏 mac 按钮、win 自更新不受影响；`FALLBACK_VERSION` 暂留上一含 mac 的版本。正式上线后恢复「Win+Mac 齐全再转正」。

**禁止**依赖 `release-desktop.yml` 的 push/tag 触发；**禁止**用 GHA 打 Windows。

### 跨轨道发布（摘要）

| 变更 | 后端 | mobile/admin | 桌面 |
|------|------|--------------|------|
| 纯后端 | 随时 deploy | 不必 | 不必 |
| 协议/fold | 同日 deploy | 同日 | 7 日内 patch |
| 纯 UI | 不必 | 随改动 | 独立 |

门禁：`pnpm conformance` 全绿。详见 docs §7.4。

### 纪律

- 不重新启用任何 deploy/release workflow 的 **push / tag / schedule 自动部署**
- `prod-*` / `desktop-v*` tag 仅作发布点标记与漂移检测
- **CI（`ci.yml`）实际不跑**：GHA 无配额（push/PR job 0 step 秒失败）；触发保留不动，配额恢复即自动复活。**发布前本地一键门禁为唯一验证**：`pnpm release:gate`（与 `ci.yml` 同构：backend ruff+unit pytest / contracts 漂移 / desktop lint+**typecheck**+test+**conformance**+shoot+web 冲烟 / mobile lint+typecheck+conformance / admin typecheck；任一步非 0 即失败）
- **新增/改接口先部署后端**；前端 deploy 脚本已内置版本漂移门禁（见 §版本漂移防护），被拦截时先补后端而非直接 `--force`
