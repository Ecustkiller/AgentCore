---
status: blueprint
code: apps/server/agentcore/
related:
  - docs/02-架构/项目结构.md
  - docs/02-架构/核心接口定义.md
  - docs/03-AI核心/执行引擎架构设计.md
skip_if:
  - 只改 API 路由（读核心接口定义）
---

# 后端架构

> **状态**：✅ 已确定（五层分层、模块体系、runtime 子包划分、垂直能力开关、评估体系）
>
> **与项目结构的关系**：本文描述完整**目标架构**的模块体系与分层约定；实际目录结构见 [`项目结构.md`](/docs/02-架构/项目结构.md)。运行参数 / 接口契约见 [`核心接口定义.md`](/docs/02-架构/核心接口定义.md)。

---

## 一、五层分层

每个业务模块遵循 `models → schemas → repository → service → router`，调用方向严格单向、不可反转：

| 层 | 职责 | 约束 |
|----|------|------|
| **models** | ORM 定义 | → 见 [核心接口定义.md §6.2](/docs/02-架构/核心接口定义.md)（无 ORM FK、软删除、文件单表等建模约定） |
| **schemas** | Pydantic request/response | 严格区分 request / detail / list item 等分层 |
| **repository** | 纯数据访问 | 接收 AsyncSession；不含业务逻辑 |
| **service** | 业务逻辑 | 构造注入 AsyncSession，内部实例化 repository，组合多 repo |
| **router** | HTTP 端点 | 薄层委托 service，用 DI 别名 |

> **service 层的边界（务实约定）**：service 是**有真业务逻辑**（多 repo 组合、编排、跨副作用、外部调用）时才设的层，**不是**每条路由的强制透传。**简单 CRUD 路由允许经 DI 直接注入 repository** 在 handler 里增删改查（如 `routes/folders.py`）——给纯 CRUD 套一层 `service.create()→repo.create()` 是仪式而非架构。判据：handler 里只有"取 repo→调一两个 repo 方法→返回"就直用 repo；一旦出现条件分支/多 repo/调 llm/外部副作用，抽 service。

---

## 二、模块组织

后端按职责分三区。下表只记录**非显然边界**；完整目录 → 见代码 `apps/server/agentcore/`。

**平台核心**（基础设施 + 平台级通用能力）：

| 模块 | 定位 | 边界说明 |
|------|------|----------|
| common | DB / Redis / 消息队列 / 对象存储 / 错误 / 日志 基础设施 | MVP 精简为 db + core |
| middleware | JWT 认证 + 请求日志 + 限流 + 服务间内部认证 | 认证走路由依赖 `api/dependencies.py`；`auth.py` JWT 中间件仍空壳；请求日志/服务间认证 ⏳ |
| auth / security | 注册/登录/JWT 刷新/令牌轮换/凭据加密 | 无 `user/` 包；实为 `auth/` + `security/`，见 [`认证与会话.md`](/docs/05-平台与运维/认证与会话.md) |
| agent | Agent & Team CRUD + 版本管理 + Fork + 语义发现路由 | ⏳ Post-MVP（MVP 用 CEO 按需 `delegate` 动态分配） |
| conversation | 对话管理 + SSE 流式聊天 + Turn 聚合层 | 导出（MD/JSON）+ 公开只读分享 ✅，见 [安全治理 §一](/docs/05-平台与运维/安全权限与治理.md) |
| runtime | ReAct 引擎 + Prompt 组装 + 工具注册 + Agent 委派 + CEO 编排 + 工具审批 | DAG 检查点 ✅（`checkpoint_after` 2a + turn 级落盘/`resume` 2b）；审批策略（用户自治三档 `users.autonomy_policy` + 沙箱→审批姿态 `runtime/sandbox_approval.py`）权威见 [安全权限与治理 §三](/docs/05-平台与运维/安全权限与治理.md) |
| llm | LLM Gateway — 多 Provider 统一调用 | |
| capability | Tool / Skill / Rule 三类能力实体的定义/绑定/版本 | ⏳ Post-MVP（MVP 内置工具直接注册） |
| file | 文件上传/下载 + 在线文档 + 工作区上下文 + RAG 检索 | 工作区文件 CRUD / 快照 / clone ✅（`api/routes/workspaces.py`、`api/routes/conversations/files.py`，见 [双模式工作区](/docs/02-架构/双模式工作区.md)）；RAG 检索（`document_chunks`）⏳ Day 2 |
| billing | LLM 调用的 token 用量 + 成本记录 | 落账/旁路走 `billing/cost_ledger_queue` + `proxy_spend_queue` 落盘出队；见 [`成本配额与计费.md`](/docs/05-平台与运维/成本配额与计费.md) |
| task | 异步任务引擎 — 消息队列 + Worker 后台执行 | ⏳ Post-MVP |

**平台业务**（面向用户的业务模块）：

| 模块 | 定位 | 边界说明 |
|------|------|----------|
| messaging | 消息 IM（找人）：人↔人单聊 + 实时 firehose + 隐私护栏 | 官方号 / 群 / 多 worker ⏳，见 [`消息IM.md`](/docs/05-平台与运维/消息IM.md) |
| webhook | Agent 触发器 + headless Run 执行（事件驱动） | ⏳ Post-MVP |
| scheduler | 提醒与日程 | ⏳ Post-MVP |
| attention | 跨会话「待办流」：聚合可恢复的暂停 Run + 待审批工具调用 | ⏳ Post-MVP |
| share | 统一分享（文件/对话，share_token + 过期） | 统一插件 ⏳ Post-MVP；**对话导出 + 公开只读分享已落地**（首切片，内建于 conversation 模块），见 [安全治理 §一](/docs/05-平台与运维/安全权限与治理.md) |
| table | 多维表格 — 结构化数据记录 CRUD | ⏳ Post-MVP |
| admin | 管理后台：管理员鉴权 + 操作审计 + 用户运营 | ✅ 已落地（`apps/admin` + `api/routes/admin/`），见 [`管理员后台.md`](/docs/05-平台与运维/管理员后台.md) |
| desktop | 桌面自动更新策略（版本政策 + 远程熔断 + 下载入口） | 远程熔断 ✅（`GET /updates/policy`，`api/routes/system.py` 读 `desktop_updates_enabled`）；灰度百分比 / 渠道 ⏳ Post-MVP |
| flags | 特性开关 / 远程配置（灰度百分比 + 最低客户端版本门控） | ⏳ Post-MVP |
| simulation | AI Town 模拟垂直（NPC / 场景 / 快照回放） | ✅ feature flag `simulation_enabled`（默认开），见 §四 |
| board | AI 协作白板后端（画布 op / 读图 / 结晶化） | ✅ 见 [`AI协作白板.md`](/docs/04-前端/AI协作白板.md) |
| vision | 图像理解（读图，qwen 视觉档） | ✅ worker 视觉入账 |

> **创作工具产物统一**：思维导图 / 画布 / 多维表格 / 在线文档等「创作工具」产物，共用 `file`（文档/上传）+ `table`（多维表格）的产物存储与 `share` 分享，不另起存储；模型见 [工具与能力系统 §8.4](/docs/03-AI核心/工具与能力系统.md)。

**`config/` 领域分组**（`apps/server/agentcore/config/`，扁平 `settings.xxx` 不变）：

| 域 | 模块 | 典型字段 |
|----|------|----------|
| database | `database.py` | `database_url`, `redis_url`, `db_echo` |
| llm | `platform.py` | platform 凭据 / 厂商 key / `billing_mode`（BYOK 用户 key 存 DB per-user，非本文件） |
| search | `search.py` | SearXNG / Tavily |
| auth | `auth.py` | JWT / cookie / CORS / 限流 |
| approval | `approval.py` | 工具审批门 |
| checkpoint | `checkpoint.py` | ask_user / plan_review / 结构化挂起 |
| engine | `engine.py` | 超时 / 治理 / tool-clear / finish-guard |
| persistence | `persistence.py` | roster / memory / compaction |
| quota | `quota.py` | 免费档配额 |
| server | `server.py` | HTTP / 日志 / 推送 |
| workspace | `workspace.py` | 存储 / 快照 / 本地 op 超时 |
| features | `features.py` | 垂直 / 实验能力开关（`simulation` / `legal`，见 §四） |
| simulation | `simulation.py` | AI Town 模拟参数 |
| paths | `paths.py` | 路径配置 |

---

## 三、runtime 子包结构

> 目标子包划分如下；实现现状 → 见代码 `apps/server/agentcore/runtime/`。

`runtime/` 是执行核心，按运行时关注点分子包：

| 子包 | 关注点 | 现状 |
|------|--------|------|
| engine | ReAct 主循环（LLM 调用 / 工具执行 / 轮次控制 / 上下文装配） | ✅ `engine/`（loop / round / governance …） |
| runs | **Run 模型**：统一执行单元——RunPlan + WaveScheduler + 执行器 | ✅ |
| journal | **Turn Journal**：每 turn append-only 事实流 | ✅ `journal/`（entries / fold / writer / fold_cache） |
| interactions | **统一交互原语**：server→client 等待的单一类型 | ✅ `interaction.py` |
| delegation | Agent 委派执行：派发 + 契约 + 熔断 + 并发预算 + 预审 | ✅ 经 `delegate` + `runs/` |
| prompt | system prompt 组装（skills / rules / workspace 注入） | ✅ `resolve/prompt` |
| events | SSE 事件类型 + 事件总线 | ✅ |
| host | 运行时宿主端口（各接缝 Protocol） | ✅ `ports.py` |
| resolve | 回合准备：agent→prompt→tools 解析 + 任务复杂度分级 | ✅ `resolve/`（prompt / profile / prepare） |
| memory | 工作记忆：工件存储 + TWM 注入 | ⏳ 无独立子包；注入散在 `runs/executor_context.py`+`runtime/context/`，长期记忆见 `agentcore/memory/` |
| suspend | 持久暂停/恢复 + checkpoint | ✅ `suspension*.py`（非独立子包） |
| sandbox | 代码执行沙箱后端 | ✅ 在 `tools/sandbox/` |

**CEO 内置编排原语**（`tools/builtin/`，与 `runtime/` 子包同构——按变更原因拆目录）：

| 原语 | 路径 | 子模块 | 现状 |
|------|------|--------|------|
| `delegate` | `tools/builtin/delegate/` | tool / drive / supervised / suspension … | ✅ |
| `ask_user` | `tools/builtin/ask_user/` | tool / suspend / result / schema | ✅ |
| `debate` | `tools/builtin/debate/` | tool / rounds / prompt / events / schema | ✅ |

> 三个一等运行时模型（Interaction / Turn Journal / Run）的目标设计见 [`执行引擎架构设计.md` §八](/docs/03-AI核心/执行引擎架构设计.md)。

---

## 四、垂直能力开关（feature flags）

平台核心行业无关；垂直 / 实验能力用**编译期 feature flag**（`config/features.py` 的 `FeatureSettings` 布尔字段）分层叠加——现状是最简机制，**不是**插件贡献点契约或约定式扫描。

| 开关 | 默认 | 关闭时行为 |
|---|---|---|
| `simulation_enabled`（AI Town 模拟） | `True` | simulation REST 路由返 404 |
| `legal_vertical_enabled`（法律垂直 v0） | `False` | 法律域 Skills 不注册进 CEO 系统 Skill 注册表、不出现在能力目录 |

- **注册方式**：所有路由在 `main.py` 里**无条件** `include_router`（含 `simulation`）；「启停」发生在**处理层**（关 → 404）或**注册层**（Skill 不进目录），而非按开关决定是否挂载路由。
- **依赖方向**：垂直代码（`simulation/` 等）可依赖核心，核心不反依赖垂直。
- **边界**：这是 v0 权宜（法律 Skills 待迁移到 per-agent 市场绑定）。「6 贡献点契约 / 约定扫描 / 白名单」式插件化**尚未实现**；若要做属架构提案，走 `06-规划`，不在此预写。

→ 见代码：`config/features.py`、`main.py`（`include_router` 段）。

---

## 五、评估体系（L0 契约硬门 ✅ / L1 结果评分 ✅ / L2 协作质量 ⏳ / L3 轨迹回流 ⏳）

> **设计立场（为什么是这个形态，代码看不出来）**：评测以「**任务结果**」为中心，而非「执行轨迹机制」。2026-06 方向评审否决了旧体系的根本假设——旧体系把 `Delegated`/`RosterMatches`/`MaxRounds` 这些**手段**当门禁、在回归测试作者自己的编排理论（按 [`dev-process.mdc`](/.cursor/rules/dev-process.mdc) 属「实现冒充需求」），且**投资倒置**（per-PR 硬门禁守最便宜最低价值的「JSON 能否解析 / 有没有调某工具」，唯一衡量用户价值的质量裁判却软门禁、从不阻断）。新体系把投资从「下重上轻」翻为「**中间重**」：L0 只兜安全 / 契约不变量，**L1 结果质量是被守的主轴**。

> **开发期提醒**：本体系当前跑在合成 `core` + milestone 用例上，**无任何真实运行数据**（真实对话 / 线上 pass_rate / 质量分都不存在）。基于这些跑分讨论或优化质量时，须先声明「目前无真实数据，以下为假设」（见 [`dev-process.mdc`](/.cursor/rules/dev-process.mdc)）。

骨架落地于 `agentcore/evals/`（离线 CLI：`python -m agentcore.evals`），四层 L0–L3：

| 层 | 判定对象 | 何时跑 | 门禁性质 | 落地 |
|---|---|---|---|---|
| **L0 契约 / 安全硬门** | 不崩、引用可解析、不编造工具调用、schema 合法、成本 / 延迟预算 | per-PR | **硬门禁（零 LLM）** | ✅ `checks.py` 不变量子集 |
| **L1 结果评分** | 用户要的拿到没有（端到端、结果维） | per-PR 小集 + nightly 大集 | **可门禁**（baseline 回归） | ✅ rubric 裁判 + milestone + pass^k |
| **L2 体验 / 信任 + 协作质量** | 可读性、诚实度、延迟、该追问是否追问；团队 milestone / 协作 | nightly | 信息性 → 逐步可门禁 | ⏳ → [`远期规划.md §2.4`](/docs/06-规划/远期规划.md) |
| **L3 轨迹回流 + 诊断** | 真实轨迹聚类找失败；团队 vs 单体「值不值」 | 现挖 / 上线后在线 | 喂数据，不直接门禁 | ⏳ → [`远期规划.md §2.4`](/docs/06-规划/远期规划.md) |

**L0 契约硬门（轨迹形状降级为诊断）**：保留不变量子集 `FinishReason`(错误态) / `NonEmpty` / `ToolArgsValid` / `HasCitations`(可解析) / `NoFabricationMarker` / `Max*`(成本·延迟预算)；`Delegated` / `NotDelegated` / `RosterMatches`(轨迹形状)**降级为诊断**——仍注册仍跑仍报告，但 `gating=False` 不计 pass/fail（→ `checks.py` `DIAGNOSTIC_CHECKS`、`runner.apply_checks`）。过度编排不再用 `NotDelegated` 标签判，改由「个体贡献=0 + L0 成本预算」度量。

**L1 结果评分（裁判从「接口位、永不阻断」扶正为主轴）**：
- **rubric 绝对裁判** `judge.LLMJudge` + `build_default_judge`（Pro 评 Flash）。
- **milestone 结构化覆盖** `LLMMilestoneJudge`：把任务子目标写成可逐条判定清单，一次结构化打分返回逐项布尔，覆盖率=加权命中比，≥阈值且无编造即过（→ `types.py` milestone/`outcome` 字段、`seed_lint.py` 结构校验）。
- **baseline 回归门**：分数落盘 + `--update-baseline`，只有**跌破** baseline 超阈才红（区别旧「全过才绿」）（→ `report.baseline_regression` + CLI `--baseline`/`--update-baseline`）。
- **pass^k**：看 k 次重跑的一致成功率，非单次。

**裁判可信工程（DeepSeek-only 适配）**：裁判是测量仪器，先校准再用。
- **校准回路**：生产裁判过人工 gold-set → 算 **Cohen's kappa** / 加权 kappa·Spearman（**不用 exact-match**，会高估 33–41pp）→ **kappa>0.6 才准上门禁** → 读分歧改 rubric/prompt 重跑；重校触发=月度 + 改 rubric / 换模型 / 判↔人分歧 >20–25%（→ `calibration.py` 纯统计零依赖 + CLI `--calibrate`；gold-set schema 见 `cases/gold/labels.example.json`）。
- **消偏**：位置偏差走 swap-and-aggregate（`LLMPairwiseJudge`，诊断用）；长度偏差 rubric 明写「不奖励长度、惩罚注水」；同家族自偏好用 **Pro 评 Flash** + 持续监测 kappa（**已知、被监测**的限制）；CoT 先理由后给分。
- **成本纪律**：裁判成本控在生产 LLM 的 **10–15%** 内。**反模式**：100% 通过=eval 太水的警报，不是胜利。

**多 Agent：milestone KPI 取代轨迹标签**：评测问题从「团队比单体好吗（研究问题）」转为「团队这一跑**交付目标了吗 + 协作得当吗**（产品质量）」——**milestone 覆盖**（结果维，不管谁干的）+ **协作质量分**（5 点 LLM：子产出有没有被 CEO 真整合）+ **个体贡献**（派了但零贡献=浪费，这才是「过度编排」的正确度量）；`team_quality` 只在 team 路径跑。`comparison.py`（等算力 `matched_single` 臂 + best-of-N 擂台 + 思考-token 对齐）**降为偶发诊断**（要做路由引擎前 / 季度手动跑，协议保留备查），不再当核心 nightly 回归。

**门禁分层**：

| 闸 | 内容 | 红线 |
|---|---|---|
| **per-PR** | L0 全部(硬) + L1 小集(廉价裁判档) + baseline 回归 | L0 任一不变量破→红；L1 跌破 baseline 超阈→红 |
| **nightly** | L1 大集 + L2 + L3 诊断 + 切片分析 | 信息性为主，回归显著才告警 |

per-PR CI 仅跑零-LLM 自测（假 provider）；真模型评测见 `evals-nightly.yml`。切片分析按 intent / 质量档 / 路径分段出均值 + 置信区间 + **分布**（防双峰被均值掩盖），CI 宽于 delta 时不宣布胜利。

**评测基建（确定性 plumbing，零真模型即可单测）**：以下三件都守「**零侵入复用 harness、不改 `pipeline`/`engine` 签名**」铁律——确定性算术 + 用例结构可零 LLM 单测，但**出数都需真模型回合**（故度量仍挂上文 L1 + 远期 §2.4 主线）。

- **提示词变体注入** `runtime/resolve/profile.py`：静态片段（`base`/`ceo_core`/`citation`/`ceo_visualization`）经 **contextvar 解析器** `resolve(key, 原常量)` 取文案——**选 contextvar 而非穿参，正是为守零侵入铁律**；无活跃变体（生产恒态）→ 逐字节不变 → **前缀缓存零影响**；变体走「空串覆盖即移除块」的**消融式**（不编造新文案）。配 `EvalCase.prompt_profile` 字段 + 注册表 `evals/prompt_profiles.py`。这是「先能 A/B 比较提示词、再谈优化」的 plumbing 前置。
- **路由准确率探针** `routing.py`：观测信号复用 `TurnOutcome.delegated`、标签即用例 `Delegated`/`NotDelegated` check（**不引新 schema 字段**；`seed_lint` 保证 `category="routing"` 用例恰好声明其一），出混淆矩阵 + 两类业务错误率（过度编排率 `FP/(FP+TN)` / 组队不足率 `FN/(TP+FN)`，正类=应当委派）。CLI `--routing`，诊断用。
- **anti-slop 风格 linter** `style_lint.py`：零 LLM 锚定检测三类 `<output_style>` 禁项（套话开场锚首句 / 客套收尾看末句 / 未授权 emoji 走 Unicode 块，**刻意排除箭头·数学符号避免误报**）；两用法 `StyleCleanCheck`(就地断言零 slop) + `style_metrics`(违规率聚合 + 逐条 offender)。

**现有代码模块处置**：`checks.py`(不变量子集 + 轨迹形状降诊断)、`judge.py`(rubric 绝对裁判 + 成对诊断裁判)、`calibration.py`(gold-set kappa 校准，新增)、`comparison.py`(降季度诊断 CLI)、`harness.py`/`types.py`/`runner.py`/`report.py`(复用 + 扩 milestone / 协作 / 体验 / 切片字段)、`routing.py`(路由准确率混淆矩阵暂留诊断，等路由引擎再定)、`cases/`(`core` 改结果 milestone、`comparison` 留诊断、新增真实轨迹回流分桶集 ⏳)、`style_lint.py`(并入 L0/L2)。

> **未落地（数据 / 上线依赖）→ 远期**：真实轨迹回流挖掘（gold-set 数据采集 + intent 分桶集）、L2 体验 / 协作轴 nightly 深化、L3 在线采样闭环、评估→路由反馈——均见 [`远期规划.md §2.4`](/docs/06-规划/远期规划.md)。**校准门依赖**：baseline 硬门禁吃 `LLMJudge` 的 pass_rate，上线前须先有 100–200 条人工 gold-set 过 kappa≥0.6 校准门——人工标注是唯一人力前置（工具已就绪）。

> 工程门禁（棘轮 / 类型债 / 结构治理 / 跨端契约门禁）见 [`部署与运维.md` §九](/docs/05-平台与运维/部署与运维.md)。

---

## 日志系统规范

## 架构

渲染按「输出目标 × 环境」分发（`ProcessorFormatter` 给每个 handler 各自的 renderer，见 `core/logging.py`）：

| 输出目标 | dev (`DEBUG=true`) | prod |
|---|---|---|
| stdout | ConsoleRenderer（彩色可读） | JSONRenderer |
| `LOG_FILE` | **JSONRenderer（JSONL）** | JSONRenderer（JSONL） |

- **文件输出始终是 JSON Lines**（一行一个 JSON、无 ANSI），与环境无关——便于工具/AI 逐行 `json.loads` / `jq` 解析。
- 外部库日志（uvicorn / sqlalchemy）经同一 `foreign_pre_chain` 渲染，格式统一。
- SQL 全量回显是独立开关 `DB_ECHO`（`db/base.py`），**不挂在 `DEBUG` 上**——开 DEBUG 不会刷屏 SQL。

## 环境变量

- `LOG_LEVEL`：debug / info / warning / error（默认 info）。
- `LOG_FILE`：日志文件路径（相对路径对仓库根解析），空=仅 stdout。**开发设 `logs/dev.jsonl`**。
- `LOG_LLM_BODIES`：是否抓取 LLM prompt/回复正文（`llm.request`/`llm.response`）。**开发默认开**、**单开关即可**（emit 在 info 级，无需再调 `LOG_LEVEL=debug`）；始终**截断+脱敏**（铁律：永不落 BYOK key / 完整正文）。**生产默认关**（正文大且敏感）。

## Logger 获取

```python
from agentcore.core.logging import get_logger

logger = get_logger(__name__)
logger.info("component.action", key=value)        # 事件名第一参数，数据用 kwargs
logger.error("component.action", error=str(e), exc_info=True)
```

- 始终用 `get_logger`，**禁止** `logging.getLogger` / `structlog.get_logger`（否则脱离统一渲染与关联上下文）。
- 不在日志里记敏感信息（BYOK key、密码、token）；消息正文只记 preview，不落完整体/文件内容。
- **代码锚定**：每条 app 日志自动带 `logger`（模块）+ `func_name` + `lineno`（`core/logging.py` 的 `CallsiteParameterAdder`，只加在 app 链、不加在 foreign 链）——`module.func:line` 直接从一行日志跳到埋点源码，无需 grep 事件名。foreign（uvicorn/sqlalchemy）行不带（它们自带 stdlib callsite）。

## 事件命名

格式 `组件.动作`（动作用 snake_case），如 `chat.turn_start`。全仓库已统一此式，**新增日志必须遵循**——禁止裸名（`pipeline_error` 这类无组件前缀的旧风格）。

## 关键事件词表（权威源：代码）

| 组件 | 关键事件 | 含义 / 排查价值 |
|---|---|---|
| `chat` | `chat.turn_start` · `chat.turn_complete` | **回合脊柱**：起点带 `preview`(用户问·~200字)/`chars`/`history`/`location`；收尾带 `finish_reason`/`rounds`/`input_tokens`/`output_tokens`/`reply_preview`(答复·~200字)/`delegated`/`workers`/`duration_ms` |
| `chat` | `chat.title_failed` · `chat.stream_error` · `chat.regenerate_error` | 标题生成失败 / 流式回合异常 / 重答异常 |
| `delegate` `revise` `worker` | `delegate.started`/`delegate.rejected` · `revise.started`/`revise.miss`/`revise.capped` · `worker.escalate` | **编排决策·为什么+输入依据**（非仅计数）：委派带 `nodes`/`call`/`parallel`(首波并行数)/`agents`(每节点「角色: 任务~80字」·上限12)——派了谁干什么；热修带 `target`/`feedback`(改什么~200字)，miss/capped=召回被拒；升级带 `kind`(normal/scope/dep)/`blocking`/`question`(求决策~200字)/`assumption`(超时回落~160字) |
| `tool` | `tool.execute_start`(debug) · `tool.execute_end`(info) | 每次工具执行：end 带 `tool`/`status`(ok/error/not_found/denied)/`duration_ms`——`log_stats` 工具质量段数据源；`duration_ms` 不含授权等待 |
| `react` | `react.round_start`(debug) · `react.round_end`(info) | 每轮 ReAct：end 带 `round`/`tools`/`input_tokens`/`output_tokens`/`reasoning_tokens`/`done`——推理质量(每轮 reasoning_tokens)+工具强度可观测 |
| `engine` | `engine.loop_nudge` · `engine.loop_finalize` · `engine.max_rounds_exhausted` · `engine.force_finalize_failed` | 收敛治理：机械循环被提醒 / 强制收尾 / 轮预算耗尽 / 兜底收尾失败 |
| `llm` | `llm.call`(info) · `llm.request`/`llm.response`(info, 需 `LOG_LLM_BODIES`, 开发默认开) · `llm.call_failed` · `llm.retry`/`timeout_retry`/`stream_retry` | **每次 LLM 调用**：call 带 `scenario`(chat/agent.*/memory/title)·`model`·`latency_ms`·`finish_reason`·in/out/reasoning/cache tokens·`stream`——LLM 层成本/延迟/质量归因（埋点：`llm/observability.py`，choke point=provider `complete` + engine `_stream_llm_round`）；request/response=截断脱敏的 prompt/回复（调 prompt 用，开发默认开、生产关）；失败/重试见 `*_failed`/`retry` |
| `run` `contract` | `contract.retry` · `contract.failed` · `run.failed` · `run.captain_failed` · `run.revise_failed` | worker 契约重试/失败、run 异常 |
| `cost` `workspace` | `cost.recorded` · `cost.ledger_write_failed` · `workspace.snapshot_created/failed` | 落账成功（`runs`/`total_nano`/`total_usd`/`models`）/失败 · 工作区快照（best-effort，warning-only） |
| `pipeline` | `pipeline.error` | 回合管线未捕获异常（带 `exc_info`） |

## Context 注入（Trace 关联）

`structlog.contextvars` 的字段由 `merge_contextvars` 自动并进**每一行**日志。封装在 `core/log_context.py`（`new_trace_id` / `bind_log_context` / `clear_log_context` / `log_context` 作用域版 / `get_log_value`），**勿直接调 `structlog.contextvars`**。

| 键 | 含义 | 绑定点 |
|---|---|---|
| `trace_id` | **一次用户交互全生命周期**（唯一关联键） | `conversation/service.py` 回合起点铸造 |
| `conversation_id` `turn_id` `user_id` `agent_id` | 会话 / 回合 / 用户 / 当前 Agent | 同上 |
| `run_id` `depth` | 委派 / DAG 子节点 | 用 `log_context(...)` 作用域绑定，退出自动还原 |

> AgentCore 是**进程内 asyncio**：委派 worker 是子 task，`contextvars` 在建 task 时拷贝——`trace_id` 自动随 worker 日志传播，无需像跨进程方案那样手工串 payload。

> **`trace_id` 已落库**（DB↔日志 join 键）：`messages` / `cost_events` / `run_sessions` 各带可空 `trace_id` 列——assistant 回复与该回合的落账行写本回合 trace，recoverable worker 写其**首次**生成回合的 trace（revise 不覆盖）；user 消息与 handoff 任务为 `NULL`（无 trace 作用域）。于是可从一条持久化回复/落账行反查整条日志链，日志轮转后仍能定位。写值点：`conversation/service.py`（本地变量回传，因落库在 `log_context` 退出之后）、`runtime/session_persistence.py`（读 contextvar）。

调试：`grep trace_id=<id>` 拉一次交互全链路；`conversation_id` 关整会话；`run_id` 下钻单个委派节点。分析工具见下文 §对话日志分析指南。

---

## 对话日志分析指南

## 它是什么 / 在哪

- **`logs/dev.jsonl`**（仓库根）= 后端 `structlog` 运行时日志，**JSON Lines**（一行一个 JSON、无 ANSI），可直接逐行 `json.loads` / `jq` 解析。
- 需后端设 `LOG_FILE=logs/dev.jsonl`（`apps/server/.env`）才会写文件；否则只有 stdout。
- 记录每个回合的**事件**：`chat.turn_start/complete`、委派、收敛治理、工具/LLM 报错、落账。
- ⚠️ **不落完整正文**（**完整**对话文本在 **Postgres** `conversations` / `messages`）。但日志现在看得到：回合的 `preview`（用户问）+ `reply_preview`（答复）各 ~200 字，以及（`LOG_LLM_BODIES` **开发默认开**时）**截断+脱敏**的 prompt / 回复正文（`llm.request` / `llm.response`）。轻量复盘"问了什么 / 答得如何"可直接读日志；要完整正文或跨回合再查库。

## 字段与关联

- 通用键：`timestamp` `level` `logger`（模块）`event`；**代码锚定** `func_name` + `lineno`（每条 app 日志自动带，`module.func:line` 直接跳到埋点源码）；关联键：`trace_id`（**唯一全链路键**）`conversation_id` `turn_id` `user_id` `agent_id`（委派子节点再带 `run_id` `depth`）。
- 维度：同一次用户交互（含进程内异步委派 worker）→ `trace_id`；整会话 → `conversation_id`；单回合 → `turn_id`；单委派节点 → `run_id`。键集合权威表见上文 §日志系统规范「Context 注入」。
- **DB ↔ 日志 join**：`trace_id` 现已落库——`messages` / `cost_events` / `run_sessions` 各带 `trace_id` 列（assistant 回复与该回合落账行写本回合 trace，recoverable worker 写其首次回合 trace；user 消息与 handoff 任务为 `NULL`）。于是可从库里一条持久化回复/落账行反查整条日志链（`grep trace_id=<id>`），日志轮转后仍能定位。

## 分析工具（在 apps/server 下跑）

```bash
uv run python scripts/log_stats.py                 # 事件分布 / 错误 / 回合质量(轮数·token·耗时·委派率) / ReAct 轮(每轮 reasoning_token·工具强度) / LLM 调用(按 scenario·model 拆延迟·token·finish_reason) / 工具质量(总+按 worker 拆 agent_id·depth) / 成本(per-turn 花费) / 收敛治理
uv run python scripts/log_timeline.py --recent     # 列出最近 5 条会话 id
uv run python scripts/log_timeline.py <conv_id>    # 合并 DB 消息 + 日志事件的会话时间线
uv run python scripts/log_timeline.py --trace <trace_id>  # 一次交互的全链路(时间线读日志文件; trace_id 现也落库, 可 SQL 反查)
```

- **时间线按 worker 嵌套**：被委派 worker 的 `react.round_*`/`tool.execute_*` 会归组成缩进块（块头 `worker <agent_id> (d<depth> · N rounds · M tools)`），一个委派的推理链可从委派点自上而下读；CEO/captain（无 `depth`）留在主干时序，串起「委派 → 各 worker → 收敛」。`--trace` 看单次委派最直观。

脚本：`apps/server/scripts/log_stats.py`、`log_timeline.py`。

## 手动查询（PowerShell / jq）

```powershell
# 所有错误
Get-Content logs/dev.jsonl | %{ $_ | ConvertFrom-Json } | ? level -eq error | Format-List event,error
# 某会话全部事件
Get-Content logs/dev.jsonl | %{ $_ | ConvertFrom-Json } | ? conversation_id -eq <id>
# 某次交互全链路（排查首选）
Get-Content logs/dev.jsonl | %{ $_ | ConvertFrom-Json } | ? trace_id -eq <trace_id>
```

有 `jq`：`jq 'select(.level=="error")' logs/dev.jsonl`。

## 提示

- 找"优化点"先看：`level=error` 聚类、`chat.turn_complete` 的 `duration_ms` / `rounds` 尾部、`engine.loop_nudge`/`engine.loop_finalize`/`engine.max_rounds_exhausted` 计数、`delegated=true` 回合的 `workers` 数、`tool.execute_end` 的失败率（`status!=ok`）与慢工具（`duration_ms` 尾部）。
- 复盘**编排决策为什么这么做**（不止计数）：`delegate.started` 带 `agents`(派了谁·干什么)/`parallel`(首波扇出宽度)、`worker.escalate` 带 `question`+`kind`(为什么升级·normal/scope/dep)、`revise.started` 带 `feedback`(为什么召回·改什么)——「为什么这么决策」直接读日志，无需查库还原。
- **联网工具全失败 / 辩论"无据可依"**（`read_url`·`web_search` 成批 `status!=ok`，或 `read_url` 1ms 内秒失败报「私有/保留地址」）：先跑 `uv run python scripts/probe_egress.py`（`apps/server` 下）查是不是**本机代理 fake-IP**——它把所有域名 DNS 应答成 `198.18.x.x` 保留段 → SSRF 正确拦截（**环境坑、非产品 bug**）；处置见脚本汇总（代理关 fake-ip / 改 `redir-host`）。
- LLM 层：`llm.call` 的 `latency_ms` 尾部（按 `scenario·model` 看谁慢/谁贵）、`finish_reason=length`（回答被截断，可能要调 `max_tokens`）或 `content_filter`、`reasoning_tokens` 异常飙高（想得费劲）；`cost.recorded` 的 per-turn `total_usd`（贵回合）。调 prompt 直接读 `llm.request`/`llm.response`——`LOG_LLM_BODIES` **开发默认开**、emit 在 info（无需再动 `LOG_LEVEL`）；生产临时开即可。
- 文件持续追加常达数十 MB，**rg/grep 偶尔漏命中刚写入的行**；验证刚触发的回合改用 `Read` 读末尾或 `Select-String`。
- 数据库连接：`agentcore:agentcore@localhost:5432/agentcore`（dev compose 默认；`apps/server/.env` 可覆盖）。
- 实现见 `apps/server/agentcore/core/logging.py`，规范见上文 §日志系统规范。
