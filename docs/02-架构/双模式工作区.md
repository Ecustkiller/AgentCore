# 双模式工作区（云 + 本地）

> **状态**：✅ 已落地——**文件夹即工作区**（工作区 = 文件夹；裸聊无空间、首次产文件懒建文件夹迁入）+ `WorkspaceBackend` 抽象 + `ServerWorkspace`（云）+ `LocalWorkspace`（本地）+ 绑定路由（模式跟着文件走）+ 文件进出 / 快照 / `git clone` / 附件驻留 / 保留期清理 + 本地→云 snapshot 交接（三段式，前后端齐全）。⏳ 仅余多 worker 化：锁 / 审批门 / 工具执行通道 / handoff 作业现为**进程内态**，多机需前置 Redis + durable queue。
>
> **定位**：让「AI 对话能选文件夹、并在该文件夹工作」，一套分层同时支撑**云**与**本地**两种**平等**模式——**不给用户云/本地开关，模式跟着「文件在哪」自动走**（§六）。
>
> → 见代码：`apps/server/agentcore/workspace/`

---

## 一、需求推导结论：两种模式平等

- **内核需求**：给每次协作一个**持久、可累积的项目空间**——多 Agent 在同一张「桌子」上靠文件协作。「在不在本地磁盘」是第二层偏好，非内核。
- **云模式（服务端托管）= 零前提的起点**：打开即用、不暴露整块磁盘；并行团队同台协作，天然可观测、可隔离。
- **本地模式（桌面执行，对标 Cursor）= 平等的另一种，后做**：开发者内环（改真实本地仓）。
- **不给用户云/本地开关**——模式跟着「文件在哪」自动走（§六）；「云优先 / 本地优先」只是构建顺序。

## 二、`WorkspaceBackend` 抽象

沿用项目「Protocol + 实现 + 工厂」同构范式（同 `LLMProvider` / `SandboxProvider` / `SearchBackend`），引擎与工具只对接口编程：

- **接口**：`read / write / list / replace / grep / execute` + `location`(server·local) + `root_label`（相对路径渲染、不暴露绝对路径）。
- **签名一次到位**：P0 即钉死结构化结果类型（`DirEntry` / `ReplaceOutcome` / `GrepResult` 等），失败走类型化异常 `WorkspaceError` 子类（`OutsideWorkspace` / `PathNotFound` / `NotAFile` / `NotUTF8` / `AmbiguousMatch` …），工具层捕获后映射回原报错文案（用户侧报错零变化）。
- **护栏 1·瘦接口**：持久化（snapshot/restore）**不进 backend**——是回合级存储策略（轴3），归独立 `StorageProvider`，由 service 在 run 前后调（§三、§四）。backend 只管「文件 + 执行」这对**必须同源**的能力（一个 backend = 一个执行平台）。
- **为何 P0 就引入抽象**：本地模式（B）是**已承诺、必然建完**的路线；接口先行（项目 house style）使新增 `LocalWorkspace` 时工具与引擎零改动。
- **唯一接缝**：`ToolContext.workspace_dir: Path` → `ToolContext.backend: WorkspaceBackend`，6 个工具（`file_ops` 4 个 + `grep` + `code_execute`）与引擎退化薄壳。`code_execute` 收编进 `backend.execute`（`ServerWorkspace` 内委托 `SandboxProvider` + 带 cwd），顺修「代码与文件不在同一目录」的潜伏 bug。

## 三、三正交轴与「本地默认云储存」判定

| 轴 | 取值 |
|---|---|
| 轴1 执行位置 | server / local |
| 轴2 文件源头 | server dir / local dir |
| 轴3 持久化 | S3 快照 / 仅本地盘 / 实时同步 |

「云 / 本地模式」是在**轴1+轴2** 选一整套。用户提的「本地默认云储存」= `轴1=local、轴2=local、轴3=cloud`——**不是第三种模式，而是 LOCAL backend 的存储策略**：

- ✅ **接受**快照 / 交接语义（git-push 式）：本地是唯一真相源，按需经 `StorageProvider` 快照到 S3，用于备份 / 跨设备 / 把活交给云端团队。
- ❌ **拒绝**实时双向同步（Dropbox 式）：双真相源 + 冲突地狱，在「并行 Agent 团队并发写」下尤其致命。

## 四、两个 Backend 实现

| Backend | 文件在哪 | 执行在哪 | 模式 | 传输 |
|---|---|---|---|---|
| `ServerWorkspace` | 服务端 `<data_dir>/workspaces/<user_id>/<folder_id>/`（文件夹即工作区）；handoff 作业 / 裸聊瞬态用内部 `…/conv/<id>/` | 服务端（复用 `SubprocessSandbox`） | 云 | 同进程 |
| `LocalWorkspace` | 用户真实本地目录 | 桌面端（op 路由到 Electron） | 本地 | 泛化审批门接缝 |

`LocalWorkspace` 的传输**不另造**：把 `runtime/approvals.py` 的「服务端挂起 `asyncio.Future` + 客户端 HTTP 回填」泛化成一条工具执行通道（`WorkspaceChannel`）——服务端挂起 + `workspace_op_required` SSE + `POST …/interactions/{id}`（kind=client_tool，§18.2 统一挂起原语）resolve 回填；桌面 `fs-service.ts` 处理全 op、渲染端 `workspaceOps.ts` 收 SSE→IPC→回填。每个 op 串行路由、回传结构化结果或同款 `WorkspaceError`，故工具 / 引擎不动。

## 五、落地能力现状

| 能力 | 现状 |
|---|---|
| **接缝 + 云模式** ✅ | `WorkspaceBackend` + `ServerWorkspace`；`ToolContext.backend`；6 工具改薄壳；`StorageProvider` 快照/恢复/下载；文件进出 + `git clone` + 附件驻留 + 保留期清理 |
| **本地模式** ✅ | `LocalWorkspace` 经 `WorkspaceChannel` 全 op 路由桌面；`local_root_id` 绑定 API + 模式条 UI；worker 审批门透传（P2d） |
| **本地→云交接** ✅ | 三段式：打包上传 → 云端 autonomous 跑团队 → diff/三方判定/回写（`HandoffJob` 专表） |
| **多 worker** ⏳ | 锁 / 审批门 / 通道 / handoff 作业为进程内态；多机需 Redis + durable queue |

> **本地真实编辑 = 正交模型的一个配置**：= 轴1 `server` + 轴2 `local_dir`，仅在「服务端 = 用户机器的可信自托管」下成立——`resolve_safe_path` 仍管越界，但允许把 `local_dir` 喂给工作区根。与「云端杜绝用户传任意服务端路径」不矛盾（前者可信运维配置，后者不可信多租户默认）。

## 六、绑定模型与「模式跟着文件走」

- **文件夹即工作区**：工作区 **就是** 文件夹（= 项目），不另设「对话工作区」概念（对标 Cursor）。顺现有 `folder_id` 模型——`folder_id` 即「在哪个工作区」。
- **裸聊懒建**：未分组对话（裸聊）**没有工作区**，零成本（随手问答不建目录 / 不进中枢）。首次真正产文件时——团队首次写（`DeferredWorkspace`）、用户上传 / `clone`、或绑本地文件夹——才**懒建一个文件夹并迁入**（命名取对话标题），此后即该文件夹工作区。
- **无云/本地开关**：路由判定（`workspace/locate.py`）——文件夹有 `local binding`（`Folder.local_root_id` + `local_subpath`，绑定只在文件夹）→ `LocalWorkspace`，否则 `ServerWorkspace`。裸聊默认云，但**桌面裸聊回合携待定本地容器根**（`local_container_root_id`）→ 首次产文件懒建**本地**文件夹（容器根 + 服务端按标题生成的 `local_subpath`，工作区对称化 D1a），与云端裸聊懒建同构；web / 手机 / 显式「云端临时对话」逃生口仍云。直接聊 / 上传 / 手机端 → 云；桌面绑本地文件夹 → 本地。
- **跨设备**：云项目跨设备同步；本地项目仅在那台机器；手机无本地文件夹概念，自动走云。
- **注入路径**：`conversation/service.py` 解析 `conversation →（文件夹空间 / 裸聊无空间）→（文件夹有绑定则本地、否则云）` → `_build_turn_backend` 构造 `WorkspaceBackend`（裸聊得 `DeferredWorkspace`，首写时懒建文件夹并迁入，回合末快照落到新文件夹；**桌面裸聊携待定本地根则懒建本地绑定文件夹**——容器根 + 按标题生成的 `local_subpath`，D1a）→ 经 `run_chat_pipeline` 注入 `base_tool_context.backend`；worker 经 `replace(...)` 自动继承。

## 七、风险与边界

| 项 | 处理 |
|---|---|
| 路径越界 | `resolve_safe_path` 下沉 `ServerWorkspace`；服务端目录由 user/folder 生成，杜绝用户传任意服务端路径 |
| 裸聊（未分组会话） | 无工作区（零成本随手聊）；首次产文件 / 上传 / 绑本地时懒建文件夹迁入（`DeferredWorkspace`）→ 即成文件夹工作区 |
| 已开始对话跨文件夹移动 | 禁止——有消息后锁定归属（前端隐入口 + 后端 409） |
| folder / 会话删除后文件 | 与软删对齐：软删原样保留可恢复；超保留期（默认 30 天，可配）物理清目录 + 记录 + OSS 备份；「彻底删除」立即清。文件归项目（文件夹）所有 |
| 同 folder 跨会话并发写 | 文件夹级锁（`workspace/locks.py`，进程内，多机再 Redis）；任务内并行小队不受限 |
| `LocalWorkspace` 下并行团队 | 单桌面通道是瓶颈；正解 = 本地内环 + snapshot 到云由云端跑并行团队（交接 e2，呼应 §三 快照语义） |
| 本地 worker 直接执行 | 本地模式被委派 worker 共用 CEO 同一回合审批门（仅 `backend.location=="local"` 透传 `ApprovalGate`）；云模式不下发门（沙箱隔离） |
| execute 通道传输期限 | 代码执行 = 代码自身超时 + 余量（默认 30s）；文件 op 走扁平期限（默认 60s）——避免长但合法的执行被传输层提前丢弃 |
| 多 worker 进程态 | 审批门 / 通道 / 锁 / handoff 作业现为单进程内存态，多 worker 需前置 Redis + durable queue |

## 八、关键决策（已定）

1. **P0 引入 `WorkspaceBackend` 抽象**：B 为已承诺路线，接口先行（§二）。
2. **不给用户云/本地开关**：模式跟着「文件在哪」自动走；直接聊 / 手机端走云（§六）。
3. **文件进出：先上传**（人人可用、结果可下载）；**`git clone` 紧随**（公开仓先、私有令牌后）。
4. **云存储 = 阿里云 OSS**（通用 S3 接口，dev MinIO，客户端库 `boto3`）；**快照 = 改过文件的任务结束后后台备份 + 手动留版本 + 随时下载**（非每写必存）。
5. **P2e 本地→云交接 = 三段式 + 派发式云作业**：`HandoffJob` 专表 + 隐藏 `mode="handoff"` 作业对话；云作业 autonomous 免审批（区别于 P2d 交互门）；冲突判定服务端权威；完成通知 MVP=轮询，作业为进程内态（重启丢在飞作业，MVP 可接受，后续前置 durable queue）。
6. **工作区升一等可寻址资源 `/v1/workspaces/{ws_id=folder:<id>}`**：@ 提及云索引 / 按文件夹浏览需直接寻址工作区，而旧契约只能「借一个对话」访问。对话级路由解析「对话 → 其文件夹」后调同一服务层（无重复逻辑）。
7. **「添加文件夹 = 建本地绑定项目」一步到位**（F2）：选 OS 目录即 `POST /v1/folders` 带 `local_root_id` 单条 insert 建项目+绑定，免 create-then-bind；否决「只浏览不建项目」的游离根（心智割裂）。**UI 入口在文件中枢 `/files` 顶栏「添加本地文件夹」**（`fsApi.addRoot()` 取根 → `useCreateFolder` 带 `localRootId`），非对话页（决策 #9）。
8. **@ 提及覆盖云端工作区**（F4）：云端工作区文件也能被 `@`，与本地根同进一套索引（前端 `FileSource` 抽象见 [`前端技术与架构.md` §8.7](/docs/04-前端/前端技术与架构.md)）。裸聊无云工作区，只索引本地根。
9. **文件夹即工作区（推倒「对话工作区」）**：工作区 = 文件夹，不再有面向用户的 `conv:` 工作区；裸聊无空间、首次产文件懒建文件夹迁入（`DeferredWorkspace`）。**根因**：旧设计无「工作区」一等概念——文件挂对话、又打「文件夹共享」补丁，被迫造全局文件页把两套拍平，导致「两处翻文件、同份双列、空态自指」。故否决**旧式**「全局文件视图（拍平对话文件）+ 对话/文件夹双份浏览」、删 `Conversation.local_root_id`（绑定只在文件夹），对标 Cursor「打开的就是一个文件夹/工作区」。**全局文件入口本身在「工作区一等概念」落地后以「跨工作区文件总览」形态重加**（`/files`，`FilesPage` + split 式 `FileWorkbench`：左栏列所有工作区=文件夹（复用 `GET /v1/workspaces` 枚举）、右栏翻选中项目的树+预览）——与被否的旧式关键区别：**不拍平对话文件、不引入游离根**，rail 与 `/conversations` 文件夹列同一枚举源故「有哪些项目」单一真相；是建立在重构成果上的**增补**而非回退。**两个视角、不设单项目主页**（2026-06 端态 I，撤销原 `/folders/:id`「单项目总览页」`WorkspacePage`）：`/files` 是**文件视角**（跨项目翻文件 + 文件夹生命周期）、`/conversations` 是**对话视角**（按文件夹筛对话 + 新建对话），两者**互跳**——`/files` 工作区根右键「查看对话」跳 `/conversations` 并筛该文件夹、`/conversations` 选中文件夹「浏览文件」跳 `/files` 并聚焦该项目根（`focusWsId` navigation state → 展开+高亮）。**否决**再设独立「单项目主页」——它只是把两视角各自已有的半边（该项目文件 / 该项目对话）重新拼一遍，制造「同份文件多处可翻」的冗余（正是本决策根因要消灭的味道）。**文件夹的生命周期（新建 / 重命名 / 删除 / 添加本地文件夹）统一归 `/files` 管**（文件夹即工作区，项目事务单一入口）；增删改不再在对话页（左下「管理文件夹」跳 `/files`）。两面虽走两条 query（`/files` 用 `GET /v1/workspaces`、`/conversations` 用 grouped 缓存的 folders），但文件夹 CRUD 经 `hooks/useFolders` 同时投影进两套缓存（`hooks/useWorkspaces` 的 `addWorkspaceFromFolder` / `patchWorkspaceFromFolder` / `removeWorkspaceForFolder`），故「有哪些项目」仍单一真相、无需手动对账。**左栏全部工作区平铺、无分区**（2026-06）：不再用可折叠根，每个工作区都摊成一个平铺小节——标题行（名字 + 云端/本地徽标 + hover 显形的「新建文件 / 文件夹」按钮）+ 其文件树直接铺在下面恒展开；工作区之间只靠「云端/本地」徽标区分，**不再分「我的工作区 / 其他项目」两段**（早期曾试「默认区平铺为 home + 其他项目分组」，因这层人为分区的措辞不准、且云端/本地徽标已足够区分而撤掉）。生命周期（重命名 / 删除 / 查看对话）归标题的右键菜单；新建 / 上传走 hover 按钮 + 右键菜单。**本地与云端一视同仁、无壳**（工作区对称化，2026-06）：桌面裸聊产文件懒建 per 对话本地文件夹（不再预塞「我的工作区」单一容器壳卡片），故每个本地工作区都是独立平铺卡片、与云端对称——无置顶 / 不可删 / 改名特例，旧 `getDefaultWorkspaceFolderId` 识别路径已删。→ 见代码 `apps/desktop/src/renderer/pages/FilesPage.tsx`。
10. **默认云 / 不做本地优先 / 按任务非按人群**（2026-06 复评；桌面默认值已于 #11 修订为 local-first，本条其余仍成立）：默认走云是「零信号态」（裸聊 / 手机 / 异步团队）的合理起手，**非断言用户身份**；不跟 Cursor 走「本地优先」——Cursor 是单 Agent 开发者 IDE，本品是面向全量知识工作者的多 Agent 平台，赛道不同（见 [`../01-产品/产品定位与品牌.md` §三](/docs/01-产品/产品定位与品牌.md)）。**不按「开发者 / 泛知识工作者」分人群**：同一用户两类活重叠，故按**当前任务**路由（即 §六「模式跟着文件走」），非按身份。本地实时编辑经 `WorkspaceChannel` 已落地（云端引擎遥控桌面）；引擎本体下沉本地（sidecar）= 速度 / 并行 / 离线的提速升级，**已启动（⏳ 进行中）**——Slice 1 走路骨架两端已落地（Python：`agentcore/sidecar/` stdio JSON-RPC server 托管现有 `run_chat_pipeline`、工作区直连本地盘、LLM 经云代理使平台 key 不下放用户机器；桌面：主进程 spawn + JSON-RPC client + 回合事件桥回 renderer，复用同一 `dispatchSSEEvent`）。会话路由已接（**用户设置开关「本地引擎」**：设置 → 模型配置，默认关，持久化 localStorage；开后绑定「本机存在的本地文件夹」的对话走 sidecar，裸聊 / 云端文件夹 / 带附件的回合仍走云链路，不动既有 local 会话）。**审批经 `respond` 结算、会话持久化、计费闭环均已落地**——审批经 sidecar `respond` 回 stdio 链路结算（进程内 `InteractionRegistry`）；**持久化**走回合结束**回写云端**（`POST .../local-turns`：云端落 user/assistant 消息 + 引用 / 团队图；**幂等可重试**——按客户端铸的 `user_message_id` 去重，桌面有限退避重试而不重复落回合，仍失败出**非阻断 toast 可手动重试**、不静默丢历史；sidecar 本机无库故 renderer 携结果回传）；**计费走云推理代理权威落账**（Slice 4a）——sidecar 的 LLM 调用全部经 `POST /v1/inference/v1/chat/completions`（以 cookie 会话兑换的**作用域受限短期令牌**作 Bearer，平台 key 永不下放本机），代理服务端解析真实凭据、过门（平台配额 / BYOK key 校验）并按真实上游 usage **权威**落 `cost_events`（每次调用新 `run_id`、`message_id=NULL`、会话粒度，汇入账户 / 会话 / 配额合计，平台 / BYOK 皆然），故 `local-turns` **不再上报 `cost_runs`**（免与代理重复计费）；renderer 订阅 `onStatus` 生命周期，本地引擎拉起失败 / 进程退出时出**针对性诊断**横幅（而非通用 network，接通原死通道）。**内置 Python 打包已落地**（方案 B：安装包随带一份独立 CPython + `--target` site-packages 装好的 agentcore，主进程 `app.isPackaged` 时据 `process.resourcesPath/sidecar` 拉起，用户机无需系统 Python / venv / uv；构建见 `apps/desktop/scripts/bundle-sidecar.mjs`，Windows 已端到端验证、mac/linux 各自 CI 构建）。**结构化挂起 durable pause/resume 已落地**——sidecar 回合在 plan_review / ask_user 检查点挂起后，帧（`TurnSuspension`：transcript + plan + completed + journal-so-far）落本机文件 `<userData>/sidecar/paused/<msg>.json`（`LocalPausedTurnStore`，一回合一文件、原子写、claim 经 `.claimed` 改名 + conversation 校验防 IDOR），应用 / 进程重启后重开会话经 sidecar `listPaused`（主进程**直读帧文件、零 Python spawn**）重现续跑卡，用户决定经 `resume` RPC claim 帧并跑 `resume_chat_pipeline`，过程事件 / 回写复用普通本地回合那套（`resumeConversationViaSidecar`）；挂起回合的原始用户消息回写前未落云，故续跑前 renderer 补注入用户气泡再对账。**与云端等价、前端不分叉**——云走 `paused_turns` 表、sidecar 走本机帧文件，二者摘要同形（snake_case `PausedTurnSummary`）。剩**Journal 执行级事实（journal 替帧的地基，path B）·离线 LLM**（→ [`../07-规划/远期规划.md` §一](/docs/07-规划/远期规划.md)）。

11. **桌面端 local-first 默认（修订 #10 的桌面默认值）**（2026-06，⏳ 进行中）：桌面客户端定位为 power-user 的**本地内环**，**新项目 / 对话默认落本地**（默认基目录 `~/Documents/AgentCore`，首启可改），对齐行业实践（Cursor / Claude Code / Codex CLI）。**路线 (i)·分层**以免与核心卖点冲突：① 桌面裸聊**懒建 per 对话本地文件夹**（落默认容器 `~/Documents/AgentCore/<标题>/`、按标题命名；**不产文件即无文件夹**、零负担——与云端裸聊同构）；想要纯云的随手问答 → 显式「云端临时对话」逃生口；② **重型并行多 Agent 团队仍交接到云跑**（§七「单桌面通道是瓶颈」的既有正解——local-first 只管内环，并行团队不退化）；③ web / 手机无本地概念，仍走云。实现上**只改「裸聊懒建落点」，不动引擎 backend 岔路**（`build_workspace` / `resolve_local_binding` 仍按 folder 绑定，故工具与引擎零改动）：回合携待定本地容器根 `local_container_root_id`，`DeferredWorkspace` 首写时在容器根下懒建带 `local_subpath` 的本地绑定文件夹（D1a）。**前置依赖**：顺滑的本地回合依赖 sidecar（§一.1 / 远期规划 §一）成熟，否则本地回合走「云端遥控桌面」每 op 往返、体验逊于「直接本地跑」。**保留 #10「零信号默认走云」**作为 web / 手机 / 异步团队的起手，**仅桌面默认值由云改本地**。落地：地基 `fsApi.ensureDefaultRoot`（桌面主进程自动建 + 授权 `~/Documents/AgentCore`）✅ + 默认值翻转（桌面裸聊携待定本地容器根、首写懒建 per 对话本地文件夹）✅ + 「云端临时对话」逃生口（命令面板，`startNewConversation(…, {cloud:true})`）✅ + 本地 / 云指示（既有 `WorkspaceModeBar`）✅ + `/files` 全部工作区平铺、无分区、本地与云端对称无壳（见 #9）✅；首启基目录确认 / sidecar 提速 ⏳。**工作区对称化已落地**（2026-06）：桌面裸聊懒建 per 对话本地文件夹（容器根 + 服务端按标题生成的 `local_subpath`，D1a）、「我的工作区」单一容器壳从数据模型消失、本地与云端对称；sidecar 与「云端遥控桌面」op 通道均按「容器根 + 子路径」起目录 / 作用域。

## 九、工作区一等资源（HTTP 契约）

工作区（=文件夹）需可独立寻址（@ 提及云索引、按文件夹浏览），非只能「借一个对话」访问，故升为一等可寻址资源。完整 REST 路由 → 见 OpenAPI（`apps/server/openapi.json`）。

**`ws_id` 编码**：`ws_id = folder:<folder_id>`（**工作区即文件夹**）——分隔符 `:`（UUID 不含、单段 URL 安全），`parse_workspace_id` 在 `workspace/locate.py`（纯函数，DB 无关）。`conv:` 仅 `_workspace_relpath` 内部保留给 handoff 作业 / 裸聊瞬态目录，公开 `/v1/workspaces` 既不产出也不接受（解析到非 `folder` → 404）。

**云/本地闸**：本地工作区文件走桌面 IPC、不走 REST——故 `location=="local"` 时 file/dir/move/clone + snapshot create/restore + **file-index** 返回 **409**；只读 snapshot list/download 放行（对象存储按 ws 键，本地项目也能列历史）。

**F2 建本地绑定项目**（文件夹路由非 ws 路由）：`POST /v1/folders` 可选 `local_root_id` → `FolderRepository.create` 单条 insert 写 `folder.local_root_id`；该文件夹随即以 `folder:<id>` + `location=="local"` 出现在枚举，前端走 IPC。沿用 §六 folder=共享项目空间语义。

**F4 @ 扁平文件列**：`GET …/file-index` → `{ data:[posix_path…], total, truncated }`。`index_files` 是 `WorkspaceBackend` **一等协议能力**（不止 @ 用）：`ServerWorkspace` 用 `os.walk` 剪 `IGNORED_DIRS`（node_modules/.git…）+ 只收文件 + 上限 5000；`LocalWorkspace` 经 workspace channel（`WorkspaceOp.INDEX_FILES`）让桌面端 `opIndexFiles` 走**同一套** `collectWorkspaceFiles`（即 @ 提及 `fsApi.listFiles` 的忽略+上限走法），故云/本地扁平视图一致。两个服务端消费方，靠 `order` 参数共用同一遍历却各取所需：① REST `file-index`（@ 提及，云专属——本地 @ 由前端直调 `fsApi.listFiles`）走 `order="path"` 字母序、**免 stat**（选择器延迟敏感）；② **worker 工作区产物清单**（多 Agent 协作，云/本地皆经 `backend.index_files`）走 `order="recent"` 按 mtime 倒序（大树里预算优先给最新文件），详见 [`编排器与CEO主Agent.md` §2.3](/docs/03-AI核心/编排器与CEO主Agent.md)。前端聚合见 [`前端技术与架构.md` §8.7](/docs/04-前端/前端技术与架构.md)。

- **枚举**（`WorkspaceSummary`）：`{ ws_id, name, location: "cloud"|"local", root_id, subpath, has_files }`（`subpath` = 本地工作区在容器根内的子路径，工作区对称化 D1a；空 = 根本身〔显式添加的本地项目〕/ 云端）。**仅列文件夹**（工作区即文件夹）——裸聊无工作区故不列；本地文件夹恒列（服务端看不到其文件，`has_files` 恒真），云文件夹带 `has_files`。
- **鉴权**：`ws_id` 解析出 `folder_id` → 复用 owner 校验；非属主 / 坏 id / 任何 `conv:` 寻址（对话不是工作区）→ 一律 404。
- **对话级路由**（`/conversations/{id}/workspace/…`）：前端对话面板的工作区入口——解析「对话 → 其文件夹」后调同一 `workspace.files`/`snapshots`/`git` 服务层，无重复逻辑。裸聊（无文件夹）：读类（list/download）返回空 / 404（绝不在磁盘建幻影目录），写类（upload/dirs/clone）先懒建文件夹再写。@ 提及索引走该文件夹的 `folder:<id>` ws 路由。**桌面裸聊回合**可携 `local_container_root_id`（待定本地容器根，决策 #11）→ 首写懒建带 `local_subpath` 的本地文件夹（D1a）；`FolderSummary` / `WorkspaceSummary` 回带该 `subpath`，桌面据此把 sidecar 引擎与 op 通道绑到「容器根 + 子路径」（`opExecute` cwd / `opIndexFiles` base / sidecar `workspaceRoot`）。
