---
status: landed
code: apps/server/agentcore/workspace/
related:
  - docs/04-前端/前端技术与架构.md
  - docs/04-前端/前端UX设计.md
  - docs/01-产品/产品路线图摘要.md
skip_if:
  - 只改聊天/编排（读 03-AI）
---

# 工作区（云端权威）

> **定位**：给每次协作一个持久项目空间。**产品定案（✅）**：**双通道观察期**——**云协作**为推荐默认（云桌权威 + 全云执行；本机落点经导入 / Diff 合回 / ZIP / 连接 Git）；**本机传统**为桌面可选入口（打开本机文件夹 = 本机权威工作区 + 本地引擎）。≠ 两平级「云/本地模式」开关；≠ 离线 /「数据不出本机」。
>
> **代码现状**：云桌主路径已落地；**本机传统入口 ✅**（Composer / 命令面板 / Ask 三件套履约 + `mode=local` 默认同侧 sidecar）。旧 `mode=local` 仍可开。**拒 Dropbox 式实时双向同步**不变。余项：⏳ ③ PR 二级（**狗粮观察后再排**）；本机通道取消条件见 §7.2（暂不排死线）。导入硬顶见下「现行默认」。
>
> **边界**：Backend / 绑定 / Sidecar / 共享空间 / 约定文档目录。记忆注入 → [记忆系统](/docs/03-AI核心/Agent记忆与知识系统.md)；文件树 UX → [前端 UX §九](/docs/04-前端/前端UX设计.md)。
>
> → 见代码: `apps/server/agentcore/workspace/`；桌面入口 `ComposerWorkspaceChip` · `importToCloud` · `mergeLandingDiff` · `cloudDeskExit`

---

## 一、云端权威（✅ 推荐主路径）+ 本机传统（观察期通道）

- **内核**：多 Agent 在同一张「桌子」上靠文件协作。
- **云协作（推荐）**：桌子 = **云桌**（打开即用、多端同一份、可隔离协作）；执行全云（引擎与盘同侧）。本机 = **碰盘动作**：导入到云、连接 Git（云 clone）、Diff 合回落点、下载 ZIP。
- **本机传统（观察期 · 桌面）**：打开本机文件夹 = **本机权威工作区** + **本地引擎**（sidecar；引擎与盘同侧）。适合已有大仓 / 必须碰本机环境；改动直接进本机目录。入口文案与差异 → §7.1–7.2、[前端 UX](/docs/04-前端/前端UX设计.md)。
- **默认与记忆**：新用户 / 无记忆 → 云；**可记上次通道**（本机用户不被每次推回云）。推荐徽标只给云。
- **G3 远程**：云工作区可从产品 UI 浅克隆 http(s) 仓；私仓用账户级 PAT（设置 · Git 凭据）。→ [工具 · G3](/docs/03-AI核心/工具与能力系统.md#git-闭环)

**仍成立**：❌ 拒绝实时双向同步（Dropbox 式）。❌ 不把本机传统卖成离线或「数据不出本机」（推理仍多经云代理；对话回写云）。

**合流**：旧 `mode=local` 与「本机传统」同通道——侧栏图标语义为「本机传统」，无「请迁移」债务徽标；⋯ / ModeControl 可安静「导入到云」（可选转换）。过桥仅机制兜底（§7.2）。

**否决（回潮）**：把本机传统标成与云**平级推荐/默认**；Composer 并排「本地引擎 / 云端过桥」实现词；过桥当新云桌或新本机入口的默认；用本机通道冒充完全离线。

## 二、三正交轴与拒绝实时同步

| 轴 | 取值 |
|---|---|
| 轴1 执行位置 | server（云协作）/ local·sidecar（本机传统）（机制见 §七） |
| 轴2 文件源头 | server dir（云桌权威）/ local dir（本机传统权威） |
| 轴3 持久化 | S3 快照 / 合回本机 / 下载 ZIP / ~~实时同步~~ |

推荐日常 = **server + server**；本机传统 = **local + sidecar**；云桌本机落点仅经显式合回/ZIP。

- ✅ 接受快照 / 交接（git-push 式）与 **显式合回**（§7.6 云桌→本机标准出口）。
- ✅ **导入到云**（本机文件夹 → 云桌权威）+ ignore / 密钥尽力排除。**现行硬顶（实现默认，可调）**：archive 约 100 MiB / 2 万文件；单文件 PUT 25 MiB（超限跳过并提示部分导入）。
- ❌ **拒绝实时双向同步**（Dropbox 式）：双真相源 + 冲突，在并行 Agent 并发写下尤其致命。
- ❌ **首期不做**：「整仓永不上传」大众例外；整树静默覆盖本机。

## 三、`WorkspaceBackend` 抽象原则

沿用 Protocol + 实现 + 工厂（同 `LLMProvider` / `SandboxProvider`）。引擎与工具只对接口编程。

- **护栏·瘦接口**：持久化（snapshot/restore）**不进 backend**——属轴3，归独立 `StorageProvider`。backend 只管「文件 + 执行」这对必须同源的能力。
- **为何 P0 引入**：本地是已承诺路线；接口先行使加 `LocalWorkspace` 时工具/引擎零改动。
- **唯一接缝**：`ToolContext.workspace_dir` → `backend: WorkspaceBackend`；`code_execute` 收编进 `backend.execute`（与文件同目录）。

→ 见代码: `apps/server/agentcore/workspace/`

`ServerWorkspace`：服务端目录 + 同进程沙箱。`LocalWorkspace`：用户真实本地目录 + 桌面执行；传输不另造——把审批门「挂起 + HTTP 回填」泛化为 `WorkspaceChannel`。Sidecar 文件 op 直连本机（不经 channel），但后台进程仍交主进程代持（sidecar 生命周期短于 dev server）。

## 四、约定文档目录约定

多幕协作副产物落工作区约定目录。**否决**另建「会话级团队约定文档」实体——复用既有项目/工作区文件心智。全部挂在 `AgentCore/文档/`（产品心智 = AI 文档统一入口；**不**进 `<rules>` 注入）。

| 目录 | 谁写 | 谁读 |
|---|---|---|
| `AgentCore/文档/research/` | MLR / 自由讨论 / 竞品笔记（`form=files`） | 后续辩论 + 主持人索引 |
| `AgentCore/文档/debate/` | 辩论收场机制 | 用户 / 后续回合按需读 |
| `AgentCore/文档/reviews/` | 审查类 `form=files` | CEO 汇总 / 用户按需 |
| `AgentCore/文档/项目/` | 厚约定文档（闸清后/普通回合按需；**探索 pending 不写**） | 用户 / 后续回合按需读 |

落盘与 handoff 摘要正交：结构化 handoff 走委派通道；完整报告叠加写文件。`<workspace_context>` 只陈述布局，不注入正文。约定文档类委派由运行时按 `stage_dirs` 填默认 `artifact_dir`（brief 仅引用约定文档路径、或业务 `artifacts` / `repair_code` 等跳过约定文档默认时不自动绑）。`文档/项目/` 常量 → `stage_dirs.PROJECT_DOCS_DIR`。

**向用户报路径（✅ 提示约束）**：收口列出落盘文件时，须用工作区相对完整路径（与上表约定文档前缀一致），以 `file_write` 回执 / `artifacts` / 交付对账为准；禁止裸写 `reviews/…` 或同一清单混用两套前缀。→ CEO `【产物路径】` · `<workspace_context>` 约定文档边界

**约定文档文件命名（✅）**：柜名 / 抽屉目录不动；规范柜内文件名。

| 规则 | 约定 |
|---|---|
| 类型前缀 | 英文小写 + 连字符（机器友好）；可读短名可中文 |
| 权威默认名 | 只在一处常量；playbook 文案 / 验收 / 技能须引用，禁第三种默认名 |
| `reviews/` 代码审计 | `code-audit-{slot}-{slug}.md`（slot=`0/1/2…`，**禁止** `audit_0` 叠进文件名）；汇总 `code-audit-summary.md`；台账同名 `.audit.json` |
| `reviews/` 日复盘 | `{YYYY-MM-DD}.md`（保持） |
| `research/` 成篇主文件 | 权威默认 `报告.md`（`output_path` 可覆盖；验收只认主路径） |
| `research/` 单角调研中间产物 | `调研要点.md`（**勿**与主文件抢名） |
| `research/` 多角 / 透镜 | `{短名}调研报告.md` / `{短名}透镜报告.md`；汇总 `汇总与命题卡.md`（辩论依赖，保持） |
| `research/` 审校 | `AgentCore/文档/reviews/审校报告.md`（本轮保持中文名） |
| `debate/` | 机制落盘名保持（本轮不动） |

旧 dogfood 产物不自动迁移。生成端 → `playbooks/audit.py` · `research_quality.DEFAULT_RESEARCH_REPORT_ARTIFACT` · `playbooks/research.py`。

**写路径扁平（✅）**：上表四个约定文档前缀下，写工具（含 `file_batch`）把前缀之后的嵌套 `/` `\` 压成 `_` 成单文件名（例 `reviews/audit-ai/01.md` → `reviews/audit-ai_01.md`）；派 `artifacts` / 约定路径时用平铺名，勿再 move「改回」斜杠子路径。→ `workspace/_paths.sanitize_write_relpath`

同树旁路（仍系统噪音，对 AI / 用户文件 UI 都隐藏；**不**注入）：`AgentCore/{index,trash,baselines}/`（code_search · 软删 · 回合基线）。与可见 `规则/` · `记忆/` · `文档/` 同根；路径感知忽略（禁止全局忽略裸名 `index` 等）。**勿与**容器路径 `~/Documents/AgentCore/` 混淆。常量 → `stage_dirs.{INDEX,TRASH,BASELINES}_REL`。

**否决**：约定文档与 `AgentCore/{规则,记忆}`「两根并存」——改为统一挂 `AgentCore/文档/`；规则/记忆仍仅 documents 注入。否决独立隐藏根 `.agentcore/`（已并入约定根旁路）。

机制 → [编排器 · MLR](/docs/03-AI核心/编排器与CEO主Agent.md)、[辩论编排](/docs/03-AI核心/辩论编排设计.md)；根夹 → [记忆 §5.0](/docs/03-AI核心/Agent记忆与知识系统.md)。

## 五、绑定：项目即工作区 · 双通道入口

### 5.1 产品主路径（✅）

- **两通道（观察期）**：
  - **云协作（推荐）**：新建 / 导入到云 / 连接 Git → 项目权威在云；执行全云。
  - **本机传统（桌面）**：打开本机文件夹 → 本机权威工作区 + 本地引擎。✅ Composer / Ask / 命令面板入口已加回。
- **Composer 入口（✅）**：上区入口动作一行（快速云 / 新建云项目 / 导入到云 / 连接 Git + **本机传统**；可标「上次」），其下弱链「了解区别」→ 对照弹窗；下区已有项目。弹层**无**「在哪工作 / 云协作推荐」说明壳；动作区（含说明）与项目列表只一条分隔（不另切云/本机）。默认云；可记上次通道。手机无本机盘 → 无本机传统。动作行无通道教学副标题；项目行仍示位置。→ §7.1。
- **合回与 ZIP（✅）**：云桌 ModeControl **Diff 勾选合回**本机落点（冲突默认留本地，禁静默覆盖）；**下载 ZIP** 与合回并排（不必先登记落点）。合回落点登记 ≠ 打开「本机传统」项目。✅ 快捷「只合回产物」。⏳ **③ PR/推远程** = 二级出口，**狗粮观察后再排**（不替代 Diff 默认）。整树覆盖不做主路径。
- **本机触点**：合回落点登记 ≠ 本机传统项目；Host / 区外挂载仍可与云桌正交（桌面在线）。
- **执行**：云协作 → **全云**（不默认过桥）。本机传统 → **本地引擎（sidecar）**。过桥 **不当**新入口（§7.2）。

### 5.2 已落地且仍有效

- **会话归属中途不改挂**：创建时绑定项目或明确无项目；**禁止**事后改写本会话 `folder_id`。要换心智 → 新开对话。
- **可达多桌（✅）**：归属不改 ≠ 手只能碰默认桌。分两档：
  - **CEO 只读跨桌**：通用 `file_*` **只绑出生桌**。摸已登记项目用 `list_project_dir` / `read_project_file`；**不**改会话挂载、**不**写入目标桌记忆。
  - **换桌写盘 / 重活**：`delegate` 带已解析 `target_folder_id`。**裸聊未点名（✅ 方案 C）**：纯对话/只读可派（worker 坐 `conv:` scratch，`write_scope=none` 禁写）；写盘 deliverable（`form=files` / `requires_files` / 非空 `artifacts`）未点名 → **禁默写 scratch**。**裸聊写盘自动建云桌（✅）**：无出生且缺有效目标、且无唯一 `turn_target_desk` 时，派工前运行时静默 `POST` 云 Folder并写入 `turn_target_desk`（命名：会话标题 → 用户预览 →「云项目」；不问 `ask_user`；同回合至多一次；**不**改会话 `folder_id`）。多项目同回合 hint 已清空 → 仍须各 task 显式点名（不自动建第三桌）。**裸聊单目标捷径（✅）**：同回合唯一 `create_project` / `resolve_project` / 自动建桌 成功后，缺省 `delegate` 可省略 `target_folder_id`。写权账本按桌×路径分区（见 [Agent协作模式 · 交接式写权](/docs/03-AI核心/Agent协作模式.md)）。
  - **产物完整预览跟落地桌（✅）**：归属不改挂 ≠ 预览只读出生桌。`delivery_status.artifacts[].workspace_id`（有 `target_folder_id` 时为 `folder:{id}`）→ 桌面 `workspace://{folder|conv}.{id}/…` 取 `/v1/workspaces/{ws_id}/files`；无该字段则回退会话出生桌。禁止改挂会话冒充、禁止拷贝进 scratch 假预览。→ [前端技术 §9.12](/docs/04-前端/前端技术与架构.md)
  - 同指挥面：写盘缺桌优先靠**运行时自动建云桌**；`create_project` 仅用户明确「新建名为…」或显式多线先建。导入 / 连接 Git 后再 `resolve`；本机传统走 `open_local_project` / `register_local_project` / `bind_local_folder`（✅ 合法产品路径，非推荐默认）。Composer 弱提示：写文件时可自动建云项目，也可先点「新建云项目」。→ 见代码: `ensure_bare_chat_auto_cloud_desk` · `tools/builtin/projects.py` · 桌面 `ComposerWorkspaceChip`
- **裸聊**：默认云 `conv:<id>` scratch；桌面新建默认云（可记上次为本地则跟记忆）。亦可作跨项目指挥面（自动建桌 / 点名 / 显式 `create_project` 后再派）。
- **用户心智（云）**：「文件在云上；要进本机就合回或下 ZIP。」**用户心智（本机传统）**：「打开的就是本机文件夹；Agent 直接改这里——仍要联网跑模型。」

### 5.3 遗留与观察

| 路径 | 状态 |
|---|---|
| 旧 `mode=local` 项目 | ✅ 仍可打开；侧栏「本机传统」语义；可选「导入到云」，非催迁 |
| `bind_local_folder` / `open_local` / `register_local` | ✅ 本机传统入口已恢复履约 |
| 「打开文件夹 = 本机传统项目」 | ✅ Composer / 命令面板 / Ask；文案用「本机传统」非「本地模式」 |
| 多 Folder 共享本机容器根 + `local_subpath` | 机制仍见代码；不推广 |

**裸聊默认云的理由**（仍成立）：① 关桌面任务停是 Multi-Agent 硬伤；② 大众交付 = 云预览 + 导出/合回；③ 多端可见；④ 对话正文回写云，隐私不支持 local-first 冒充「数据不出本机」。

**否决**：auto-promote 裸聊为 Folder（**≠** 写盘时静默建云桌供 `turn_target_desk`——建桌不改会话归属）；会话中途改 folder；本机传统与云**平级推荐/默认**；Composer 并排「本地引擎 / 云端过桥」；用改会话挂载冒充跨项目；给通用 `file_*` 加换桌参数；只读跨桌写入目标桌记忆；过桥当新入口默认；「整仓永不上传」大众例外（私有化/离线另案）；本机传统冒充完全离线；扫用户原文硬拦发送「先建项目」。

云 scratch / 云项目产物 = 预览 + **ZIP 导出** + **Diff 合回**（单向，与拒双向同步对称）。平台代付**不为云执行新增配额维**——推理 `quota_*` 天然封顶；真实负载后再议。

## 六、关键边界

| 项 | 约束 |
|---|---|
| 路径 | 相对工作区根 POSIX；裸 `/` 与 `\` → `.`；其它绝对路径拒。禁止工具私有 `if path=="/"` |
| 归属变更 | **不支持**事后改挂本会话到另一项目（要换心智→新开对话）。同对话：**CEO 只读跨桌**（`list_project_dir` / `read_project_file`）；**写**须委派 worker（`target_folder_id`）。文件搬运仍是显式动作 |
| 删除 | 软删可逆分轨：云端/sidecar → `AgentCore/trash/`（`GET|POST …/trash` 一键 list/restore，保留期对齐 `workspace_retention_days`）；本地默认 → 系统回收站（**不**承诺产品一键还原）。无系统回收站时桌面兜底仍写 `AgentCore/trash`，可由桌面 IPC 还原。硬删旁路仅针对同树内部区 `AgentCore/{index,trash,baselines}`。删**含 trash 的祖先**（含裸 `AgentCore`）→ **按子项展开**（先硬清三 zone，再逐项软删可见子树），禁止整树移入自身回收区；`WorkspaceIOError` → API 422。删规则/记忆/文档仍可逆 |
| 脚本/shell 破坏可回滚 | Local：`code_execute`/`terminal`/`host_shell` 命中破坏性删启发时，须有本回合 zip 基线（`AgentCore/baselines/`）否则 `FORCE_APPROVAL`；顶层整项目树另窄闸（白名单清 `node_modules`/`.venv` 等不误拦）。桌面通道 Local 经 `ensure_turn_baseline` 落盘/探测非空 zip（gate 问 ready，不给服务端假 `Path.root`）；Sidecar `ServerWorkspace(location=local)` 仍走 Path zip。普通回合基线仍 best-effort、失败不阻断。还原 = overlay 尽力（有体积/忽略目录门），非完整镜像。UI：有基线即可进「改动」恢复，不依赖 `file_*` 产物。无 git 时上下文软提示 `git.init_baseline`（不硬拦派工）。→ `destructive_fs` · `turn_baseline` · `safety_breaker` |
| 本地并行团队 | 同回合多本地根（不同 `root_id`）✅ 经每目标一 `WorkspaceChannel` 并行；sidecar 进程内多根仍 ⏳ C2。单通道容量有限时仍可 snapshot 交接云端放大并行 |
| 引用区外文件 | 单文件仍优先复制进 `attachments/`。对话级区外目录挂载（`external/<别名>/…`）与绑定 / 打开项目 **正交**：桌面在线即可（云 scratch 亦可），不改 binding。**产品主轴（✅ 2026-08-10）**：**AI（+桌面解析）负责定位文件夹；用户只做允许/拒绝**——与行业「指明目标再授权」一致。**否决**区外链失败时弹系统选文件夹作主修复；**否决**「先在文件树看见未授权目录再点授权」。**只读**：工具 `external_mount_readonly`（`approval=never`）按 path 与/或 `well_known`+`target_name` **静默挂载为主**（无授权决策卡；敏感整盘/用户目录根是否改轻确认另议，本轮不改）；grant 链**无**系统选文件夹；成功后同回合热挂载（云 backend）；模型面日常仍走 `external/`，**禁止**把 abs 当 `file_*` 日常路径。**整理/写回**：`ask_user`+`grant_organize_folder` 仍须用户确认（卡上展示 `display_label`，非完整 abs）；只读挂过 ≠ 已授写；授权对象必须是**具体文件夹**（可读标签），禁止空心「请授权本机」。**歧义（✅）**：定位到 2～3 个候选时，卡上让人选「是 A 还是 B」（各一 `grant_organize_folder` + 不同 well_known/target_name/path；仍非系统选文件夹）。→ 见代码: `prompt` / `skills` / `ask_user`。**失败分型（✅）**：对人区分「没定位到」(`not_found`) vs「定位到了但本机不让读」(`permission_denied`)；解析侧 `EACCES`/`EPERM` 不再压成「找不到该目录」。失败后引导用户补充线索或处理系统权限后再说「继续」，**不**改走选文件夹。→ 见代码: `resolveGrantAbsPath`。**口头同意闭环（✅）**：用户已明确「可以整理 / 允许」→ 须立刻发确认卡并履约；卡上「其他…」短允许表同走真 grant；成功或失败都要可见反馈，禁止空泡 / 空心再等确认。→ 见代码: `AskDecisionBody` · `isOrganizeOralConsent` · prompt/skills。**登记**：服务端只存 alias/root_id/mode；持久 abs 只在桌面（`fs-session-grants.json`）；mount 工具参数可含 path（运输例外，与用户原文同级）。**可见/可撤**：工作区面板列本对话挂载并可撤销（成功后须有「本对话已授权：…」锚点，避免人去错侧栏找）。⏳ 本地引擎（sidecar）mid-turn 热推 `externalMounts` 未齐——下回合/resume 注入仍可用 |
| ⏳ 本机 Host | 与工作区绑定**正交**的第三能力面（观测/开系统面板/受控动作）；运输同属桌面回填通道族；云 scratch 亦可装配。→ [工具与能力 · Host](/docs/03-AI核心/工具与能力系统.md) |
| 多 worker | 锁/审批门/通道/handoff 现为进程内态；多机需 Redis + durable queue ⏳ |

HTTP / `ws_id`（`folder:` / `conv:` / `shared:`）→ 见代码: OpenAPI + `workspace/locate.py`。本地 `location=="local"` 时 REST 写/索引返回 409；只读 snapshot list/download 放行。

## 七、Sidecar（本地引擎）与执行路径

比 `LocalWorkspace`（云端引擎 + `WorkspaceChannel` 遥控桌面）更深一层：引擎本体可搬到桌面子进程跑同一 `run_chat_pipeline`，工作区直连本地盘。**产品心智上须与「工作区是否本地」对齐本机传统、与「过桥」拆开**——见下。

### 7.1 三层勿混（已确认）

| 层 | 用户在选什么 | 示例文案 |
|---|---|---|
| **工作区 / 通道** | 云协作 vs 本机传统（观察期双入口） | 「云协作（推荐）」= 云桌权威；「本机传统」= 打开本机文件夹 |
| **执行路径** | Agent 循环跑在哪 | 云协作 = 全云；本机传统 = 本地引擎；过桥 = 仅机制兜底，不当选项 |
| **模型 / 计费** | 推理从哪出 | 推理仍多经云代理；**不得**把「本机传统 / 本地引擎」卖成离线或「数据不出本机」 |

Composer（定案）：用户选 **云协作**（快速云 / 云项目 / 导入 / 连接 Git）或 **本机传统**（打开本地文件夹）；差异须在选项上写清。**否决**并排「本地引擎 / 云端过桥」实现词。→ [前端 UX · 模型与自主度](/docs/04-前端/前端UX设计.md)

**入口差异（须露出）**：

| 入口 | 一句话 | 配套短注 |
|---|---|---|
| **云协作（推荐）** | 文件在云桌，多端同一份；要落本机用合回 / ZIP | 不直接改你电脑上的原文件夹 |
| **本机传统** | 打开本机文件夹；Agent 直接改该目录 | ≠ 离线；模型调用仍走网络 |

### 7.2 产品路由定案（✅ 双通道观察期）

**已落地**：

| 项 | 定案 |
|---|---|
| **云协作（推荐）** | 文件权威云桌；执行 **全云**（`ServerWorkspace`）；**永不默认过桥** |
| **本机传统（桌面）** | 打开本机文件夹 = 本机权威 + **本地引擎（sidecar）**；入口级可见，非设置高级隐藏开关 |
| **默认** | 新用户 / 无记忆 → 云；**可记上次通道**（本机传统行可标「上次」） |
| **过桥** | 仅未走 sidecar 的旧 local / 机制兜底；**不当**新入口选项 |
| **产品面** | 文案「云协作」「本机传统」；取消「本地引擎」作独立第三选项名；禁止卖成离线 |
| **代码 / 落地** | ✅ Composer/Ask/open·register；`mode=local` 默认同侧 sidecar（显式强制关除外） |
| **手机** | 无本机盘 → 无本机传统入口 |
| **取消本机通道（观察）** | 云路径在目标用户上无稳定「大仓痛 / 本机工具链刚需」差评，且合回/导入够用 → 再收入口；**暂不排死线** |
| **续跑** | **路由意向 ≠ 恢复事实**：已有 `origin=sidecar` / 本机活回合续跑跟本地事实 |
| **Toast** | 默认云路径静默；本机传统启动失败可诊断，禁止常态「本地引擎已关闭」吓大众 |
| **否决** | 两平级「云/本地模式」无推荐；过桥当新默认；静默默认 sidecar 冒充「已取消本机」；本机传统冒充离线 |

过桥下 `browser_*` 跟执行走云：无本机 Bridge 时装配 gVisor Sandbox（右坞 SSE 直播）；有健康 Bridge 的真·本地引擎仍走 Local 真页。→ [工具与能力 · browser](/docs/03-AI核心/工具与能力系统.md)

机制能力（stdio JSON-RPC、降级规则、窄票、outbox 等）仍以下文为准。

### 7.3 本地引擎相对过桥：不可等价替代什么

- **不可被过桥「做成一样」的**：引擎与本机盘同侧 → 文件工具循环无「云下指令 → 桌面再读盘」往返；重读盘/多工具更不易把 `WorkspaceChannel` 挤死。过桥只能少跳、跳得稳，不能变零跳（除非引擎也在本机，或盘改到云上）。
- **今天并不交付的**：真正离线、数据不出本机（对话回写云、推理出网）。断线产品档仍是 **断线只读**（不能发送/改文件/跑 AI）。
- **本机传统为何用本地引擎而非过桥**：大仓 + 多 Agent 狂读写时差别可以很大；观察期用「真·同侧」验证需求，而不是用过桥冒充本机通道。

### 7.4 与完全离线 / 私有化

- **完全离线** ⏳（本机推理 + 本机消息库 + 本机执行）：需要「本机运行时」形态；**打开今天的 sidecar / 本机传统 ≠ 离线**。保留代码资产降低以后重做成本；详细提案不在公开仓。
- **私有化**：私有云只部署服务端 → **不依赖**桌面 sidecar；本机/内网一体、断网可用 → 依赖本机（或内网）运行时，与 sidecar 同类而非必须原开关。

### 7.5 同 folder 多对话（云端）

✅ **定案 A′**：同项目多对话应可并行跑 prepare / LLM / 读；**写盘与 snapshot manifest RMW 单层串行**（`workspace_lock`，禁止同 key 嵌套）。已撤云端整回合握锁（避免第二对话静默排队假「思考中」）。写锁短等须诚实等待态（如「等待工作区…」），禁止空 Thinking 冒充。Sidecar 路径本就不握该整回合锁。→ 见代码: `workspace/locks.py` · `ServerWorkspace` mutation / `execute` · `workspace/snapshots.py`

### 7.6 显式合回（✅ 云桌→本机标准出口）

**已落地**：

1. **遗留「后台云端」handoff**：本机快照 → 云上改 → Diff 合回（`handoff_jobs` · `BackgroundTaskReview`）。
2. **云桌标准出口**：ModeControl **导出 ZIP / 导出到本机**；**登记合回落点**；**Diff 勾选合回**（纯桌面 zip↔落点 · `mergeLandingDiff` · `MergeLandingReview`；冲突默认留本地；不经 `applyHandoffJob`）。

| 项 | 定案 |
|---|---|
| 与过桥 / 本机传统 | 云桌 **不**靠过桥；本机传统项目 **不**经合回当主写盘（已在本机权威上直写）。合回 = 云桌→本机单向出口 |
| 副本 / 云桌 | 日常权威在云桌；合回是单向写出。**否决**项目级 Dropbox 镜像 |
| 写回默认 | **② Diff 合入本机**（勾选 / 全部接受；冲突须提示）✅ |
| 写回旁路 | ✅ **① 只合回产物**（快捷）；⏳ **③ 合回=开 PR**（二级；**观察后再排**，不挡大众 Diff） |
| 用户心智 | 「点了才进本机」——禁止暗示已自动双向同步 |
| 入口 | ✅ 云桌 ModeControl 合回 / ZIP / 只合回产物；遗留 handoff（「后台云端」）降权至 ModeControl 高级/遗留，Composer「＋」不再常驻；手机无本机盘 → 无合回面、可 ZIP |
| 否决 | 实时双向同步；合回静默覆盖；用合回冒充本机传统工作区 |

→ 见代码: 桌面 `cloudDeskExit` · `mergeLandingDiff` · `mergeArtifactsOnly` · `mergeLandingPreference`；handoff `conversation/handoff_jobs.py`

**观察项（不排期）**：③ PR 二级；last-merge base 精化三方；服务端 bulk zip 导入（大仓性能）；本机传统通道是否长期保留（§7.2 取消条件）——均待真实反馈再开。

### 7.7 机制约束（实现）

- **路由（机制；以 §7.2 为准）**：云协作全云；本机传统 sidecar；过桥不当新默认。启动失败（出输出前）可降级云；中途失败不降级。
- **健康 ≠ 绑定判定**：健康只决定「要不要拉起」，**不掺进绑定判定**——否则污染续跑 / 列暂停帧。
- **Office 文档能力（云 = 本地）**：附件分流预解析（docx/pdf/pptx 等 → 可读 `*.md`）与工作区 `file_read` **透明抽取**（同一 markitdown 桶；读时默认不写旁路，若已有 `path.md` 可优先读）以及确定性 `md_to_docx` / `md_to_pdf`（fpdf2，禁 Playwright 主路径以免破 sidecar 同能力），在云 API 与 sidecar **同能力**。xlsx/csv 等不抽文本，仍走 `code_execute`。依赖在 `pyproject` 主依赖与 `[project.optional-dependencies].sidecar` 对齐；`bundle:sidecar` 冒烟校验 `docx` / `fpdf2` + markitdown 抽取 + md→docx/pdf。**否决**为瘦身砍掉 Office 却仍跑同一管线（缺库 → 整轮 `PIPELINE_ERROR`）。预解析 / 透明抽取失败仍不阻塞回合（路径提示回落；office/PDF 勿一律教 `code_execute`）——那是解析语义，不是「平面缺依赖」。
- **岔路 B（否决 A）**：平台 key 不下放；推理经云代理；计费权威在云、不从客户端上报。名册同构：桌面铸 **folders 窄票**（`POST /v1/folders/token`）注入 sidecar；`list/resolve/create_project` 与换桌 binding 问云 `GET/POST /v1/folders`（及 `GET /{id}`），**不**打本机 PG、**不**把 access 塞进引擎。对话日志 + 规则/记忆同构：桌面铸 **account 窄票**（`POST /v1/account/token`）注入 `accountAuth: {baseUrl, apiKey}`（`baseUrl`=`…/v1/account`）；`search_conversations` / `read_conversation` 问云 `POST …/conversations/search|read`；`assemble_turn_rules` / `remember` / `DocumentMemoryStore`（consult / update_project_profile 经同一 store）问云 `POST …/rules/*` 与 `…/memory/*`——仅 account（或 access）可调、引擎最小面，**不**把 UI 对话/文档/记忆编辑器 CRUD 对窄票敞开。无票仍走本机 DB。开回合另注入 `folderId` + 本地绑定（`localRootId`/`localSubpath`）供记忆 workspace key，避免为归属/钥匙硬连本机库。
- **持久化**：sidecar → 本机 outbox → 主进程回写云；Renderer 退出回写职责。混合态下对话正文全量回写云——**不得承诺「数据不出本机」**。大众不装 Docker/本机 PG；开发 compose 仅开发者联调。
- **挂起帧**：settlement 预写即消费帧（决策即事实，不还帧）。**否决**：保留帧 + journal 对账改写 paused[]（双真相源对账 = 补丁绊线禁止形态）。挂起帧与 outbox **不并同一状态机**。
- **恢复面**：默认路由变更须重估双面恢复；路由意向 ≠ 恢复事实——刷新后无条件并行查本地+云。
- **契约 α（本地回合恢复）**：刷新 = **双面恢复**（云消息投影 ∥ 本地 recovery/attach）；resume 中靠本地 attach 续活态，**勿只信云薄壳**；outbox 是活态缓冲；终局投影权威 = writeback → `local-turns` → `turn_journal`。不冲 β；不放宽 hydrate、不透传 unsynced journal（掉帧/审计员另案）。→ `outbox.py` / `outbox-writeback.ts`；水合 → [前端技术 · 打开对话水合](/docs/04-前端/前端技术与架构.md)。
- **产品对外（档 A · 断线只读）**：断网/服务不可用时可浏览已缓存对话与本机文件（只读）；不能发送、改文件、跑 AI。完全离线 ⏳ 见 §7.4。

→ 见代码: `apps/server/agentcore/sidecar/`；打包 `apps/desktop/scripts/bundle-sidecar.mjs`；预解析 / 透明抽取核 `workspace/attachment_parse.py`；挂载点 `tools/builtin/file_ops.py`（`FileReadTool`）。

## 八、共享空间 vs 项目工作区

跨用户云端共享文件空间（成员制协作），与「公开分享」（对话只读快照）是两回事；与项目（Folder）**平行、独立实体**。

| | 项目工作区 `folder:` | 共享空间 `shared:` |
|---|---|---|
| 本质 | 会话分组 + 记忆 + 工作区三合一 | 只有「文件」一个面 |
| 归属 | owner 独占 | 成员制（viewer / editor / owner） |
| 挂载 | 会话继承项目绑定 | 会话级挂载为云端第二根 `shared/<别名>/` |
| 本地 | 本机传统可绑；云协作路径不新建本机权威 | **仅云**（否决本地共享 / 双向同步） |

**否决 Folder+成员**：鉴权面圈死在新模块，免在「owner 独占」不变量上开洞；免每个 Folder 功能都要答「成员可见吗」。重启条件：若做「成员对话进项目、共享记忆」的完整项目协作，再交 `folder_members` 治理税。

其它边界：仅定向邀请（不做链接加入）——桌面邀请入口为 **好友多选 + 精确搜用户名/ID**（非好友仍可搜邀；已成员/待接受灰显；所有者可取消 pending）；可编辑成员的 Agent 可直写（角色即授权，不加第二道门）；**仅云执行对话可挂载**（sidecar 无跨运行时双根）。v1 不做：公开文件链接、实时协同、成员对话/共享记忆、组织概念。
