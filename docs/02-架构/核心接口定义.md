---
status: landed
code: apps/server/agentcore/
related:
  - docs/03-AI核心/执行引擎架构设计.md
  - docs/04-前端/前端技术与架构.md
skip_if:
  - 纯 UI 样式改动
---

# 核心接口与数据模型定义

> **状态**：✅ 已落地节（§一–§五、§七）以代码为真相源，各节留单指针 + 设计理由；⏳ 目标节（§六 部分表结构 / Redis 跨进程态）保留契约说明。
> **目的**：定义模块间接口协议与核心数据类型。已落地部分避免与实现漂移；未落地目标 schema 保留完整契约。

---

## 总览

本文档定义 AgentCore 的核心接口和数据模型。各模块依据这些接口独立开发，通过抽象协议耦合而非具体实现耦合。

### 接口分类

| 类别 | 文档章节 | 说明 |
|------|---------|------|
| LLM 抽象层 | §一 | Agent 与 CEO 主 Agent 调用 LLM 的统一接口 |
| 工具系统 | §二 | Agent 调用工具的协议 |
| 沙箱执行 | §三 | 代码执行隔离 |
| API 层 | §四 | 前端↔后端 REST + SSE |
| 认证 API | §五 | 注册/登录/刷新/登出 Cookie 会话 |
| 数据库模型 | §六 | PostgreSQL 持久化 Schema |
| 前端状态 | §七 | Zustand Store 类型定义 |

> CEO 编排 / `delegate` 契约、执行引擎内部数据结构、Run 模型已在各自文档中详细定义，本文不重复。交叉引用见 §八。

---

## 一、LLM Provider 抽象层

→ 见代码：`apps/server/agentcore/llm/`

### 1.1 核心协议

LLMProvider 是 LLM 调用的统一抽象（`complete` 非流式 / `stream` 流式），实现为 OpenAI 兼容的 `OpenAICompatibleProvider`（`llm/provider/openai_compatible.py`）；多厂商经 `ProviderRouter`（`llm/provider/router.py`）按 `provider/model` 前缀路由。

设计约束（代码看不出来的部分）：
- `ModelTier`（fast/strong）是**CEO 经 `delegate` 对外输出的受控词汇**（worker 能力档位），本身不是模型名；运行时经 `agent_profile()` 映射到内部 `ModelProfile`——解耦「`delegate` 输出契约」与「内部模型实现配置」。
- 思考模式由 `thinking` + `reasoning_effort` 控制；DeepSeek 思考模式下 tool call 必须回传 `reasoning_content`。

### 1.2 场景画像（ModelProfile）

单一事实源：每个用 LLM 的场景（chat / agent.fast·strong / memory / title）都是一个命名 profile（`ProfileParams`），持有「采样参数 + ReAct 轮次预算 `max_rounds`」。**model 与 profile 解耦**——turn 级 model 由 `TurnProfiles.model` 单点解析，`build_request(model=…)` 显式传入，profile 本身不含模型名；调用点只解析 profile + model，不在各处手抄参数。

设计约束：
- profile 是**内部实现**，不暴露给 `delegate` 调用方；CEO 经 `delegate` 只产出 `ModelTier`，经 `agent_profile(tier)` 单点映射到 `agent.<tier>` profile。
- `max_rounds` 仅 ReAct 路径（`engine.loop`）消费；一次性 `complete` 的场景取默认。

模型分配策略见 [`技术架构与基础设施.md` §二](/docs/02-架构/技术架构与基础设施.md) 与 [`DeepSeek V4 参考`](/docs/03-AI核心/DeepSeek-V4-API参考.md)（按角色分档权威）。

### 1.3 与其他模块的关系

→ 见代码：`apps/server/agentcore/llm/profiles.py`（场景 profile：`PROFILES` / `agent_profile` / `build_request`）+ `llm/resolve.py`（model 与凭据解析）。

---

## 二、工具系统

→ 见代码：`apps/server/agentcore/tools/`

### 2.1 工具协议

Tool 统一协议（`schema` 元信息 + `execute` 执行）、工具调用/结果/上下文/审批三态等数据类型已落地。

设计约束：
- **审批三态** `never / grantable / always`：审批门与工具来源无关，内置/市场/连接器工具在执行层只认 `ToolSchema.approval` 一个字段（详见 [`执行引擎架构设计.md` §五](/docs/03-AI核心/执行引擎架构设计.md)）。
- `ToolResult.output` 超长由 `__post_init__` 自动截断（`output_limit` 可上调，read 类工具用以保全正文），避免污染上下文窗口。
- 研究类工具（`web_search`/`read_url`）及 **`delegate`（核验 worker 经其带回的 web 源）** 可在 `ToolResult.citations` 附带结构化来源（`{url,title,snippet,site}`）；ReAct 循环跨回合聚合去重后经 `citations` SSE 事件下发并持久化到 `messages.citations`，前端渲染为来源卡片。来源 dict 本身是 UI 元数据；但引擎会给每个来源分配一个规范号（= 卡片下标）并**回灌进该工具的模型可见输出**（`[来源编号] …[n]=url`），让 CEO 照号输出 `[n]` 角标、与卡片天然对齐（A2）。`delegate` 单次调用仅携带**本次新增**（去重后）citations，回合收口 backstop merge 幂等、编号稳定（法律垂直来源卡台账，见 [`远期规划.md §4.5`](/docs/06-规划/远期规划.md)）。

### 2.2 MVP 内置工具

「分类即工具包、全部 opt-in」的装配策略见 [`执行引擎架构设计.md` §五](/docs/03-AI核心/执行引擎架构设计.md)。

---

## 三、沙箱执行（SandboxProvider）

→ 见代码：`apps/server/agentcore/tools/sandbox/`

### 3.1 MVP 实现：SubprocessSandbox

默认受限 subprocess 沙箱（非隔离边界）；`GVISOR_ENABLED=true` 时云端 worker 切 `GVisorSandbox`(runsc) 真隔离（`tools/sandbox/gvisor.py`，Linux only，默认关）。

**MVP 实际措施**：临时目录隔离（每次执行独立 tmpdir）+ `asyncio.wait_for` 超时 + stdout/stderr **2KB 分块异步捕获**（`on_output` 回调驱动 `tool_use_progress` 流式推送，live UI 累积、重载看 `tool_use_end` 权威态）+ 执行后清理。

**目标安全措施**（尚未落地）：
- `resource.setrlimit` 限制 CPU 时间和内存
- 禁止 import 危险模块（通过 seccomp 或 import hook）
- 网络出站阻断（iptables 规则）

---

## 四、API 层（前端 ↔ 后端）

→ 见代码：`apps/server/agentcore/api/`（路由 + `schemas/`；挂载见 `main.py`；OpenAPI 为前端类型唯一真相源）

### 4.1 REST API

端点与请求/响应模型的唯一真相源是 FastAPI 路由 + 自动生成的 OpenAPI（前端类型由 `openapi-typescript` 生成）。

> 认证路由详见 [`../05-平台与运维/认证与会话.md`](/docs/05-平台与运维/认证与会话.md)。

### 4.2 SSE 实时流

两种形态（本质差别：对话页「对方」是服务端 AI，POST 即可造出回复；消息页「对方」是另一个人的客户端，服务端须把消息扇出给在线对端）：

- **对话页（找 AI）**：不走独立连接端点——`POST /v1/conversations/{id}/messages` 的响应体即 `text/event-stream`，执行事件随之推送。封装见 `api/sse.py`，事件类型详见 [`执行引擎架构设计.md` §二](/docs/03-AI核心/执行引擎架构设计.md)。
- **消息页（找人 IM）**：每用户一条长连 firehose `GET /v1/realtime`（server→client），把别人发来的消息扇出到你的所有在线连接（多端）；发送仍走 `POST /v1/messages/chats/{id}/messages`。fan-out 进程内 pub/sub 起步（`messaging/hub.py`），多 worker 上 Redis/NATS，换实现走 `ChatEventPublisher` seam（[`消息IM.md` §四](/docs/05-平台与运维/消息IM.md)）。事件信封 `{ type, ... }`，P0 类型 `chat_message`；Cookie 鉴权、断连按 `last_read_message_id` 拉增量补偿（best-effort，非持久投递）。

### 4.3 核心请求/响应模型

→ 见代码：`apps/server/agentcore/api/schemas/`（路由层做 ORM→Pydantic 映射；前端 TS 类型由 OpenAPI 自动生成）。

### 4.4 SSE 事件类型

SSE 事件契约不在此重复定义。事件信封为 `{ type, timestamp, payload }`，语义见 [`执行引擎架构设计.md` §二](/docs/03-AI核心/执行引擎架构设计.md)（后端 `runtime/events/`、前端 `types/events.ts` 对齐）。

---

## 五、认证 API 契约

> 认证（注册/登录/刷新/登出/me）**已落地**，端点契约、数据模型（`users` / `credentials` / `invites` / `refresh_tokens`）、令牌轮换、Cookie 与 CSRF 约束、IDOR 堵漏的完整说明，以代码为准统一收录于 [`../05-平台与运维/认证与会话.md`](/docs/05-平台与运维/认证与会话.md)。本文不再重复。

---

## 六、数据库模型（PostgreSQL）

→ 见代码：`apps/server/agentcore/db/models/`、`db/repositories/`、`db/migrations/versions/`

### 6.1 核心表

完整 schema 的真相源是 ORM + Alembic 迁移链。

> 用户长期记忆不用独立表，改为文件树里 `ai_maintained=true` 的 rule 文件（见 [`Agent记忆与知识系统.md`](/docs/03-AI核心/Agent记忆与知识系统.md) §五、§八）。
>
> 白板 `boards` 表（scene blob + CAS `version`，<256KB 内联 JSONB / ≥256KB 落 S3）+ `/v1/boards` CRUD（照 `folders.py` 范式）+ 视觉读图入账 `cost_events.role=vision`，见 [`AI 协作白板`](/docs/04-前端/AI协作白板.md) §七/§九.4。

### 6.2 数据模型架构约定 ✅ 已确定

> 从实践中沉淀的数据建模原则，指导新系统的 schema 设计。

#### 主键与引用

- 主键统一 UUID（Python `str(uuid4())`）
- **目标与现状均不使用 ORM ForeignKey**（`db/models/` 实际无 FK 声明），引用关系通过 `*_id` 字段在应用层维护——简化迁移、避免级联锁、允许跨模块松耦合

#### 软删除架构

带 `deleted_at` 字段的表采用查询层默认过滤：ORM `select` 默认自动过滤 `deleted_at IS NULL`，需查已删除数据须显式 opt-out。选择「全局 event + 按实体白名单分批」而非：
- 全局绑基类（一次反转所有表，波及大量故意不过滤的查询）
- 纯人工纪律（漏一处即静默泄露已删数据）

#### 版本体系

- Agent 用独立 `agent_versions` 表维护版本链
- 文件用 `file_versions` 表
- Team 不版本化

#### 文件模型单表设计

文件系统采用单表 + 维度收敛设计（`kind` × `role`），不拆多张领域表——单表是「文件夹 = 上下文边界、一棵树什么都能放」的物理基础。拆表会打碎统一内容树。

| 维度 | 含义 |
|------|------|
| `kind` | 节点类型（folder / document / upload / base），DB CheckConstraint |
| `role` | AI 上下文角色（rule / general / attachment 等）；`rule` 由 `ai_maintained` 区分用户规则与 AI 记忆 |

非树资产（头像等）剥离到独立 asset 模块（不在树、无 parent_id 语义）。

#### JSONB 策略

JSONB 列不盲加 GIN 索引。真正的扩展杠杆是「先用高选择性主键/外键列限定作用域 + 应用层分页」。复查触发条件：单作用域记录到万级、出现跨作用域 JSONB 查询、或特定操作符成为高频慢查询。

#### ORM 是 Schema 真相源

全部表由 Alembic 迁移链维护；以 Alembic head 为准。冷启动 = `alembic upgrade head`（建 schema）+ seed（灌初始数据），职责分离。默认值一律用 `server_default`（数据库级）。

#### 索引策略

索引随 ORM 列定义一并维护。

### 6.3 目标 Schema（规划）⏳

> **实现现状**：实际已建表以 ORM（`db/models/`）为准。以下为**未建表**规划模块（非线上库快照）：

| 模块 | 核心表 | 状态 |
|------|--------|------|
| agent | agents, agent_versions, agent_teams, team_members | Fork 自引用 ⏳ |
| conversation | turns | ⏳ |
| capability | tools, skills, rules, agent_entity_bindings | ⏳ |
| file | document_chunks | RAG / 上下文注入 ⏳ |
| billing | user_quotas | 配额覆盖落在 `users` 表 ✅；独立 quotas 表 ⏳ |

### 6.4 运行时状态（Redis）⏳

> **实现现状**：✅ 回合级结构化挂起/恢复已落地（`paused_turns` 表 + `runtime/suspension*.py` + `POST …/resume`）；✅ Turn Journal 已落地（`turn_journal` 表 + `runtime/journal/`）。⏳ Redis 跨进程检查点 / 交互队列未走 Redis——server 目前仅在 `RATE_LIMIT_BACKEND=redis` 时用 Redis 做可选分布式限流（`middleware/redis_rate_limit.py`，默认内存档）。

Redis 承载 ReAct 检查点快照、交互等待/结果队列、审批授权集合等跨进程态（具体 key 模式以落地实现为准）。

Turn Journal（`turn_journal` 表）为每 turn 的 append-only 事实流，主键 `(turn_id, seq)`，九类事实。回合内一切事实只写这里，messages/turns/SSE/快照/历史回放皆其投影。（✅ 已落地，见 `runtime/journal/`：entries / fold / persist）

---

## 七、前端状态类型（Zustand Store）

→ 见代码：`apps/desktop/src/renderer/stores/`、`types/events.ts`

### 核心类型

设计约束（代码看不出来的部分）：
- `RunPhase` 等执行状态枚举须与后端 `runtime/runs/types.py` 对齐——前端状态机由 SSE 事件驱动，双端不一致会导致渲染错位。
- 图（团队 DAG）由 ExecutionState 投影出的 runs 经 `buildGraphStructure`（`components/graph/helpers.ts`，`useGraphLayout` 消费）按需生成，不独立持有真相，避免双源不一致；`useGraphStore`（`stores/graph.ts`）仅持布局偏好（`layoutKind`）。

---

## 八、模块间接口依赖总结

```
┌─────────────┐     LLMProvider      ┌─────────────┐
│  CEO Agent   │ ──────────────────→ │ LLM 抽象层   │
│  (runtime)   │                      │ (DeepSeek)  │
└──────┬──────┘                      └─────────────┘
       │ delegate → RunPlan                   ↑
       ▼                                     │ LLMProvider
┌─────────────┐     Tool Protocol    ┌──────┴──────┐
│  执行引擎    │ ──────────────────→ │  Agent 运行时│
│  Scheduler  │                      │  (协程)     │
└──────┬──────┘                      └──────┬──────┘
       │ SSE Events                          │ Tool.execute()
       ▼                                     ▼
┌─────────────┐                      ┌─────────────┐
│  API 层     │                      │  工具系统    │
│  (FastAPI)  │                      │  ToolRegistry│
└──────┬──────┘                      └──────┬──────┘
       │ REST + SSE                          │ SandboxProvider
       ▼                                     ▼
┌─────────────┐                      ┌─────────────┐
│  前端       │                      │  沙箱       │
│  (Electron) │                      │  (subprocess)│
└─────────────┘                      └─────────────┘
```

### 接口契约原则

1. **类型是真相源**：后端 Pydantic model / dataclass 是唯一类型来源，前端类型由 OpenAPI 生成
2. **Protocol 优于继承**：所有抽象用 `Protocol`（结构子类型），不用 ABC
3. **数据不可变**：跨模块传递的数据对象应视为不可变，修改需创建新实例
4. **错误用异常**：模块内部用异常传递错误，API 边界转为 HTTP 状态码 + JSON body

### 交叉引用

- CEO 编排 / `delegate`：[编排器与CEO主Agent.md](/docs/03-AI核心/编排器与CEO主Agent.md)
- Run 模型 / WaveScheduler：[执行引擎架构设计.md](/docs/03-AI核心/执行引擎架构设计.md)
- 运行时三模型（Interaction / Turn Journal / Run）：[执行引擎架构设计.md §八](/docs/03-AI核心/执行引擎架构设计.md)
- SSE 事件协议：[执行引擎架构设计.md §二](/docs/03-AI核心/执行引擎架构设计.md)
- 长期记忆 / TitleGenerator：[Agent记忆与知识系统.md](/docs/03-AI核心/Agent记忆与知识系统.md)
