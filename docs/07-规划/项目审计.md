# 项目审计（首轮：全栈健康体检）

> **状态**：🗂️ 首轮已收尾（2026-06-29，见 §十一 结案）的工作文档（审计计划 + 问题登记册）。非现状蓝图，剩余 P3 各自走 `dev-process` 决策流程立项。
>
> **本文是什么**：一份**可执行的审计计划**——先定清楚「审什么 / 怎么判 / 按什么序 / 产出什么」，再据此一步步审。审计**只发现与登记问题，不在本轮顺手改代码**（改动各自立项，契约级改动须人确认）。
>
> **不止静态**：除了"代码看着对不对"，本轮还要求**每个功能端到端跑得通**（见 §二 D 维度）——审计输出既有「问题登记册」，也有「功能清单 + E2E 证据」。

---

## 一、目标与边界

### 锁定的方向（2026-06-29 决策）

| 维度 | 选择 |
|---|---|
| 套餐 | **全栈健康体检** = 架构一致性 + 代码健康 + 文档漂移 + **功能端到端验证（E2E 可用性）** |
| 范围 | **全 monorepo**：`apps/server` + `apps/desktop` + `apps/mobile` + `apps/admin` + `packages/`（+ `website`/`promo` 轻扫） |
| 节奏 | **广度优先 → 先建「问题登记册 + 功能清单」→ 按优先级深挖** |
| E2E 彻底度 | **逐个功能手动实跑（穷尽，非分层），"慢慢来"** |

> **E2E 彻底度（2026-06-29 补充决策）**：每个功能都要给出实跑结论，不止登记盲区。节奏放慢可接受；但仍**先跑现成自动证据 / 零-LLM** 再人肉补，真模型旅程**省着花**（决策：零-LLM/假 provider/Flash 优先、每条只跑一次）。两类成本区分见 §二 D。决策齐全，已进入执行。

### 不在首轮范围（避免发散，后续可单独立轮）

- 安全深挖（认证/会话/IDOR/BYOK 密钥/沙箱逃逸）——仅在体检中顺手记**显眼**风险，不做系统性渗透式审查。
- 成本/性能 profiling（token/计费路径/热点）。
- 产品完整度矩阵（MVP 6 模块 + 垂直 vs 实际）。
- 补测（**不写新自动化测试**；E2E 验证靠跑现有测试/向量 + 手动 happy path 实跑，缺口只**标注**不补）。

### 锚定与免责

- 审计锚定**当前 main 工作树状态**。本仓多 Agent 并行开发、大量 WIP 未提交（白板 / 辩论 / vision / board 等）；正在动的区域会在登记册标注「⚠️ WIP，可能很快变化」。
- 自检读代码用 **Read**，不与 `git status`/`git diff` 对账（见 `.cursor/rules/git-workflow.mdc`）。

---

## 二、审计维度与判定标准

四条镜头，每条给「查什么 + 判定依据（锚到蓝图/规则）+ 怎么查」。前三条是静态（读代码/对蓝图），第四条 D 是动态（功能真的跑得通）。

### A. 架构一致性（代码 vs 蓝图）

| 检查项 | 判定依据 | 方法 |
|---|---|---|
| 后端五层单向调用 | `后端架构.md §一`：`models→schemas→repository→service→router` 不可反转（router 不直接碰 DB、repository 无业务逻辑） | 抽查各模块 `routes/`、`repositories/`、`service` |
| 模块依赖硬约束 | `项目结构.md §二`：`api` 不依赖 runtime/llm/tools；`runtime` 不依赖 conversation/api；`core/db/llm/tools` 零向上依赖 | 按表逐条 grep import 反向引用 |
| runtime 子包边界 | `后端架构.md §三`：engine/runs/journal/interactions/events/resolve… 关注点不串味 | 抽查跨子包 import |
| 垂直插件反向依赖 | `后端架构.md §四`：核心不得 import 任何垂直（`legal_*` 等）；白名单为空可独立启动 | grep 核心→垂直 import |
| 契约漂移 | `protocol-conformance.mdc`：两端 fold 行为对齐；OpenAPI 单一源 | 跑 `pnpm conformance`、查 `openapi.json` 与 schemas 是否同步 |
| 「实现冒充需求」 | `dev-process.mdc`：是否有为复用旧实现而裁剪的需求、用「技术方便」反推的设计 | 阅读 + 对照 docs 设计意图 |

### B. 代码健康

| 检查项 | 信号 | 方法 |
|---|---|---|
| 补丁绊线味道 | `dev-process.mdc` 绊线：兜底/对账/自愈/特例类逻辑；同一根因改 ≥2 层；同一接缝第 3 次打补丁 | grep `fallback`/`workaround`/`HACK`/`兜底`/`特例` + 阅读热点 |
| 死代码 / 未用导出 | 无引用的函数/模块/导出 | grep 引用计数、`ruff F401` 等 |
| 重复实现 | 多处近似逻辑（各端本应分叉的除外） | 语义检索同义实现 |
| 超大模块 / 函数 | 单文件/单函数过长、圈复杂度高 | 按行数排序定位热点后阅读 |
| Lint / 类型债 | `ruff`（E/F/W/I/N/UP/B/A/SIM）、`biome`、`tsc --noEmit` | 跑工具收基线 |
| 错误处理完整性 | 生产级要求：完整错误处理、合理日志（`dev-process.mdc`） | 抽查关键路径 try/except、SSE/IPC 边界 |
| TODO/FIXME 债 | 标注未决 | grep `TODO`/`FIXME`/`XXX` |

### C. 文档漂移

| 检查项 | 判定依据 | 方法 |
|---|---|---|
| ✅/⏳ 标注 vs 实际 | `doc-governance.mdc`：标 ⏳ 但已实现=文档滞后；标 ✅ 但缺失=虚标 | 抽样蓝图断言回代码核对 |
| `code:`/`→ 见代码` 指针有效性 | frontmatter `code:` 与正文指针应指向真实路径 | 校验路径存在 |
| 契约文档 vs 代码 | `核心接口定义.md` REST/SSE/DB 形状 vs 实现 | 抽查高频契约 |
| 索引/路由准确性 | `docs/索引.md` 任务路由表链接有效 | 校验链接 |

### D. 功能端到端验证（E2E 可用性）

目标：不止"代码看着对"，而是**功能从用户入口到结果回显真的跑得通**。先建**功能清单**（§四之二），逐条判定 E2E 证据与结论。

**证据分层（按成本/可靠性，优先用现成的）**：

| 层 | 证据来源 | 说明 |
|---|---|---|
| L1 自动 E2E | `tests/integration/`、`pnpm conformance` 向量、`evals`(L0/L1)、前端 vitest + fold、preview 回放向量 | 已有自动证据 → 直接采信，跑一遍看绿 |
| L2 实跑 happy path | 终端已在跑后端(`uv run python -m agentcore`) + `pnpm dev`；对关键旅程手动走一遍 | 缺自动证据时，用运行环境实证主路径 |
| L3 盲区 | 既无自动证据也难手动复现 | 记为 finding（维度=E2E、标"E2E 盲区"），给补法建议 |

**判定结论**：✅ 通过 / ❌ 失败（附现象） / ⚠️ 盲区（无证据）。失败按 §三 定级（核心功能不可用 = P0/P1）。

> **彻底度（决策：逐个功能实跑、慢慢来）**：每个功能最终都要 ✅/❌ 结论，L3 盲区只是"还没验"的中间态，不是终点。
>
> **两类成本要分清**：
>
> | 成本 | 是什么 | 谁买单 |
> |---|---|---|
> | Cursor AI（我） | 驱动每个 E2E、读输出、记结果的时间/用量 | Cursor 用量 |
> | 产品 AI（DeepSeek） | 跑到真模型的功能（聊天/派单/辩论/vision）每次真实 API 调用 | 项目 DeepSeek/BYOK 账单 |
>
> **省钱策略**：能用 L1（零-LLM/假 provider/集成测试/conformance）验的就不碰真模型；纯后端/前端功能（文件/认证/工作区/admin 等）压根不调 LLM；真模型旅程优先 Flash、每条只跑一次。真正烧 DeepSeek 的只有 AI 核心那几类。

---

## 三、严重度分级

| 级别 | 含义 | 例 |
|---|---|---|
| **P0 阻断** | 破坏不变量 / 契约 / 安全；线上会出事 | 分层硬约束被破、契约漂移致两端 fold 不一致、密钥泄漏路径 |
| **P1 高** | 显著健康/一致性问题，应尽快修 | 补丁绊线第 3 次、核心路径缺错误处理、蓝图与实现严重背离 |
| **P2 中** | 应改善，不紧急 | 重复实现、超大模块、文档 ⏳/✅ 误标 |
| **P3 低** | 轻噪音 / 清理项 | 死代码、TODO 债、lint 噪声、失效链接 |

---

## 四、产出表格式（登记册 + 功能清单）

### 四之一 问题登记册

每条一行，字段固定：

| 字段 | 说明 |
|---|---|
| ID | `AUD-NNN` 递增 |
| 维度 | 架构 / 健康 / 文档 / E2E |
| 区域 | 如 `server/runtime`、`desktop/renderer`、`packages` |
| 位置 | `路径:行` 或模块 |
| 描述 | 一句话说清「问题是什么」 |
| 根因 | 为什么会这样（避免只描述表象） |
| 严重度 | P0–P3 |
| 建议 | 修法方向（不含本轮实现） |
| 状态 | 待确认 / 已确认 / 已修 / 搁置 |

> 登记册随执行**就地追加到本文 §七**；若条目过多再拆到同目录 `项目审计-登记册.md`。

### 四之二 功能清单（E2E）

每个功能一行，回答"它真的跑得通吗"：

| 字段 | 说明 |
|---|---|
| 功能 | 用户可感知的能力（如"辩论编排""派单 delegate""工作区文件读写""登录刷新"） |
| 入口→结果 | 端到端路径一句话（前端入口 → 后端 → LLM/存储 → 回显） |
| 证据层 | L1 自动 / L2 实跑 / L3 盲区（见 §二 D） |
| 证据指针 | 测试/向量/eval 路径，或实跑步骤 |
| 结论 | ✅ 通过 / ❌ 失败 / ⚠️ 盲区 |
| 关联 | 失败/盲区对应的 `AUD-NNN`（如有） |

> 功能清单随执行**就地追加到本文 §七之二**。

---

## 五、覆盖范围与巡检序列

广度优先，按**价值密度**排序（皇冠明珠先扫）。每个区域过一遍 A/B/C 三镜头，登记问题，不当场深挖。

D（E2E）**以功能而非目录为单位**、横跨多区，故并行进行：扫到哪个区就把该区涉及的功能登进 §四之二 功能清单，能跑的现成自动证据顺手跑、关键旅程顺手实跑。

| # | 区域 | 重点子区 | 备注 |
|---|---|---|---|
| 1 | `server/runtime` | engine / runs / journal / delegate / debate / events / resolve | 执行核心，最高优先 |
| 2 | `server` 平台 | api / conversation / db / llm / tools / config / security / billing | 五层 + 硬约束主战场 |
| 3 | `server` 横切 | conformance / evals / vision / sidecar / middleware | 契约门禁 + 评估体系 |
| 4 | `apps/desktop` | renderer/stores / services / sse / protocol fold / components | 体量最大的前端 |
| 5 | `apps/mobile` | stores / services / 协议 fold | 跨端对齐 |
| 6 | `apps/admin` | 路由 / 鉴权 / 审计页 | 运营台 |
| 7 | `packages/` | contract-types / contract-rest-types / protocol-conformance / design-tokens | 跨端契约单一源 |
| 8 | `website` / `promo` | 轻扫 | 营销站，仅记显眼问题 |

---

## 六、方法与工具

**自动信号（先收基线，再人读）**：

| 目标 | 命令（仓库根 / 对应包） |
|---|---|
| 后端 lint | `uv run ruff check apps/server` |
| 后端测试 | `uv run pytest`（`apps/server`） |
| 前端 lint + token 门禁 | `pnpm -C apps/desktop lint` |
| 前端类型检查 | `pnpm -C apps/desktop typecheck` |
| 前端单测 | `pnpm -C apps/desktop test` |
| 协议漂移巡检 | `pnpm conformance` |
| REST 类型同步 | `pnpm gen:types`（查是否漂移） |

**人读（工具抓不到的部分）**：分层方向、依赖反转、补丁绊线、实现冒充需求、文档漂移——这些必须靠阅读 + 对照蓝图判定。用 Read / Grep / 语义检索定位，禁止脚本批量改源码。

**E2E 实跑（D 维度）**：

| 目标 | 命令 / 入口 |
|---|---|
| 后端集成 E2E | `uv run pytest apps/server/tests/integration` |
| 评估 happy path | `python -m agentcore.evals`（L0 零-LLM 子集可先跑） |
| 协议端到端对齐 | `pnpm conformance`（向量 → fold → golden） |
| 前端离线态回放 | `pnpm -C apps/desktop dev:web` + `#/preview`、`pnpm -C apps/desktop shoot`（无头截图） |
| 真实运行环境 | 终端已在跑：后端 `uv run python -m agentcore`、`pnpm dev`（关键旅程手动走查） |

> 实跑只验证、不改代码；需要真模型的旅程按现有 dev 环境走，注意成本（参 `llm.mdc`）。

---

## 七、产出表（执行中填充）

### 七之零 基线快照（自动信号·零真模型·2026-06-29）

| 信号 | 结果 | 备注 |
|---|---|---|
| 后端 `ruff check` | ✅ 0（F821×42 已修→0；风格债 AUD-002 于 2026-06-30 清零） | All checks passed（exit 0）|
| 前端 `tsc --noEmit` | ✅ 0 | 无类型错误 |
| 协议 `pnpm conformance` | ✅ 39/39（mobile + desktop） | fold 零漂移 |
| 前端 `vitest run` | ✅ 595/595（59 文件） | 日志中 401/TypeError 为负路径测试断言错误处理，非失败 |
| 后端 `pytest` | ✅ 2188 passed（7m11s，exit 0） | 187 测试文件，仅 4 个 deprecation 警告 |
| evals L0（零-LLM 门） | ✅ 41 用例 lint OK（core11/routing5/comparison13/probe5/mast7）+ `test_evals_*` 单测在 pytest 绿 | `--lint-only` 校验用例结构；L0 不变量(`checks.py`)走假 provider 单测 |
| 后端运行（终端 2） | ⚠️ reload 噪音 | `uvicorn --reload` 在并行编辑触发热重载时取消在途流式响应，自动恢复；环境噪音非 bug |

> **成本提示**：`python -m agentcore.evals`（裸跑）默认 `layer=2` **会调真实 DeepSeek**（连 `--layer 1` 也只是跳过裁判、仍跑真实模型回合）；唯一零-LLM 是 `--lint-only`（+ `test_evals_*` 假 provider 单测）。`后端架构.md §五` 表述准确（"per-PR 仅跑零-LLM 自测""出数都需真模型回合"），真模型评测(L1/L2)按省钱策略 deferred。`evals/cases/gold/labels.json` 仅随仓库给 `.example` 模板、需运维自建（`--calibrate` 未配置即 exit 2，设计内）。

### 七之一 问题登记册

| ID | 维度 | 区域 | 位置 | 描述 | 根因 | 严重度 | 建议 | 状态 |
|---|---|---|---|---|---|---|---|---|
| AUD-001 | 架构/健康 | server/conformance | `vectors/{single_agent,multi_agent,debate,legal,gates}.py` | 5 个 vector 文件在返回注解用 `SSEEvent` 但未导入（`board.py` 已正确导入）；被 `from __future__ import annotations` 掩盖→运行不报、conformance 仍绿，但 `ruff check` 因此红（42×F821） | 创建/重构 vector 文件时漏导入，注解延迟求值掩盖 | P2 | 5 文件各补 `SSEEvent` 导入（参照 `board.py`） | ✅ 已修（补 5 处导入，F821 42→0） |
| AUD-002 | 健康 | server | 多文件（含 tests/、WIP 新文件） | 174 条风格债：E501 长行×148 + I001 import 乱序×16 + F401 未用导入×4 + B905×3 + SIM×3 | 并行新增代码未过 lint | P3 | `ruff check --fix`（22 可自动）+ 人工裁长行 | ✅ 闭环（2026-06-30）：`ruff check` 0 违规——真代码 E501×26/B905×3/SIM108×1/F401×1/I001×2 已修，LLM 内容模块 `legal_skills.py`（E501×55，同 `skills.py` 先例）per-file-ignore；详见 §十一之四 |
| AUD-003 | 架构 | server/api | `api/routes/{capabilities,tools,files,model_modes,inference,…}.py`、`api/schemas/messages.py` 等 15+ 文件 | `api` 层直接 import `runtime`/`llm`/`tools`（如 `runtime.prompt/skills`、`tools.catalog/builtin`、`llm.factory.build_provider`、`runtime.events/journal/turn_runs`），违反 `项目结构.md §二` 硬约束「api 不依赖 runtime/llm/tools，只经 schemas+service」 | 分层随功能演进侵蚀；或文档约束已过时（SSE 流式聊天本就耦合 runtime） | P2 | ✅ 真侵蚀局部 B 修：`files.py` 的 provider 构建下沉 `assist.rewrite`、`favicon` 经 `core.net` 共享 SSRF/net 解除 api→tools；良性耦合（类型/事件/常量/投影）按现实写入 §二 api 行 | ✅ 已闭环（边界测试守） |
| AUD-004 | 架构 | server/core | `core/logging.py:27` | `core/logging.py` import `agentcore.config`，与「core 零向上依赖」存在张力 | 日志需 settings（级别/路径）；config 是否算地基属边界 | P3 | config 视为 core 之下的配置地基，§二 core 行已加注 | ✅ 已闭环（边界测试允许 core→config） |
| AUD-005 | 架构/文档 | server/tools | `tools/builtin/*` 共 19 文件（delegate/debate/ask_user/revise/escalate/post_note/read_notes/board_read/consult_*） | `tools` 大量 import `runtime.*`/`llm.*`/`memory.*`，违反 `项目结构.md §二`「tools ⊥ 所有业务模块」——但这与 `后端架构.md §三`「内置编排原语(tools/builtin)与 runtime 同构」**自相矛盾** | 编排原语(delegate/debate/...)按设计就耦合 runtime，但 §二 约束表没区分「叶子工具 vs 编排原语」 | P2 | §二 已拆「叶子工具 ⊥ runtime/llm」/「编排原语豁免」，与 §三 对齐 | ✅ 已闭环（边界测试守叶子工具） |
| AUD-006 | 架构/文档 | server/api+db | `routes/folders.py`（已确认）+ ~28 路由 import `db.repositories` | router 经 DI 直接拿 repository 在 handler 里增删改查（如 `folders.py` `repo.create/update/soft_delete`），**无 service 层**——与 `后端架构.md §一`「router 薄层委托 service、不直接操作 DB；service 实例化 repository」不符（实际 CRUD 是 router→repo 四层） | 五层 service 对简单 CRUD 是冗余样板，实务跳过；service 只用于复杂流(stream_chat) | P2 | §一 五层已加注「简单 CRUD 路由允许 DI 直用 repository；service 仅复杂/多-repo/编排流强制」 | ✅ 已闭环（文档承认现实） |
| AUD-007 | 架构 | server/llm | `llm/key_service.py:24`、`llm/byok.py:23` | `llm` import `db.repositories.UserLlmKeyRepository`，违反「llm ⊥ 所有业务模块」 | gateway 需读用户 BYOK 密钥 | P3 | `byok`/`key_service` 定位为凭据桥，§二 与网关本体明确区分 | ✅ 已闭环（边界测试豁免凭据桥） |
| AUD-008 | 健康 | server/api | `api/routes/admin/`（原 `admin.py` 983 行 → 9 文件） | 单路由文件聚合 14 个管理端点，远超同类；可维护性观察项（非缺陷） | 管理端点多且逻辑重，长期单文件累积 | P3 | 按域拆为 `admin/{overview,users,usage,system,audit_logs,conversations,observability}.py` + `_shared.py` + `__init__.py` 聚合；`main.py` 不变；OpenAPI 路径/顺序保持 | ✅ 已拆（56 集成测试通过） |
| AUD-009 | E2E/测试 | server/api | `routes/{folders,sharing,capabilities,search,devices}.py` | 这些 CRUD 路由**无专属集成测试**，端到端无断言（L1 盲区）；其余平台旅程集成覆盖齐 | 测试随主干旅程优先铺，边缘 CRUD 漏 | P3 | 复盘补 happy-path 集成测试（至少增删查各一） | ✅ 已闭环（2026-06-30）：5 路由集成测试齐——folders/sharing/capabilities 先期已补，本次补齐 `test_search_api.py`(8) + `test_devices_api.py`(7)=15 测全绿（含 auth 门 / owner-scope IDOR / 设备 token 归属移交 / 搜索 fan-out·snippet·limit·types） |
| AUD-010 | 健康 | desktop | `renderer/whiteboard/engine.ts`（1512 行，全仓最大单文件）、`components/manual/MechanismContent.tsx`（1234 行） | engine.ts 逻辑密集、单文件 1512 行偏大（whiteboard 为新功能 WIP，已有 `__tests__/{ops,scene,clone,layout,snap,selectionOps}`）；MechanismContent 多为静态讲解 JSX（长但扁平，低风险） | 新功能快速成形期单文件累积 | P3 | WIP 收口后考虑把 engine.ts 按子系统拆（geometry/render/history/ops 已分，engine 仍厚）；已抽 transform.ts + keymap.ts | ⏳ 部分推进（2026-07-01：transform+keymap+gestures 抽出、engine 1650→1424、指针手势测试网 12→22、修 WB-006 多选 scale 死支）·余有状态 FSM（PointerController）待白板收口 |
| AUD-011 | 文档 | desktop | `docs/04-前端/前端技术与架构.md §9.2/9.3/9.6` | 代码指针 `stores/conversation.ts`、`stores/execution.ts` 已重构为目录（实为 `stores/conversation/store.ts`、`stores/execution/{types,frames,project,debate}.ts`），指针失效；同文档其余指针（ipc-contract/fs-service/fileSource/services/sources）均属实 | 文件→目录重构后未回写文档指针 | P3 | 5 处指针回写为目录（含额外 `approval.ts`→`approvals.ts`） | ✅ 已修 |
| AUD-012 | E2E/测试 | mobile+admin | `apps/mobile/**`、`apps/admin/**` | 测试覆盖不对称：mobile 仅靠 `protocol/conformance.run.ts` 跑 fold（39/39✅），**无组件/页面测试**（ChatPage/AssistantView/DebateView 等未测）；admin **零测试**；对比 desktop 595 vitest | admin 属内部工具、mobile 镜像 desktop+共享 fold，测试优先级低（务实取舍） | P3 | 复盘按风险补关键组件测试（mobile 聊天/审批、admin 用户/分析页 happy-path） | ✅ 闭环（2026-06-30）：mobile 半（聊天/审批）15 测 + admin 半（用户/分析页）10 测，见 §十一之四 |

> **META 结论（架构主题）**：AUD-003 / 005 / 006 / 007 是**同一根因**——文档的理想分层蓝图与务实现实**系统性脱节**。`项目结构.md §二` 硬约束表（api⊥runtime/llm/tools、tools⊥一切、core/llm 零向上依赖）+ `后端架构.md §一` 强制五层，描述的是**理想态**；实际代码（务实、测试全绿）走的是 router→repo 直连 CRUD、api/tools 按需耦合 runtime/llm。**这不是「代码错」，是「文档没跟上」**。复盘建议：统一把约束表改写成现实可执行的分层契约（区分叶子工具 vs 编排原语、CRUD vs 编排流），而非逐条改代码迁就过时文档。

### 七之二 功能清单（E2E）

> **证据语义**：L1 = 协议向量（conformance，39/39 ✅）+ 假 provider/确定性单测，证明**管线/投影/逻辑**端到端通；"真模型是否产出好结果"属 **L2**（省钱策略下按关键旅程逐条 deferred，未跑不等于不通）。

| 功能 | 入口→结果 | 证据层 | 证据指针 | 结论 | 关联 |
|---|---|---|---|---|---|
| 单聊（文本/推理流） | 用户发消息 → CEO 流式回复 | L1 | conformance `single_agent_text`；vitest `streamConversation` | ✅ 管线（假模型）；真模型质量待 L2 | — |
| 单聊工具调用 | CEO 调 web_search 等 → 结果回显 | L1 | conformance `single_agent_tool`；`test_web_tools`/`test_grep_tool` | ✅ 同上 | — |
| 引用 citations | 工具结果 → 带来源编号回答 | L1 | conformance `single_agent_citations` | ✅ 协议层 | — |
| 记忆召回 consult_memory | 翻记忆主题 → 全文注入作答 | L1 | conformance `single_agent_consult_memory`；`test_memory_injection` | ✅ 管线 | — |
| 多 Agent 派单 delegate | CEO delegate → 并行子任务 → 合并 | L1 | conformance `multi_agent_delegate`；`delegate/test_execute,nesting,supervised,checkpoint` | ✅ 管线/逻辑；协作质量待 L2 | — |
| 辩论 debate | debate 工具 → 主持人多轮 → 双产物 | L1 | conformance `multi_agent_debate/_followup/roundtable_*`；`test_debate_tool/moderator` | ✅ 管线 | — |
| 升级 escalate | worker 阻塞 → 升级求决策 | L1 | conformance `multi_agent_escalation/blocking_escalate*` | ✅ 管线 | — |
| 审批门 approval | 危险工具 → 暂停审批 → 续跑 | L1 | conformance `approval_paused/_resolved_continue`；`test_sidecar_paused` | ✅ 管线 | — |
| 计划复核 plan_review | 计划 → 暂停复核 → 续跑 | L1 | conformance `plan_review_paused/_resolved_continue` | ✅ 管线 | — |
| ask_user 询问 | CEO 追问 →（非）阻塞 → 回答续跑 | L1 | conformance `single_agent_non_blocking_ask`；`test_ask_user_nonblocking` | ✅ 管线 | — |
| 计划修订 replan | 晚绑定/scope steer → plan_revised | L1 | conformance `multi_agent_plan_revised/lead_subplan_*` | ✅ 管线 | — |
| 队员热修 revise | 对已完成 worker 下达 revise | L1 | conformance `multi_agent_revision`；`test_revise` | ✅ 管线 | — |
| 检查点/恢复 resume | 暂停 → 持久化 → 恢复续跑 | L1 | conformance `single_agent_checkpoint`；`test_resume_*`/`test_runs_session` | ✅ 管线 | — |
| 白板 board_ops | CEO board_ops → 桌面应用 → 收尾 | L1 | conformance `board_ops_applied/_tool_failed`；`test_board_*channel` | ✅ 管线 | — |

> **平台 / CRUD 旅程**（证据 = `tests/integration` 套件，`pytest` 2188 全绿；不需真模型）：

| 功能 | 入口→结果 | 证据层 | 证据指针 | 结论 | 关联 |
|---|---|---|---|---|---|
| 注册/登录/令牌 auth | 注册→登录→刷新→登出 | L1 | `integration/test_auth_api`；`test_csrf_middleware` | ✅ | — |
| BYOK 密钥 llm_key | 用户模型密钥增删改查→网关取用 | L1 | `integration/test_llm_key_api`；`test_byok` | ✅ | AUD-007 |
| 用量/配额/账本 usage | 查用量→扣额→配额拦截 | L1 | `integration/test_quota_api`,`test_cost_ledger`；`test_costing` | ✅ | — |
| 推理代理 inference | 桌面经服务端代理调模型→限频/铸券 | L1 | `test_inference_proxy`,`test_inference_mint_rate_limit` | ✅ | — |
| 限频 rate-limit | 超频→429 | L1 | `integration/test_rate_limit_api`；`test_rate_limit` | ✅ | — |
| 工作区 workspaces | 工作区/会话/锁 CRUD | L1 | `integration/test_workspace_api`；`test_workspace_locks/channel` | ✅ | — |
| 文件附件 files | 上传→附加→工作台读 | L1 | `integration/test_file_assist_api`；`test_workspace_files`,`test_attachment_context` | ✅ | — |
| 用户记忆 memory | 记忆 CRUD→召回注入 | L1 | `integration/test_memory_api`；`test_memory_routes/extractor/applier` | ✅ | — |
| 会话导出 export | 导出会话全文 | L1 | `test_conversation_export` | ✅ | — |
| 站内消息 messaging | 用户间消息/hub 投递 | L1 | `test_messaging_service`,`test_messaging_hub` | ✅ | — |
| 头像 avatars | 上传/取头像 | L1 | `integration/test_avatar_api`；`test_avatars` | ✅ | — |
| 实时附着 realtime | SSE/WS attach 重放+续传 | L1 | `integration/test_realtime_api`；`test_attach_api` | ✅ | — |
| 停止 stop | 中断在跑回合 | L1 | `integration/test_stop_api` | ✅ | — |
| 模型模式 model_modes | 切换模型模式 | L1 | `test_modes` | ✅ | — |
| 管理后台 admin | 用户/审计/统计 14 端点 | L1 | `integration/test_admin_api`,`test_admin_audit_api` | ✅ | AUD-008 |
| 留存清理 retention | 会话/工作区到期清理 | L1 | `integration/test_retention`,`test_turn_journal_cleanup`；`test_workspace_retention` | ✅ | — |
| 文件夹/分享/能力/搜索/设备 | folders/sharing/capabilities/search/devices CRUD | L1 | `integration/test_{folders,sharing,capabilities,search,devices}_api.py` | ✅（2026-06-30 补齐 search/devices，5 路由集成测试齐） | AUD-009 |

### 七之三 区域巡检进度

| # | 区域 | 架构 | 健康 | 文档 | E2E | 小结 |
|---|---|---|---|---|---|---|
| 1 | `server/runtime` | ⚠️ | ✅ | ✅ | ✅L1 | runtime⊥conversation/api 成立；无超大模块（≤646）、绊线关键词=设计内降级阶梯（误报）；§三 子包声明全部属实（无漂移）；14 功能 L1 证据齐；api 越层见 AUD-003 |
| 2 | `server` 平台 | ⚠️ | ✅ | ⚠️ | ⚠️L1 | 架构→META 结论(AUD-003/005/006/007)；健康好：真实 HACK/FIXME/workaround=0、TODO 仅 2 处(pricing 标 Phase2)、broad-except 集中于网络/持久化/后台(合理)，唯 admin.py 897 行=P3(AUD-008)；E2E：16 平台旅程集成覆盖✅，folders/sharing 等 5 路由 L1 盲区(AUD-009) |
| 3 | `server` 横切 | ✅ | ✅ | ✅ | ✅L1 | 横切底层(middleware/cache/push/sidecar/vision/messaging/workspace)**零向上依赖** api/conversation；vision 新模块=干净 Protocol 端口+独立计费+诚实「未配置」降级；evals 零-LLM 门 41 用例全绿、`后端架构.md §五` 表述准确；gold labels 仅 .example(设计内) |
| 4 | `apps/desktop` | ✅ | ✅ | ✅ | ✅L1 | renderer 分层标准(components/services/stores/pages/lib/protocol)、**Electron 边界严守**(renderer 零直连 node/electron/fs)、conformance fold 39/39 单源；健康:真实绊线=0，唯 `whiteboard/engine.ts` 1512 行(AUD-010,WIP)；`前端技术与架构.md` 质量高、仅 store 指针小漂移(AUD-011)；E2E:vitest 595 + `#/preview`+`shoot` CI 渲染门 |
| 5 | `apps/mobile` | ✅ | ✅ | ✅ | ⚠️L1 | web SPA **零 electron/node**、镜像 desktop 分层、fold 单源；0 绊线，ChatPage 802 中等；文档 `§7` 覆盖（黑名单减法）；E2E：fold conformance✅；聊天/审批组件测试已补(AUD-012✅ §十一之四) |
| 6 | `apps/admin` | ✅ | ✅ | — | ⚠️ | 标准 React dashboard；0 绊线，ConversationsPage 734 中等；E2E：用户/分析页 happy-path 已补(AUD-012✅ §十一之四) |
| 7 | `packages/` | ✅ | ✅ | ✅ | ✅ | 4 包(contract-types/contract-rest-types/protocol-conformance/design-tokens)**纯契约、零反向依赖** apps；生成文件 11515 不计、events.ts 786=事件 schema 单源；conformance 39/39 单源 |
| 8 | `website`/`promo` | ✅ | ✅ | — | — | Next.js 营销站；0 绊线，page.tsx 687 为扁平 JSX 文案；轻扫无异常 |

---

## 八、执行节奏与产出

1. **基线**：跑自动信号（lint/type/test/conformance）+ 现成自动 E2E（integration/evals/向量），收一份客观底，结果摘要入 §七。
2. **广度扫描**（区域 1→8）：每区过 A/B/C 三镜头登记问题入 §七之一；同时把该区功能登进 §七之二、跑现成证据、关键旅程 L2 实跑。
3. **阶段复盘**：登记册 + 功能清单成形后，按严重度 + 区域汇总，给「优先级 backlog」（含 E2E 失败/盲区）。
4. **深挖**：经人确认，选 top 风险 + E2E 盲区逐条深挖（可能跨区追链、补关键旅程实跑）。
5. **修复**：不属本轮；每项各自按 `dev-process` 立项，契约级改动先提案再改。

**产出物**：本文（计划 + §七之一 登记册 + §七之二 功能清单）；阶段末一份「优先级 backlog + 风险摘要 + E2E 可用性概览」。

**人决策节点**：① 计划过目放行 → 开扫；② 登记册/功能清单成形后定深挖与补验优先级；③ 是否就地转入修复。

---

## 九、阶段复盘（广度优先 pass 完成 · 2026-06-29）

> 8/8 区域全部过 4 镜头（架构/健康/文档/E2E）。本节是 §八 第 3 步的综合产出。

### 九之一 总体结论

**项目工程质量健康、无 P0/P1。** 12 条登记、**零严重/高危缺陷**。广度优先 pass 即时分布为 1 已修 / 3 P2 / 8 P3；经 §十 决策执行后，截至收尾 8 条已闭环、4 条 P3 确认 deferred 各自立项（详见 §十之五、§十一）。**结案后跟进（2026-06-30）：AUD-009 + AUD-012 补测闭环、AUD-002 风格债清零 → 现 11 闭环 / 1 deferred（010）。**

- **基线全绿**：`ruff`(F821 已修)·`tsc`·conformance 39/39·`vitest` 595·`pytest` 2188·evals 零-LLM 41。
- **代码卫生罕见地好**：**全 monorepo 真实绊线(HACK/FIXME/workaround)=0**（仅 2 处有范围的 `TODO(Phase 2 定价)`）；broad-except 集中在网络/持久化/后台（容错合理）。
- **边界多数干净**：Electron renderer 零直连 node/electron；`packages` 纯契约零反向依赖；mobile 纯 web-SPA；后端横切模块零向上依赖 api/conversation。

### 九之二 严重度分布

| 严重度 | 数 | 条目 | 收尾现状 |
|---|---|---|---|
| P0/P1 | **0** | — | — |
| P2 | 4 | AUD-001 / 003 / 005 / 006 | ✅ 全部已闭环（001 补导入；003/005/006 同 META，§十之五 落地 + 边界测试护栏） |
| P3 | 8 | AUD-002/004/007/008/009/010/011/012 | ✅ 7 闭环（002/004/007/008/009/011/012）；⏳ 1 deferred 立项（010）（AUD-009/012 补测、AUD-002 风格债于 2026-06-30 闭环） |

### 九之三 优先级 backlog（按主题聚类·收尾状态）

**① 架构契约对齐（原唯一 P2 主题）— ✅ 已落地** — AUD-003/005/006(+004/007/011 同源)
> 文档理想分层蓝图 vs 务实现实系统性脱节，且 `项目结构.md §二` 与 `后端架构.md §三` 两处**自相矛盾**。已按方案 A（对齐契约文档 + 真侵蚀处局部改代码 + 边界测试护栏）执行完毕，**一次决策清 6 条**（003/004/005/006/007/011，详见 §十之五）。

**② 测试覆盖补强（P3）** — AUD-009 ✅ 闭环 / AUD-012 ✅ 闭环
> CRUD 路由(folders/sharing/...) + mobile 组件 + admin(零测试) 缺端到端断言。**AUD-009（5 条 CRUD 路由集成测试）+ AUD-012（mobile 聊天/审批 + admin 用户/分析页 happy-path）均已于 2026-06-30 补齐闭环**（详见 §十一之四）。

**③ 工程杂项（P3）** — AUD-002(风格债·✅)/004(core→config·✅)/008(admin.py 拆分·✅)/010(whiteboard engine 1512 行 WIP·deferred)
> 002/004/008 已闭环；仅 010 deferred（详见 §十一）。

### 九之四 风险摘要

- **最大风险其实是「文档可信度」而非「代码缺陷」**：新人/AI 按 `项目结构.md §二` 硬约束开发，会与现实代码冲突、误判为 bug 去"修"——这正是本审计前期踩的坑（AUD-003 一度按违规处理）。**优先级最高的动作是对齐契约文档**。
- 真实模型质量（L2/L3）按省钱策略**未实跑**：所有 ✅ 均为 L1（管线/协议/逻辑层），"真模型产出好不好"仍是已知盲区（评估体系 nightly 主线，非本轮）。

### 九之五 E2E 可用性概览

| 层 | 覆盖 | 状态 |
|---|---|---|
| L1 协议/管线 | conformance 39/39 + 后端 integration(2188) + 桌面 vitest 595 + evals 零-LLM 41 | ✅ 充分 |
| L1 盲区 | mobile 组件（聊天/审批已补 15 测 ✅，余页面 deferred）、admin（CRUD 路由 folders/sharing/capabilities/search/devices 集成测试 ✅；用户/分析页组件 happy-path 已补 10 测 ✅） | ✅ AUD-012/009 已闭环（余 mobile 次要页面 deferred） |
| L2 真模型质量 | 关键旅程真跑（省钱策略 deferred） | ⏳ 未跑（已知） |

### 九之六 建议下一步（人决策）

1. ✅ **META 架构决策已拍板并落地**：选方案 A 对齐契约文档 + 真侵蚀局部改代码 + 边界测试护栏（§十之五，清 6 条）。
2. 选 top 风险**深挖**或就地**补关键测试**（AUD-009/012，本轮已划出范围、需立项）。
3. 工程杂项：AUD-002 风格债已清零（2026-06-30）；仅余 AUD-010 候白板 WIP 收口后拆。下一轮可开安全/性能/产品完整度专项（§一 已排除项）。

---

## 十、决策提案：架构契约对齐（AUD-003/005/006/007，含 AUD-004/011）

> `dev-process`：架构选型属「AI 提案 → 人确认」。本节原为提案；**已拍板选方案 A 并落地**（执行记录见 §十之五，终态见 §十一）。以下保留提案原文供追溯。

### 十之一 问题本质（坐实的矛盾）

| 文档 | 原文 | 与现实 |
|---|---|---|
| `项目结构.md §二` | `tools/` 不依赖**所有其他业务模块** | ❌ tools/builtin 19 文件依赖 runtime/llm（AUD-005） |
| `后端架构.md §三:109` | CEO 编排原语（delegate/debate/ask_user）**就放在 `tools/builtin/`、与 runtime 同构** | ✅ 现实如此 |

→ **两文档自相矛盾**：§二 要 tools 零依赖，§三 又把"必然依赖 runtime 的编排原语"安在 tools 里。无论选 A/B，这处矛盾都**必须**消解。

其余同源：`api⊥runtime/llm/tools`（AUD-003）、五层强制 service（AUD-006，实际 CRUD 走 router→repo）、`llm⊥db`（AUD-007，gateway 读 BYOK 密钥）、`core⊥config`（AUD-004）。

**关键参照**：`后端架构.md §四` 已点明真正要守的不变量是 **「核心 ⊥ 垂直插件」**（白名单空时平台核心独立启动/测试/发布）——这条**现实已满足**。§二 的细粒度模块表是比它更严、且未被执行的"理想约束"。

### 十之二 方案 A — 对齐文档到现实（重写约束契约）✅ 建议

代码务实、2188 测试全绿、§四 真不变量已守 → **改文档，不改代码**：

1. **§二 拆 `tools` 行**：`叶子工具(web/file_ops/grep/code_execute…) ⊥ runtime/llm`；`编排原语(delegate/debate/ask_user/escalate/revise…) 允许依赖 runtime/llm`（与 §三:109 对齐）→ 清 AUD-005 + 消矛盾。
2. **§二 改 `api` 行**：`api 路由可依赖 runtime 的类型/事件/投影 与 tools.catalog；编排执行经 conversation.service 收敛` → 清 AUD-003。
3. **§一 五层加注**：`简单 CRUD 路由允许经 DI 直用 repository；service 层仅复杂/多-repo/编排流强制`（承认 router→repo 四层）→ 清 AUD-006。
4. **§二 改 `llm` 行**：`llm gateway 可读 db 的 BYOK 密钥仓储`（或注入）→ 清 AUD-007；**`core` 行加注 `config 为 core 之下的地基`** → 清 AUD-004。
5. **加护栏（关键，防再漂移）**：把对齐后的约束写成**可执行的 import 边界测试**（import-linter / 单测扫依赖），让契约由 prose 变门禁。
6. AUD-011（前端 3 处 store 指针）顺手回写——与 A/B 无关、纯文档修。

- **成本**：纯文档 + 1 个边界测试，零代码改动、零回归风险。**收益**：清 4-5 条 + 消矛盾 + 防未来漂移。

### 十之三 方案 B — 强制代码补层（让代码贴合理想文档）

1. AUD-003：所有 api→runtime 经 `conversation.service` 收敛、路由去耦合。
2. AUD-005：要让"tools⊥一切"字面成立，须把 delegate/debate/ask_user **移出 `tools/` 进 `runtime/`**——**但这与 §三:109 自身设计冲突**，故 B 对此条**不自洽**（仍得改 §三）。
3. AUD-006：给 28 个 CRUD 路由各补 service 层（大量样板）。
4. AUD-007：把 `UserLlmKeyRepository` 注入 llm gateway。

- **成本**：跨 15+ 路由 + 28 CRUD + 搬迁编排原语，高 churn、高回归风险，且**仍逃不掉改 §三**。**收益**：贴合理想文档（纯洁性）。

### 十之四 建议

**选 A**（或 A 为主的混合）：代码已务实正确，B 是"为纯洁性付大改造 + 回归风险"且对 AUD-005 不自洽。若某条路由确有"重逻辑塞在 router"的真异味，再就该条局部应用 B 的精神（最小化）。**A 的护栏（边界测试）是让契约长期可信的关键**。

### 十之五 执行记录（偏 B 的分类 A，已落地）

按「A 默认 + 真侵蚀局部 B + 边界测试」执行，结果如下：

- **局部 B（真侵蚀，改代码）**
  1. `api/routes/files.py`：AI 改写逻辑里**在 handler 内 `build_provider` 并直调 provider** → 下沉到 `assist/rewrite.py:rewrite_selection_for_user`（含凭据解析 + provider 构建）。路由回归薄委托，恢复 `api ⊥ llm(执行)`。✅ 14 测试通过。
  2. `api/routes/favicon.py`：曾 import `tools.builtin.web` 的私有 `_is_safe_url` + `_net`（api→tools 越层 + 复用私有符号）。新建 **`core/net.py`** 承载**无状态共享 net/SSRF 原语**（`web_timeout`/`describe_net_error`/`site_of`/`classify_url`/`is_safe_url`）；`tools/builtin/web/_net.py` 仅保留有状态熔断器并 re-export；`read_url.py` 保留本地 `_is_safe_url` 包装器（保 `test_web_tools` 的 monkeypatch 语义）。favicon 改为只依赖 `core.net`，api→tools 边解除，且 favicon 与 read_url **共享同一份 SSRF 守卫**（无重复/漂移）。✅ 119 测试通过。

- **分类 A（良性耦合，改文档承认现实）**：`项目结构.md §二` 约束表重写（api 行=不构建 provider/不驱动 pipeline；llm 网关本体 db-free + 凭据服务 `byok`/`key_service` 为凭据桥豁免；tools 拆叶子/编排原语；core 可依赖 config）；`后端架构.md §一` 五层加注（CRUD 路由可 DI 直用 repository）。清 AUD-003(良性部分)/004/005/006/007。

- **护栏（边界测试）**：新增 `tests/test_arch_boundaries.py`（AST 扫真实 import），4 条断言把上述契约变为**可执行**：① 路由不 import `llm.factory`/`runtime.pipeline|engine`；② llm 网关本体（除 byok/key_service）不 import `db`；③ core 零向上业务依赖；④ web 叶子工具不 import runtime/llm。✅ 4 通过——文档↔测试↔代码三者现已一致，下次漂移即红。

- **顺手**：AUD-011 前端 5 处 store 指针回写为目录（额外修正 `approval.ts`→`approvals.ts`）。

> 剩余 P3（独立排期·确认 deferred，处置见 §十一）：AUD-010 `whiteboard/engine.ts` 1512 行拆分。（~~AUD-002 ruff 风格债~~ ✅、~~AUD-009 CRUD 路由补测~~ ✅、~~AUD-012 mobile/admin 组件测试~~ ✅、AUD-008 admin 拆分 ✅ 均已完成）

---

## 十一、结案（收尾 · 2026-06-29）

> 首轮全栈健康体检收尾。广度优先 pass（§九）+ 架构契约对齐的决策与执行（§十）均已完成；本节给最终状态与剩余项处置，作为本轮**终态结论**。

### 十一之一 最终结论

**首轮审计完成，项目工程质量健康——零 P0/P1；12 条登记中 8 条已闭环、4 条 P3 确认 deferred 并各自立项。结案后跟进（2026-06-30）：AUD-009 + AUD-012（组件测试两半）补测闭环、AUD-002 风格债清零 → 现 11 闭环 / 1 deferred（010）。**

- **唯一 P2 主题已清**：文档理想分层蓝图 vs 务实现实系统性脱节（且 `项目结构.md §二` 与 `后端架构.md §三` 自相矛盾），已按方案 A 对齐契约文档 + 真侵蚀处局部改代码 + 新增**可执行边界测试**（`apps/server/tests/test_arch_boundaries.py`），**文档 ↔ 测试 ↔ 代码三者一致，再漂移即红**（§十之五）。
- **基线全绿**（见 §七之零 基线快照）：conformance 39/39、pytest 2188、vitest 595、tsc 0、evals 零-LLM 41；F821 已清零。
- **已知盲区诚实保留**：L2 真模型质量按省钱策略 deferred（属评估体系 nightly 主线，非本轮）；§一 列出的安全深挖 / 性能 profiling / 产品完整度矩阵不在首轮范围。

### 十一之二 剩余项处置（结案时 4 条 → 现 1 条 P3·deferred·各自立项）

> 均落在 §一「不在首轮范围」内（本轮不写新测试；风格债 / 拆分各自立项），故终态为**确认 deferred**，非"未完成"。
> **结案后跟进（2026-06-30）：AUD-009（CRUD 路由集成测试）+ AUD-012（组件测试两半）补测闭环、AUD-002（ruff 风格债清零）已闭环、移出本表（详见 §十一之四），剩 1 条（仅 AUD-010）。**

| ID | 维度 | 一句话 | 处置 |
|---|---|---|---|
| AUD-010 | 健康 | `whiteboard/engine.ts`（全仓最大单文件，WIP） | 部分推进（2026-07-01）：抽 `transform.ts`（纯几何）+ `keymap.ts`（快捷键策略）+ `gestures.ts`（拖拽橡皮筋/45°）、补指针手势测试网（engine.test 12→22，含多选 scale）+ 修 WB-006 多选 scale 死支；engine 1650→1424。余有状态交互 FSM（PointerController）待白板收口再拆 |

### 十一之三 下一步（人决策）

1. ~~**补测专项**~~ ✅ 全闭环（2026-06-30）：~~AUD-009~~ ✅ ＋ **AUD-012** ~~mobile 半（聊天/审批）~~ ✅ ＋ ~~admin 半（用户/分析页）~~ ✅ —— 补测专项收口，无剩余续单。
2. ~~**工程杂项清理**（AUD-002）~~ ✅ 风格债清零（2026-06-30）；仅余 **AUD-010** 候白板 WIP 收口后再拆。
3. **下一轮专项审计**：首轮排除的安全深挖 / 成本性能 profiling / 产品完整度矩阵，按价值另立轮。（**成本/性能 profiling、产品完整度矩阵（含创作工具 / 产物体系 + 白板深度两条延伸轮）、评估体系就绪度 / L2 真模型质量均已另立轮并闭环**，详见 §十一之五。）

### 十一之四 结案后跟进（2026-06-30）

> 结案为 2026-06-29；本节记录结案后按 `dev-process` 单点立项落地的跟进，避免改写 §九/§十一之一-三 的当日快照。

- **AUD-009 闭环（补测专项首单）**：为 `search` + `devices` 两条路由补 happy-path 集成测试（folders/sharing/capabilities 先期已由并行工作补齐），AUD-009 五路由集成覆盖至此齐备、由 L1 盲区转 ✅。
  - 新增 `apps/server/tests/integration/test_devices_api.py`（7 测：auth 门 / 注册→列表「token 不回显」/ 幂等 upsert / platform 闭集校验 / 幂等注销 / **token 跨用户归属移交** / IDOR 隔离）。
  - 新增 `apps/server/tests/integration/test_search_api.py`（8 测：auth 门 / 缺 `q`→422 / 三类实体 fan-out / message snippet 高亮偏移 / `types` 过滤 / 空段省略 / 每段 `limit` 上限 / **owner-scope IDOR**）。纯逻辑（snippet/types/ILIKE 转义）仍由 `tests/test_search.py` 单测守，集成测试不重复。
  - 验证：`ruff` 清、新增 15 测真连 PostgreSQL 全绿。纯加测试、零生产代码改动、零契约变化。

- **AUD-012 mobile 半闭环（补测专项次单）**：mobile 此前仅有 `protocol/conformance.run.ts` 跑 fold、无组件测试；本次为 mobile 立 Vitest 组件测试基建并补「聊天 + 审批」两关键交互件 happy-path，AUD-012 的 mobile 半（聊天/审批）至此覆盖、admin 半（用户/分析页）见次条。
  - 新增基建：`apps/mobile/vitest.config.ts`（jsdom 经测试头 `// @vitest-environment jsdom` 按需开）+ `package.json` `test` 脚本 + devDeps（`vitest`/`@testing-library/react`/`jsdom`），与 desktop 既有 harness 对齐。
  - 新增 `apps/mobile/src/components/__tests__/PauseCard.test.tsx`（9 测：approval/checkpoint/plan_review 三态渲染 / 批准·驳回提交 `resolveInteraction` / 反悔文案门控 / 提交失败回滚）。
  - 新增 `apps/mobile/src/components/__tests__/AssistantContent.test.tsx`（6 测：纯聊天走 Markdown / 来源引用列表 / **收到的上下文隐藏逐字系统提示**（决策②）/ 辩论叠加 / 内联工具步中文标签·状态 / 多 Agent 团队图）；重叶子（Markdown/DebateView/TeamView）以 stub 替身，聚焦 AssistantContent 自身分支组合。
  - 验证：`vitest` 15/15 绿、`tsc --noEmit` 0、新增两件 `biome` 清（其余 biome 漂移属并行 WIP 文件、非本次引入，未动）。纯加测试 + 基建、零生产代码改动、零契约变化。

- **AUD-012 admin 半闭环（补测专项收尾单）**：admin 控制台此前零测试；复用并行工作已搭好的 Vitest 基建（`vitest.config.ts` + `test` 脚本 + RTL/jsdom devDeps），补「用户页 + 分析页」两关键页 happy-path，AUD-012 至此 mobile+admin 两半全覆盖、补测专项收口。
  - 新增 `apps/admin/src/pages/__tests__/UsersPage.test.tsx`（5 测：花名册渲染（自标记/计数）/ **本人行禁操作护栏**（角色选择 + 停用 disabled）/ 停用他人→`updateUser`＋toast＋行翻「启用」/ 空态 / 加载错误→「重试」恢复）；`UserDetail`/`QuotaDialog` 以 stub 替身隔离。
  - 新增 `apps/admin/src/pages/__tests__/AnalyticsPage.test.tsx`（5 测：成本镜窗口合计＋Top 花费＋趋势 / BYOK 计费口径 / 切「健康」镜按需拉 observability（仅活动镜发请求）/ 错误行下钻会话复盘 / 会话 ID 表单进复盘）；趋势图（charts）以 stub 替身、聚焦页面自身布局与分镜。
  - 验证：`vitest` 10/10 绿、`tsc --noEmit` 0（admin 无 biome 门，tsc + 测试即其质量门）。纯加测试、零生产代码改动、零契约变化。

- **AUD-002 闭环（工程杂项·风格债清零）**：`apps/server` `ruff check` 由 88 违规清至 **0**（首轮 174 → `--fix` 清 22 → 本次清余 88），分两类处置：
  - **真代码债**（逐文件 StrReplace 手改、Read 自检、禁脚本批改）：E501×26（报告/日志 f-string 拆行、注释/docstring 回流；长 worker 提示串用 `\` 续行或相邻字面量拼接**逐字保内容**）+ B905×3（`zip(..., strict=True)` 显式声明等长不变式）+ SIM108×1（if/else→三元）+ F401×1（删未用 `delete` 导入）+ I001×2（`--select I001 --fix` 排序）。
  - **LLM 内容模块豁免**：`runtime/legal_skills.py` 的 E501×55 全是「答辩状作战室 / 三方研判」Skill 正文（中文字符串字面量），与既有 `skills.py`/`planner.py`/prompt 模块同性质——按先例加 `per-file-ignores` 豁免 E501（回流会改 CEO consult 到的内容，故豁免而非裁行）。
  - 验证：`ruff check` All checks passed（exit 0）；改动模块定向单测 322 绿（identities/notewall/skills/playbooks/evals 提示词快照等）+ 边路模块 import/编译冒烟。零行为变化、零契约变化。

### 十一之五 · 专项轮次（结案后另立轮）

> 首轮（§一–十一之四）为全栈健康体检；§一「不在首轮范围」排除的「安全深挖 / 成本性能 / 产品完整度」等方向，结案后各自另立轮、有独立文档，闭环结论回登于此（不改写 §九 / §十一之一–三 的当日快照）。**下列为审计全系列总览**，按主题分组。
>
> **专项稿退役（2026-07-01 · 折中收敛）**：10 个专项工作稿已退役删除——**持久安全不变量**沉淀进 [`05-平台与运维/安全权限与治理.md` §十](/docs/05-平台与运维/安全权限与治理.md)、**仍 OPEN 的 deferred / 独立项**汇入 [`远期规划.md` §3.3](/docs/07-规划/远期规划.md)（评估 / 沙箱并入 §2.4 / §2.1）、**逐轮结论**留存本节作单一存档。代码内 `(项目审计-XXX专项 …)` 出处引用即指向本节对应条目（全程见 git）。
>
> **轮次编号小注**：各专项标题的「第 N 轮」按其立轮时各自承接链计数，存在并行轮次（`成本性能专项` 与 `创作工具产物体系专项` 均标「第七轮」）；本总览以**主题分组 + 结案时间**为准，不以线性编号为唯一锚。

#### 安全深挖系列（承 §一「安全深挖」排除项；第二–五轮链式 + 动态红队收尾）

> 结案后按价值链式立轮（每轮承上轮「后续专项」决策），**全部已闭环、零未决 P0/P1/P2**；本系列的**动态红队 PoC 收尾** ＝ 下方「其余专项轮次」中的「前端安全红队专项」。

- **安全专项（第二轮）✅ 闭环** — 原专项稿（已退役）`项目审计-安全专项.md`。7 维静态安全审计（密钥机密 / 认证 / IDOR / 注入 / 沙箱 / SSRF / 限频）＋ 依赖供应链扫描 ＋ 动态回归验证。皇冠明珠（密钥泄漏 S1 / 多租隔离 S3）均评「强」；登记 8 条（3×P2 全修：CSRF bearer 豁免接缝 / cloud `code_execute` 无隔离 / git clone SSRF；5×P3：3 修 2 另立项），含 20 条回归测试。依赖漏洞双端归零（JS 10〔全 dev 工具链〕＋ Python 1 升级修复）；安全控制真跑全绿（单元 248 ＋ 集成 38）。拆出 3 独立项：真沙箱（gVisor/nsjail）/ pinned-IP transport（DNS rebinding）/ 仓储 owner 作用域结构化。
- **提示注入专项（第三轮）✅ 闭环** — 原专项稿（已退役）`项目审计-提示注入专项.md`。6 维间接提示注入审计，**出全审计第一条 P1**（PI-001 渲染侧零点击外泄信标）。登记 6 条（1×P1 ＋ 3×P2 ＋ 2×P3），6/6 已修：确定性半边收口（markdown 图降级点击外链 / `code_execute` 回合内逐次不搭乘授权 / `read_url` 新域名＋长查询观测告警）＋ 共享 base 提示 `<untrusted_content>` 把工具 / 网页 / 文件 / 记忆 / 跨 Agent 文本框定为「数据非指令」＋ 记忆固化候选确定性「指令样式」过滤。注入根治属开放难题，本轮为工程化缓解。
- **前端 XSS 专项（第四轮）✅ 闭环** — 原专项稿（已退役）`项目审计-前端XSS专项.md`。3 端（desktop / mobile / admin）HTML 注入面审计。渲染 sink 本已护住（react-markdown 三端无 `rehype-raw` ＋ 默认 urlTransform ＋ mermaid `strict` ＋ KaTeX `trust:false` ＋ PI-001 图片降级）；登记 4 条全 P3、4/4 已修，本轮是 **Electron 进程硬化 ＋ CSP 纵深**：三端全量 `script-src 'self'`（无 unsafe-eval，实测 mermaid 走 dynamic-import chunk 不需 eval）＋ `sandbox:true` ＋ will-navigate 守卫 ＋ openExternal scheme 白名单（单源 `isSafeExternalUrl`），＋28 单测红队 payload 全谱。
- **IPC 权限面专项（第五轮）✅ 闭环** — 原专项稿（已退役）`项目审计-IPC权限面专项.md`。5 维 preload / IPC 权限面审计，命题「renderer 被攻破后能干什么」。文件路径边界一流（根显式授权 ＋ `resolveLexical`/`realInside` 双守卫，与服务端 `resolve_safe_path` 同源，无穿越）；登记 4 条（1×P2 ＋ 3×P3），4/4 已修：执行面缺口（`execute` op / `openPath` 可执行类型 ＝ 宿主 RCE 两头、主侧零门控）经人拍板补 **主侧 native 确认门**（`fs/execGate.ts`，renderer 无法伪造）＋ 删未用通用桥 ＋ 补 IPC 缝入参校验层。

#### 其余专项轮次（成本性能 / 产品 & 产物完整度 / 评估 + 动态红队，按结案时间）

- **成本/性能 profiling 专项 ✅ 闭环（2026-06-30）** — 原专项稿（已退役）`项目审计-成本性能专项.md`。三向静态扫描（LLM 成本面 / DB 查询面 / 编排热路径）+ 主审亲核，**结论：架构「健康且克制」、零 P0/P1**（全链路 async、扇出有界、提示走前缀缓存友好布局、journal 读批量无 N+1、长对话有压缩）；登记 11 条，全为规模化前该补的优化 / 防御纵深。人逐项确认后落地，**P0/P1/P2 全清**：
  - **PERF-001** ✅ `messages` 复合索引 `(conversation_id, created_at)`（迁移 `c6a3e9f1b4d8`）+ 删冗余单列（`d9b2f5a1c7e4`）；托底全 App 最高频「打开对话」读，零契约零回归。
  - **COST-001** ✅ 注入记忆读侧确定性 cap（默认 4000 chars/文件）：拦异常膨胀、确定性截断保 DeepSeek 前缀缓存。
  - **PERF-003** ✅ 消息列表 `runs` 投影按 journal 版本读时缓存（新 `runtime/journal/fold_cache.py`）：纯服务端、不改契约、不伤 UX（`runs` 仍随列表返回）。
  - **COST-004** 🟡 提示预算闸先「仅观测」（埋 `cost.prompt_assembled`），A 级「软闸·仅裁易变尾」待真实负载数据再开。
  - 余 8 条 P3 deferred：待真 profiler（慢查询日志 / py-spy 火焰图 / 用量报表）量化再排序——**开发期无真实数据、不凭猜优化**（`dev-process` 纪律）。

- **前端安全红队专项 ✅ 闭环（2026-06-30）** — 原专项稿（已退役）`项目审计-前端安全红队专项.md`。动态红队（本地信标 oracle + 无头浏览器 PoC）坐实桌面端两条链路 **6 个洞，全部已修并验证**：① **渲染期零点击外泄信标**（RB-1 vega `data.url` / RB-2 mermaid `<img>` / RB-3 markmap markdown 图片）——**DOMPurify 不防此类**（只挡脚本不挡取资源），故 mermaid `strict` 也中招；主修在浏览器层**收紧 CSP `img-src 'self' data: https:` → `'self' data: <后端源>`**（构建期经 `electron.vite.config.ts` 注入后端地址），任意第三方远程图被网络层拦死、覆盖现有与未来引擎，vega 另加 `makeSafeLoader` 兜 fetch 取数。② **IPC 危险动作门**（GATE-1 `openPath` 黑名单缺口 + GATE-2 Windows 文件名末尾点/空格归一化绕过 + GATE-3 `execute` 确认框隐藏 `stdin`）——开文件门**黑名单翻白名单 + 文件名归一化**一招同堵 1/2，确认框补全「全部影响执行的输入」。验证：`execGate` 单测 11/11、`tsc` 0、生产 `build` 产物 CSP 注入值正确、PoC 修复前/后由「出网 200」转「被 CSP 拦」。剩余 `connect-src` 收紧为低优先纵深项（需先有 XSS 才能发、且 `script-src 'self'` 已拦），deferred。

- **产品完整度矩阵专项 ✅ 闭环（2026-07-01）** — 原专项稿（已退役）`项目审计-产品完整度专项.md`。从**产品视角**对 MVP 6 模块（聊天 / 多Agent / 角色分配 / 工具 / 进度 / 记忆）做「应有能力 → 实际实现」正向推导矩阵 + 关键链路代码核验，**结论：6 模块 Day-1 核心能力悉数落地、有 L1 证据、零 P0/P1**；登记 5 条全 P3 均收口——**未发现「实现冒充需求 / 半成品冒充完整」**（抽查 `delegate` 真带角色/工具集/模型档、记忆自动抽取真接线、MCP 确属延后而非偷偷缺席）。所谓「不完整」几乎全是文档主动标注的 ⏳/Day2/延后（MCP / pgvector / 能力域 / 市场 Skill / 跨 Agent 记忆 / 运行时上下文工程，代码核验属实）。终态：PROD-001（辩论前端统一辩论室）**结案复核坐实已由并行开发落地**（`DebateStream.tsx` 唯一主视图、旧范式退场、纯渲染层不动协议）、PROD-002/005 ✅、PROD-003（创作工具占位）/ PROD-004（L2 真模型盲区）确认 deferred。

- **创作工具 / 产物体系完整度专项 ✅ 闭环（2026-07-01）** — 原专项稿（已退役）`项目审计-创作工具产物体系专项.md`。深挖产品完整度专项 §八之四 / PROD-003 划出的「创作工具 / 产物体系」（非 MVP-6）：对 `工具与能力系统.md §三/§3.4`「✅ 已确定」的产物体系设计做「设计应有能力 → as-built」正向推导，**结论：设计已定、实现克制、占位诚实、零 P0/P1/P2**——**白板（canvas）是唯一全链路落地的创作产物**（用户编辑器 `/whiteboard` + `board_ops` Agent 共编 + `Board.scene` 类型化持久化，正中「两副面孔」、非占位非冒充）；表达形态层（mermaid/markmap/vega）✅ view-only；文档 / 可运行产物在受控能力层有实质；多维表格 / 幻灯片 / 表单 + 集成连接器 / 工作流为透明「即将开放」占位（后端零实现）。登记 5 条全 P3：ART-001（指针 §8.4→§3.4·已修）/ ART-003（「文档」磁贴文案对齐 Markdown 典范·已修）/ ART-002（透明占位·deferred）/ ART-004（表达形态 vs 创作产物同名异物·**✅ 已修 2026-07-01**：`工具与能力系统.md §3.1` 铁律后增「同名异物」澄清注，点破「导图/mind map 既是 `markmap` 只读表达形态、又是可编辑导图文档创作产物」，归类只认「通道 + 是否留可编辑产物」判据、不认形态名）/ ART-005（L2 盲区·deferred）。

- **AI 协作白板深度完整度专项 ✅ 闭环（2026-07-01）** — 原专项稿（已退役）`项目审计-AI协作白板专项.md`。承上轮把白板钉为「皇冠明珠 / 体系活样板」后的**单产物纵深复核**：以 `AI协作白板.md`（设计=as-built 说明书）为单一事实源，对 M1 引擎 / M2（结构化 ops + 读图）/ M3（贴源·crystallize·迭代）/ 数据契约 / 视觉计费 逐接缝「设计应有 → as-built → L1 证据」正向推导 + **实跑 147 个 L1 测试全绿**（后端 46 pytest + 前端 101 vitest）。**结论：白板是名副其实的标杆——全栈真实落地、零半成品冒充、零实现冒充需求、零 P0/P1/P2**；`board_ops` / `board_read`(vision) / `BoardChannel` 双路 / M3 投影 / `role=vision` 计费链 / `/v1/boards` 全 CRUD（IDOR 安全 + CAS 不覆盖）逐条核验属实非 stub；设计文档本身是模范「设计=as-built 诚实说明书」（✅ 都兑现、⏳ 读图实跑唯一缺口且无 key 时干净降级不冒充）。登记 7 条全 P3：WB-001（2026-06-27 引擎反转后 13 处描述当前画布的注释/类型仍写「Excalidraw」·含 `boards.py` 错描 scene 形状·**已修**）/ WB-002（读图端到端实跑·人选视觉模型+配 `VISION_API_KEY`·deferred）/ WB-003（M3 v1 干净接缝·待后端文件信号·deferred）/ WB-004（实时协作·远期留缝不预建·deferred）/ WB-005（引擎状态机测试姿态·✅ 已覆盖 2026-07-01：`engine.test.ts` 22 集成测试含 9 类指针手势 + `keymap.test.ts` 11）/ WB-006（多选 scale 手柄绘制却不可点·hit-test 接缝漂移致 scale 分支死代码·✅ 已修 2026-07-01：handleAt 归一到渲染同源 `selectionHandlesScreen`）/ WB-007（rotation「已铺管线·零产出」接缝·2026-07-01 排查：render/hit-test/persist 全认 `rotation`，但无任何 producer——无旋转工具·手势·手柄、无工具栏/样式/菜单动作、AI `board_ops` 闭合动词集也不含，仅手改持久化 JSON 可置；因**不画假控件**故非 WB-006 式「可见却坏」bug，属**前瞻预留能力·无产出**·**已标记 reserved**（`types.ts` rotation 注记）·MVP 未暴露·待交互 FSM/PointerController 收口再补旋转手柄+`rotate` 态±board_op·deferred）。

- **评估体系就绪度 / L2 真模型质量专项 🗂️ 结案（2026-07-01）** — 原专项稿（已退役）`项目审计-评估体系就绪度专项.md`。收口前三轮（产品完整度 PROD-004 / 创作工具 ART-005 / 白板 WB-002）反复 deferred 的同一根线「L2 真模型质量 / 评估体系就绪度」：以 `后端架构.md §五` 为事实源，对 eval harness 逐层「设计应有 → as-built → L1 证据」正向推导 ＋ **实跑 167 passed ＋ `--lint-only` 四门全绿（core 11 / comparison 13 / routing 5 / gold 30）**。**结论：评估主线基建已就绪、非「P1+ 半成品」——L0 确定性 Check / L1 `LLMJudge`＋milestone 裁判 / L2 成对裁判 / kappa 校准回路 / baseline 回归门 / MAST 聚合 / prompt profiles / CI nightly 均已落地**；真正缺口收敛为「数据 / 预算 / 人标」三项**非代码缺口**。登记 5 条全 P3：EVAL-001（`evals/__init__` docstring「P1+ 待落」滞后·**✅ 已修**）/ EVAL-002（原 `提示词优化方向.md`「无真 labels.json」滞后·**✅ 已消解**·经核实该文件已整体退役删除、内容迁入 `远期规划.md` 且迁后描述已准确，无 stale 残留）/ EVAL-003（gold 30 条「暂定·待你核」AI 起草值待人核·deferred）/ EVAL-004（真模型出数需 `EVAL_DEEPSEEK_API_KEY`＋预算·deferred）/ EVAL-005（L3 轨迹回流设计内 ⏳ 远期·deferred）。

- **前端接缝排查（白板外）第一批 ⏳ 部分闭环（2026-07-01）** — 承白板 WB-006/007 的两类接缝签名（「渲染画了却没接」/「已铺管线·零产出」）从白板扩到聊天 / 工具 / 记忆 / 派单前端 + 运行时事件契约。3 面并行探（事件 producer↔consumer · 工具/记忆/派单 · 聊天〈探子中止·部分覆盖〉）：**47 类 SSE 事件在桌面+server 侧无整类 producer/consumer 缺口**（唯一字段接缝即下 FE-003）。登记 4 条全 P3：FE-001（`consult_memory` 漏在 `stores/execution/types.ts` 的 `TOOL_LABELS`，而 `TOOL_META` 有且其 docstring 命令「keep the two in sync」→ `toolLabel("consult_memory")` 在图节点实时行 / run 详情漏出英文 id·**✅ 已修**）/ FE-002（`RunDetailBody` 已完成工具行印原始 `tc.toolName`、**同组件** `:222` 实时行却用 `toolLabel`·**✅ 已修**：统一 `toolLabel()`）/ FE-003（**`run_id` 掉线致并发 worker 工具调用错配**：后端 `tool_exec.py` 每个 `tool_use_start/end` 都带 `run_id`，但前端 `frames.ts` fold 丢弃、`project.ts` 把调用挂「第一个 running 的 run」（注释「tool events 非 run-scoped」已过时）→ `width>1` 多 worker 并发时每-run 工具调用 / 图 `toolCount` / 文件产物**归属错**·**✅ 已修**：frame 透传 `runId` + 按 `runId` 归属、CEO 无 tag 回退旧启发式、补 2 回归测·`execution.test` 80→82 全绿·biome/tsc 净）/ FE-004（`model_mode` 有后端无前端——**经复核实为 by-design·非 gap**：质量档 UI（设置页 + 对话 `ModeSelector` + 前端词表）**内测已退役**，非漏建〔`前端UX设计.md §十三` + `编排器与CEO主Agent.md §2.1`〕；根因是**内测 Pro 撤出用户 ceiling → 用户侧坍缩单 Flash 档、无可选**，故质量档机制整体休眠——预设/ceiling/`resolve_profile_set`/DB 表全保留、「ceiling 加回 Pro 即零迁移恢复」。残留的 `modelMode`/`defaultModelMode` 前端 hydration 属休眠期**有意保留的残管**（恢复即复用），非需清死代码。**结论：无需建 picker、无需加 reserved 注——已双文档记录在案**）。聊天前端探子重跑已收口（2026-07-01）：辩论 steering / 审批 / 升级 / resume / 引用[n] / 消息输入（发送·停止·后台·@·追问）/ 后台任务 / 各 gate 卡均核为**已接线**，聊天层整体干净；续扫补 3 条**低置信观察**（非确定 bug，均属判断/需确认级，未改）：FE-005（`AssistantMessage` footer 门在 `content.length>0` → 纯 process 时间线回合〔content=""〕无 copy/retry/delete 行；需先确认此态是否真终态·med）/ FE-006（`NonBlockingAskCard` 紧凑 chip 未显 `opt.detail`，而阻塞 `AskUserFields` 显为次级行「帮看懂取舍」；紧凑设计取舍·可加 tooltip·minor）/ FE-007（`debate/model.ts:66` 注释「点击交锋边高亮对应发言格」，但 `debate/` 内无 onClick/highlight 态；陈旧注释 or 未建功能）。

- **前端接缝排查（白板外）第二批 ⏳ 部分闭环（2026-07-01）** — 承第一批同两签名扩到第一批没覆盖的四面（画板/canvas ops · 分享 sharing · 管理员 observability · 记忆整合），4 面并行探子（均带 FE-004 教训：判 gap 前先查 `docs/` 是否记为有意休眠/退役）。净收获：**2 个真 GAP 修 + 1 漂移修 + 3 漂移登记 + 一批 by-design 闭案**。已修：FE-008（**GAP·已修**：桌面 `whiteboard/ops.ts` board_ops applier 的 move/set_text/delete 只解析 `op.id`、丢 `op.ref`——但后端 `board_ops.py:194` 校验 `id||ref`、契约 `events.ts` 记同批 ref、applier 自身注释也写「id or ref」→ AI「同批建节点再改/删」以 ref 指向时**静默 no-op**；修 `const key = op.id ?? op.ref`〔`resolve` 早已同吃 ref/id〕+ 补 3 ref 回归测·ops 16→19 绿）/ FE-009（**GAP·已修**：辩论 `debate/rounds.py` first_round 经 WaveScheduler 收 `BatchMetrics` 却**只 logger.info、不 emit**，而 delegate `drive.py:144` 会 `emit(batch_metrics_event)` → 辩论回合诊断模式的调度/并行时间线空白；修：补 emit + `asdict`〔BATCH_METRICS 非任何 conformance 向量所载、随 RUN_PLAN 已入 journal，辩论回合同样过 surface 门〕、`journal_config` 注释 delegate→delegate/debate、补 1 测·debate 41 绿）/ FE-010（**DRIFT·已修**：`TOOL_LABELS` 有 `escalate` 却缺 `post_note`/`read_notes`/`amend_note`/`board_ops`/`board_read`〔FE-001 同类〕→ 补 5 词）。顺手清掉 3 漂移（本轮均闭）：FE-011（**✅ 已修**：`apps/admin` 补 `AUDIT_ACTION_LABELS`「回放对话」+ `ACTION_FILTERS` 筛选项 + `AuditTarget` 的 `conversation`→`/replay/:id` 回放深链·admin tsc 净）/ FE-012（**✅ 已修**：`消息IM.md` firehose 事件表补 `memory_updated`〔`consolidation.py` 实发·`realtime.ts` 实消费〕）/ FE-013（**✅ 部分修·余 by-design**：分享弹窗改为「冻结 `share.title` ≠ 当前实时标题时显『快照标题：…』」把被闲置的 `ShareSummary.title` 用起来〔重命名后区分旧快照〕；`ShareListResponse.total` 冗余无害留观察、生产裸 `/shared/<id>` 经 `shareLink` 已正确拼 API origin〔`sharing.ts:17` docstring 已注〕故非 bug）。RESERVED/观察：移动端无会话分享（人定）· 记忆 `memory_updates.load_map` 零调用〔未来「记忆动态」feed〕· 健康窗 token 总量未渲染〔health=可靠性 / cost=token 分栏，或有意〕· 团队便签 amendment 徽标未镜像 `supersede_mode`。by-design 闭案一批：画板 folder_id 扁平列表 / scene_blob_ref v1 缓 / board_read 视觉 env-gated；分享统一 file-share Post-MVP / favicon 不进分享 / 分享纯 REST 无 SSE；admin 桌面 turn_metrics·LLM 窗 ⏳待后端 / mobile batch_metrics no-op / turn_spans log-only / 协作质量窗口级非 per-turn / overview 趋势数组只在分析渲 / admin 独立 app；记忆 mobile lite·无实时 firehose·legacy writeMemory·编辑器不自动重载〔toast+CAS〕·mobile 深链·consult_rule 未实现。验证：ops 19 / debate 41 绿、biome+ruff 净、tsc 触及文件净（3 条既存辩论并行开发错〔Arena/DebateStream `model` prop、execution.test `stance` 帧〕与本批无关）。

- **接缝排查轮 ✅ 闭案 + 可复用配方（2026-07-01）** — 白板内（WB-006/007）→ 白板外两批（FE-001…013）的「接缝」专项就此收口，方法论沉淀如下，供后续审计直接复用。
  - **两类接缝签名（判据）**：① **「渲染画了却没接」**（drawn-but-not-wired）＝一侧有 UI/控件/动词/handler，却没把它的输出真正喂到另一侧、或输出被静默丢弃〔例 WB-006 多选 scale 手柄画了不 hit-test；FE-008 board_ops `ref` 目标被 applier 丢〕；② **「已铺管线·零产出」**（plumbed-but-zero-producer/consumer）＝字段/事件/路由/DB 列/类型在一侧已定义并承载，另一侧却无 producer（无人写/发）或无 consumer（无人读/渲）〔例 WB-007 rotation 全链认却无 producer；FE-003 `run_id` 后端发前端丢；FE-009 辩论 BatchMetrics 收了不 emit〕。
  - **FE-004 铁律教训（防误报）**：判「有后端无前端 / 有前端无后端」为 gap **之前，先 grep `docs/`** 看是否记为**有意休眠 / 退役 / ⏳待后端**——大量「缺口」实为 by-design（质量档 UI 内测退役、admin LLM 窗待后端、mobile lite 减法…）。**弱证据不冒充 bug**：置信五档 `GAP / DRIFT / RESERVED / BY-DESIGN / UNSURE`，只有 GAP·DRIFT 才动手。
  - **可复用排查配方**：① 按面切分（画板/分享/admin/记忆/…）派**并行只读探子**，每子代理带「两签名 + FE-004 先查文档教训 + 五档置信 + 必附 `file:line` 证据」；② 探子回报的 GAP **先 Read 源码复核**（别信行号——FE-004 正是复核翻的案）；③ 复核为实的 GAP·DRIFT 才修，改完 vitest/pytest + biome/ruff + tsc 自检；④ RESERVED 进 `远期规划`、BY-DESIGN 记档闭案、UNSURE 留观察。
  - **全账**：白板 WB-006（修）/ WB-007（reserved）＋ FE-001…013。真 bug 修 3（FE-003 并发工具归属 / FE-008 board_ops ref / FE-009 辩论调度埋点）· 漂移修 6（FE-001/002/010/011/012 ＋ FE-013 部分）· 复核翻案 1（FE-004 by-design）· 留观察 3（FE-005/006/007）· by-design 闭案一大批。**规律：每批稳定 ~2 真 bug，二批后边际递减（第二批多为 by-design）故收口**。

> 本轮交付：本文（计划 §一–六 + §七 登记册 / 功能清单 / 区域进度 + §八 节奏）+ §九 阶段复盘 + §十 架构契约对齐决策与执行 + §十一 结案 + §十一之四 结案后跟进 + §十一之五 专项轮次。首轮审计正文就此收尾；补测 backlog（AUD-009 + AUD-012 mobile/admin 两半）已全数补齐闭环。
