# 评估体系后端落地设计（Layer 1 确定性 + Layer 2 LLM 裁判）🗂️

> **定位**：[`后端架构.md §五 评估体系`](/docs/02-架构/后端架构.md) 已确定**两层目标模型**（Layer 1 离线确定性硬门禁 / Layer 2 在线 LLM-in-the-loop 软门禁），MVP 阶段 **Layer 1 已落地可跑**（`agentcore/evals/`：types + harness + 确定性 Check + runner + report + CLI + 8 种子用例 + 假-provider smoke 自测；`python -m agentcore.evals` 即出确定性分；离线工具尚未接 CI），Layer 2 裁判 + baseline 门禁 + CI 接入未落地（详见下方状态）。本文是该体系的**后端先行落地蓝图**：把抽象的两层映射到现有运行时代码（`react_loop` / `run_chat_pipeline` / `EventSink` / `LLMProvider.complete`）的具体模块、接口、黄金任务集与分步交付（现状文档定方向、本文给落地）。
>
> **为何先做评估**：「优化 AI 功能」的元前提是**能度量**。当前改 prompt / 换质量档 / 调收敛治理全凭手感，无法判断变好变坏，也防不住回归。本体系是后续一切 AI 优化（上下文工程 / 可靠性硬化 / 团队质检 / 智能路由）的方向盘与防回归网。
>
> 状态：🗂️ 蓝图；**P0 已落地**（评估类型 + harness〔`react_loop`/`run_chat_pipeline` + `RecordingSink`〕+ 确定性 Check + seed_lint + runner + report + CLI + 8 种子用例 + 假-provider smoke 自测，见 `apps/server/agentcore/evals/` 与 `apps/server/tests/test_evals_*.py`；离线工具尚未接 CI）；**P1+ 待落**（LLM 裁判 / baseline 回归门禁 / CI 接入）。落地完成后迁入 `后端架构.md §五` 并退役本稿。

---

## 〇、决策摘要

| 维度 | 决定 |
|---|---|
| 分层 | 沿用 §五：**确定性信号**（判定不调 LLM）+ **LLM 裁判**（真模型语义打分）。按「判定是否需真模型」划 CI：零 LLM 自测进每-PR 硬门禁、真模型评测随 nightly 软门禁（见 §八） |
| 侵入性 | **零生产风险**——只消费 `react_loop` / `run_chat_pipeline` 的**现有返回值** + `EventSink` 的事件，不改引擎/管线一行 |
| 运行入口复用 | 单 Agent → `react_loop`；团队 → `run_chat_pipeline`。**不新增运行入口** |
| 不挂起 | team 路径强制 `approvals_enabled=False` → 关掉 ask_user/plan_review 挂起，评测绝不空等超时 |
| 用例形态 | 黄金用例 = 数据（**JSON**，走 stdlib、无新依赖）+ 可选工作区现场（fixture 目录）；加一例 = 加数据，不是加代码 |
| 裁判 | LLM-as-judge 用 `LLMProvider.complete()`，`scenario="eval.judge"` 做成本归因；裁判模型用 Pro 求稳 |
| 非确定性 | `samples=k` 重跑取通过率/均分；路由准确率靠**多采样命中率**（chat/agent 档均 `thinking=True`，思考模型常不吃 temperature，temp=0 仅辅助、不作硬门禁承诺） |
| CI 策略 | **每-PR 零 LLM 硬门禁** = `seed_lint` + 假-provider 自测（+可选 cassette 回放）；**真模型评测（确定性 Check + LLM 裁判）仅 nightly/手动**（耗钱、flaky、软门禁） |
| 判定口径 | 沿用 §五：**规则断言全过 且 裁判分 ≥ 3** 才算通过 |
| 模块落点 | `apps/server/agentcore/evals/`（与 `agentcore` 同树、可 import 运行时；不被服务代码引用、不进镜像） |

**一句话**：评估体系 = 把一组黄金任务喂给**真实运行路径**，用**确定性断言**（消费事件流 + 返回值，判定零 LLM）+ **LLM 裁判**（真模型语义打分）双信号判定，产出可与 baseline 对比、可挂 CI 的回归报告。

---

## 一、关键洞察：拼图已现成，落地是「纯增量」

§五此前未落地易被当作「要造一大套」。实则三块地基**全部现成、可离线复用**，落地主要是**组装 + 数据**：

| 需要的能力 | 现成件 | 证据 |
|---|---|---|
| 离线跑单 Agent 一轮 | [`react_loop`](/apps/server/agentcore/runtime/engine.py)，返回 `(content, reasoning, usage, rounds)` | `tests/test_engine_governance.py` 已用「脚本化假 provider + 裸 `EventSink()` + `ServerWorkspace`」离线驱动 |
| 离线跑团队整回合 | [`run_chat_pipeline`](/apps/server/agentcore/runtime/pipeline.py)，返回 `content/finish_reason/citations/runs(执行日志)/cost_runs` | 管线已是纯函数式入口，依赖均可注入 |
| 截获过程事实（工具调用/委派/引用） | [`EventSink`](/apps/server/agentcore/runtime/events.py)，可裸构造、`execution_journal()` 取离散事件 | 管线即用 `sink.execution_journal()` 落 `runs` |
| LLM 裁判 | [`LLMProvider.complete()`](/apps/server/agentcore/llm/protocol.py) 非流式打分；[`build_provider()`](/apps/server/agentcore/llm/factory.py) 给真实 DeepSeek | `scenario` 字段已为成本归因预留 |
| 成本/用量 | `cost_runs`（管线已折算）+ [`runtime/costing.py`](/apps/server/agentcore/runtime/costing.py) | 每用例成本直接读，不重造 |
| 质量档可控 | [`resolve_profile_set` / `default_profile_set`](/apps/server/agentcore/llm/modes.py) | 可分 economy / quality 跑分对比 |

**结论**：Layer 1 完全消费上面的返回值与事件，零成本、零侵入；Layer 2 仅多调一次真实 LLM 裁判。**不需要给引擎/管线加任何 hook**。

---

## 二、模块落点

```
apps/server/agentcore/evals/
  __init__.py          # ✅ 仅导出纯静态部分（types + checks 注册表 + seed_lint），不拉 runtime
  types.py             # ✅ EvalCase / TurnOutcome / *Report dataclass + Check/Judge/Harness Protocol
  harness.py           # ✅ 把一例跑过真实 Agent → 归一化 TurnOutcome（RecordingSink + 现有运行路径）
  checks.py            # ✅ Layer 1 确定性 Check 实现 + 名字注册表
  judge.py             # ⏳ P1：Layer 2 LLMJudge（provider.complete()，按 rubric 打分）
  runner.py            # ✅ 加载用例 → 跑 → Check（+裁判待 P1）→ 聚合 EvalReport
  report.py            # ✅ 报告聚合 + JSON 序列化 + 控制台美化
  seed_lint.py         # ✅ Layer 1 维度①：种子完整性 lint（用例 JSON 静默失败检测）
  __main__.py          # ✅ CLI: python -m agentcore.evals --suite core --layer 1 --mode economy
  cases/<suite>/*.json # ✅ 黄金任务集（数据，JSON；首批 cases/core/ 8 例）
  fixtures/<name>/      # ✅ 部分用例需要的工作区现场（挂为 ServerWorkspace root）
  baselines/*.json     # ⏳ P2：已提交的基线报告（跑分对比基准）
```

另（✅ 已落地）：`apps/server/tests/test_evals_smoke.py` + `test_evals_checks.py` + `test_evals_seed_lint.py`——用**假 provider** 跑 single 路径、直测 `RecordingSink` 事件还原与纯映射函数、逐个 Check 与 seed_lint 分支，零成本验证 harness/checks/runner/seed_lint 本身不坏。这是 **Layer 1 能上每-PR CI 的关键**：harness 自身的回归由它在 PR 上守，真实模型只在 nightly 跑。

> **目录抉择**：放 `agentcore/evals/` 而非独立 `apps/server/evals/`——前者 `import agentcore.runtime...` 最顺、与 `tests/` 同 import 根；它不被任何服务代码 import，故不影响运行时、也无需进 Docker 镜像。（备选：独立 `apps/server/evals/` 更「物理隔离」，但 import 与打包配置都要多绕，收益不抵。）

---

## 三、核心接口（dataclass + Protocol，贴现有代码风格）

```python
# evals/types.py（提案）
from dataclasses import dataclass, field
from typing import Literal, Protocol

EvalCategory = Literal["qa", "retrieval", "team", "tool_use", "no_fabrication", "routing"]
RunPath = Literal["single", "team"]   # single→react_loop, team→run_chat_pipeline

@dataclass
class EvalCase:
    id: str
    category: EvalCategory
    user_message: str
    path: RunPath = "single"
    mode: str = "economy"                       # 质量档，可分档跑分对比
    toolset: Literal["ceo", "worker"] = "ceo"   # 单 Agent 用哪套工具（只读子集/全集）
    workspace_fixture: str | None = None        # 相对 evals/fixtures/，挂为 ServerWorkspace root
    history: list[dict] = field(default_factory=list)
    checks: list[dict] = field(default_factory=list)   # [{name, args}]，名字注册表解析
    # 期望委派角色等断言参数统一进 checks 的 args（如 {name:"RosterMatches", args:{expected:[...]}}）
    rubric: str | None = None                   # Layer 2 裁判准则；None=跳过裁判
    samples: int = 1                            # 重跑次数（治非确定性，取通过率/均分）

@dataclass
class TurnOutcome:
    """harness 把一次真实运行归一化成可断言的事实。"""
    content: str
    finish_reason: str
    tool_calls: list[tuple[str, str]]   # (name, args_json) 按序
    citations: list[dict]
    delegated: bool
    roster: list[str]                   # 实际委派出的角色（team 路径，来自 run_plan.agents/runs + run_completed.role）
    rounds: int
    usage: dict
    cost_usd: float
    latency_ms: int
    error: str | None = None

@dataclass
class CheckOutcome:
    name: str
    passed: bool
    detail: str = ""

@dataclass
class JudgeVerdict:
    score: float          # 1–5
    passed: bool          # score >= 阈值(默认 3)
    rationale: str

@dataclass
class CaseReport:
    case_id: str
    category: str
    checks: list[CheckOutcome]
    judge: JudgeVerdict | None
    outcome: TurnOutcome
    # passed = 规则断言全过 且 (无裁判 or 裁判通过)  —— §五 判定口径

@dataclass
class EvalReport:
    cases: list[CaseReport]
    # 聚合：按类通过率 / 裁判均分 / 总 token·$ / 与 baseline 的回归项

class Check(Protocol):           # Layer 1 确定性断言
    name: str
    def run(self, case: "EvalCase", outcome: "TurnOutcome") -> CheckOutcome: ...

class Judge(Protocol):           # Layer 2 语义裁判
    async def score(self, case: "EvalCase", outcome: "TurnOutcome") -> JudgeVerdict: ...

class Harness(Protocol):         # 「怎么离线跑一例」与评分逻辑解耦
    async def run_case(self, case: "EvalCase") -> TurnOutcome: ...
```

---

## 四、harness 接现有运行路径（零侵入）

```python
# evals/harness.py（提案骨架）
class RecordingSink(EventSink):
    """在现有 EventSink 上挂钩，捕获过程事实供断言。
    - tool_calls：从 tool_use_start 取 (name, args)
    - roster：从 run_plan(agents/runs) 与 run_completed.role 取角色（run_started 不含 role）
    其余事件照常入 execution_journal()。"""

async def run_case(case: EvalCase) -> TurnOutcome:
    backend  = ServerWorkspace(root=_fixture_root(case), sandbox=SubprocessSandbox())
    provider = build_provider(_eval_credentials())   # 真实 DeepSeek（必须自带 eval key，见 §十三）
    profiles = resolve_profile_set(case.mode, custom_modes={}, ceiling=settings.selectable_models)
    sink     = RecordingSink()
    t0 = time.monotonic()
    try:
        if case.path == "single":
            tools = build_ceo_tool_registry() if case.toolset == "ceo" else build_builtin_registry()
            profile = profiles.get("chat")             # 走 mode 解析，与 team 同口径
            content, _r, usage, rounds = await react_loop(
                messages=[*_history_messages(case.history),
                          LLMMessage(role="user", content=case.user_message)],
                llm=provider, tools=tools, sink=sink,
                tool_context=_ctx(backend), profile=profile, citation_sink=[],
            )
            # react_loop 不返回 finish_reason：镜像 pipeline 按轮数推导（degraded 当前不 emit）
            finish = "end_turn" if rounds < profile.max_rounds else "max_rounds"
            cost = price_usage(usage, profile.model)   # runtime/costing.py 现算（react_loop 不回 cost）
            return TurnOutcome(content=content, finish_reason=finish, rounds=rounds,
                               tool_calls=sink.tool_calls, citations=sink.citations,
                               delegated=False, roster=[], usage=usage, cost_usd=cost,
                               latency_ms=_ms(t0))
        # team 路径：approvals_enabled=False 关掉 ask_user/plan_review 挂起，绝不空等超时
        result = await run_chat_pipeline(
            conversation_id=new_id(), user_message=case.user_message, history=case.history,
            sink=sink, user_id="eval", backend=backend,
            approvals_enabled=False, profile_set=profiles,
        )
        return TurnOutcome(content=result["content"], finish_reason=str(result["finish_reason"]),
                           citations=result["citations"], delegated=bool(result["runs"]),
                           roster=sink.roster, rounds=result["rounds"],
                           usage=_usage(result), cost_usd=_sum_cost(result["cost_runs"]),
                           latency_ms=_ms(t0))
    except Exception as e:                              # react_loop/pipeline 失败 → error 态
        return TurnOutcome(content="", finish_reason="error", error=str(e),
                           tool_calls=sink.tool_calls, citations=[], delegated=False,
                           roster=[], rounds=0, usage={}, cost_usd=0.0, latency_ms=_ms(t0))
```

要点：
- **不新增运行入口**。单 Agent 走 `react_loop`（轻、快，测工具/引用最准；失败抛异常→捕获为 `error` 态，finish_reason 按轮数推导）；团队走 `run_chat_pipeline`（拿 `runs` 执行日志判委派 + `cost_runs` 算成本）。
- **team 路径强制 `approvals_enabled=False`**：连带关掉 ask_user/plan_review 挂起，否则用例触发它们会空等到 600s 超时（注：eval 用 `ServerWorkspace`/`location="server"`，worker 审批本就不 gate，但挂起原语仍会装配，必须显式关）。
- **成本**：单 Agent 的 `react_loop` 只回 usage、不回 cost，用 `runtime/costing.py` 的 pricing 按 usage+model 现算；团队直接读 `cost_runs`。
- **temperature**：`ModelProfile.temperature` 本就存在，强制 temp=0 只需 `replace(profile, temperature=0.0)`——但 chat/agent 档均 `thinking=True`，**思考模型通常不吃 temperature**，故路由确定性主要靠多采样命中率（§六），不靠 temp=0。
- **隔离**：`user_id="eval"` + 独立 fixture 工作区 + 隔离 data_dir（避免 `default_memory_store()` 读到残留 `eval.md`）；DeepSeek key 由 `_eval_credentials()` 从 eval 专用环境变量读（BYOK 下平台 key 为空，见 §十三）。

---

## 五、确定性信号（判定层：无需裁判的 Check）

> **「确定性」是 Check 的属性（判定不调 LLM），与「何时跑」无关**。同一批 Check 既服务 nightly 真模型跑的**廉价过程信号**，也服务 per-PR 的脚本化/回放断言。**哪些进 per-PR 零 LLM 硬门禁、哪些随 nightly 真跑，见 §八**——别把「确定性 Check」直接等同于「Layer 1 每-PR 硬门禁」（原 §5.1 的「零 LLM」指判定时不调模型，事件流仍需来自脚本化 provider 或 cassette 回放）。

对位 §5.2 两维度：

| §5.2 维度 | 落地 | 何时可零 LLM |
|---|---|---|
| **种子完整性 lint** | `seed_lint.py` 静态校验用例 JSON（必填字段、非法 `category`/`path`/`toolset`、`checks` 名不在注册表、既无 checks 也无 rubric、`samples`≥1、id 唯一） | ✅ 纯静态，per-PR 硬门禁 |
| **执行轨迹断言** | 从 `RecordingSink` + 返回值还原 `TurnOutcome`，按声明式 `checks` 裁定 | 事件流来自**真跑**(nightly) 或**脚本化/cassette**(per-PR)；判定本身零 LLM |

首批确定性 Check（`checks.py` 注册表）：

| Check | 断言 | 适用类别 |
|---|---|---|
| `FinishReason` | finish == `end_turn`（非 error/max_rounds；单 Agent 由轮数推导） | 全部 |
| `NonEmpty` | content 非空、长度 > 阈值 | qa / team |
| `ToolCalled` | 调用了 `args.tool`（如 web_search） | retrieval / tool_use |
| `ToolArgsValid` | 工具入参 JSON 合法、含必填键 | tool_use |
| `HasCitations` | citations ≥ `args.min` 且正文 `[n]` 与卡对齐 | retrieval |
| `Delegated` | `delegated == True`、无失败 run | team |
| `RosterMatches` | 实际 roster ⊇ `args.expected`（角色） | routing |
| `MaxRounds` | rounds ≤ `args.budget` | 全部 |
| `NoFabricationMarker` | 无「声称调用了未提供工具」等编造痕迹 | no_fabrication |

> **roster 来源**：`RosterMatches` 的实际 roster 由 `RecordingSink` 从 **`run_plan`（`agents`/`runs` 计划期角色）与 `run_completed.role`** 捕获——**不是** `run_started`（其载荷只有 run_id/agent_id/parent_run_id/kind/revision，无 role）。
> **路由准确率属确定性断言**（截获委派目标与 golden 比对、不需裁判）：但因思考模型的非确定性，它**不进 per-PR 硬门禁**，而随 nightly 真跑、以多采样命中率作软指标（见 §六）。

---

## 六、Layer 2 维度落地（在线 LLM 裁判，软门禁）

对位 §5.3：

| §5.3 维度 | 落地 |
|---|---|
| **路由准确率** | 团队用例跑 k 次，统计 roster 命中率（与 `RosterMatches` 的 `args.expected` 比对）；思考模型非确定，故以**多采样命中率**作软门禁趋势指标（temp=0 仅辅助，不保证可复现） |
| **输出质量** | `LLMJudge.score(case, outcome)`：把 `case.rubric` + `user_message` + `outcome.content`(+citations) 组成裁判 prompt，`provider.complete(scenario="eval.judge")` 出 `{score 1-5, rationale}` |

裁判设计：
- **裁判模型用 Pro**（稳定优先）；裁判 prompt 要求**先给理由后给分**、强制 JSON 输出、锚定 rubric 维度（正确性/相关性/引用支撑/不编造）。
- **多采样**：`samples=k` 对「跑 Agent + 裁判」整体重复，报告**均分 + 通过率**而非单次，压住方差。
- **成本归因**：裁判调用打 `scenario="eval.judge"`，与被测回合的 token/$ 分开统计。

> **faithfulness 局限（已知）**：裁判判「引用是否支撑结论」时只看 `TurnOutcome.citations` 的 snippet，**看不到模型实际读到的整页/工具全文** → 该维度判得浅。彻底解决需把工具输出（tool message 全文）也喂裁判，但显著增成本与 prompt 长度；P1 先接受此局限，作为「方向性信号」而非铁证。

判定口径（§五 5.3）：**规则断言全过 且 裁判分 ≥ 3** 才算该用例通过。

---

## 七、黄金任务集（分类 × 双层信号）

| 类别 | Layer 1 确定性 | Layer 2 裁判维度 |
|---|---|---|
| `qa` 单答问答 | FinishReason / NonEmpty / MaxRounds | 正确性 + 相关性 |
| `retrieval` 检索引用 | ToolCalled(web_search) / HasCitations / 角标对齐 | 引用是否支撑结论（faithfulness） |
| `team` 团队编排 | Delegated / 无失败 run | 产出是否整合各 worker |
| `routing` 路由准确 | RosterMatches（roster ⊇ 期望角色） | 多采样命中率（软指标） |
| `tool_use` 工具正确 | ToolCalled / ToolArgsValid | — |
| `no_fabrication` 不编造 | NoFabricationMarker | 是否坚守「不知道就说不知道」 |

首批 6–8 个种子用例覆盖以上类别各 1–2 例；`fixtures/` 下放检索/文件类用例所需的小工作区现场。

---

## 八、CI 策略：什么进每-PR、什么随 nightly

真实模型评测 = **花钱 + 不确定 + 要 key**，故按「判定是否需要真模型」而非「确定性与否」划线（修正对 §5.1 的理解）：

| 跑法 | 触发 | provider | 门禁 | 覆盖 |
|---|---|---|---|---|
| **零 LLM 自测** | **每 PR** | 假 provider（脚本化） | **硬门禁**（阻断合并） | `seed_lint`（纯静态）+ harness/checks/judge-解析 单测 + `test_evals_smoke`（脚本化跑通单/团队两路）+（可选）cassette 回放轨迹断言 |
| **真模型评测** | **nightly / 手动** | 真实 DeepSeek（CI 注入 eval 专用 key） | **软门禁**（告警 + 上传报告） | 确定性 Check（§五，跑在真输出上）+ LLM 裁判（§六）+ 多采样 + baseline 对比 |

- **关键澄清**：确定性 Check 本身判定零 LLM，但要断言**真实模型行为**（真调了 web_search 吗、真委派了吗）就得先真跑 → 归 nightly。per-PR 硬门禁守的是**评估系统自身不回归**（用假 provider），不烧 token。
- **cassette（可选增强，backlog）**：录制一次真跑的事件流，存为 fixture，per-PR 回放跑确定性 Check——让「真实行为的轨迹断言」也能进硬门禁。P0 不承诺，先用脚本化 smoke。
- **CI 安全**：`tool_use`/`team` 用例若命中 `code_execute` 会在 CI 真起子进程（`SubprocessSandbox`）；首批用例**避开 code_execute**，或在受控沙箱跑（见 §十）。
- **baseline 回归**：`runner` 把真模型报告存 `baselines/*.json`；新跑对比 → 标回归（检查项新失败 / 裁判均分跌 > 阈值）；`--update-baseline` 显式更新。
- CI 接线见 [`部署与运维.md §九 工程门禁`](/docs/05-平台与运维/部署与运维.md)（与棘轮/契约门禁同处挂载）。

---

## 九、分步交付

| 阶段 | 内容 | 产出 |
|---|---|---|
| **P0 骨架 + 确定性** ✅**已落地** | types + harness（包 `react_loop`/`run_chat_pipeline` + `RecordingSink`）+ seed_lint + 首批 Check + report(JSON) + CLI + 8 种子用例（`cases/core/`）+ 假-provider `test_evals_smoke`/`test_evals_checks`/`test_evals_seed_lint` 进 tests/ | `python -m agentcore.evals` 真跑出确定性分；**进每-PR 硬门禁的是 seed_lint + smoke 自测**（非真模型分） |
| **P1 LLM 裁判** | Judge Protocol + LLMJudge（rubric→分+理由，`provider.complete`）+ 多采样聚合（路由命中率 / 输出质量分） | 语义质量可度量 |
| **P2 baseline + 回归** | 基线落盘 + 对比报告 + 阈值门禁 + `--update-baseline` | 改动可验证、防回归 |
| **P3 CI 接入** | nightly/手动 workflow（跑真模型评测、传报告）+ per-PR 仅零 LLM 自测 | 常态化回归护栏 |

---

## 十、不做 / 边界

- **不做评估 UI**——纯离线工具，报告走 JSON + 控制台。
- **不拿真实模型评测卡每-PR**——只 nightly/手动；每-PR 仅零 LLM 自测（seed_lint + smoke）。
- **不重造成本/定价**——复用 `runtime/costing.py` 与 `cost_runs`。
- **不改引擎/管线**——只消费现有返回值与事件；若发现必须加 hook 才能断言某事实，先停下提报（可能是引擎可观测性缺口，单独决策）。
- **首批不评 `code_execute`**——它在 CI 真起子进程（`SubprocessSandbox`），有副作用与 flaky 风险；待受控沙箱就绪再纳入（见 §八 CI 安全）。

---

## 十一、落地后文档同步

- [`后端架构.md §五`](/docs/02-架构/后端架构.md)：落地后把「实现现状：未落地」段按本文改写为现状（标 ✅），并把 Layer 1/Layer 2 维度表补「→ 见代码 `agentcore/evals/`」指针；**现状目录只留指针，不搬本文细节**。
- [`索引.md §07`](/docs/索引.md)：登记本提案条目（已随本文创建一并加）。
- 本文：体系落地后退役，内容并入 `后端架构.md §五`（迁移即删旧，不留废稿）。

---

## 十二、测试计划（harness 自测，随各层落）

- **seed_lint**：非法 category / 未注册 check 名 / 缺 fixture / `RosterMatches` 的 `args.expected` 拼写 → 加载期报错。
- **harness**（假 provider）：single 路径还原 `tool_calls`；team 路径从脚本化 `run_plan`/`run_completed` 事件还原 `roster`、`delegated`、`cost_usd`。
- **checks**：每个 Check 的过/不过分支（用构造的 `TurnOutcome` 单测，零 LLM）。
- **judge**（假 provider 返回固定 JSON）：rubric 渲染 + 分数解析 + 阈值判定 + 坏 JSON 容错。
- **runner**：聚合（按类通过率/均分）+ baseline 对比标回归 + `--update-baseline`。
- 以上均零真实 LLM，构成 `test_evals_smoke` 与单测，可进 PR 硬门禁。

---

## 十三、需拍板的决策点

| 决策 | 选项 | 现状（P0 已采用） |
|---|---|---|
| 模块目录 | `agentcore/evals/` vs 独立 `apps/server/evals/` | ✅ `agentcore/evals/`（import/打包最顺） |
| eval 的 DeepSeek key | BYOK beta 下平台 key 为空、`build_provider(None)` 不可用 → eval 必须自带 key | ✅ `EVAL_DEEPSEEK_API_KEY`（可选 `EVAL_DEEPSEEK_BASE_URL`）；未设则回落 `settings` 全局 key（本地开发便利）。建议低额度账号 + nightly 限次 |
| 首批用例规模 | 6–8 例起步 vs 更全 | ✅ 8 例（`cases/core/`，覆盖 6 类各 1–2 例），再按优化方向扩 |
