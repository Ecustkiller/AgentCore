# 手机端落地设计（桌面端减能力层 · 各端全新建实现 + 协议一致性巡检 · 为 AI 开发设计）🗂️

> **定位**：把产品从「桌面唯一客户端」扩展到**手机端**。产品心智：**手机端 = 桌面端 − 手机物理做不到的能力层**（黑名单减法，非白名单新设计）。
>
> **实现策略（关键）**：**各端全新建实现**——桌面与手机各自拥有完整 stores / services / 协议 fold / 组件，**零共享业务实现**；唯一共享的是**协议一致性规范**（测试向量 / golden / 事件类型，非实现）+ 设计 token。代码层不是「共享核」，而是「**双实现 + 漂移巡检**」。
>
> **为何这样**：见 §二（全新建在 AI 开发下的真实收益）+ §六（协议巡检如何兜住双实现的唯一硬伤 drift）。
>
> **关键约束 · 项目主要由 AI 开发**：第一判据是「最适合多 Agent 并行 + 有界上下文的 AI 开发」，非单纯工程审美。
>
> **状态**：🗂️ 提案。策略方向已确认（⏳ 未落地）。落地后现状回写 [`前端技术与架构.md`](/docs/04-前端/前端技术与架构.md) §七/§九/§十，本稿退役。本目录仅 `07-规划`。

---

## 〇、决策摘要

| 维度 | 决定 |
|---|---|
| 定位 | **手机端 = 桌面端 − 能力层**（黑名单减法）。同一产品、同一后端，减去手机物理做不到的能力，**其余全留** |
| 实现策略 | **各端全新建实现**（桌面 / 手机各自 stores / services / fold / 组件，**零共享业务实现**）。**否决「共享实现核」**（原 B 方案，理由见 §二） |
| 唯一共享面 | **协议一致性规范**（测试向量 / golden / `ProjectedTurn` schema）+ 事件契约类型 + 设计 token——**均为规范 / 契约，非业务实现** |
| 漂移防护 | **三支柱巡检**：① 后端权威向量 + golden oracle ② 共享类型 + `assertNever` 穷尽性 ③ AI 可操作 `pnpm conformance` + CI 门禁。规则落 [`protocol-conformance.mdc`](/.cursor/rules/protocol-conformance.mdc) |
| 减法边界 | 手机端**干脆不实现**本地文件 / MCP / 常驻等能力；无需共享 port 抽象（各端只建自己有的） |
| 为 AI 开发 | build-new 的 AI 收益（爆炸半径收束 / 上下文局部 / 零协调最大并行 / 重写便宜）；drift 由巡检兜，**非靠零重复** |
| 传输 + 打包 | **web SPA → Capacitor 套壳**上架 iOS/Android + 原生推送。**否决 RN**（弃 DOM 复用）、**否决纯 PWA**（推送残缺） |
| 平台粘合（不可避免新增） | bearer-token 认证、FCM/APNs 推送、响应式布局、触控适配 |
| 新基建 / 依赖 | pnpm workspace + `packages/`（**当前无**）、conformance harness、Capacitor / 推送 SDK、后端 fixture/golden 导出 + bearer-token / 推送端点。按 `dev-process`：**AI 提案 → 人确认** |

**一句话**：手机端是桌面端**减能力层**的**独立重建**——双实现各自演进、爆炸半径互不波及，**唯一的硬伤（协议漂移）用后端权威的一致性巡检兜住**。

---

## 一、产品定位：手机端 = 桌面端 − 能力层

### 1.1 被减掉的（且仅这些）= 依赖 Electron 主进程 / 操作系统的能力

| 减掉 | 出处 |
|---|---|
| 本地文件工作区（IPC `fsApi` / fs-service） | [`§8.7`](/docs/04-前端/前端技术与架构.md) `LocalRootSource` |
| 本地 Markdown 落盘编辑（CAS 写本地盘） | [`§8.8`](/docs/04-前端/前端技术与架构.md) |
| 本地 MCP stdio 进程池 | [`§7.1`](/docs/04-前端/前端技术与架构.md) |
| 后台常驻 / 关窗到托盘 | [`§7.7`](/docs/04-前端/前端技术与架构.md) |
| 全局热键 / Spotlight 召唤 | [`§7.4`](/docs/04-前端/前端技术与架构.md) |
| 自动更新（交给应用商店） | [`§7.6`](/docs/04-前端/前端技术与架构.md) |
| OS 集成（右键「分析」、文件关联、taskbar badge） | [`§7.4`](/docs/04-前端/前端技术与架构.md) |
| 本地 sidecar 引擎（dev） | [`远期规划 §一`](/docs/07-规划/远期规划.md) |

### 1.2 剩下的全留（云端撑的，浏览器里照跑）

完整聊天 + 流式、多 Agent 协作图（[`components/graph/`](/apps/desktop/src/renderer/components/graph)）+ run 下钻、审批 / 检查点 / `ask_user`、成本呈现、消息 IM、对话管理 / 文件夹 / 搜索、**云端工作区文件浏览**、attention（传输换原生推送，概念不变）。

### 1.3 减法边界（全新建下极简）

全新建下，手机端**直接不实现**本地文件 / MCP / 常驻 / 热键等能力——**无需 `if(platform)`、无需共享 port 抽象**。桌面端内部的 `caps{}` / `fsApi` 接缝是桌面自己的事；手机端连本地源都不建。云端模式的对话功能（含 `workspaceSource(REST)` 文件浏览）手机端照建。

---

## 二、为何「全新建实现 + 巡检」而非「共享实现核」

**选定 = 各端全新建**（原 C 的形态），配协议一致性巡检消除 C 唯一硬伤（drift）。

**全新建在 AI 开发下的真实收益：**

- **爆炸半径收束**：手机件全在 `apps/mobile`，agent 改手机**物理上动不了**桌面。共享核相反——一改可能两端都炸。
- **上下文局部**：agent 载一个手机纵向切片即可干活，不追进共享包、不理解桌面怎么用。
- **零协调 / 最大并行**：无「共享实现核」这个序列化协调点，两端 agent 几乎零碰撞。
- **重写成本 AI 付得起**：传统「别重复」主要靠「人写码贵」，AI 削弱了这条。

drift（原 B 靠零重复换来的优势）由 §六 巡检找回。

> **被否决 · 共享实现核（原 B）**：共享 = 耦合 = 爆炸半径跨两 app；对有界上下文 AI，**跨 app blast radius 比 drift 更难控**；共享核还是并行 agent 的协调点（序列化）。
>
> **被否决 · 一棵树降级（原 A）**：分支税 + 手机沦为压扁的桌面。
>
> **诚实代价**：选二**不消除**双实现维护（契约一变仍改两套 fold）；巡检 harness 成本 ≈ 直接共享那层薄 fold（原 B）的成本，且还多付双实现。**所以选二的理由必须是上述收益（爆炸半径 / 上下文局部 / 独立演进），不能是省事。**

---

## 三、与既有决策的关系（多为保留、一处撤销）

> **重要**：**全新建比原 B 更贴合既有决策**——[`§7.2`](/docs/04-前端/前端技术与架构.md) 当初就定「桌面独立演进、不共享业务组件」。选二基本沿用，只新增「共享规范 + apps/mobile」。

| 旧决策 | 现状 | 原因 |
|---|---|---|
| [`§7.2`](/docs/04-前端/前端技术与架构.md)「桌面端独立演进、**非共享 packages 业务组件**」 | **基本保留 + 扩展** | 桌面 / 手机各自独立、仍不共享业务实现；仅新增共享『协议一致性规范』+ token + `apps/mobile` |
| [`§五`](/docs/04-前端/前端技术与架构.md) 设计原则「**桌面优先，不做移动端适配**」 | **撤销** | 手机端成一等客户端；改为「桌面 / 手机双端，各自原生布局」 |
| [`§10.5`](/docs/04-前端/前端技术与架构.md)「域卡片不设共享包，**等有真实跨端消费者再从桌面端提取**」 | **保留** | 即便有手机消费者，也选择**重建而非提取**；只共享 token / 类型 / 规范 |

> 落地时上述段落在 [`前端技术与架构.md`](/docs/04-前端/前端技术与架构.md) 就地改写，本表退役。

---

## 四、架构与结构

```
apps/desktop/   现有渲染层(stores/services/fold/组件) — 不动，照常独立演进
apps/mobile/    全新实现：自己的 stores/services/协议 fold/组件
                + 响应式·Capacitor 壳 + 平台粘合(bearer-token / 推送)
apps/website/   (已存在·营销站，非产品客户端)

packages/                       ← 当前无，需建 pnpm workspace
  protocol-conformance/         ← 仅规范：测试向量 + golden + ProjectedTurn schema + 通用断言(非实现)
  contract-types/               ← 后端 schema → 事件/REST 类型，两端共享
                                  (现手写于 types/events.ts，提为共享)
  design-tokens/                ← OKLCH token，两端共享(现于 desktop globals.css，提取)
```

**后端 = 漂移震中**：一处拥有「事件类型 + 向量 + golden」三者单一源（见 §六）。

---

## 五、实现处置：全新建 + 极小共享集

| 层 | 处置 |
|---|---|
| 协议 / 状态 fold（SSE→state、run 树投影、resume/审批状态机） | **各端全新建实现**；**对齐共享 conformance 规范**（§六） |
| 组件 / chrome / 交互 / 布局 / 导航 | **各端全新建** |
| 事件契约类型 | **共享**（生成 / 对齐） |
| 设计 token | **共享** |
| 纯渲染叶子（Markdown / Diagram / KaTeX 等纯三方包装） | **可机会性共享**（无 drift、无平台分叉），非强制 |

**原则**：**不共享任何业务逻辑实现**；共享面 = 规范 + 契约类型 + token（+ 可选纯渲染叶子）。

---

## 六、三支柱漂移巡检（选二的安全网 · 核心机制）

**核心洞察**：协议层是纯函数 `fold(events[]) → ProjectedTurn`。drift = 两端 fold 对同一事件序列产出不同 state，**机械可测**，前提 = 共享向量 + 中立裁判。思路：**后端做输入（向量）与期望输出（oracle）的单一源，两端都对后端对帐。**

### 支柱 1 · 后端权威向量 + golden oracle（共享测试向量）

- **向量** = 后端导出的**真实 SSE 事件序列**（代表性场景：单聊回合 / 多 Agent `delegate` / 审批 pause-resume / 错误 / 跨回合）。后端 turn pipeline + [`turn_journal`](/docs/03-AI核心/执行引擎架构设计.md) 现成，加一个 fixture 导出口。
- **golden** = 后端对每条向量投影出的**期望规范化 state**。后端知道语义真相 → 它当裁判。这解决「两端不一致时谁对」：都对 oracle，不是互相对。
- **schema** `ProjectedTurn{ messages, runs(树), status, pendingInteraction, cost }`，平台无关、可序列化。每端实现 `fold → ProjectedTurn` 暴露测试快照（内部 store 形状可不同）。
- 放 `packages/protocol-conformance/`。**共享的是规范(spec)、不是实现**——和「实现各写」不冲突（多实现共用一套一致性测试是经典模式，如浏览器 WPT）。

### 支柱 2 · 共享类型 + 穷尽性（契约一致性检查）

- 两端 import **同一套**事件类型（现手写于 [`types/events.ts`](/apps/desktop/src/renderer/types/events.ts) 的 `SSEEventType` 判别联合，约 30 个类型；提为共享 / 后端生成）。
- 每端 fold 用判别联合 `switch` + `assertNever` 兜底。后端加一个事件类型 → 重生成 → 两端 fold **编译失败**直到处理。
- 最便宜最响的绊线：**编译错 > 红测 > 静默**。

### 支柱 3 · AI 可操作层（让 AI 拓两端偏差）

- 一条命令 `pnpm conformance` 跑两端 conformance + 类型检查，单一红绿；CI 门禁。
- 失败输出**为 AI 设计**：打 `ProjectedTurn` diff（golden vs 各端 actual），精确指到分叉的字段 / run / status——agent 不用加载另一端就能定位。
- **巡检 agent 任务**：跑 conformance，红了就把落后那端对齐到 golden（oracle 已指明谁错 → agent 能自主修）。
- **rule**（[`protocol-conformance.mdc`](/.cursor/rules/protocol-conformance.mdc)）：动了一端协议 fold → 必跑 conformance；后端契约变了要更新向量；CI 卡。并行 agent 被强制过闸。

### 让漂移结构上不可能静默

后端是震中：一处拥有（a）事件类型（b）向量（c）golden。契约一变 → 两端编译错（类型）+ 红测（向量 / golden）→ **drift 永远是「响」的**。这正是项目评估体系「黄金集 + harness + CI」的同款哲学（见 [`后端架构.md §五 评估体系`](/docs/02-架构/后端架构.md)）。

---

## 七、为 AI 开发设计（对策已随选二更新）

| 失败模式 | 选二下的对策 |
|---|---|
| **漂移** | 由 **§六 三支柱巡检**兜（后端权威 oracle + 编译穷尽 + AI 可操作），**非靠零重复** |
| **不可验证** | conformance **就是**验证网——协议 fold 反而被向量 / golden 重测覆盖；chrome 仍难验但各端低风险 |
| **分支税** | 全新建 = **零跨平台分支**（每端独立树），比原 B（还共享 headless hook）更彻底 |

**决策启发式（选二版）**：

> **放手新建一切；唯一红线 = 协议 fold 必过 conformance；契约变更只走后端单一源。**

---

## 八、平台粘合层（不可避免的新增，不算违反减法）

| 粘合 | 内容 |
|---|---|
| 认证 | 现为 httpOnly cookie（桌面靠 `app://` 自定义 origin 搞定）。手机 web 是新 origin、Capacitor 壳是 `capacitor://` 非常规 origin → **后端加 bearer-token 模式**给移动端（见 [`认证与会话.md`](/docs/05-平台与运维/认证与会话.md)） |
| 推送 | 手机一切后台 SSE 即断 → **原生推送 + 回前台拉 `/v1/attention`** 补盲（复用桌面 [`§7.5`](/docs/04-前端/前端技术与架构.md) 双通路概念） |
| 响应式 | 手机壳自管触控布局；不背桌面布局假设 |
| 触控 | 点击目标 ≥44px、hover 改长按 / 显式按钮、右键菜单 → action sheet |

---

## 九、传输与打包

| 路线 | 结论 |
|---|---|
| **web SPA → Capacitor 套壳** | **选定**：同一套 web 代码打 iOS/Android 原生包，拿到上架 + 原生推送（FCM/APNs），近零重写 |
| 纯响应式 web（手机浏览器） | P1 验证可用；但**无可靠原生推送**（iOS PWA 推送残缺），不能作终态 |
| React Native | **否决**：UI 要重写、用不了 DOM 渲染叶子；与「web→Capacitor」目标冲突 |
| 纯 PWA | **否决**：推送 / 后台残缺（同桌面 [`§7.1`](/docs/04-前端/前端技术与架构.md) 否决 PWA 的理由） |

---

## 十、缺口与新工作

**✅ 直接复用**：整套后端 REST + SSE、（提取后的）事件类型、桌面端现有实现保持不动。

**❌ 缺口（净新工作）**：

| 缺口 | 现状 | 需补 |
|---|---|---|
| pnpm workspace + `packages/` | **当前无**（仅 `apps/` 各自 package.json） | 建 workspace 地基，承载共享规范 / 类型 / token |
| `packages/protocol-conformance` | 无 | 向量 + golden + `ProjectedTurn` schema + 通用断言 + `pnpm conformance` |
| 后端 fixture / golden 导出 | 无 | 从 turn pipeline / `turn_journal` 导出真实事件序列 + 投影期望 state |
| 共享事件类型 | 手写于 [`types/events.ts`](/apps/desktop/src/renderer/types/events.ts) | 提取到 `packages/contract-types`（理想后端 schema 生成），两端 import + `assertNever` |
| `packages/design-tokens` | 在 desktop `globals.css` | 提取 OKLCH token，两端共享 |
| `apps/mobile` 全量 | 不存在 | 全新建：自己的 stores/services/fold/组件 + Capacitor 壳 + 推送 + bearer-token |
| 移动认证 | 仅 httpOnly cookie | 后端 bearer-token 模式 + 前端 token 存储 |
| 推送基础设施 | 无 | 后端「设备注册 + 推送下发」端点 + FCM/APNs；源自 attention / 审批事件 |

---

## 十一、分期交付

| 阶段 | 范围 | 产出 |
|---|---|---|
| **P0**（巡检地基 · 选二的安全前置） | pnpm workspace + `protocol-conformance`（向量 / golden / schema）+ 后端导出 + CI `pnpm conformance` + 共享类型 / token 提取 | 双端协议有了「权威裁判 + 红绿门禁」 |
| **P1**（web 瘦客户端） | `apps/mobile` 全新建（含**自己的 fold，过 conformance**）+ bearer-token；跑通看进度 / 审批 / 聊天 / 消息 / 云端文件 | 可用移动 web 端，验证「桌面减能力层」定位 |
| **P2**（上架 + 推送） | Capacitor 套壳 iOS/Android + 原生推送（源自 attention / 审批事件） | App Store / Play 包 + 推送闭环 |
| **P3 ⏳**（选配） | 仅当 web-in-shell 体验不足时，选择性上 RN 重做局部 chrome | — |

> **推荐**：**先做 P0 巡检地基**——没它，P1 重建协议 fold = 裸奔（drift 无人抓）。P0 完成后 P1 才安全。

---

## 十二、风险与边界

| 风险 | 缓解 |
|---|---|
| 未覆盖的 drift（向量没覆盖到的边角） | 后端真实场景导出 + 发现协议 bug 就加一条复现向量（棘轮） |
| 双实现维护成本 | **接受**——换爆炸半径收束 / 上下文局部 / 独立演进；契约变更靠巡检变「响」 |
| 巡检 harness 成本 ≈ 共享薄核 | 已知（见 §二诚实代价）；选二理由是别的收益、非省事 |
| 设计两端漂移 | token 共享兜底（[`color-tokens.mdc`](/.cursor/rules/color-tokens.mdc)） |
| 范围蔓延（手机想做本地能力） | 守「减法」边界：手机不实现即没有；要做须重新决策 |

---

## 十三、规则对接

- **跨端前端**（[`cross-platform-frontend.mdc`](/.cursor/rules/cross-platform-frontend.mdc)，**本次按选二更新**）：各端全新建、零共享业务实现、共享面 = 规范 / 类型 / token、协议 fold 必过 conformance。
- **协议巡检**（[`protocol-conformance.mdc`](/.cursor/rules/protocol-conformance.mdc)，**本次新增**）：三支柱巡检纪律（向量 / golden、`assertNever` 穷尽、`pnpm conformance` 门禁）。
- **配色 token**（[`color-tokens.mdc`](/.cursor/rules/color-tokens.mdc)）：两端只用语义 token，禁硬编码。
- **开发流程**（[`dev-process.mdc`](/.cursor/rules/dev-process.mdc)）：新依赖（workspace / Capacitor / 推送）AI 提案 → 人确认；禁脚本批量改源码。

---

## 十四、落地后回写

P0–P2 落地后，把现状写回**现状目录**并退役本稿：

- [`前端技术与架构.md §七 / §九 / §十`](/docs/04-前端/前端技术与架构.md)：桌面端架构改为「桌面 / 手机双端独立实现 + 协议一致性巡检」；§7.2 扩展、§五撤销、§10.5 保留（按 §三）。
- [`技术架构与基础设施.md`](/docs/02-架构/技术架构与基础设施.md) / [`项目结构.md`](/docs/02-架构/项目结构.md)：新增 pnpm workspace、`packages/`、`apps/mobile`。
- [`认证与会话.md`](/docs/05-平台与运维/认证与会话.md)：bearer-token 模式；推送下发端点。
- 现状目录如需提及未落地部分，只留一行 `→ 提案：见 07-规划/手机端落地设计`。
