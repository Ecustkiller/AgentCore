# 就绪路线图（内测 → 发布的剩余工作总账）🗂️

> **定位**：把散落在各专题文档的未落地项收敛成**一张剩余工作总账**，回答「离 Go 还差什么」。**只做收敛与排序，不复述实现细节**——细节以各正式文档为准（部署见 [`../05-平台与运维/部署与运维.md`](/docs/05-平台与运维/部署与运维.md)、双模式工作区见 [`../02-架构/双模式工作区.md`](/docs/02-架构/双模式工作区.md)、成本配额见 [`../05-平台与运维/成本配额与计费.md`](/docs/05-平台与运维/成本配额与计费.md)）。**清单清空即退役。**
>
> 状态来源：对照当前代码库逐项核对（文件存在性 + 代码内容）。✅ 已落地项不再单列，仅在下方 TL;DR「已落地」一行带过。

## 状态图例

- 🟡 部分落地（核心在、有缺口）　⏳ 已设计、代码未实现　🔧 运维 / 人工动作（非代码项，无法由代码验收）

---

## 〇、净结论（TL;DR）

- **代码 / 仓库**：仅剩一件——把本地已改好的 5 个 workflow 提交推送到 `master`（见 #1）。
- **其余**：一份运维 checklist（设密钥 / 配 CI secrets / `enable` 定时单元 / 灾备演练）+ 1 项待验证（跨源 cookie）+ 1 项等首发（官网 CTA）。
- **决策点**：原 §五 两项均已 ✅ 拍板——默认档＝经济 Flash、主干＝`master`，事实已入正式文档（`llm/modes.py`、部署与运维.md §二/§三/§七），不再单列。
- **已落地**：P0/P1/P2 其余项（基础设施 / DB 迁移 / 预置账号 / 配额硬拦截 + 限流 / 双模式工作区全链 / electron-builder 打包 / 后端容器化 / 一键部署 + CI 双触发 / 生产地址 baked-in）均已落地，细节见各正式文档（每项原有「落地去向」指针）。

---

## 一、剩余清单

> 自检顺序：先 #1（推送后 CI 才活）→ #2/#3 部署期运维动作 → #4 验证 → #5 首发后自动解析。

| 项 | 状态 | 缺口 / 下一步 | 去向 |
|---|---|---|---|
| **推送 workflow**（原 P2-5 + §五#2） | 🟡 | 5 个 workflow 本地已改好但未推远端：`ci.yml` / `build-backend.yml` / `deploy-backend.yml` / `deploy-website.yml`（git 触发已改 `master`）+ `release-desktop.yml`（仍 untracked）。**提交推送到 `master` 才在 GitHub 生效**（否则 tag 触发不会跑 CI、release 不会建草稿） | 部署与运维.md §二/§三/§七 |
| **关键密钥 + CI secrets**（原 P0-3 + P2-4 尾） | 🔧 | 部署时设 `DEEPSEEK_API_KEY`（L2 软约束，缺失则调用失败、不阻启动）；配 CI secrets（ACR×3 + SSH×3~4）+ 服务器一次性前置 | 部署与运维.md §八/§三 |
| **定时单元上线**（原 P1-5 + P1-6 尾） | 🔧 | 备份/巡检脚本与 systemd 单元均已入仓；部署时 `enable --now agentcore-backup.timer` + `agentcore-healthcheck.timer`、配 `ALERT_WEBHOOK_URL`；放量前跑一次完整恢复演练 + 开异地副本 | 部署与运维.md §七 7.7/7.8 |
| **跨源 cookie 端到端**（原 P2-6 尾） | ⏳ | `app://agentcore` 自定义协议 + 后端 CORS 放行 + cookie `SameSite=None` 均已就位；跨源 cookie 全流程待对真实后端验证 | 前端技术与架构.md §7.2/7.6 |
| **官网下载 CTA**（原 P2-7） | 🟡 | CTA 已接入指向 `AgentCore-releases/releases/latest`；旧 release 已清空，**待新版首发才解析** | 部署与运维.md §二 |

---

## 二、P3 · 放量 / 正式发布前（长期 backlog，非内测阻塞）

- electron-updater 自动更新 + 灰度 / 熔断（部署与运维.md §七 7.6）
- 特性开关架构（部署与运维.md §七 7.9 已设计，代码未落地）
- Redis 分布式状态（多副本前置）——限流已留 `RateLimiter` protocol 抽象作为注入缝
- pgvector / 文档树记忆（记忆系统设计终态）
- 命令面板 Tier 2 / 语义搜索 Tier 3：在 Tier 1 关键词全局搜索（已落地，见 [`../04-前端/前端技术与架构.md` §9.8](/docs/04-前端/前端技术与架构.md)）之上叠加命令导航 / 动作 + pgvector 语义检索
- 工程门禁（部署与运维.md §九）：类型债棘轮 + 跨端契约 `gen:types` + god-file 棘轮（部分未接 CI）
- 集中日志聚合 / 漂移哨兵（日志轮转 + structlog + /readyz 巡检均已落地，仅聚合与哨兵未接）
- MCP / A2A 协议、持久 Agent 实体、记忆管理 UI
- 监控栈（Prometheus）、异地备份副本、E2E 聊天集成测试
- 自定义桌面端图标（现用默认，原 P2-1 尾）
