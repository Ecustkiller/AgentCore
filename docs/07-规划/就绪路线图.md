# 就绪路线图（内测 → 发布的剩余工作总账）🗂️

> **定位**：把散落在各专题文档里的「⏳ 未落地」收敛成**一张带优先级 / 工作量 / 风险 / 依赖 / 验收 / 落地去向的总账**，回答「离 Go 还差什么」。**只做收敛与排序，不复述实现细节**——细节以各正式文档为准（部署见 [`../05-平台与运维/部署与运维.md`](../05-平台与运维/部署与运维.md)、双模式工作区见 [`../02-架构/双模式工作区.md`](../02-架构/双模式工作区.md)、成本配额见 [`../05-平台与运维/成本配额与计费.md`](../05-平台与运维/成本配额与计费.md)）。**落地后对应行迁入正式文档、本表清空即退役。**
>
> 状态来源：对照当前代码库逐项核对（文件存在性 + 代码内容），非凭计划。

## 状态图例

- ✅ 已落地（代码在）
- 🟡 部分落地（核心在、有缺口）
- ⏳ 目标已设计、代码未实现
- 🔧 运维 / 人工动作（非代码项，无法由代码验收）

---

## 〇、净结论（TL;DR）

**真正没做的「代码项」**：P1-7（strong 仍走 Flash）；CI workflow 触发已本地改 `master`，待提交推送生效（见 §五 #2）。（官网下载 CTA 已接入；旧 release 已全清、`RELEASE_GH_TOKEN` 已配，CTA 待新版首发恢复。）

**需人拍板的决策点**：见 §五。

**其余**多为运维 / 人工动作（P0-3 设密钥、P0-5 手测、P1-5 灾备演练）或已落地（P0-1/2/4/6、P1-1/2/3/4、P2-2 大体）。

---

## 一、P0 · 技术内测 Go 线

> 全绿才算「技术内测」可 Go。

| 项 | 状态 | 现状 / 缺口 | 落地去向 |
|---|---|---|---|
| P0-1 基础设施 + 单进程后端 | ✅ | `deploy/docker-compose.server.yml`（PG+Redis+SearXNG，infra-only）已就位；后端默认单进程（`python -m agentcore` 单 worker），与「审批 / 锁 / 限流均进程内态」一致 | 已在 部署与运维.md §三 |
| P0-2 执行 DB 迁移 + 写文档 | ✅ | `本地开发.md` 已含 `uv run alembic upgrade head`（注明首启必做） | 已在 本地开发.md |
| P0-3 关键密钥到位 | 🔧 | JWT 非默认值有 fail-closed（部署与运维.md §八 L0）；`DEEPSEEK_API_KEY` 为 L2 软约束（缺失则调用时失败、不阻启动）。**实际设值是部署动作** | 运维 checklist |
| P0-4 预置账号 + 邀请码 | ✅ | `scripts/create_admin.py` + 注册受邀请码门控 + `本地开发.md`「初始账号」 | 已在 本地开发.md |
| P0-5 关键路径手测贯通 | 🔧 | 注册→登录→对话→委派(出 delegate 图)→工具→成本落账，需真跑一遍 | 运维 checklist（Go/No-Go） |
| P0-6 （测联网）SearXNG | ✅ | compose 含 searxng + `deploy/searxng/settings.yml`（大陆引擎收敛） | 已在 部署与运维.md §三 |

---

## 二、P1 · 技术内测强烈建议

| 项 | 状态 | 现状 / 缺口 | 落地去向 |
|---|---|---|---|
| P1-1 配额硬拦截（发送前检查 + 429） | ✅ | `conversation/quota.py` enforce + route 层接线 + 429；本会话**另加按用户消息限流**（route 层内存滑窗）。多 worker 的 Redis 滑窗仍为 ⏳ | 成本配额与计费.md §一 |
| P1-2 限内测名额 / 发号控制 | ✅ | 邀请码机制即临时护栏；实际发多少号是运维 | 运维动作 |
| P1-3 隐藏 / 降级占位页 | ✅ | **内测期不隐藏入口**（已拍板）：`Sidebar.tsx::NAV_ITEMS` 5 个主入口常显、无门控字段（原 `comingSoon` 过滤已移除）。未完成能力改走**页内友好占位**降级（非死页）：消息 / 文件为真实页；探索页「公共市场」标「即将上线（Day 2）」；工具箱卡片网格「AI 工具」可用、其余创作卡标「即将上线」 | 前端UX设计.md |
| P1-4 委派 worker 工具审批 | ✅ | `tools/builtin/delegate.py` 在 local 模式把审批门转发给 worker（双模式 P2d「执行门」）；cloud 沙箱隔离不门控 | 安全权限与治理.md / 双模式工作区.md §九⑧ |
| P1-5 迁移前 pg_dump + 灾备演练 | 🔧 | 策略已成文（部署与运维.md §七 7.7：迁移前自动快照 / 干净恢复 / 异地副本 / **演练**）；但 `deploy/scripts` 仅 `init-db.sh`，无 `backup.sh` / `restore.sh` 入仓，演练本身是运维动作 | 部署与运维.md §七（脚本待入仓） |
| P1-6 基础可观测（日志 + /readyz 巡检） | 🟡 | structlog JSON + `/livez` `/readyz` `/version` 已有（`api/routes/system.py`、`buildinfo.py`）；日志聚合 / 巡检 cron / 漂移哨兵未接 | 部署与运维.md §七 7.3/7.8 |
| P1-7 恢复 agent.strong→Pro | ⏳ | `llm/config.py` `_STRONG_MODEL = DEEPSEEK_V4_FLASH`（测试期降本，**单一翻转点**，改回 `DEEPSEEK_V4_PRO` 即恢复设计）。**需决策**（见 §五） | llm/config.py（一行翻转） |

---

## 三、P2 · 普通用户内测 Go 线

> 「下载安装包 + 连公网」可用才算普通用户内测可 Go。

| 项 | 状态 | 现状 / 缺口 | 落地去向 |
|---|---|---|---|
| P2-1 桌面端 electron-builder 打包 | ✅ | `apps/desktop/electron-builder.yml`（NSIS/DMG/AppImage、MVP 不签名、精简 `files` 仅打 `out/`）+ `build:win/mac/linux/unpack` 脚本；已实跑出 `AgentCore-0.1.0-win-x64.exe`(86MB) + `latest.yml`(updater feed)。⚠️ 国内本地构建需设 `ELECTRON_MIRROR`/`ELECTRON_BUILDER_BINARIES_MIRROR`（CI 在 GitHub Actions 不需要）；自定义图标待补（现用默认） | 已在 部署与运维.md §7.6 |
| P2-2 本地工作区接通对话 | ✅ | 双模式工作区 P0/P1/P2a–d 已落地（binding 端点 + `WorkspaceModeBar` 选目录绑定 + 本地 op 通道 + 降级重连 + 迁移 + 测试）；**P2e 三段**（snapshot 上传 / 云端在快照上跑团队 / diff 回写，均含测试）前后端均已落地（前端＝工作区「交接」tab：派发 + 轮询 + PR 评审 / 逐文件解冲突） | ../02-架构/双模式工作区.md §六 |
| P2-3 后端 Dockerfile + 容器化入仓 | ✅ | `apps/server/Dockerfile`（uv 多阶段 + 非 root + 构建期烘焙 `GIT_SHA`/`BUILT_AT`→`/version`）+ `deploy/docker-compose.app.yml`（api 叠加 server compose、服务名直连基础设施）；worker 容器暂缓（当前进程内协程，无独立 worker 入口）。镜像构建 + 冒烟（非 root/导入/alembic）已验证 | 已在 部署与运维.md §三 |
| P2-4 `deploy-server.sh` 一键部署 + CI 自动触发 | ✅ | `deploy/scripts/deploy-server.sh`：解析 SHA→pull→起基础设施→迁移前 `pg_dump` 快照→`alembic upgrade head`→`compose up`→`/readyz` 健康检查→记 `.last-deployed-sha`；自更新防护 + 回退（传旧 SHA 跳正向迁移）+ 分段计时。CI 双触发已接：`build-backend.yml`(push master→构建 api 推 ACR 公网端点) + `deploy-backend.yml`(push `prod-*` tag→SSH 跑本脚本，对齐 §7.2 发布点)。🔧 余配 secrets（ACR×3 + SSH×3~4）+ 服务器一次性前置 | 已在 部署与运维.md §三 |
| P2-5 release CI（desktop-v* tag→安装包草稿） | 🟡 | `release-desktop.yml` 本地就绪（`desktop-v*` tag→构建→`--publish` 草稿 release，含 tag↔`package.json` version 校验）；发布仓 `Lawofall/AgentCore-releases` 已存在(PUBLIC)、`RELEASE_GH_TOKEN` 已配（A2 复用 gh token，验证 push/admin=true）、旧测试 release 已全清。⏳ `release-desktop.yml` 仍未推远端（untracked）→ tag 触发 CI 暂不会跑，须先提交推送到 `master` | 部署与运维.md §七 7.6 |
| P2-6 `VITE_API_URL` 生产地址 baked-in + 连通性 | ✅ | `apps/desktop/.env.production` 烘焙 `https://app.fashitianxia.xyz/api`（prod 产物已验证替换、消除 `localhost` 兜底）。连通性已补齐：prod renderer 经 `app://agentcore` 自定义协议加载（`main/index.ts`，headless 冒烟验证 index/asset/root 均 200）+ 后端默认 CORS 放行 `app://agentcore`（`config.py`）+ cookie `SameSite=None`（`production.env.example`；启动期 fail-closed 校验须配 `Secure`）。⏳ 跨源 cookie 端到端流待对真实后端验证 | 已在 前端技术与架构.md §7.2/§7.6 |
| P2-7 官网下载 CTA（+ deploy-website 分支） | 🟡 | 主干认 `master`（见 §五 #2）：`deploy-website.yml` 远端 git 触发本就是 `master`（本地误改 `main` 待还原）；`--branch=main` 系 Cloudflare Pages 生产分支、保留。官网下载 CTA 已接入（`apps/website/src/app/page.tsx` 顶栏/Hero/结尾/页脚→ `AgentCore-releases/releases/latest`）；⚠️ 旧 release 已清空，CTA 待新版首发才解析 | 部署与运维.md §二 |

---

## 四、P3 · 放量 / 正式发布前（非内测阻塞）

> 基本未开始；逐条留作长期 backlog。

- electron-updater 自动更新 + 灰度 / 熔断（目标见 部署与运维.md §七 7.6）
- **特性开关架构**（部署与运维.md §七 7.9 已设计，代码未落地）
- Redis 分布式状态（多副本前置）——限流已留 `RateLimiter` protocol 抽象作为注入缝
- pgvector / 文档树记忆（记忆系统设计终态）
- **工程门禁**（部署与运维.md §九）：类型债棘轮 + 跨端契约 `gen:types` + god-file 棘轮（部分⏳ 未接 CI）
- MCP / A2A 协议、持久 Agent 实体、记忆管理 UI
- 辩论范式差异化 UI、检查点卡片（前端UX目标态.md）
- 监控栈（Prometheus）、漂移哨兵、异地备份副本、E2E 聊天集成测试

---

## 五、决策点（需人拍板）

| # | 决策 | 备选 | 影响 |
|---|---|---|---|
| 1 | **P1-7 strong 档模型** | 内测继续 Flash 降本 | 多 Agent 输出质量 vs LLM 成本 / 定价真实性 |
| 2 | **主干分支事实**（连带 P2-7）✅ 已拍板＝`master` | 实测：远端唯一分支＝`master`、默认分支 master、`main` 不存在(404)；`ci.yml` 却误触发 `branches:[main]`→从未跑过。文档已统一订正回 `master`（部署与运维.md §二/§三/§七）。✅ 四个 workflow 的 git 触发/注释已本地改 `master`（`ci.yml`/`build-backend.yml`/`deploy-backend.yml`/`deploy-website.yml`，`--branch=main` Pages 生产分支保留）；⏳ 待提交推送到 `master` 才在 GitHub 生效 | 文档+workflow 已订正；待推送 |
