# 前端技术选型与 UI 设计方案

> **状态**：已确定

---

## 一、技术栈

| 类别 | 选择 | 版本 | 理由 |
|------|------|------|------|
| 构建工具 | electron-vite | latest | HMR 3-5x 快于 Webpack，2026 年 Electron 项目事实标准 |
| 打包分发 | electron-builder | latest | 支持 NSIS/DMG/AppImage，自动更新成熟 |
| UI 框架 | React | 19.x | 生态最大，与 AG-UI 语义对齐 |
| 状态管理 | **Zustand** | 5.x | 1.2KB，零样板，selector 精确订阅 |
| 服务端状态 | ⏳ TanStack Query | 5.x | 缓存、重试、乐观更新（**未引入**；会话等数据走 Zustand + 直接 fetch） |
| 样式 | Tailwind CSS | 4.x | 零运行时、AI 友好、开发速度快 |
| 图标 | lucide-react | latest | 轻量 SVG 图标，与 Tailwind 自写组件配合 |
| UI 组件库 | ⏳ shadcn/ui | — | Radix + Tailwind，源码可控（**未引入**；当前为 Tailwind 自写组件） |
| 流程图 | @xyflow/react (React Flow) | 12.x | 任务依赖图可视化，7M 周下载 |
| 自动布局 | ELK.js | latest | 支持层次/树形布局，配合 React Flow |
| IPC 通信 | 类型安全 contextBridge | - | 共享类型定义，编译期校验 |
| 测试 | Vitest + Playwright | 3.x | 单元 + E2E |
| 代码质量 | Biome | 1.x | lint + format，比 ESLint+Prettier 快 30x |

---

## 二、状态管理策略

### 统一 Zustand

所有客户端状态统一使用 Zustand 管理。通过正确的 selector 模式实现精确订阅。

实际 Store 划分与类型以 [`核心接口定义.md` §7.1](核心接口定义.md) 为准（`useConversationStore` / `useExecutionStore` / `useGraphStore` / `useUIStore` / `useSidebarStore` / `useFilesStore` / `useUserStore`）。⏳ 服务端数据（会话历史、模板列表等）原设计走 TanStack Query，**当前未引入**，由 Zustand store + `services/api.ts` 直接 fetch 管理。

→ 见代码：`apps/desktop/src/renderer/stores/`

### 强制规则

> 所有从 store 读取状态的组件必须使用精确 selector，禁止 `useXxxStore()` 不带参数直接获取整个 store（否则任意状态变更都会触发该组件重渲染）。

---

## 三、流式输出性能优化

> **实现现状**：`OutputBuffer` / `useAgentsStore` **未实现**；流式文本走 `useConversationStore.appendToLastMessage`（→ 见代码 `apps/desktop/src/renderer/stores/conversation.ts`、`services/streamConversation.ts`）。下表优化策略为设计目标，虚拟滚动等尚未落地。

### 问题

每个 Agent 的 LLM 流式输出约 30 token/s，4 Agent 并行 = 120 次状态更新/s。
性能瓶颈不在状态管理库，而在 DOM 渲染。

### 优化策略

| 优化 | 解决什么 | 实现 |
|------|----------|------|
| Token 批量更新 | 减少 state 更新频率 | requestAnimationFrame 批量刷新 |
| 虚拟滚动 | 长输出不卡 | 只渲染可见区域 DOM 节点 |
| 进度条用 CSS | 60fps 动画 | `transition` 而非逐帧重渲染 |
| 主面板单焦点 | 只渲染当前 Agent 输出 | 其他 Agent 存 store 不渲染 |

```typescript
// ⏳ 设计目标：token 批量更新（未实现）
// 当前实现：content_delta / run_output_delta 直接 appendToLastMessage
```

### 性能边界

| Agent 数 | 状态更新频率 | 是否有问题 |
|---------|------------|-----------|
| 1-4 | ~120/s | 无问题 |
| 5-10 | ~300/s | 无问题（正确用 selector） |
| 10-20 | ~600/s | 需要节流 |
| 20+ | 不现实的场景 | - |

---

## 四、IPC 通信方案

### 核心原则

- 所有 IPC 通道和消息类型定义在 `shared/ipc-contract.ts`
- preload 和 renderer 引用同一套类型
- 编译期发现类型错误

### 契约与封装

IPC 调用/事件契约（invoke 请求-响应、main→renderer 事件推送）与 preload 的类型安全封装已落地：

→ 见代码：`apps/desktop/src/shared/ipc-contract.ts`（契约类型）、`apps/desktop/src/preload/index.ts`（contextBridge 封装）、`apps/desktop/src/main/fs-service.ts`（主进程 fs 服务）

---

## 五、窗口布局设计 ✅ 已确定

> 参考 `C:\Project\1` 的 IDE 式布局架构。核心原则：**全局侧栏 + 页面级自定义布局**，不再使用全局三栏固定结构。

### 全局骨架（AppShell）

```
┌─────────────────────────────────────────────────────────────┐
│  flex h-screen w-screen overflow-hidden                     │
├──────────────┬──────────────────────────────────────────────┤
│  <aside>     │  <div flex-1 flex-col>                       │
│  全局侧栏     │  ┌────────────────────────────────────────┐ │
│  w-60/w-14   │  │ 顶栏 h-10 (拖拽区 + WindowControls)    │ │
│              │  ├────────────────────────────────────────┤ │
│  品牌/折叠    │  │  <main flex-1 min-h-0>                 │ │
│  搜索        │  │    <Outlet /> → 页面级布局              │ │
│  主导航      │  │                                        │ │
│  会话列表    │  └────────────────────────────────────────┘ │
│  用户底栏    │                                              │
└──────────────┴──────────────────────────────────────────────┘
```

AppShell 只负责：全局侧栏 + 顶部标题栏 + `<Outlet />`。所有页面内容由路由对应的页面组件自行决定内部布局（单栏、双栏、三栏等）。

### 侧栏设计

| 维度 | 方案 |
|------|------|
| 展开宽度 | `w-60`（240px） |
| 折叠宽度 | `w-14`（56px），仅显示图标 |
| 动画 | `transition-[width] duration-200` |
| 状态管理 | Zustand `useSidebarStore`（collapsed、expandedSections） |
| 折叠方式 | 手动切换按钮，非响应式自动折叠 |

**侧栏内容结构（自上而下）：**

| 区域 | 内容 |
|------|------|
| 头部 | 品牌 Logo + 侧栏折叠/展开按钮 |
| 搜索 | 全局搜索入口 |
| 主导航 | 见下表（全部扁平，无子导航） |
| 会话列表 | 对话历史列表（flex-1 可滚动） |
| 底栏 | 用户头像 + 设置入口 |

**主导航项：**

| 导航项 | 路由 | 图标 | 说明 |
|--------|------|------|------|
| 对话 | `/` | MessageSquare | 新建/继续对话，主入口 |
| 消息 | `/messages` | Mail | 独立消息功能（后续实现） |
| 文件 | `/files` | FolderOpen | 本地文件管理 |
| 工具箱 | `/toolbox` | Wrench | MCP 工具管理、插件入口 |
| 探索 | `/explore` | Compass | 发现公共 Agent/模板/工具 |

- 导航项可挂 badge（对话挂 attention 计数，消息挂未读数）
- 折叠状态下仅显示图标 + badge 圆点
- 会话列表紧跟导航下方，占据侧栏剩余空间

### 会话列表设计

> **实现现状**：侧栏会话列表已落地（扁平列表 + `useConversationStore`）；用户文件夹分组、`GET /v1/conversations/grouped`、右键菜单等 **未实现**（→ 见代码 `apps/desktop/src/renderer/components/sidebar/Sidebar.tsx`、`stores/conversation.ts`）。以下为 UX 目标态。

**分组方式：用户文件夹 + 未分组**

```
对话                    [+]
───────────────────────
▼ 📁 工作项目 A     [...]
  │ 🟢 实现用户登录模块
  │    设计 API 接口
▼ 📁 学习笔记       [...]
  │    机器学习基础
▼ 未分组
  │ 🟡 帮我写个邮件
  │    分析这段代码
```

| 配置 | 方案 |
|------|------|
| 分组 | 用户创建文件夹 + 未分组（文件夹可绑定本地目录） |
| 排序 | 按最后活动时间降序 |
| 展开/折叠 | 每个文件夹组可折叠，默认第一组展开 |
| 数据加载 | ⏳ 启动时拉取 `GET /v1/conversations/grouped`（**未实现**；当前为分页 `GET /v1/conversations`） |
| 实时更新 | SSE 推送标题生成后即时刷新 |
| 折叠态 | 侧栏折叠时会话列表完全隐藏 |

**对话项显示内容：**

| 元素 | 说明 |
|------|------|
| 状态指示器 | 🟢 Agent 执行中 / 🟡 等待审批 / 无 = 无活动 |
| 标题 | AI 自动生成（流式更新） |
| 时间戳 | hover 时 tooltip 显示完整时间 |

**交互操作：**

| 交互 | 行为 |
|------|------|
| 左键点击对话 | 导航到 `/conversations/:id` |
| 右键对话 | 菜单：重命名 / 移到文件夹 / 归档 / 删除 |
| 右键文件夹头 | 菜单：重命名 / 连接本地目录 / 新建对话 / 删除 |
| [+] 按钮 | 新建对话（导航到 `/`） |
| 文件夹头点击 | 展开/折叠组 |
| 加载中 | Skeleton 占位动画 |
| 空列表 | 友好图标 + 「开始第一次对话 →」入口 |

**状态管理：**

- `useConversationStore`：会话列表、当前对话、消息流
- ⏳ `useConversationListStore` / `useFoldersStore`：**未实现**（原设计用于跨组件标题刷新与文件夹）
- 组件局部 state：展开/折叠、菜单开关

### 页面级布局规则

| 路由 | 布局模式 | 说明 |
|------|----------|------|
| `/`（新对话） | 单栏居中 | 欢迎页 + InputBar，max-w-4xl mx-auto |
| `/conversations/:id` | 单栏聊天区 + TaskCard + 全屏 GraphOverlay | 消息流 + 多 Agent 任务卡片；协作图经 `useUIStore.graphOpen` 全屏覆盖 |
| `/messages` | 左可调宽房间列表 + 右消息区 | 后续实现 |
| `/files` | 左固定文件树 + 右预览区 | 文件树宽度固定 288px |
| `/toolbox` | ToolLayout 包裹 + 工具网格 | 工具管理与配置 |
| `/explore` | 页面内联 `max-w-*` + 网格 | 浏览公共资源（⏳ 无 `PageContainer` 组件） |
| `/more/*` | 左分类导航 + 右设置内容 | 设置入口（路由 `/more`，非 `/settings/*`） |
| 插件路由 | ToolLayout 包裹 + 插件自定义 | ⏳ 插件架构未实现 |

### 对话页详情面板（DetailPanel）

> **实现现状**：DetailPanel 组件 / `useDetailPanelStore` **未实现**。多 Agent 详情当前靠 `TaskCard` + 全屏 `GraphOverlay`（`useUIStore.graphOpen`）（→ 见代码 `components/chat/TaskCard.tsx`、`components/graph/GraphOverlay.tsx`）。以下为 UX 目标态。

对话页是 AgentCore 的核心页面，原设计采用主内容 + 右侧可拖拽详情面板的布局：

```
┌─────────────────────────────────────────────────────────┐
│  /conversations/:id                                     │
├──────────────────────────────────────┬──────────────────┤
│                                      │ ← 拖拽手柄       │
│  ┌────────────────────────────┐      │  DetailPanel     │
│  │ 消息流 + 任务卡片           │      │  (280~560px)     │
│  │                            │      │                  │
│  │ ...                        │      │  [动态Tab头]     │
│  │                            │      │  ┌───────────┐  │
│  ├────────────────────────────┤      │  │ Tab 内容   │  │
│  │ 输入框                      │      │  │           │  │
│  └────────────────────────────┘      │  └───────────┘  │
└──────────────────────────────────────┴──────────────────┘
```

#### 面板物理参数

| 配置 | 值 |
|------|-----|
| 最小宽度 | 280px |
| 最大宽度 | 560px |
| 默认宽度 | 400px |
| 持久化 | localStorage (`agentcore:detail-panel-width`、`agentcore:detail-panel-open`) |
| 开关 | 可完全关闭（简单任务时隐藏，体验同 ChatGPT） |
| 拖拽实现 | 手写 pointer 事件（`onPointerDown` + `pointermove`，不引入第三方库） |
| 状态管理 | ⏳ Zustand `useDetailPanelStore`（**未实现**；当前 `useUIStore.graphOpen` 控制全屏图） |

#### 动态 Tab 模式

**不使用固定 Tab 栏**，而是用户点击不同元素时动态打开新 Tab：

| 配置 | 值 |
|------|-----|
| 最大 Tab 数 | 6（超出时挤掉最旧 Tab） |
| Tab 可关闭 | 是（多 Tab 时每个 Tab 显示 × 按钮） |
| Tab 全关闭 | 面板自动隐藏 |
| Tab ID 生成 | 由 TabView 类型 + 关键 ID 组合（去重） |

#### Tab 类型定义

```typescript
export type TabView =
  | { type: 'task-progress'; messageId: string }
  | { type: 'reasoning'; messageId: string; title: string }
  | { type: 'run-detail'; runId: string; messageId: string; title: string }
  | { type: 'task-graph'; messageId: string }
  | { type: 'conversation-artifacts'; conversationId: string };
```

#### 各 Tab 内容

| Tab 类型 | 触发方式 | 内容 |
|----------|---------|------|
| `task-progress` | 点击任务卡片进度区域 / 消息上的进度入口 | 总进度条 + 所有 Agent 状态列表（点击 Agent 下钻） |
| `run-detail` | 点击 task-progress 中的 Agent 行 / 任务卡片 [展开] | 单个 Agent/run 完整详情：任务、思考、输出、工具调用、子任务、资源消耗 |
| `task-graph` | 任务卡片 [图] 按钮 / 多 Agent 任务自动打开 | React Flow 缩略 DAG 图 + [最大化] 进入全屏图视图 |
| `reasoning` | 点击消息气泡的「思考」入口 | LLM reasoning 内容（流式 Markdown） |
| `conversation-artifacts` | 点击对话级产物入口 | 本对话所有产物聚合视图 |

#### run-detail 内部区段结构

| 区段 | 默认状态 | 内容 |
|------|---------|------|
| 头部 | 始终显示 | Agent 头像 + 角色名 + 状态 + 实时计时 |
| 任务 | 展开 | 编排器分配的任务描述 |
| 错误 | 展开（仅失败时） | 错误信息红色高亮 |
| 思考过程 | 折叠 | LLM reasoning（可展开查看） |
| 输出 | 展开 | 实时流式内容 / 最终输出（Markdown） |
| 工具调用 | 展开 | 工具调用列表 + 各调用详情 |
| 子任务 | 展开 | 子 run 列表（点击打开新 run-detail Tab） |
| 资源消耗 | 折叠 | 模型 / token 数 / 费用 / 耗时 |

#### 数据源策略

> **实现现状**：`useRunsStore`、`message.runs` 快照、按 messageId 隔离执行槽 **均未实现**。当前执行态由 `useExecutionStore` 全局持有（→ 见代码 `stores/execution.ts`）。

```
⏳ 设计目标：
实时执行态（runs store byMessage[messageId]）
        ↓ 优先
消息快照（message.runs，flattenRuns 拍平后按 runId 查找）
```

- 统一 run 概念：编排步骤和委派子 Agent 都是 run，详情走同一套组件
- 实时态流式更新 → 面板自动跟随
- `message_end` 时快照存入 message.runs 持久化供回放，释放实时槽

#### 面板自动打开规则

| 场景 | 行为 |
|------|------|
| 多 Agent 任务启动 | 自动打开面板，激活 task-progress Tab |
| 单 Agent 任务 | 不自动打开（保持 ChatGPT 体验） |
| 用户手动关闭 | 记住偏好，下次不自动打开（直到用户再次手动打开） |
| 面板已开 + 新任务 | 在已有 Tab 旁新增 Tab |

### 页面宽度规范

> **实现现状**：`PageContainer` 组件 **未实现**；各页面在组件内联 `max-w-* mx-auto` 约束（→ 见代码 `pages/`）。下表为宽度梯度设计目标。

主内容区原设计使用 `PageContainer` 组件统一约束宽度：

| 梯度 | 类名 | 最大宽度 | 适用页面 |
|------|------|----------|----------|
| content | `max-w-4xl mx-auto` | 896px | `/`（新对话）、`/more/*`、详情页、表单 |
| canvas | `max-w-[1200px] mx-auto` | 1200px | `/explore`、`/toolbox`、`/my-agents`、网格列表 |

**实现规格：**

```typescript
const WIDTHS = {
  content: 'max-w-4xl',    // 896px
  canvas: 'max-w-[1200px]', // 1200px
} as const;

// 外层: h-full overflow-y-auto（内置滚动）
// 内层: mx-auto w-full px-6 py-8 + 宽度档位
```

| 参数 | 值 | 说明 |
|------|-----|------|
| 横向内边距 | `px-6`（24px） | 所有页面统一 |
| 纵向内边距 | `py-8`（32px） | 所有页面统一 |
| 窄屏行为 | `w-full` | 自动占满，无横向留白 |
| 宽屏行为 | 居中 + 两侧留白 | 到档位上限后居中 |
| 滚动 | 组件内置 | `overflow-y-auto` 在外层 |

**页面使用对照：**

| 页面 | 档位 | 理由 |
|------|------|------|
| 新对话欢迎页 | content | 线性阅读，居中聚焦 |
| 设置各子页（`/more/*`） | content | 表单型内容 |
| 探索 | canvas | 卡片网格，需要宽度 |
| 工具箱 | canvas | 工具网格列表 |
| 对话页 | 不使用 | 自有布局（聊天 + TaskCard / GraphOverlay） |
| 文件页 | 不使用 | 自有布局（文件树 + 预览） |

### 设计原则

| 原则 | 说明 |
|------|------|
| 全局只管壳 | AppShell 不预设右面板，页面自行决定布局 |
| 对话页突出差异 | 多 Agent 时 TaskCard + 全屏协作图，展示 Agent 协作信息 |
| 简单任务零噪音 | 单 Agent 时无 TaskCard，体验同 ChatGPT |
| 桌面优先 | 不做移动端适配，少量 breakpoint 用于窗口极窄场景 |
| 页面自治 | 各页面/插件自定义内部布局，互不干扰 |

---

## 六、项目结构

> 目录结构以 [`项目结构.md` §三](项目结构.md) 为唯一真相源（与实际代码对齐，含 `main/` `preload/` `shared/` `renderer/`）。本节只保留前端特有的组织判据；Agent 状态面板与任务流程图详见 [`前端UX架构设计.md`](前端UX架构设计.md)。

**目录组织原则**（参考 §九 前端运行时架构模式）：

| 层 | 职责 | 判据 |
|---|---|---|
| `components/` | 纯渲染 | 默认扁平，仅内聚体系才开子目录 |
| `pages/` | 路由页 | 页面级组合，决定内部布局 |
| `stores/` | Zustand 状态 | 所有状态 |
| `services/` | SSE 流与副作用 | 流式会话消费（`streamConversation.ts`） |
| `lib/` | 纯函数 / API client | 无副作用 |

---

## 七、桌面端架构 ✅ 已确定

> 桌面端是唯一产品客户端（无独立 Web 端）。定位：**常驻本地、会协作的 AI 团队**——后台干活、碰本地文件、需要时敲你一下。

### 7.1 桌面价值主张（vs Web/PWA）

| 能力 | 网页天花板 | 桌面解锁 |
|------|-----------|---------|
| 异步任务 + 审批门禁 | 关标签即断 | 后台常驻 + 原生通知 + 审批闭环 |
| 全局热键召唤 | 埋在标签页里 | Spotlight 式悬浮窗 |
| 文件工作区 | 只能上传 | 本地文件直连 / Agent 读写磁盘 |
| MCP 工具层 | 浏览器跑不了 stdio | 本地 MCP 运行时（常驻进程池） |

被否决：PWA（无常驻/通知/文件）、全原生 UI（贵且漂移）、Tauri（团队无 Rust，WKWebView 有差异，备选）。

### 7.2 主进程架构

> **实现现状**：主进程当前仅创建窗口 + `registerFsIpc()`（→ 见代码 `apps/desktop/src/main/index.ts`、`main/fs-service.ts`）。`connection-manager`、`mcp-pool`、`local-store` **均未实现**。

```
主进程 (Node.js)
├─ ⏳ connection-manager  ← 独立 attention 轮询 + 通知（未实现）
├─ ⏳ mcp-pool            ← MCP server 常驻进程池（未实现）
├─ fs-service          ← 本地文件 IPC 服务（已落地）
└─ ⏳ local-store         ← sql.js，对话/消息读缓存（未实现）
renderer (Vite + React Router，本地)
└─ 桌面业务组件 + IPC ↔ 主进程；SSE 直连后端
         │ API (HTTPS)
         ▼
云端 api-server（LLM 推理 · Agent 运行时 · 数据持久化）
```

| 决策 | 结论 | 理由 |
|------|------|------|
| renderer 加载 | 本地（prod 走 custom protocol `app://`） | 给本地 renderer 正式 origin，CORS/CSP/cookie 可控 |
| SSE 连接 | renderer 直连后端（非 IPC 转发） | 长连接高吞吐，IPC 序列化开销不可接受 |
| 本地数据层 | ⏳ sql.js（读缓存优先，非离线写） | **未实现**；当前会话数据由后端 API 提供 |
| 组件策略 | 桌面端独立演进（非共享 packages 业务组件） | 桌面优先 = 自由演进 > 两端一致 |

### 7.3 安全基线

- `contextIsolation: true` + `sandbox: false`（当前）+ renderer 关 `nodeIntegration`
- 原生能力只经 preload 白名单 IPC 暴露
- httpOnly cookie 持久 session（Chromium OS DPAPI/Keychain 加密），不额外上 safeStorage

> 注：原设计目标 `sandbox: true`；当前 `webPreferences.sandbox` 为 `false`（→ 见代码 `main/index.ts`）。

### 7.4 OS 深度集成

- **资源管理器右键菜单**：安装期注册 shell verb，`--analyze <path>` 唤起，授权目录后 Agent 可读
- **文件关联双击**：electron-builder `fileAssociations`，`.md/.txt/.json/.csv/.yaml` 等
- **全局热键**：Spotlight 式召唤悬浮窗
- **Taskbar/Dock badge**：未读通知计数
- **深链**：`openDeepLink(target)` 统一入口，本地 renderer 已加载走 React Router，否则 `loadURL`

### 7.5 通知双通路

| 通路 | 覆盖场景 | 机制 |
|------|---------|------|
| 方向 A：SSE 桥接 | 桌面正持有 SSE 流 | `approval_required` / `ask_user` / `message_end` 等事件桥接到原生通知 |
| 方向 B：Attention 轮询 | 未持有 SSE 流的盲区 | `GET /v1/attention`（45s 间隔），补 durable paused turns + pending approvals |

A+B 共用去重集（稳定 id），避免同一待办双弹。

### 7.6 自动更新

- 静默下载 + 用户决定安装时机
- 检查频率：启动 + 每 4h + `powerMonitor 'resume'`
- 远程熔断：后端 `updates_enabled = false` 才暂停（fail-open）
- 双通道：beta / stable；灰度分桶 `stagingPercentage`
- 平台：Windows + macOS（Win 优先），Linux 附带

### 7.7 关窗 = 隐藏到托盘

attention 轮询 + SSE 挂在渲染进程，`close` 默认 `preventDefault + hide`（仅 `before-quit` 放行销毁）。可配置：托盘开关「关闭时最小化到托盘」。

---

## 八、本地文件系统设计 ✅ 已确定

> 把本机磁盘文件提升为 **Agent 可直接读写的一等对象**。核心原则：**本地 = 唯一真身，云端不持久化文件字节**。

### 8.1 隐私边界

| 边界 | 含义 | 策略 |
|------|------|------|
| **at-rest** | 不把文件字节/正文存进服务端 | 默认守住；用户显式「备份/分享」为知情例外 |
| **in-transit** | Agent 读文件时正文临时发给 LLM | 接受（同 Cursor 把代码发给模型） |

铁律：Agent 要理解文件 → 正文必须进 prompt；at-rest 默认不外发。

### 8.2 Client-Tool 桥架构

> **实现现状**：`client_tool_requested` SSE 桥、IPC 回传 `client-tool-result` **均未实现**（→ 见代码 `services/streamConversation.ts` 事件类型预留）。

云端运行时 + 本地文件桥（与 Cursor / Claude Desktop 同构）：

```
云端 executor 命中 client-bound 工具（文件读写）
  → emit SSE client_tool_requested{op, params}
  → Redis BLPOP 阻塞（keyed by conv_id + tool_call_id）
[SSE] → Electron 渲染层 → IPC → 主进程 fs-service 在授权根内执行
  → POST /v1/conversations/{id}/client-tool-result 回传
  → resolve → 续跑 ReAct
```

设计选择：
- **原地阻塞而非 durable 快照**：文件读写秒级；桌面须在线才能执行——断连即 cancel、本轮干净失败
- **不落 Postgres**：client_tool 请求是临时态，持久化会把路径写库违 at-rest，只用 Redis
- **安全护栏**：桌面 fs 桥的授权根 allowlist + realpath 边界校验，云端视为不可信请求方

### 8.3 对话产物落点

由「本轮有无可写本地根」一个开关决定：

| 运行语境 | 落点 |
|---------|------|
| 桌面 + 有可写根 | 只挂本地写工具，产出写本地原件 |
| 桌面 + 无根（随手新对话） | 懒创建：首次写文件时在默认根创建目录，之后同上 |
| 无桌面（headless / 自动化） | 禁产：不挂写工具；要产物须声明显式出口（webhook 等） |

### 8.4 文件树设计原则

- **一棵本地主树**：以本地授权目录为根，非"本地/云端两个平级源"
- 无全局树 store——各目录节点各自懒读子项并 watch，互不串扰
- 选中/改名/新建态由组件局部持有、单向下传
- fs 操作在 IPC 边界返回判别结果（`{ ok, reason }`）而非抛异常
- `fs.watch` 跟随展开状态，磁盘变更即刷新

### 8.5 本地目录授权模型

- 授权 = 持久契约（非会话参数）——per-target 显式授权
- 作用域变更对用户与 Agent 同等显式
- 兜底只追加、不替换、留痕迹
- 绝对路径不出本机（错误语言按受众分层）

### 8.6 项目身份模型（dotfile）

> **实现现状**：`.agentcore/project.json` dotfile **未实现**。

**本地文件夹 = 项目**：文件夹根下 `.agentcore/` dotfile 记录云端项目编号（类比 `.git/`），桌面打开即认、换机即认、零绑定仪式。

```
<项目文件夹>/
  .agentcore/
    project.json     # { schemaVersion, id, name, createdAt }
    rules/*.md       # 文件夹级规则
    memory/*.md      # 文件夹级记忆
```

**决策：云端权威 + dotfile 记号**（否决本地发号）。云端继续发编号、管对话，dotfile 只负责"记号"——保留手机/网页/平板等无本地文件夹设备的接入能力（纯本地身份会从结构上焊死手机端）。

| 旧（per-device 绑定） | 新（dotfile 自带） |
|---|---|
| 映射存设备 `preferences`，换机丢失须重绑 | dotfile 跟文件夹走，同步即带、打开即认 |
| 解析方向：云端号 → 查设备表 → 路径 | 解析方向：打开路径 → 读 dotfile → 云端号 |

**复制文件夹（同号）**：学 Git 对 `cp .git/` 的态度——允许两副本指同一云端项目，并发写云端时复用三方冲突检测（冲突保留两份、绝不自动覆盖）；要独立副本须显式"另存为新项目"。

---

## 九、前端运行时架构模式 ✅ 已确定

> 渲染层的组件组织、状态归属和数据流决策。

### 9.1 组件组织原则

按职责分层，非按功能域：

| 层 | 职责 | 判据 |
|---|---|---|
| `components/` | 纯渲染 | 只消费不持有状态 |
| `pages/` | 路由页 | 页面级组合 |
| `stores/` | Zustand 状态 | 所有状态 |
| `services/` | SSE 流与副作用 | 流式会话消费（`streamConversation.ts`） |
| `lib/` | 纯函数 / API client / mapper | 无副作用的可纯计算 |

`components/` 默认扁平、仅当功能多文件协作内聚成体系才开子目录。刻意不预先按功能域切深目录——浅层 + 少数子目录比深路径更易定位。

### 9.2 Store 关键拆分

> **实现现状**：`useRunsStore` / `useDetailPanelStore` / 按 messageId 隔离 / `message.runs` 快照 **均未实现**。当前为 `useConversationStore` + `useExecutionStore` + `useGraphStore`（→ 见代码 `stores/`）。下表为设计目标。

| Store | 职责 | 关键决策 |
|---|---|---|
| `conversation` | 消息列表 + 消息状态机 | 不含执行态（**已落地**） |
| ⏳ `runs` | 统一 run 树 | 独立于 chat，按 messageId 隔离执行槽（**未实现**） |
| ⏳ `detail-panel` | 面板 tab | 仅存引用（指针），不存数据（**未实现**） |

**run 树独立成 store** 是核心决策：执行态（编排步骤 + 委派子 Agent 统一为一种 run）与消息态正交，分开既防 chat reducer 膨胀、又让 run 按 messageId 隔离。

### 9.3 按 messageId 隔离执行态

> **实现现状**：**未实现**。当前 `useExecutionStore` 为会话级全局执行态。

`useRunsStore` 用 `byMessage: Record<messageId, RunExec>`，每条消息只读自己的执行槽——若用全局单例，同一会话连发多回合时第二条消息会读到上一回合残留步骤（节点串台）。

`message_end` 时把该槽 snapshot 为 run 树存进 `message.runs` 持久化供回放，随即 clear 释放实时槽，长会话不累积。

### 9.4 单一数据源、两层投影

聊天区分成两层：**inline 信号层（状态轨）** 和 **详情面板层**：

- 两层同口径取数——实时优先执行槽，回落快照
- 面板 tab 只存引用不存数据（渲染时现从 run 树取），流式中 run 树更新 → 面板自动跟随
- 编排步骤与委派子 Agent 是同一种 run，面板用同一套下钻（不区分来源）

### 9.5 事件归属显式化

所有后端事件在出口统一带 `message_id`，前端各 handler 一律按 `event.message_id` 归属。无差别注入 = 新增事件类型零改动自动路由，无「可路由事件白名单」需维护。

---

## 十、设计系统 ✅ 已确定

> **实现现状**：视觉令牌定义在桌面端本地 `apps/desktop/src/renderer/styles/globals.css`（**非**共享包 `theme.css`）。部分内容（跨端共享 `theme.css`、全端令牌统一）为远期目标。

### 10.1 令牌唯一源

桌面端当前以 `globals.css` 为唯一令牌源。跨端共享 `theme.css` 为远期目标，改令牌改 `globals.css`。

### 10.2 色彩令牌设计

统一 **OKLCH** 调色，**hue 255 蓝为品牌色**：`primary` / `ring` / `info` 走品牌蓝（主 CTA、活跃、进行中），中性表面带极淡同色相冷调，整体归为一个色族。状态色为一等令牌：`success`（绿，成功/已完成）、`warning`（琥珀，警告/检查点）、`info`（蓝，信息）、`destructive`（红，错误/停止）；`accent` 是中性 hover 表面，**不表达状态**。侧栏并入标准 `sidebar` 令牌族（含 `sidebar-primary` / `sidebar-ring`），保留 `--sidebar-gradient` 竖向渐变（`bg-sidebar` 叠加渐变 = 极淡蓝调浅底）。

令牌清单、语义、「执行状态 → 令牌」映射与禁用项（禁硬编码调色板/hex、禁 `hsl(var(--…))` 包裹、禁拿 `accent` 冒充成功色）统一由规则约束 → 见 `.cursor/rules/color-tokens.mdc`；令牌值见 `apps/desktop/src/renderer/styles/globals.css`（`:root` / `.dark`）。

### 10.3 中性色冷调

中性色（背景/表面/边框/文本）在 `globals.css` 的 `:root` / `.dark` 直接带极淡 hue 255 冷调，与品牌蓝同色相，整屏归为一个色族。桌面端是唯一客户端（见 §七），无需 `.theme-desktop` 之类 per-端 scope 隔离——直接着色即可，不再走"中性基色 + 桌面变体覆盖"的跨端模型。

### 10.4 暗色模式

Light-first，但脚手架已就绪：`globals.css` 的 `.dark` 覆盖块已是完整 OKLCH 令牌，`@custom-variant dark` 也已配置。当前仅缺「主题切换器 + 给 `<html>` 挂 `.dark`」的入口，补上即可启用。

### 10.5 组件复用边界

| 层 | 策略 |
|---|---|
| **令牌** | 全端共享（已统一） |
| **原语**（button/input/dialog） | 共享包已有，桌面端不强迁，新组件上机会性收敛 |
| **域卡片** | 不设共享包。等有真实跨端消费者再从桌面端提取 |

---

## 十一、桌面端插件架构 ✅ 已确定

> **实现现状**：插件注册表、`getPluginRoutes()`、`getPluginNavItems()`、插件 IPC 前缀 **均未实现**（→ 见代码 `main/index.ts` 仅窗口 + fs IPC）。以下为架构设计目标。

### 11.1 插件分类

| 类型 | 特征 | 示例 |
|------|------|------|
| 垂直工具 | 有独立页面、可能有本地存储 | 法规库、表格、Arena |
| 基础设施 | 有独立生命周期、push 事件 | 文件系统、MCP |
| 轻量工具 | 仅主进程 I/O + 简单页面 | 脱敏、日程 |

### 11.2 六贡献点（类比后端 Vertical）

| 贡献点 | 消费方 |
|--------|--------|
| 路由注册 | router（经 `getPluginRoutes()` 注入） |
| 侧栏导航 | app-shell（经 `getPluginNavItems()` 注入） |
| IPC Handlers | 主进程注册表（自动加 `app:{id}:` 前缀） |
| 生命周期 | 应用启动/退出时调用 |
| 推送事件 | 主进程 → renderer 单向推送 |
| 跨插件服务 | 其他插件经 `getPluginService()` 调用 |

### 11.3 IPC 类型安全

三层结构建立编译时契约：

```
plugins/{name}/ipc-schema.ts   ← 单一真相源
        ↓
plugins/{name}/main.ts         ← TypedHandlers<Schema> 约束实现
        ↓
preload/index.ts               ← IpcBridge<Schema> + AssertIpcBridge 断言
```

新插件必须先写 schema 再写 handler；旧插件按需迁移。

### 11.4 错误隔离

每个插件路由自动包裹 ErrorBoundary（注册表注入，插件开发者无感知）。单插件崩溃仅影响其自身路由区域，不触发全局错误。

### 11.5 设计决策

| 决策 | 理由 |
|------|------|
| 显式注册（非约定扫描） | 当前体量下显式 import 更可控、可 tree-shake |
| IPC Schema 渐进迁移 | 避免大爆炸重写 preload |
| ErrorBoundary 注入在 registry | 零额外代码 |
| 路由嵌套在统一工具布局下 | 侧栏/顶栏/面包屑一致 |

---

## 十二、SSE 事件消费规范

后端 SSE 事件（定义于执行引擎 §十二）到前端 Zustand Store 的映射关系。消费入口为 `services/streamConversation.ts`（单次 `fetch` + `AbortController` 本地中止；**无**断线重连 / 401 刷新 / 服务端 stop 接口）。

### 事件→Store 映射表

#### 已接线（`dispatchSSEEvent` 已实现）

| SSE 事件 | 目标 Store | 更新字段/行为 | UI 组件影响 |
|----------|-----------|-------------|-----------|
| `message_start` | conversation | 创建新助手消息条目 | MessageList 追加气泡 |
| `content_delta` | conversation | `appendToLastMessage` 追加文本 | MessageBubble 流式 Markdown |
| `message_end` | conversation + execution | 消息标记完成；execution 收口状态 | 流式结束，按钮状态恢复 |
| `error` | conversation | 追加错误文本并 finalize | MessageBubble 显示错误 |
| `title_generated` | conversation | 更新对话标题 | Sidebar 标题更新 |
| `run_plan` | execution | 设置计划结构（agents + steps） | TaskCard 出现；GraphView 从 execution 派生节点 |
| `run_started` | execution | 步骤→running、Agent→working | AgentNode `animate-pulse` |
| `run_output_delta` | execution | 追加 Agent 输出增量 | TaskCard 内 Agent 输出 |
| `run_completed` | execution | 步骤结果 + Agent→completed | 节点状态更新 |
| `run_failed` | execution | 步骤→failed、Agent→error | 错误状态展示 |
| `run_progress` | execution | 更新聚合进度（completed/total） | TaskCard 进度 |
| `approval_required` | execution | 设置 pendingCheckpoint、status→paused | CheckpointCard |
| `approval_resolved` | execution | 清除 checkpoint、恢复 running | CheckpointCard 关闭 |
| `tool_use_start` | execution | 添加工具调用记录，状态=running | TaskCard 工具区 |
| `tool_use_end` | execution | 更新工具调用结果 | TaskCard 工具结果 |

#### 类型预留（`types/events.ts` 已定义，handler 未实现）

| SSE 事件 | 设计目标 Store | 说明 |
|----------|---------------|------|
| `reasoning_delta` | conversation | 追加推理过程增量 |
| `ask_user_requested` | execution + ui | 添加待回复项；聚焦输入框 |
| `run_retrying` | execution + graph | Agent 状态→retrying |
| `task_state_updated` | execution | 更新工作记忆快照 |
| `pipeline_summary` | execution | 存储执行汇总 |
| `client_tool_requested` | — | 本地文件桥（见 §8.2，未实现） |

### SSE 连接管理

| 场景 | 行为 |
|------|------|
| 发送消息 | `POST /v1/conversations/{id}/messages` 返回 SSE 流（单次 `fetch`） |
| 本地中止 | `AbortController.abort()` 中断当前 fetch |
| ⏳ 断线重连 | **未实现**（原设计：指数退避 1s/2s/4s，最多 5 次） |
| ⏳ 401 过期 | **未实现**（原设计：静默令牌刷新后重建连接） |
| ⏳ 恢复暂停 | **未实现**（原设计：`POST .../resume` 返回新 SSE 流） |
| ⏳ 停止生成 | **未实现**（原设计：`POST .../stop` + 前端 close SSE） |

### 消费层实现模式

SSE 事件经 `dispatchSSEEvent` 统一消费，按 `event.type` 路由到对应 Store（单一入口，不在组件中直接监听）：

→ 见代码：`apps/desktop/src/renderer/services/streamConversation.ts`（SSE 流消费与事件分发）、`stores/`（各目标 Store）

### 设计约束

- **单一入口**：所有 SSE 事件经 `services/streamConversation.ts` 统一分发，不在组件中直接监听（`services/sse.ts` 已删除）
- **Store 最小更新**：每条事件只更新必需的 Store 字段，不做全量替换
- ⏳ **对账兜底**：`reconcileAllRuns()` **未实现**
- **乐观更新**：发送消息时立即在 conversation store 创建 user message 条目，无需等后端确认

---

## 十三、开放问题

- [ ] Agent Panel 是否需要支持拖拽调整宽度？
- [ ] 是否需要"极简模式"——只显示聊天，隐藏所有面板？
- [ ] 暗色/亮色主题切换的优先级？
- [ ] 本地 MCP 打包内置策略（dev 模式 vs 生产包）？
- [ ] 代码签名证书方案（影响 Win11 顶层右键菜单和 SmartScreen）？
