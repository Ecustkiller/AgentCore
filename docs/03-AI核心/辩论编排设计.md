---
status: landed
code: apps/server/agentcore/tools/builtin/debate.py
related:
  - docs/03-AI核心/运行时总览.md
  - docs/04-前端/前端UX设计.md
skip_if:
  - 只改普通 delegate DAG（读编排器）
---

# 辩论编排设计

> **定位**：辩论 / 交叉审查能力的**单一权威源**。把辩论从「一个执行形状（`stance`/`round` 展示标记 + CEO 手搓 DAG）」重设计为「一个由**主持人（Moderator）**驱动、**过程与结论双产物**、为用户决策与认知负责到底的产品能力」。
>
> **状态**：✅ 已落地现状。主持人循环、三形态、双产物、辩手跨轮带记忆、辩论视图均已实现并过测试（含 conformance 跨端对齐）；底层执行复用现有 DAG 调度，未重建引擎。
>
> → 见代码：CEO 工具 `tools/builtin/debate/`（`tool.py` + `schema.py`）、主持人循环 `runtime/debate/moderator.py`、数据模型 `runtime/debate/types.py`、事件 `runtime/events/run.py`（收场 `debate_result` + 逐轮增量 `debate_round_started` / `debate_round`）与 `runtime/events/interaction.py`（逐轮交互 `debate_round_decision_required` / `_resolved`，§六）、桌面呈现 `apps/desktop/src/renderer/components/chat/debate/`（双产物面 `DebateBody`（在 `DebateCompare.tsx`）+ 归一 `model.ts`，挂于画布放大态「交锋叙事」页 `components/graph/CanvasZoomedTurn.tsx`；**聊天不再内联辩论卡**）+ 逐轮决策卡 `components/chat/DebateRoundDecisionCard.tsx`（指挥台）、手机呈现 `apps/mobile/src/components/DebateView.tsx`。

---

## 核心定位与重设计动机

辩论是 MVP Day1 范式之一（见 [`Agent协作模式.md §一`](/docs/03-AI核心/Agent协作模式.md)）。旧设计把它压成 `delegate` 上的三个展示标记（`stance`/`group`/`round`，display-only）+ 一段教 CEO 手动展开跨轮 DAG 的 Skill 配方，执行器/调度器完全忽略这些标记。这在「形状是数据不是模式」的技术教条下是自洽的，但**从用户需求倒推，它牺牲了辩论真正的价值**：

| 旧设计的产品级缺陷 | 后果 |
|---|---|
| 「轮次」是技术概念泄漏到产品层 | 让 CEO（甚至用户）去管理「第几轮、谁依赖谁」，把实现细节当成了功能 |
| 产物缺失 | 跑完只有「正反并排卡 + CEO 一段简短概览」，再甩 `ask_user` 让用户**自己**在正反里选——辩论很热闹，却没为用户的决策负责到底 |
| 辩论质量无人把关 | 正反可能各说各话（没真交锋）、强度不对等、轮数到了却没收敛 |
| 二元 pro/con 过度简化 | 真实需求里的「红队单向批判」「多方圆桌」被硬塞进正/反两栏 |
| 老板插不上话 | 辩论一旦开打就是黑盒跑完，用户无法中途掌舵 |

**重设计主张**：辩论需要一个**有状态的主持人层**来承载「发起对抗 → 逐轮判收敛 → 裁决 → 产出过程与结论」。当初否决「独立 Arena 子系统」（独立 SSE / 状态机 / `arena` RunKind）否得对——那是过度工程；但连带把「辩论需要主持人」也否了，是把洗澡水和孩子一起倒了。本设计把主持人加回来，**但它不是独立执行引擎，而是 CEO 之下、辩手之上的一个编排角色**，底层仍用现有的并行委派 + 上游产物注入执行。

> **用户拍板的四条方向**（本设计的地基）：① 辩论的**过程本身也是一等产物**（不为出结论而降级过程）；② 引入主持人/裁判一等角色；③ 支持红队单向 + 多方圆桌（破二元）；④ 轮次由收敛自停、对用户隐藏（用户/CEO 永不设轮数）。

---

## 一、产品目标与三问

**目标**：用对抗性多视角思考，帮用户得到一个他能信、考虑过反面的**结论或决策**，同时让他能看懂**思想如何交锋**。

| 三问 | 答案 |
|---|---|
| 谁用 | 面向大众的决策者 / 学习者 / 方案作者 |
| 解决什么真实问题 | ① 帮我做决策（选 A 还是 B）；② 压力测试我的方案（红队挑刺）；③ 学懂一个有争议话题（多方观点光谱）；④ 娱乐性思辨（少数） |
| 产品上如何呈现 | 用户只抛「问题」或选一个「形态」，不碰任何参数；产出**过程（交锋叙事线）+ 结论（决策简报）双产物** |

**核心洞察**：用户要的几乎从来不是「轮次」这个过程指标，而是辩论的**产物**——更可信的结论 + 看得懂的交锋。「轮次」是手段，必须对用户隐藏、由系统自调。

---

## 二、支点：主持人（Moderator）循环

主持人是「**主持 + 裁判 + 书记**」三合一的有状态编排角色。一轮循环五步：

1. **定本轮议题**：首轮把用户问题拆成争议焦点；后续轮基于上轮未决分歧设本轮焦点。
2. **派各方发言**：底层 = 一波并行委派（辩手 worker），自动注入对方上轮产物（复用执行器现成的「## 前置结果（来自 X）」注入，见 [`执行引擎架构设计.md`](/docs/03-AI核心/执行引擎架构设计.md)）。
3. **裁判本轮**：有没有**真交锋**（反方是否真回应了正方，而非各说各话）？是否**还在产生新论点**？
4. **写本轮小结**：争议焦点 / 已达成共识 / 仍存分歧——这是「过程产物」的骨架。
5. **决策下一步**：收敛 → 出决策简报收场；未收敛 → 进入下一轮；遇关键分歧 → 可选请老板掌舵（见 §六）。

> 「轮数」= 这个循环跑了几次，由第 3 步的收敛判断决定。**用户和 CEO 都不再声明轮数**——这是旧「CEO 一次 `delegate` 静态展开 2N 个节点」的根本性改变（旧法轮数先验固定、无法运行期收敛）。

**主持人 ≠ 通用语义裁判**：[`多轮编排与队员热修.md §七`](/docs/03-AI核心/多轮编排与队员热修.md) 决定「暂不做判『写得好不好』的通用 LLM 质量校验，沿用机械 contract」。本设计不触碰该决策——主持人的「裁判」是**辩论领域内的交锋质量与收敛判定**（判「是否针锋相对、是否还在产生新论点」），是辩论这个能力的固有职责，不是给普通 worker 产物做通用质量门。两者语义正交。

---

## 三、三形态统一模型（破二元）

参与方从 `pro/con` 泛化为「**立场标签**」（支持 / 反对 / 某视角名 / 红队 / 方案方），一套主持人循环参数化出三形态：

| 形态 | 参与方 | 交锋模式 | 收敛判据 | 产物侧重 |
|---|---|---|---|---|
| **正反辩论** | 正 / 反 2 方 | 对称攻防 | 无实质新论点 / 焦点已澄清为价值之争 | 简报先行 |
| **红队挑刺** | 你的方案（被审）+ 1~N 红队 | 单向攻击 → 方案方回应 | 风险挖尽 / 方案方已修补 | 风险清单 + 加固建议先行 |
| **多方圆桌** | 3~N 个视角 | 多边碰撞 | 观点光谱已铺满 | 过程 / 观点地图先行 |

用户只选「形态」（甚至只抛问题、由 CEO 自动判定是否值得对抗性论证），**永不碰参与方数量 / 轮数 / 依赖**。

---

## 四、双产物：过程与结论都是一等

辩论结束**交付两样，都做好，不分主次**（用户拍板第①条）：

### 4.1 结论产物 —— 决策简报

- 争议焦点（双方真正分歧在哪）
- 各方去水后的**最强论点**
- **关键事实分歧 vs 价值/偏好分歧**：前者 AI 能据证据帮判，后者必须交用户定
- 主持人的倾向性判断 + **置信度**（如「基于事实反方更站得住；但若你更看重 X，则正方成立」）
- 给用户的建议 / 仅剩需你拍板的点

### 4.2 过程产物 —— 交锋叙事线（三层渐进披露）

这是「过程本身有用」的落地，不是旧的「正反两栏平铺」，而是被主持人组织过、带攻防关系的叙事：

| 层 | 内容 | 服务对象 |
|---|---|---|
| **L1 焦点小结流** | 每轮一句焦点 + 小结，串成一条「认知推进线」 | 速读者：30 秒看懂整场怎么演进 |
| **L2 逐轮攻防** | 每轮 = 本轮议题 + 各方发言 + 主持人点评（谁回应了谁、谁让步 / 新增了什么） | 理解者：看思想如何被攻击与捍卫 |
| **L3 论点级全文 + 交锋边** | 各方完整发言（按 `run_id` 取辩手节点）+ 论点级交锋边「谁驳谁」（裁判逐轮抽取的 `from→to` + 驳的要点，载于 `clashes`，前端据本轮 `sides` 解析成名字/身份色） | 深读者：较真细节、看清谁针对性反驳了谁 |

### 4.3 呈现顺序按形态自适应

- **决策类**（正反 / 红队）：决策简报先行，过程叙事线紧随其后可展开。
- **探讨 / 学习类**（多方圆桌、纯思辨）：过程叙事线先行，简报作为「小结」收尾。

前端落点见 [`前端UX设计.md §四`](/docs/04-前端/前端UX设计.md)。

---

## 五、收敛与轮次治理

**收敛完全由主持人逐轮自判**（§二第 3 步裁判的 `converged`）——没有任何「最小轮数」机械门槛强制多轮。**终止条件**（满足其一即停）：

1. **收敛**：裁判判定各方无实质新论点（开始重复）。
2. **焦点澄清**：分歧已归结为价值 / 偏好之争（AI 判不了，交用户）。
3. **红队耗尽**：无新风险可挖（红队形态专用）。
4. **安全上限**：达到 `max_rounds` 硬上限——纯保险丝（防失控的断路器兜底，**非目标值**，收敛永远可早于它发生）。

前三条都是裁判的判定结果；第 4 条是与内容无关的硬上限。

**「别过早收敛」搬进裁判的逐轮标准**（而非外部计数兜底）：保护过程质量（呼应用户第①条）靠的不再是「跑满 N 轮才许收」，而是喂给裁判逐轮的收敛偏好——

- **第 1 轮（开场立论）默认继续**：各方此时通常只是各自亮出立场、尚未真正接火（`real_clash=false` 是常态），裁判默认判「未收敛」以逼出下一轮真交锋；**仅当**命题空泛到开场就无新论点、无可再辩时才许收。
- **`thorough` 调收敛松紧**：`True`（认真辩透）= 仍有新论点 / 未决关键交锋时不轻易收，挖尽实质分歧；`False`（快速）= 核心交锋一清晰即收、不恋战。
- **快速单轮**（`max_rounds=1`）：一次对碰即收，裁判不强求多轮。

这根治了旧机械楼层的病：trivial 命题（「测试一下」「随便聊聊」）被强逼跑满 N 轮 → 续写者每轮输入几乎相同 → 产出冗余的「修订 v2」节点。

| 形态 / 模式 | `max_rounds`（安全上限） | `thorough` |
|---|---|---|
| 正反 / 红队（认真辩透） | 5 | `True` |
| 圆桌（探讨型，铺光谱即可、略紧） | 4 | `True` |
| 快速对碰（任意形态，轻量意图） | 1 | `False` |

`thorough=False` 对**所有形态**（含圆桌）一律快速单轮——轻量意图不该被强制多轮。轮数永不暴露给用户设定：用户只选形态（或由 CEO 据意图判 `thorough`），收敛由主持人逐轮自调，`max_rounds` 只兜底。

---

## 六、老板可掌舵：逐轮交互（opt-in）

用户是老板，应能在辩论中途介入（不微操辩手，只对主持人表达，承袭 [`多轮编排与队员热修.md §一`](/docs/03-AI核心/多轮编排与队员热修.md) 的心智）。✅ 已落地：**opt-in 的逐轮交互**——主持人在每轮边界停一次，把「再辩 / 加角度 / 收场」的方向交回老板。

### 6.1 触发与门槛（默认不打扰）

- `debate(interactive=true)` 才武装逐轮交互；默认 `false` = 辩论一气呵成（旧行为，不停）。
- 二次门槛 `interactive_armed`（= 服务端 `checkpoint_enabled`）：检查点整体关闭时辩论也绝不挂起，与其它检查点共用一个总开关。
- 仅当「武装 + 有活跃用户在线应答」时主持人才停；超时 / 无人应答 → 回落裁判的自动收敛，辩论永不卡死。

### 6.2 在哪停、谁说了算

- 主持人**每轮小结后、判收敛前**触发一次轮边界（`Moderator.run` 的 `on_round_boundary` 钩子，§7.1；与 emit `debate_round` 叙事的 `on_round` 是两个正交钩子）。
- 用户的选择**覆盖**裁判的自动判定：裁判要收敛但用户选「继续辩」→ 续一轮；裁判要续辩但用户选「够了」→ 提前收场（新 `stop_reason=user_concluded`）。
- 不答 / 超时 → 决策记 `timeout`，按裁判原判推进（收敛则收场、未收敛则续轮）。

### 6.3 三选一决策卡

每个轮边界 emit 一张决策卡，卡上带裁判对本轮的判读（`converged` / `rationale`，作为默认建议高亮），三选一：

| 选择 | 语义 | 落到引擎 |
|---|---|---|
| 继续辩一轮 | 再来一轮（裁判要收也续） | `continue`，无 focus |
| 按此角度继续 | 给下一轮指定一个议题角度 | `continue` + `focus`（覆盖下一轮焦点） |
| 够了，出结论 | 立即收场出简报（裁判没收也收） | `conclude`（`stop_reason=user_concluded`） |

> 蓝图原列「继续辩 / 追加角度 / 聚焦到 X / 够了」四项；「追加角度」与「聚焦到 X」同为「给下一轮一个 focus」，合并为「按此角度继续」一项 → 三选一。

### 6.4 机制：进程内挂起（非耐久检查点，落地修正）

蓝图设想「复用 `plan_review` / `ask_user` 的耐久挂起」；落地修正为**进程内挂起**——

- **为何不耐久**：辩论的整个状态——主持人循环、各辩手的 `RunSession`——活在**正在 `await` 的协程里**，无法像 plan 检查点那样耐久序列化重建。故轮边界走进程内挂起（`runtime/interaction.py` 的 `InteractionRegistry`，与阻塞式 `escalate` 同源的 ClientRequestBridge），新增 `InteractionKind.DEBATE_ROUND`，**不写 `TurnSuspension`、不进 journal**。
- **结算路由**：前端把选择 POST 到统一 resolve 端点（`POST /v1/conversations/{cid}/interactions/{id}`，`kind=debate_round`，载 `decision` + `focus`，schema `ResolveDebateRoundInteraction`）→ 唤醒挂起的协程。
- **事件**（一对，**transport-only**，不进 journal，与 `debate_round` 同辙）：`debate_round_decision_required`（轮边界 emit，带 `decision_id` + 本轮 focus/summary + 裁判 `converged`/`rationale`）/ `debate_round_decision_resolved`（`continue`/`conclude`/`timeout` + `focus`）。卡片的「已继续 / 已结论」由 `_resolved` 实时流翻面，**不**由 POST 翻面（守单一数据路径）。
- **重载契约**：决策事件 transport-only → 重载后卡片不复现（`debateDecisions` 恒空），用户的选择已体现在**实际发生的轮次**与收场 `debate_result`（`user_concluded` 等 `stop_reason`），故一致。

### 6.5 前端落点

决策卡渲染在画布**指挥台**（`components/graph/CanvasDecisionPanel.tsx`，与上报 / 检查点 / 放行同处），desktop-live、计入「待你拍板」角标；与 `RunEscalation.id` 同例**从 conformance `ProjectedTurn` 剥离**（transport-only，不参与跨端对齐）。卡件 `components/chat/DebateRoundDecisionCard.tsx`，呈现见 [`前端UX设计.md §四`](/docs/04-前端/前端UX设计.md)。

---

## 七、技术落点（现状）

### 7.1 主持人 = `debate` 工具内的确定性编排循环（非 LLM 编排角色）

辩论是 CEO 的一个**编排工具** `debate`（`tools/builtin/debate/`，`ToolCategory.ORCHESTRATION`，与 `delegate` 同类、非终结）。CEO 识别对抗性意图 → 调 `debate(motion, form, sides[, thorough])` → 工具内驱动主持人循环 → 交回双产物供 CEO 用自己的声音收尾。

主持人 `Moderator`（`runtime/debate/moderator.py`）是一个**确定性 Python 循环**，不是自由发挥的 LLM agent：循环骨架（定议题 → 派辩手 → 裁判 → 小结 → 决策下一轮）由代码控制，每一步的判断（议题 / 裁判 / 小结 / 简报）各走一次 `provider.complete` 出结构化 JSON（坏 JSON 容错降级）。「怎么派一轮辩手」经 `RoundRunner` 协议注入（由 `DebateTool` 实现），主持人本身只管编排与判定。

- **为何是确定性循环而非 LLM 编排角色**（对蓝图初稿的修正）：初稿设想「主持人 = `can_delegate` 内置编排角色（depth 1 的 LLM agent），CEO 委派给它」。实现改为确定性循环——① 循环 / 收敛 / 轮次治理是确定逻辑，交代码比交 LLM 自由发挥更可控、可测（注入返回脚本化 JSON 的 fake provider，零成本测全流程 / 收敛 / 双产物）；② 省掉一层 LLM 编排 agent 的 token 与不确定性；③ 仍满足「主持人是 CEO 之下、辩手之上的一层」——只是这层是工具内的循环，不是图上一个 LLM 节点。
- **委派深度**：辩手是 `depth+2` 的叶子（CEO depth 0 → 工具内 `build_run_plan(depth=self._depth+2)`），仍卡在 `MAX_DELEGATION_DEPTH=2` 内（见 [`执行引擎架构设计.md §8.4`](/docs/03-AI核心/执行引擎架构设计.md)）。

### 7.2 复用现有执行地基（不重建）

| 能力 | 复用项 |
|---|---|
| 首轮各方发言 | `build_run_plan` 一波并行辩手 + `build_agent_executor` + `WaveScheduler`（执行器现成） |
| 辩手跨轮带记忆 | `RunSession` 留人 + `continue_run` 续写：`DebateTool` 按 `side.key` 留住每个辩手的 session，第 k 轮对同一辩手 `continue_run`，把对方上轮论点当 feedback 注入，辩手在**自己的 transcript** 上续写——根治旧法「每轮全新失忆 worker」（[`多轮编排与队员热修.md`](/docs/03-AI核心/多轮编排与队员热修.md)） |
| 用量 / 账目 / 引用 | `WorkerResultAccumulator`：辩手各记一行账、主持人自身 LLM 调用折一行账（role=主持人），经 pipeline 折回回合总账，与 `delegate` 一致 |
| 中途挂起（逐轮交互） | 进程内挂起 `InteractionRegistry`（与阻塞式 `escalate` 同源，**非**耐久 `plan_review`/`ask_user`），`Moderator.on_round_boundary` 钩子挂载，见 §六 |

> 蓝图曾把跨轮续写写成「把 `revise` 工具下放给主持人」；实现直接用底层 `continue_run`（`revise` 的同源原语），无需下放工具——`DebateTool` 自己持有辩手 session 并续写。

### 7.3 节点结构：CEO → 主持人 → 辩手（主持人为完成态节点）

团队图呈现三层、进度正确收尾：

- **CEO 不进图**（与 `delegate` 一致，CEO 是主气泡）。
- **主持人是一个完成态节点**：`debate` 工具 emit `run_started`（`parent_run_id` = CEO 的 captain run）→ 收场 emit `run_completed`（带耗时 / 成本 / 简报摘要 / role=主持人）。团队进度因此把主持人计入并正确收尾，不再有「永久 pending 的编排节点」。
- **辩手挂在主持人下**（`parent_run_id` = 主持人 run_id），逐轮声明（前端 dedupe 跨轮重复声明）。展示标记仍 display-only：`stance` 仅正反 2 方有意义（builder 只认 `pro`/`con`），`group=debate:{form}`、`round=轮次`。

> **决策：主持人作为完成态节点**（落地时定）。备选「主持人不进图、辩手直接挂 CEO」会让团队图丢掉「谁在主持」这层、且进度分母不含编排成本；现选「主持人补全生命周期」，图体现 CEO→主持人→辩手 层级、进度准确。

### 7.4 双产物与 SSE

- **数据模型**（`runtime/debate/types.py`）：`DebateResult` = `brief`（决策简报）+ `rounds`（叙事线逐轮单元）+ `narrative_first`（按形态调呈现顺序）。
- **给 CEO 的文本**：`DebateResult.to_ceo_output()` 渲染简报 + L1 焦点小结流（L2/L3 全文不塞 CEO 上下文，省 token）。
- **收场事件（journal 权威）**：收场 emit 一个 `debate_result` 事件（`runtime/debate/types.py` · `DebateResult.to_event_payload()`：form / motion / stop_reason / sides / rounds / brief），承载全量叙事线（逐轮 焦点 / 裁判 / 小结）+ 决策简报；**各方发言全文不在事件里**（体量大），靠 `rounds[*].sides[*].run_id` 关联执行图的辩手节点取回。进 journal、可重载回放；契约过 conformance（`ProjectedTurn.debate` verbatim，桌面 / 手机两端 fold 对齐，见 [`protocol-conformance.mdc`](/.cursor/rules/protocol-conformance.mdc)）。
- **逐轮实时事件**：`Moderator` 每轮 emit 一对增量——`debate_round_started`（焦点定下、发言**前**）与 `debate_round`（裁判 + 小结**后**，携 focus / summary / verdict + 各方辩手 `run_id` + L3 交锋边 `clashes`，与 `debate_result.rounds` 逐轮单元同构）。二者 **transport-only**（不进 journal）：只服务进行中实时叠加——三端 fold 累积成 `ProjectedTurn.debateRounds`，让用户进行中就看到逐轮焦点 / 小结 / 裁判（圆桌 / 红队无 `stance`、过去进行中是空白，尤其受益）。**重载契约**：journal 无逐轮事件 → `debateRounds` 恒空、全量叙事线已在 `debate` 事件里，故一致。前端把进行中与收场逐轮当同构孪生**归一为单一 `DebateModel`**（live→收场 不卸载重挂、辩手按 `run_id` 跨相位复用；见 [`前端UX设计.md §4.1`](/docs/04-前端/前端UX设计.md)）。
- ✅ **老板中途掌舵**：opt-in 逐轮交互已落地（§六）——`on_round_boundary` 钩子在每轮小结后、判收敛前停一次，进程内挂起（`InteractionKind.DEBATE_ROUND`，非耐久）等用户「继续辩 / 加角度 / 出结论」，一对 transport-only 决策事件（`debate_round_decision_required` / `_resolved`）驱动指挥台决策卡。

---

## 八、被否决 / 决策演进

- **独立 Arena 子系统**（独立 SSE + 状态机 + 阶段轮转引擎 + `arena` RunKind）——**仍否决**。主持人不是独立执行引擎，底层复用 DAG 调度；本设计只补回「主持 / 收敛 / 产物」的产品层。
- **旧法：CEO 一次 `delegate` 静态展开 2N 节点 + 手搓跨轮 `depends_on`**——**被替代**。理由：轮数先验固定（无法运行期收敛）、CEO 手搓交错依赖图易错、`round` 与 `depends_on` 无一致性校验。改由主持人逐轮推进。
- **`round` 展示标记**——从「CEO 预先拍、可能与依赖脱节的 display-only 字段」**降级**为「主持人逐轮推进时自然产生、天然与执行一致」的叙事单位。旧的「round/依赖脱节」根因随之消失。
- **机械「最小轮数」楼层**（`min_rounds`，达门槛前不许判收敛）——**被替代**为「主持人逐轮自适应收敛」（§五）。理由：楼层不看内容、把 trivial 命题也逼满 N 轮，续写者每轮输入近乎相同 → 产出冗余的「修订 v2」；「别过早收敛」的意图改由裁判逐轮标准承载（第 1 轮默认继续、`thorough` 调收敛松紧），`max_rounds` 仅留作纯安全上限。`RoundPolicy` 随之去掉 `min_rounds` 字段、新增 `thorough: bool`。
- **二元 pro/con 专属**——**被替代**为「立场标签」泛化模型（§三），支持红队 / 圆桌。
- **主持人 = LLM 编排角色（depth 1 的 `can_delegate` agent）**——**落地时改为**「`debate` 工具内的确定性循环 + 每步嵌入式 LLM 判定」（§7.1）。理由：循环 / 收敛逻辑确定化更可控可测、省一层编排 agent 的 token 与不确定性；「主持人在 CEO 与辩手之间」这一层由工具内循环 + 图上完成态节点表达，无需是图上一个 LLM 节点。
- **辩论独立 SSE 通道 / 散乱事件族**——**仍否决**。收场全量叙事线由单个 `debate_result` 结构化事件承载（§7.4，journal 权威，发言全文走辩手 run、靠 `run_id` 关联）；逐轮实时只补一对**克制的 transport-only 增量**（`debate_round_started` / `debate_round`，§7.4 已落地）——走现有 SSE、不进 journal、不另起通道、不铺成事件族。逐轮交互的决策事件（`debate_round_decision_required` / `_resolved`，§6.4）同样克制成一对、同走现有 SSE。
- **逐轮交互复用耐久 `plan_review` / `ask_user`**——**落地时改为**「进程内挂起」（§6.4）。理由：辩论的主持人循环 + 辩手 `RunSession` 活在 await 中的协程里、无法像 plan 检查点耐久序列化重建；故走与阻塞式 `escalate` 同源的 `InteractionRegistry`（新增 `InteractionKind.DEBATE_ROUND`，不写 `TurnSuspension`、不进 journal），结算仍走统一 resolve 端点。重载后决策卡不复现，用户的选择已体现在实际轮次 + 收场 `stop_reason`，故契约一致。

---

## 关联

- [`Agent协作模式.md §7.4`](/docs/03-AI核心/Agent协作模式.md)：辩论在协作范式中的位置（本文是其权威展开）。
- [`多轮编排与队员热修.md`](/docs/03-AI核心/多轮编排与队员热修.md)：辩手跨轮带记忆所复用的 `RunSession` + `revise` 续写原语。
- [`执行引擎架构设计.md §8.4`](/docs/03-AI核心/执行引擎架构设计.md)：底层 DAG 执行、委派深度、上游产物注入。
- [`前端UX设计.md §四`](/docs/04-前端/前端UX设计.md)：双产物（决策简报 + 三层交锋叙事线）的前端呈现。
- [`编排器与CEO主Agent.md §四`](/docs/03-AI核心/编排器与CEO主Agent.md)：老板介入复用的 `ask_user` / `plan_review` 检查点。
