---
status: landed
code: apps/server/agentcore/
related:
  - docs/03-AI核心/编排器与CEO主Agent.md
  - docs/03-AI核心/运行时三模型与挂起.md
  - docs/04-前端/协作图与双视图UX.md
skip_if:
  - 只改 delegate/replan 字段（读编排器）
---

# 检查点与开工卡

> **权威**：`ask_user`、`team_preview` / `plan_review` / `stage_card`。挂起机制 → [运行时三模型](/docs/03-AI核心/运行时三模型与挂起.md)；编排字段 → [编排器](/docs/03-AI核心/编排器与CEO主Agent.md)；前端卡 → [协作图 UX](/docs/04-前端/协作图与双视图UX.md)。

## 一、`ask_user`（通用澄清）✅

**定案（已落地）**：取消「开场 / 开工提案」产品位与场面账；`ask_user` 仅为 CEO 在 ReAct 循环中的**通用澄清步**（可与检索/读文件等任意穿插，可连续多次）。参照通用 Agent（Cursor / Claude Code 式），非垂直生成器式选项闸。

| 项 | 决定 |
|---|---|
| 开场仪式 / kickoff 味 UX / 提案体硬闸 | **取消** |
| 澄清方式 | 多轮短问；**Agent 自主**决定何时问、问什么 |
| 「就这样开做」开场主 CTA | **不作为**产品主路径 |
| 场面账（`style_options` / `format_options` + 风格/演讲/自动化硬门禁） | **取消**；缺信息靠短问，错了再改 |
| `team_preview` | **不动**（组队确认 ∪ 能力授权，与 ask 正交） |

**统一机制**：单一 `ask_user` 工具；`blocking`（默认 true）= 模型判断是否冻回合。「该结束还是挂起」归运行时。`blocking=false` 须写明将采用的默认，否则拒调。阻塞检查点 = 挂起即收口，冷路 `POST …/resume`。浏览器需用户登录时 CEO 设 `browser_login=true`（强制 blocking；与 worker `escalate(browser_login)` 同形「需要你登录 / 已登录，继续」卡，**禁止**把完整 escalate 开给 CEO）。

**卡上 CTA（桌面/手机）**：主「提交」=`decision: continue`；次「跳过」= 拒答硬停（wire 仍 `decision: stop`，工具结果「用户跳过未回答。」+ `ToolEffect.INTERACT`）。**不是**空 continue「按默认继续」；整回合协作停止（`user_stop`）与此正交，不归本卡。`team_preview` / `plan_review` 次要键仍为「停止」。

**何时不用**：简单问答直答；信息已够 → 直接干 / `delegate`；意图都复述不出 → 可先正文澄清，或短 ask——不搞开场提案墙。

**否决**：开场问题墙；独立 `kickoff` 工具；场面硬账门禁；ask 与 `team_preview` 并成超级卡；多步 Wizard。

**现状**：桌面/预览前端统一为通用澄清壳（`AskDecisionBody`）。后端普通 ask intent 默认 `decision`；场面账（`style_options` / `format_options`）**已退役**，不再出现在工具 schema / SSE / resume wire。建站无风格时 `design_prompt_block` 软注入 `s_default`；`web_quality_scan` 仍查 DESIGN.md「用户选定风格 id」。→ 见代码: `CheckpointCard.tsx` · `tools/builtin/ask_user/`、`runtime/runs/website_style.py`

## 二、检查点与团队预审

| 原语 | 触发 | 语义 |
|---|---|---|
| `ask_user` | CEO 关键岔路 / 信息不够 | CEO 工具效应（通用澄清，见 §一） |
| `plan_review` | step 标 `checkpoint_after` | 波间结构挂起；前置强制 CEO 把关摘要 |
| `team_preview` | `delegate`/`debate` fan-out 前 | 计划确认 ∪ 能力授权（公共 kickoff gate） |

**delegate 卡**：≥2 worker，或本地 `command=kickoff` 需能力授权 → 挂卡。动作：授权并开工（可嘱咐；可**排除岗** / **单向收紧写盘**为仅文字＝`form=prose`，不硬卸写工具）/ 停止。仍被其他纳入队员 `depends_on` 的岗不可排除。队员行展示写盘能力标签（`可改文件` / `仅文字报告`，由 `deliverable.form` 推导）。对抗视角走独立 `debate` 工具与辩论开工卡；**不**再经 delegate task 的 stance/round 展示标记挂卡或画「辩论」徽章（该假通道已收口）。  
**debate 卡**：顶层一律弹；动作：授权开赛（可嘱咐）/ 停止。`research_first` 键已退役——「庭前取证内化」指质量前提（案卷桥 / Evidence Pack / 发言期有界检索入台账），**≠** 庭前调查员舰队，勿再当开工前先调研关卡或代派取证员流程 → [辩论编排 §五](/docs/03-AI核心/辩论编排设计.md)。

**阶段推进卡 `stage_card`**：幕间一步授权（如「按此开辩」= 机制直起、`skip_kickoff`）。**否决**二步制与「发消息即 orphan」。→ 见代码: `runtime/kickoff/stage_card.py`

**跳过**：单 worker+`finalize`；同回合已有阻塞 `ask_user` 已确认；含 `checkpoint_after` 时计划半边让位；`team_kickoff=skip` 放行两半边；嵌套/`light`/续跑不挂。`team_kickoff=always` 在仍该挂的场景总挂。

**专用卡**（`card` 参数，不新增 kind）：`proposal_pick` / `risk_ack` / `organize_plan` / `daily_review`（站立任务·每日复盘勾选墙）；主拍板四选一通则在提示词层。权限三轴 → [安全 §三](/docs/05-平台与运维/安全权限与治理.md)。`plan_review`/checkpoint **不受**三轴影响。
