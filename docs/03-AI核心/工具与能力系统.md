---
status: blueprint
code: apps/server/agentcore/tools/
related:
  - docs/03-AI核心/执行引擎架构设计.md
skip_if:
  - 只改单个 builtin 工具实现细节
---

# 工具与能力系统

> **边界**：工具清单与装配 → **本文**；`terminal` 审批 / pty → [安全 §三](/docs/05-平台与运维/安全权限与治理.md)；本地 Backend / 终端 tab → [双模式工作区](/docs/02-架构/双模式工作区.md)。
>
> → 见代码：`apps/server/agentcore/tools/`（契约与成员序以声明名册 + 快照测试为准，**勿在本文枚举**）

---

## 一、Tool / Skill / Rule 边界

| | Tool | Skill | Rule |
|---|---|---|---|
| 是什么 | 副作用 / 读写 / 需审计 | HOW 教法（按名拉取） | 行为约束（进 `<rules>`） |
| 装配 | 声明式 `ToolRegistration` → registry | 系统 Skill 常备目录；市场 Skill ⏳ 绑定后进集 | Document `role=rule`（见 [记忆系统](/docs/03-AI核心/Agent记忆与知识系统.md)） |
| 注入 | LLM function calling | ≤阈值全文；超阈值 = 目录 + `consult_skill` | always / on_demand |

**单源**：经统一 `AgentEntityBinding` 绑到 Agent（能力域 ⏳；MVP 固定注册内置工具）。运行时只读「本地侧」，不碰市场侧。Skill 永不自动注入工具（仅 `suggested_tools`）；系统 vs 市场 = 两类来源、同一 `SkillRegistry` + `consult_skill`。

**表达形态 ≠ 工具**：mermaid / markmap / vega-lite = 直写代码块 + 前端渲染；需持久可编 → 创作产物。

**协议**：MVP = 内部 `Tool` Protocol + OpenAI FC；**MCP Client** ✅（→ [§四C](#四c本机-mcp-client)）。审批 `never`/`grantable` → [安全 §三](/docs/05-平台与运维/安全权限与治理.md)。CEO/worker 分集 → [执行引擎 §五](/docs/03-AI核心/执行引擎架构设计.md)。

→ 见代码：`tools/registration.py`、`runtime/skills.py`、`tools/catalog.py`

---

## 二、引用质量闸

`web_search` / `read_url` 双通道：**来源卡**（`citations_event` = 成稿实际引用）与**回合证据台账**（除 `blocked` 外全量登记）。档位单源 `citation_tier_for_url`——禁第二套分级器。

| 档位 | 来源卡 | 台账登记 |
|---|---|---|
| `blocked` | 出口剔除、永不挂卡 | **永不进** |
| `weak` | 仅成稿 `#rN` 引用时挂卡 + 弱源徽标 | 登记宽、`citable=true` |
| `official` / `media` / `unknown` | 同上 | 登记宽 |

- **登记仍宽 / 成稿闸窄**：台账除 `blocked` 外均可登记；成稿 `#rN` 闸 = `deep_read ∪ selected`（对齐辩论 `commit_research` 精神）。search-only 默认不得进成稿闸；`finish_guard` / settle reconcile 用 draft 子集，不用全量 `citable_ids()`。例外：`AgentCore/文档/research/*.md` 成功落盘时，正文已引用且可登记的 `#rN` 升为 `selected`（供 CEO 汇总继承队员笔记引用；伪造/越界仍不过闸）。
- **`selected`**：settle 时正文实际引用且已 `deep_read` 的 id 持久标 selected；调研方向笔记落盘亦可 promote（见上）；跨回合 hydrate 保留。可选 `doc_kind`（登记时可写；否则 title/snippet 启发式）。
- **书目形态闸**：正文以学位论文/期刊式著录绑定 `#rN` 时该 id 须 deep_read；元数据呈开题/答辩/公告类 → 回炉（禁域名黑名单）。无 `#rN` 绑定的 GB/T `[D]`/`[J]` 著录亦回炉。台账接通时，**worker 合同闸**对内容类落盘文件复用同一套 `citation_quality_reworks`（与 `finish_guard` 同源）；调研 `research/*.md` 遇未绑定书目时不再文末补脚注装锚。
- **调研两阶段引用**：`citation_mode=two_phase`（playbook/CEO 盖戳），或落盘/声明路径在 `AgentCore/文档/research/` / `AgentCore/文档/reviews/` → 阶段 A 草案**不**跑成稿引用闸；同 worker 升 B 后再验；`immediate` 显式退出。draft 不进 `delivery_status.artifacts` 主清单。→ 见代码：`runtime/runs/research_quality.py:is_two_phase_citation_deliverable`
- **跨回合 hydrate**：新回合 `#r` 台账从历史 assistant `evidence_ledger` 合并进引擎核；LLM history 可不带 ledger 全文。
- `search_policy`：per-run 结构化信号（禁 prompt 触发）；`debate_evidence` 额外硬剔 weak / 商城词典等。
- 前置：query 词数/CJK 加权上限（超限可机械规范化并明示；无法规范化才拒）；CJK 查询剔无 CJK 命中；缓存键 casefold + 拉丁词序归一。
- 名单无真实分布依据——以合成样本校准（目前无真实数据，以下为假设）。

→ 见代码：`runtime/citations.py`、`runtime/evidence_ledger.py`、`tools/builtin/web/`

---

## 三、沙箱

代码执行：subprocess + `SandboxProvider`；云端 gVisor（`GVISOR_ENABLED`，默认关）已生产化——禁网默认、产物 copy-in/out；sidecar 保留 subprocess。→ [安全 §五](/docs/05-平台与运维/安全权限与治理.md)、[部署与运维](/docs/05-平台与运维/部署与运维.md)

`browser_*`：统一 BrowserSession——工具名不变；**`browser_navigate` / `click` / `type` / `scroll` / `snapshot` / `console` 为 CEO+worker**（与 `host_shell` / 本地 `terminal` 并列；captain 直调尽量不弹审批）；**`browser_screenshot` 仍 worker-only**（视觉验收）。宿主 **桌面** Local（Electron `WebContentsView` + DesktopBrowserBridge，**非 CDP**）\| Sandbox（云端 gVisor · Playwright）；**Web / 无 Electron → 仅 Sandbox**（地址栏 create+navigate，拒本机 localhost）。`browser_console` 返回会话内环形缓冲：页面 console 消息 + pageerror/未捕获异常（Sandbox）或 console-message + 主框 did-fail-load（Local）；硬上限截断，禁止回传密码/超大 blob——修码白屏定锚用，**不**硬闸「没 console 不许 patch」。AI 永不经手密码（password 硬拒 → worker：`escalate(browser_login=true)`；CEO：`ask_user(browser_login=true)`）。用户明确「右坞打开 / 浏览器打开 / 帮我看页面 / 短搜短点」时 CEO 自己调 browser_*（勿为此 delegate；「随便搜」省略过重验收）；截图验收仍可 delegate。mutation（navigate/click/type/scroll）成功结果含当前 `snapshot_version` **与新 ref 表**（`untrusted_web_content.elements`，Playwright MCP 同形；仅必要时再调 `browser_snapshot`）；回合内发模型前投影只保留最近一次完整 elements/aria（更早字段级 `omitted`，canonical/journal 仍全文）。`complexity_hint=light` 不再盖短 `max_rounds`，浏览器路径不再依赖「勿 auto-light」规避。

**打开本会话 HTML 站点（定案 · 甲 + 乙降级）**：桌面 Local 上 `browser_navigate` 支持 workspace 相对 HTML 路径，走与用户产物「完整预览」同源的 `workspace://`（该对话的 workspace partition；URL host 须等于 partition 绑定 cid）；打开后可继续 click/type/snapshot。公网 URL 仍走该对话的 web partition（按 `conversation_id` 切开，不与其它对话共享 cookie）。Sandbox / 无 Bridge → 相对路径诚实失败（引导点产物完整预览），禁止假成功。→ 见代码：`tools/builtin/browser.py` · 桌面 `browser/host.ts`；产品蓝图 → [前端技术 §9.12](/docs/04-前端/前端技术与架构.md)

### 写码验证双环（内环诊断 / 外环门禁）✅

行业对齐：改码循环用**语言服务增量诊断**，交付验收才跑**全量项目 CLI**。禁止把全仓 `tsc` 当修码自检、禁止靠盲加超时掩盖误用。

| 环 | 工具 / 机制 | 用途 |
|---|---|---|
| **内环** | `code_diagnostics` + 写盘成功自动附加 | 本地 TypeScript LanguageService（桌面 `WorkspaceOp.diagnostics`）；按改动路径秒级反馈。云端 / 无 LS → 诚实 `unavailable`，**不**用全仓 `tsc` 冒充。不计入外环验绿 |
| **外环** | `test_run` | 验收员 / task·`playbook_args.verify`：install · test · typecheck · build · 显式 command。超预算 = 验证未完成（`contract_failure`），不进工具熔断 |

预算分档（外环墙钟，非内环重试燃料）：`typecheck` / `build`（及同形 command）**600s**；`install` / `test` / 其它 **300s**。验绿诚实口径仍 = **落盘 ∩ 外环真绿**（✅ S3：引擎不再按 `completion_criteria` kind 硬判批次完成）。修码 playbook：靠角色提示 + `verify_policy` 分流（不再靠 `tasks[].tools` 白名单裁工具）；全量 typecheck 仅验收/外环。调查/审查角色默认 `verify_policy=inner`（结构化，非文案猜）：`test_run` 拒全仓 typecheck/build，改道 `code_diagnostics` / browser；验收员可显式 `outer`。→ 见代码：`tools/builtin/code_diagnostics.py` · `write_diagnostics.py` · `test_run.py` · `runtime/runs/worker_budget.py` · 桌面 `fs/workspace/diagnostics.ts`

### 执行三分（短 / 有界验证 / 长驻）

| 面 | 工具 | 用途 |
|---|---|---|
| 短执行 | `code_execute` | 会很快自行退出的内联代码 / 短命令；硬顶仍短（≤60s）；同会话多 worker 的 `code_execute` 串行排队（`test_run` / `terminal` 不在此锁内） |
| 有界项目验证 | `test_run` | **外环**项目 check（**install** / test / typecheck / build / 显式 verify）；分档分钟级预算、可流式；超预算 = 验证未完成（`contract_failure`），不进「工具坏了」熔断。同 `execution_id` 内 sibling 相同验证（解析后 argv 指纹）**inflight 合流 + 结果复用**，避免双 worker 各烧满预算；任一队员成功写盘后 cache **失效**（generation 丢弃陈旧入缓存）。执行中 busy=`verify`（不挡 idle patrol / `cancel_worker`）。云端装包 = allowlist egress（netns + 代理）+ 包装源白名单 env + 拒 `--registry`；持久工作区 rw-bind（install 例外），`node_modules` 落盘，短命沙箱不整树 base64 回传。本地装包 = 权限轴 + 钉源 env（不吃主机 `registry_egress_available`），装在用户绑定目录。无授权 / 云无 chokepoint → 诚实甲降级（结构自检 / export_to_local），勿空转。修码自检走内环 `code_diagnostics`，勿用本工具冒充内环 |
| 长驻 | `terminal` | 开发服务器等永不退出进程；`long_running` 分流保持；**CEO+worker**（仅 `location=local`）；schema `NEVER`，`start` 运行时升审批；CEO 限纯启/停/读，改码仍 delegate |

外环验绿（task / `playbook_args.verify`）**默认走** `test_run`。项目级慢命令（`npm/pnpm/yarn install|ci|test|build`、`tsc`、`pytest`/`vitest`/`jest` 等）进 `code_execute` → **硬拒**并改道 `test_run`（与长驻改 `terminal` 同构；`contract_failure`，不进工具熔断；与已删 kind 正交）。装包闭环：`check=install` 或 `check=command` + 正规 install 形态（`--prefix`/`working_directory` 相对路径；禁 `cd&&`）→ 再 build/typecheck。真短脚本仍走 `code_execute`（≤60s）。云端沙箱 / GRANTABLE 与 `code_execute` 同级治理（`code_execution_enabled_for`）。

写工具 miss 回执分角色：CEO 面 → 须 `delegate`；worker 面工具已默认全开（含写盘），`form=prose` 只约束交付形态与进度催促（不要求 `str_replace`），不靠硬卸写工具。Local 破坏性删（`code_execute`/`terminal`/`host_shell` 启发命中）→ 回滚主（回合 zip 基线）+ 窄闸辅；与 `file_delete` 软删分轨对齐 → [双模式工作区 · 脚本破坏](/docs/02-架构/双模式工作区.md) · [安全权限 · 熔断](/docs/05-平台与运维/安全权限与治理.md)。无 git 时上下文软提示 `git.init_baseline`（不硬拦开工）。→ 见代码：`tools/builtin/test_run.py` · `code_execute.py` · `project_verify.py` · `runtime/engine/tool_exec.py` · `workspace/destructive_fs.py`

**长文落盘（✅）**：成篇交付默认短骨架 + 按节 `file_append` / `str_replace`；完整无省略的超长正文允许一次 `file_write`（不硬拒字数）。**成篇体量若含交付残缺标记**（「中间省略」「已保留首尾」等）→ **硬拒**（须骨架+SECTION 按节填，或一次写完完整正文）。写工具参数解析失败 → 过程线人话「改成分段继续」，模型回执保留技术细节并强制分段；连续失败**不**停用写文件。同 path 连续 ≥2 次同类写硬拒（成篇后禁 append / 代码结构完整性拒）→ 合流 `force_segmented`：收窄 `file_append`，保留 `file_write`/`str_replace`。空必填参数（如 `str_replace` 空 `old_string`）仍为 `contract_failure`，但指纹收敛为稳定 sentinel，使 REPEATED_FAILURE / validation streak 能收口（禁止靠换 path/new_string 刷空参）。CEO 识别用户粘贴的旧「参数不是合法 JSON」报错时禁教转义。→ 见代码：`tools/builtin/file_ops.py` · `runtime/engine/tool_exec.py` · `runtime/loop_controller.py`

**工作区 Office/PDF 读（✅）**：`file_read` 对 docx/pdf/pptx/odt/rtf **透明抽文本**（复用附件预解析核；读时默认不写 `*.md`）；xlsx/csv 等不抽、改 `code_execute`。扫描件给明确 notice，不做复杂 OCR。→ [双模式 §七](/docs/02-架构/双模式工作区.md)

**工作区 zip 解压（✅）**：大包持久展开主路径 = `archive_extract`（工作区 zip → 指定目录；zip-slip 拒；Local/云同形直写）。**禁止**假定「仅 `code_execute` 沙箱解压成功 = 工作区已可见」。写路径 sanitize **保留**有意义前导 `_` / `.`（如 `_inventory`）。→ 见代码：`tools/builtin/archive_extract.py` · `workspace/_paths.py`

**`file_read` 区间视图（✅）**：整读与 `offset`/`limit` 统一为编号行 + 页脚「第 a–b 行，共 N 行」；超默认行数只返回视图窗，**禁止**静默丢尾、**禁止**在正文插入通道头尾省略。`ToolResult.output_limit` 覆盖整段视图，避免通用 4k 头尾裁把「系统视图截断」写进可抄进 `old_string` 的正文。通道/预算截断标记（`[系统视图截断·非磁盘内容]`）与交付残缺标记（「中间省略…」）**语义拆开**——后者仅用于写路径完整性检测。

**同 path `file_read` 天花板（✅）**：只约束**整读**空转（`FILE_READ_SAME_PATH_MAX`）；带 `offset`/`limit` 的分段读不计次、不触顶。硬拒条件 = 投影窗口仍有该 path 正文 + 已达上限 + 无再读授额（`contract_failure`）。正文已被 `tool_clear` 清理、或 `file_write`/`file_append`/`str_replace` 写成功重置计数并授核对读时，允许再整读。**视图截断 ≠ 交付残缺**（见上节区间视图 / `core.text`）。→ 见代码：`tools/builtin/file_ops.py` · `runtime/engine/tool_clear.py` · `core/text.py`

**确定性导出 Word/PDF（✅）**：`md_to_docx` / `md_to_pdf` 把工作区 `.md` 导出为同目录同名 `.docx` / `.pdf`（共享 `docs_export`；PDF 引擎 fpdf2）。用户要「PDF / 可分享」时成文主路径 = 落盘 `.md` → `md_to_pdf` → handoff；**禁止**多份 HTML 顶替 PDF，**禁止** `code_execute`+reportlab 做主路径。云 = 本地同能力。→ 见代码：`docs_export/` · `tools/builtin/md_to_docx.py` · `tools/builtin/md_to_pdf.py`

**FILESYSTEM 容量合同 ≠ 活性超时（✅）**：字节/行/条目/Office 抽取成本触顶 → 早失败 `contract_failure` + 可执行下一步（不计工具熔断累计）。墙钟只表示通道/协程**活性挂起**（文案禁原样重试；仍计熔断，steer 强调缩小范围/换策略）。云 `ServerWorkspace` 整读上限与桌面 Local `WORKSPACE_READ_MAX=5MiB` 对齐。Local 通道 deadline **由外层工具活性预算派生**（非第二套独立 60s）；`timeout_ms` 下发桌面 AbortSignal，迟到 settle 仍 stale no-op。首活性超时 → 通道 sticky **dead**（同通道 inflight 失败信封收口 + 后续 op fail-fast，防连环 60s）。通道有界并发（默认 2）：仅约束 Local `WorkspaceChannel` 同时 await 桌面的 op；超额排队（计入外层墙钟），dead 后排队者拿槽即 fail-fast——云盘/`workspace_lock` 不动。→ 见代码：`workspace/limits.py` · `workspace/channel.py` · `runtime/tool_deadline.py` · `runtime/engine/tool_exec.py`

---

## 四、跨会话工具边界

`search_conversations` / `read_conversation`：**Worker-only**（产品层恒开，无用户总闸）；CEO 经 `delegate` 查阅。用户 `@` 对话 → 服务端深读，不信客户端浅文。

→ [记忆 · 跨会话对话日志](/docs/03-AI核心/Agent记忆与知识系统.md)；见代码 `conversation/log_export.py`

---

## 四B、本机 Host（第三能力面）✅ P0–P2 · P3 `host_shell`

> 与 Workspace / Browser 正交：结构化观测本机 OS、打开系统面板、受控动作，以及 Cursor 同款本机通用命令。详细定案在维护者本地；此处只留已确认契约。

| 项 | 契约 |
|---|---|
| 可用性 | 仅桌面 + 回填通道可达；与 folder/bind 正交（裸聊云 scratch 亦可） |
| 通道 | `ChannelProfile`（`X-Client-Platform` → surface / `desktop_online` / `can_bind_folder`）为单源；缺头 / 未知 → fail-closed（两旗均为假）；**禁止**用 `location=local` 抬升 `desktop_online`（与鉴权 aud 的 legacy 默认 desktop 分离） |
| 运输 | ClientTool / channel 回填（对标 external grant / `desktop_notify`）；**禁止**以 DesktopBrowserBridge loopback 冒充云可达。`DesktopClientChannel` 在 `desktop_online` 时装配（含云 backend） |
| 工具类 | 独立 `host_class`；**不**挂 `execution_class`；L2/L3/`host_shell` **不**吃 kickoff/`command=auto` 静默授 |
| 分工 | CEO：L1（`never`）+ **GRANTABLE 唯一例外** `host_shell`（`grantable`）；另持本地 `terminal`（非 Host 面、非 GRANTABLE schema）；L2/L3 仅 worker（**不**因 P3 自动给 CEO） |
| 权限 | PermissionAxes `host`：`off`/`ask`/`session` → [安全 §三](/docs/05-平台与运维/安全权限与治理.md) |
| P0 | `host_ping` · `host_info` · `host_audio_devices` · worker `host_open_settings(sound)` · 能力行 `host=` · 改写云 `reach_line` |
| P1 | L1 `host_storage` · `host_power` · `host_network_summary` · `host_apps`（有界摘要）；L2 面板扩至 `sound|display|network|apps|about`；GRANTABLE 审计对齐 |
| P2 | L3 受控白名单（worker · GRANTABLE · host_class · 禁 execution_class / kickoff 静默授）：`host_audio_set_default`（须先经 `host_audio_devices` 观测）；`host_service_restart`（起步仅 `Audiosrv`）；禁注册表深写 / 关防火墙 / 任意 sc；CEO 永不持 L3 |
| P3 ✅ | `host_shell`：CEO+worker · GRANTABLE · host_class；参数 `command` + 可选 `timeout_seconds`（≤120）；P3 首版**无** `cwd`（默认 home/shell cwd）；Win PowerShell / Unix `$SHELL -lc`；毁灭性 + **长驻命令**启发式硬拒（dev server 等须改 `terminal`）；Computer Use 仍 ⏳（与 MCP 分项） |
| 否决 | 放宽工作区 cwd；单点音频特例工具；Web/手机假装已查本机 |

`<workspace_context>`：能力行加 `host=`；云 reach =「工作区在云；本机 Host 以 `host=` 为准」。

→ 见代码：`desktop/channel.py` · `tools/builtin/host.py` · `runtime/pipeline/prepare.py`

---

## 四C、本机 MCP Client ✅

> 桌面配置本地 stdio MCP Server，工具经现有 Tool 管线进 worker（审批 / Journal / SSE 同径）。不做 AgentCore 对外 MCP Server；不替换 builtin；Computer Use / 远程 SSE·HTTP MCP / 按项目绑定 = 另项。

| 项 | 契约 |
|---|---|
| 运输 | **仅 stdio**；桌面主进程用官方 `@modelcontextprotocol/sdk`（`Client` + `StdioClientTransport`）拉起；回填对标 Host/`DesktopClientChannel`（`mcp_op_required` ClientTool）；**禁止**云端假连本机 stdio；Web/手机无「已接本地 MCP」假象 |
| 谁持有 | **默认仅 worker**；CEO 工具集不含 MCP |
| 审批 | MCP 工具一律 **`grantable`**（未知 Server 不得 `never`）；走**通用 Approval** 卡（挂起 / fulfill / 拒绝同径），不另开「信任 Server」轴；云工作区 + 桌面在线时 worker 仍须对 MCP/Host 弹卡（本机副作用） |
| 装配 | **账户/会话级**：用户启用的 Server → 本回合 worker 工具集（prepare/resume 经桌面 `list_tools` 发现）；不做按项目 / Agent 绑定 |
| 失败 | Server 未启 / 握手失败 / 崩溃 / stderr 诊断 → **降级为无该批工具**（错误明细含 stderr 尾 + 日志 + 能力行 `mcp=`），不阻断纯聊；**禁止**整段 ignore stderr |
| UX | 工具箱「集成 · 连接器」增删启停；手册「MCP（本机连接器）」 |

→ 见代码：`desktop/channel.py` · `tools/mcp/` · 桌面 `mcp-service.ts`（SDK）/ `mcpOps.ts` · `ConnectorsPage`

---

## 五、否决 / 待定

| 否决 | 理由 |
|---|---|
| 另造 `consult_capability` | 与 `consult_skill` 重复 |
| 宿主 Playwright 直连沙箱 browser | 需打 netstack 洞 |
| 「除 blocked 外全量」塞进 `citations_event` | 毁主卡信噪 |
| pandoc / `code_execute` 作 md→docx 主路径 | 确定性转换器更稳 |
| 为瘦身从 sidecar 砍掉 Office 却仍跑同一管线 | 缺 `docx`/`fpdf2` → 本地回合硬挂；云=本地同能力（见[双模式 §七](/docs/02-架构/双模式工作区.md)） |

待定：MCP 之外协议、非 LLM 工具计费、可观测细则；远程 MCP 运输（§四C 二期）。
