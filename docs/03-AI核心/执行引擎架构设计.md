# 执行引擎内部架构设计

> **状态**：已确定方向

---

> **现状**：多 Agent 编排 = CEO 在 ReAct 循环里调 `delegate` → `runtime/runs/`（`build_run_plan` + `WaveScheduler`）。§十二~§十四 为已落地运行时；§十五~§十七 为设计方向；§十八 为目标模型（Interaction / Turn Journal / Run）。
>
> → 见代码：`apps/server/agentcore/runtime/`
>
> **章节编号说明**：§二–§十一已废弃，外链请用 §十二 / §十五 / §十八。

## 核心定位

执行引擎是 CEO 主 Agent 与 worker 实例之间的调度中枢。它接收 CEO 经 `delegate` 下达的子任务（`build_run_plan` 据 `depends_on` 建 DAG），解析依赖图，管理 worker 生命周期，协调并行/串行执行，处理失败与取消，并向 UI 推送实时状态更新。

---

## 一、基础概念（DAG 调度，早期设计）

> DAG 调度概念提要；运行时以 §十二~§十四 为准，§十八 为目标模型。以下只保留代码看不直观的设计约定。

### 步骤状态与调度

计划是 DAG：每个 step 声明 `depends_on`，执行引擎扫描「依赖已满足且未启动」的 step 并发启动（受 `max_parallel` 限制）。步骤状态 `queued → running → completed`，异常分支 `failed` / `skipped`（枚举见 `runtime/runs/types.py` 的 `RunPhase`；失败重试由 `RunPolicy.on_failure=retry` 在 `runs/wave.py` 内部完成（硬上限 3 次退避），对外仍以 `FAILED` 收口并 emit `run_failed`）。并发模型为 asyncio 协程——多 Agent = 多 asyncio Task 共享事件循环（Agent 本质是 I/O 密集的 LLM 调用，协程足够）。

### 检查点决策语义

> **本篇权威**：检查点 / `plan_review` / 2b 续跑语义以本节与 §暂停与恢复 为准；编排器、协作模式等文档仅外链此处。

检查点分两类，**实现现状不同**：

- **CEO `ask_user` 检查点（✅ 已落地）**：CEO 主动调内置工具 `ask_user` 暂停回合、征询用户——这是 CEO **唯一的发问原语**，开场引导（开工提案卡）与执行途中的高代价岔路**共用一卡一机制**（开场味已并入，原独立的非挂起 `kickoff` 工具已撤销，见 [`编排器与CEO主Agent.md` §四](/docs/03-AI核心/编排器与CEO主Agent.md)）。载 `message`（开场白 / 现状，必填）+ 可选 `context` / `assumptions`（起步计划只读）/ `questions`（≤5，每题 `kind=choice|text`、`multiple` 决定单/多选、开场预填 `default`）/ `style_options`（视觉类）。桌面端以**提交 / 停止**两态作答：提交把各题选择 + 风格 + 自由补充拼成一段可读 `note`（答复模型 α）续跑、`selected` 留空（`CheckpointResponse` 一等字段仍在，供非桌面端按 `questions` 选项过滤回传），停止结束本回合。线路枚举 `CheckpointDecision` 仍为 `continue / adjust / stop`（超时落 `timeout`），其中 `adjust` 已合并入「提交」、仅留作老对话回放（故 `continue` 现也读 `note`）。这是一个**工具效应**而非调度器结构挂起——`ask_user` 经统一挂起原语 `InteractionRegistry`（与工具审批、本地工具同构，§18.2）等用户经 `POST …/interactions/{id}` 回值，答复回流 CEO 的 ReAct 循环；`stop` 走 `ToolResult.terminal` 优雅收口本回合。问答入回合日志可回放。该工具属 `ToolCategory.INTERACTION`（**非** ORCHESTRATION，故不触发引擎的 CEO→synthesis 边界），仅在 `settings.checkpoint_gate_enabled` 且有交互用户（审批开）时装配，worker 委派路径不给。
- **DAG `checkpoint_after` 结构化挂起（✅ Phase 2a 已落地）**：step 带 `checkpoint_after` 且 `COMPLETED`、下游仍待跑时，`WaveScheduler` 在波间 `await` `plan_review` 交互（`kind=plan_review`，continue/adjust/stop——adjust 把备注作为 steer 注入未跑下游的 `RunSpec.steer`、由 executor 渲染为最高优先级指示块；stop/abort 后未跑尾部统一标 `SKIPPED`），经 `InteractionRegistry` 与 `POST …/interactions/{id}` resolve；事件 `plan_review_required`/`plan_review_resolved` 入 journal 可回放；前端见 [`前端UX设计.md` §三/§五](/docs/04-前端/前端UX设计.md)。**✅ 2b 已落地**：turn 级落盘（`paused_turns` 帧）+ `POST …/resume` 续跑（**纠正设计：不加** `RunPhase.PAUSED` 第三终态，回合级落盘，见 §暂停与恢复）。⏳ 余项：跨进程 Redis（多 worker）+ 治理强制层（声明即必停、非 CEO 自声明，见 [`../07-规划/远期规划.md` §2.3](/docs/07-规划/远期规划.md)）。

> **勿与工具审批混淆**：`approval_required` / `approval_resolved` SSE 事件**不是**检查点，而是**工具审批闸门**（GRANTABLE 工具暂停待用户授权），已落地（按 `settings.approval_gate_enabled` 开关）。CEO 自身不持 `GRANTABLE` 工具，故实际在**本地模式 worker** 层生效（worker 继承 CEO 门，双模式 P2d），云端 worker 不 gate（隔离沙箱）。审批语义见 [`../05-平台与运维/安全权限与治理.md` §三](/docs/05-平台与运维/安全权限与治理.md)。三者（审批 / `ask_user` 检查点 / 本地工具）现共用 `runtime/interaction.py` 的 `InteractionRegistry`（§18.6 ClientRequestBridge）同构挂起-恢复，单一 resolve 端点 `POST …/interactions/{id}`（按 kind 判别）。

> **开工提案卡是 `ask_user` 的一种形态**：CEO 的开工提案卡（开场引导）是 `ask_user` 的「开场味」内容形态，**与途中拍板共用同一挂起机制**——曾有独立的非挂起 `kickoff` 工具，开发期已并入 `ask_user`、`kickoff_proposed` 事件已删。本原语里唯一的**终态** `ToolEffect.INTERACT` 是用户选「停止」时优雅收口本回合；提交则是 `CONTINUE` 续跑。决策分档与「为何不做成两个工具」见 [`编排器与CEO主Agent.md` §四](/docs/03-AI核心/编排器与CEO主Agent.md)。

### 失败与取消

可重试错误（超时 / 429 / 5xx）退避重试；不可重试错误跳过该 step 或升级用户；关键 step 失败时取消传播到所有下游依赖。完整收敛治理（重试 / 熔断 / 早停 / 反思）见 §十四。

### 性能约束

| 指标 | 约束值 |
|---|---|
| 最大并行 Agent | 8（`max_parallel`：`DEFAULT_MAX_PARALLEL` 单波 / `MAX_PARALLEL_DELEGATIONS` 整树，二者同步） |
| 单次 delegate 最大步骤数 | 10（`MAX_DELEGATION_TASKS`，超出截断） |
| 事件传播延迟 | < 50ms（目标） |

### Agent 间产物传递

每次执行在调度器内持有各 worker 产物，供下游依赖注入读取。

> **实现现状**：`WaveScheduler.completed[run_id]` 持有各节点 `RunState`，下游按 `depends_on` 取上游 `RunState.content`（按 `result_handling` 全文 / 摘要）。计划热修改由 CEO 多次调 `delegate` 增量追加（§十八 `adaptive`）。

---

## 十二、SSE 事件协议 ✅ 已确定

后端 → 前端的实时通信采用 SSE（Server-Sent Events），定义标准事件类型。

### 事件清单

> 真相源：后端 `runtime/events.py` 的 `EventType` + 前端 `types/events.ts`（二者已对齐，共 28 类）；下表为语义说明，新增/调整事件须同改两处与本表。

| 事件 | 触发时机 / 语义 |
|------|---------------|
| `message_start` | 助手回合开始 |
| `turn_saved` | 用户消息落库后即发（模型运行前），回传权威 `user_message_id`——前端据此把乐观气泡换成真实行 id；中途失败重试从已存回合重生成（不重发用户消息） |
| `content_delta` | 文本增量 |
| `reasoning_delta` | 推理增量（思考模式） |
| `tool_progress` | CEO captain 正在拼装某个工具调用的**参数**（主要是 `delegate` 任务书）：载 `tool_name` + 已流式参数字符数。气泡级，是 `run_tool_progress`（run 级，worker 用）的孪生——captain 的声音走气泡，且大 `delegate` 调用在 `run_plan` 之前就拼装（此时尚无团队图，run 级事件会被前端丢弃），故单独发它驱动气泡的「正在生成 {工具}…」实时行，补上 delegate 前的空白期。**纯传输不入 journal**（调用执行后由气泡正文/团队图接管） |
| `tool_use_start` | 工具开始执行 |
| `tool_use_end` | 工具执行完成 |
| `approval_required` | ✅ 工具审批闸门：GRANTABLE 工具暂停待用户授权（CEO 路径；worker 委派路径不 gate） |
| `approval_resolved` | ✅ 审批落定（approve / approve_always / approve_always_files / deny；超时=deny），前端清除内联提示 |
| `checkpoint_required` | ✅ CEO 检查点：调 `ask_user` 暂停回合征询用户（统一开场引导 + 途中拍板，载 `question` / `context` / `assumptions` / `questions` / `style_options`）。**入回合日志**（`_JOURNAL_EVENT_TYPES`），故可随消息回放——区别于纯瞬态的 `approval_*` |
| `checkpoint_resolved` | ✅ 检查点落定（continue / adjust / stop / timeout；`note` 载补充/停止收尾语、`selected` 载勾选项），前端把内联检查点卡片翻为已决态 |
| `plan_review_required` | ✅ DAG 结构化挂起：`checkpoint_after` step 完成且下游仍待跑，波间暂停待计划评审（continue/adjust/stop） |
| `plan_review_resolved` | ✅ 计划评审落定（continue/adjust/stop/timeout；adjust 将备注注入未跑下游 `RunSpec.steer`） |
| `workspace_op_required` | ✅ 本地工作区 op 通道（双模式 P2）：服务端 `LocalWorkspace` 请求绑定桌面端对真实本地目录执行 op（read/list/grep…）并回传结果（keyed by `request_id`）。**仅传输、不入团队 journal**，仅本地模式出现 |
| `handoff_snapshot_done` | ✅ 本地→云 handoff（P2e/e1）：本地工作区经通道归档并快照到对象存储，载新 `snapshot_id`；handoff 专用流上一次性发出 |
| `handoff_job_started` | ✅ 本地→云 handoff（P2e/e2）：基线快照完成、云端团队任务已派发，载 `job_id`（前端轮询状态）；SSE 关闭后云端任务后台续跑 |
| `handoff_apply_done` | ✅ 本地→云 handoff（P2e/e3）：所选结果改动经通道写回本地，载逐文件结果（applied/skipped/conflict/error）+ 计数 |
| `run_plan` | 预声明一批 run 节点（DAG 规划完成时；每节点带 `parent_run_id`，供前端图在 `run_started` 前即按委派父子成组） |
| `run_started` | 一个 Agent run 开始（编排步骤与子 Agent 的统一节点） |
| `run_output_delta` | run 自身答案文本增量 |
| `run_reasoning_delta` | run 自身思考链增量（run 级归属，与 `run_output_delta` 对称）：仅 worker 用，承载 worker 思考全文。CEO captain 根 run 的思考走气泡（`reasoning_delta`），不归 run 级 |
| `run_tool_progress` | worker 正在拼装某个工具调用的**参数**（如 `file_write` 的文件正文）：载 `tool_name` + 已流式参数字符数。这些字节既非 content 也非 reasoning，且 `tool_use_start` 要等参数拼完才触发，否则写大文件的几分钟里节点全程空白——故单独发它驱动节点/详情的「正在生成 {工具} · N 字」实时行。**纯传输不入 journal**（节流按参数增量发；重载时该 run 由 journal 的 `tool_use_start/end` 显示成稿调用，实时进度已无意义） |
| `run_completed` | run 成功完成 |
| `run_failed` | run 失败 |
| `run_progress` | 一批 run 的聚合进度 |
| `message_end` | 回合结束（含 `finish_reason` / usage） |
| `error` | 不可恢复错误 |
| `title_generated` | 会话标题生成 |
| `citations` | 本回合 `web_search`/`read_url` 来源聚合去重，emit 于 `message_end` 前；单 Agent 路径经 `CHAT_CITATION_HINT` 规范 `[n]` 角标与卡片下标对齐，团队 worker 不注入 |

### finish_reason 枚举

| 值 | 含义 |
|---|---|
| `end_turn` | 正常完成 |
| `max_rounds` | 达到最大轮次限制 |
| `degraded` | ✅ 连续返回空响应（无正文+无工具）即便 fallback 重跑后仍空——收口为降级而非空白（B2，见 §十四 收敛表）|
| `unproductive` | ✅ 连续多轮全失败且无正文，提前早停并强制无工具收口（B2 无产出早停，见 §十四 收敛表）|
| `error` | LLM 不可恢复失败 |
| `cancelled` | 手动取消或断开 |

### 契约生成策略

> **实现现状**：⏳ 未接入 CI。`.github/workflows/ci.yml` 当前仅 ruff + pytest + 前端 lint/test，无 dataclass→TS 自动生成与 `git diff` 门禁。

后端是 SSE 类型的唯一真相源。目标做法：
- 后端用 dataclass 定义每个事件的 payload schema（声明 = 构造同一对象，杜绝漂移）
- 由脚本反射 dataclass 自动生成前端 TypeScript interface
- CI 门禁：重跑生成器 + `git diff --exit-code`，拦契约漂移

---

## 十三、执行管线全貌 ✅ 已确定

> 从 HTTP 请求到 SSE 输出的完整后端调用链。

### 端到端流程

HTTP 请求经 `conversation/service.py` → `runtime/pipeline.py` ReAct 循环 → SSE 事件流出。

### Phase 1: Prepare

CEO 只读/检索子集 + `delegate`；worker 全集固定注册；`AgentResolver` 为 Phase 2 方向。

**历史重建原则**：每回合取**最近 N 条** user/assistant 文本（`conversation/history.py::load_chat_context` → `list_recent`）。工具 I/O 与委派详情属展示遥测（持久化供前端详情面板），不回放进 LLM 上下文——避免跨轮累积烧 token。超长会话在此之上再叠一层**压缩**（见下「长对话压缩」）。

> 历史曾误用「取最旧 N 条」（`list_by_conversation(offset=0)` 排序 `asc`），>40 条的会话会**静默丢最近上下文、只喂开头**；已修为「取最近 N 条」。

### Phase 2: Execute（ReAct 循环）

见 §十四 详述。

### Phase 3: Finalize

Finalize 在 pipeline 外由 `stream_chat` 执行；⏳ 无 Turn 表。回合收尾按需触发**长对话压缩**（见下）与离线记忆整合，均 off the user path。

**断连收尾契约**：各步骤独立 `try/except`，失败只 warning，不中断已完成的 SSE 流。刻意「能存多少存多少」而非全有或全无——用户已离开，保住已产生的消息比元数据一致性更重要。

### 长对话压缩（compaction）✅ 已落地

> 实现：`conversation/compaction.py`（压缩 runner + token 触发）、`conversation/history.py::load_chat_context`（注入）、`conversations` 三列（`compaction_summary` / `compacted_through` / `compaction_input_tokens`）。

**定位**：不是防溢出（DeepSeek 1M 不溢出），而是治 **context rot**（超长上下文质量随长度下降，与缓存无关）+ **缓存失效成本**（exact-prefix 缓存有 TTL，慢节奏 / 隔夜回来全量重算时长历史真烧钱）。稳态成本已被前缀缓存软化，故此项是**质量项、非紧急**。把**早于 recency 窗口**的旧轮折叠成一份结构化滚动摘要（已确立事实 / 决策 / 未决问题 / 文件路径，逐字保留标识符·数字·路径），回合改喂 `[摘要] + 最近若干轮原文`。

**关键契约**：

- **触发**：回合 input tokens（DeepSeek 实报，即 prompt 实际大小）越 `compaction_trigger_input_tokens`（默认 64K，对齐 `llm.mdc`）→ 回合收尾 fire-and-forget 后台压缩。自限：折叠后下回合 prompt 缩回阈值下，仅当尾部再次涨过阈值才重压；漏触发由下一次越阈回合自愈（无需 sweeper）。回合总 token 是 CEO prompt 的上界，触发偏保守（宁早勿晚），无可折叠内容时 runner 空转不调 LLM。
- **水位线**：`compacted_through`（最后被折叠消息的 created_at）使重触发幂等，并让长积压**由旧到新、增量**分多次折叠追平。
- **缓存铁律**：摘要**算一次、落库、跨回合复用**；绝不每回合重算——否则改写 prompt 前缀打穿 DeepSeek exact-prefix 缓存（见 `runtime/prompt.py`）。摘要作 **assistant 块**置于恒定 system 之后，故 system 前缀恒命中，仅「摘要 + 尾部」在重压时重新缓存。
- **健壮**：BYOK 取会话主人的 key（同记忆整合）；一次 flash 非思考调用（World B 窄任务，同 title / memory）；折叠量不足 `compaction_min_fold_messages` 不调 LLM；任何失败（超时 / 空输出）留存状态不变、下次重试，绝不写空摘要、绝不影响已完成回合。
- **与记忆整合区分**：离线记忆整合读**原始**最近消息 reconcile 进记忆文件（`load_recent_history`），**不读**摘要块；压缩只作用于 CEO 回合窗口（`load_chat_context`）。
- **与 A1（执行级 journal）关系**：作用在 `messages` 表已持久化的 user/assistant 文本层，**不依赖** journal，可独立演进。

### Trace 关联

> **实现现状**：⏳ `trace_id` 跨链路与 NATS 未实现。

一次用户交互跨多个执行上下文（HTTP、后台 worker、委派 worker）。目标：用统一 `trace_id` 作关联键，在管线入口铸一次，随 NATS payload 流到 worker。排查时 `grep trace_id=<id>` 即得全链路。

---

## 十四、ReAct 循环与收敛治理 ✅ 已确定

> **实现现状**：`engine.react_loop` = 轮次上限（`ModelProfile.max_rounds`）+ LLM provider 层 3 次重试（`llm/deepseek.py`，429/5xx/timeout）+ **收敛治理 `LoopController` 已接入**（重复调用 / A-B-A-B 交替 / 重复失败的滑窗检测 → 两段式 `NUDGE` 反思注入 / `FINALIZE` 强制收口）+ **引擎级工具超时兜底已接入**（B1：`engine._run_one` 用 `asyncio.wait_for` 按 `ToolCategory` 分级兜底，超时回喂模型并计入失败；orchestration/interaction 豁免）+ **空响应 degraded + 引擎级 fallback 已接入**（B2：某轮无正文+无工具=空响应，经 `LoopController` 两段式——首次切 `profile.fallback_model`（Flash→Pro 升档）重跑该轮、连续第 2 次空则 `FinishReason.DEGRADED` 收口；gated `settings.engine_fallback_enabled`）+ **工具失败熔断 + 无产出早停已接入**（B2：`LoopController` 按工具累计失败数 2 次回喂「勿重试」/ 3 次本回合停用该工具（引擎重算 tool_defs 剔除）；连续 `engine_unproductive_threshold` 轮「全失败且无正文」则提前早停、强制无工具收口并收口为 `FinishReason.UNPRODUCTIVE`；阈值进 `settings.engine_tool_failure_warn/disable/unproductive_threshold`）+ **反思注入已接入**（B2：第 4 轮起每 3 轮按节奏注入「进度复盘」prompt，`engine_reflection_start_round/interval` 可配，与 circuit-breaker steer 同轮则让位）。仍缺：结构化 `RoundOutcome`/`LoopDirective` 协议（表内最后一项 ⏳）。**工具审批已在执行层生效**：`engine._execute_tools` 对 `GRANTABLE` 工具经 `ApprovalGate` 挂起待授权（按 `settings.approval_gate_enabled`）；CEO 自身已不持有 `GRANTABLE` 工具（协调者工具边界），故审批实际在 worker 层生效——本地模式 worker 继承 CEO 的门（双模式工作区 P2d），云端 worker 不 gate（隔离沙箱）。

### 基本循环

`message_start` 后逐轮迭代至 `profile.max_rounds`；✅ GRANTABLE 审批挂起；✅ 工具超时兜底（B1）；✅ 空响应 degraded + fallback 升档 / 工具失败熔断 / 无产出早停 / 反思注入（B2，见下表）；⏳ 结构化轮协议为目标态。

### 收敛治理（LoopController）

> **实现现状**：**大部落地**。`loop_controller.py` 已实现并接入 `react_loop`：滑窗检测三类机械循环（重复调用 / A-B-A-B 交替 / 重复失败）→ 两段式干预（首次 `NUDGE` 注入锚定「观察到的重复事实」的反思，再次 `FINALIZE` 强制无工具收口）；工具失败熔断（累计 2 次劝阻 / 3 次停用，与滑窗机械检测互补——按工具累计、参数无关）、无产出早停（连续全失败无正文 → `FinishReason.UNPRODUCTIVE`）、反思注入（第 4 轮起每 3 轮注入进度复盘 prompt，节奏驱动）亦已接入。下表为完整目标机制，仍为目标态的只剩：结构化 `RoundOutcome`·`LoopDirective` 协议。

所有「循环要不要继续」的判断统一收敛到 LoopController，引擎每轮交结构化 `RoundOutcome`，按 `LoopDirective` 行动：

| 机制 | 阈值 | 行为 |
|------|------|------|
| 重复调用检测 | 相同签名连续 3 轮（滑窗） | ✅ 锚定事实的 `NUDGE` 反思；再次触发则 `FINALIZE` 强制无工具收口 |
| A-B-A-B 交替 | 两动作振荡连续 3 轮 | ✅ 同上两段式干预 |
| 重复失败检测 | 同工具同方式失败连续 3 轮 | ✅ 同上两段式干预 |
| LLM 重试 | 3 次指数退避 | ⏳ provider 层已落地；引擎级 fallback 未落地 |
| Fallback 模型 | 空响应（首次） | ✅ 切 `profile.fallback_model`（Flash→Pro）重跑该轮；gated `settings.engine_fallback_enabled`；已是 Pro 则跳过。⏳ 裁剪 system prompt / 异常路径 fallback 未做 |
| 工具失败熔断 | 累计失败 2/3 次（`engine_tool_failure_warn/disable`，可配） | ✅ 按**工具累计、参数无关**计失败（区别于按签名的「重复失败检测」）：2 次回喂「勿原样重试」、3 次本回合停用该工具（引擎重算 `tool_defs` 剔除，全停则 `tool_choice=none` 逼出正文）|
| 无产出早停 | 连续 3 轮全失败且无正文（`engine_unproductive_threshold`，可配） | ✅ 机械检测未命中但持续空转的兜底：超阈即强制无工具收口、收口为 `FinishReason.UNPRODUCTIVE`（任一轮有正文 / 有工具成功即清零）|
| 反思注入 | 第 4 轮起每 3 轮（`engine_reflection_start_round/interval`，可配） | ✅ 节奏驱动（区别于事件驱动的 NUDGE）：到点注入「进度复盘」prompt（复盘已确认事实 / 距目标差距 / 下一步动作，锚定轮数）；与 circuit-breaker steer 同轮则让位避免叠加 |
| 空响应 | 连续 2 轮（`engine_empty_response_threshold`） | ✅ 无正文+无工具=空响应；首次切 fallback 模型重跑，连续第 2 次 → `FinishReason.DEGRADED`（经 `finish_override_sink`→`RunState.finish_override`→pipeline）|
| 工具超时 | 默认 60s / 执行类 90s（`tool_*_timeout_seconds`，可配；`ToolSchema.timeout_seconds` 可逐工具覆盖） | ✅ `engine._run_one` `asyncio.wait_for` 兜底，超时回喂模型且计入失败；orchestration/interaction 豁免 |
| 协作取消 | asyncio 取消传播 | ✅ 用户停/断连 `producer.cancel()` 经 `gather` 协作传播到在飞 await；子进程 cancel-safe（超时/取消均 kill、不留孤儿）。显式 `CancellationToken` 暂不引入（无消费方） |

### SSE 桥接

执行侧与输出侧经**无界 `asyncio.Queue`** 解耦。背压只对可丢弃事件（`content_delta` / `reasoning_delta`）生效，生命周期/终止事件保证送达。

**前端兜底对账**：消息落定时递归收口所有悬挂的 running/queued run 节点——后端可靠送达是主路径，前端对账是 last-resort 兜底。

### 暂停与恢复（回合级落盘续跑）

> **实现现状**：✅ Phase 2b 已落地。**纠正设计：不引入 `RunPhase.PAUSED` 第三终态**——结构化挂起暂停的是**整个回合**（CEO loop + delegate 子 DAG），而非单个 Run 节点（已完成的 worker 是真 COMPLETED、未跑的是真 pending）；硬塞 `paused` 终态会污染调度器 / 执行器 / 计费每个 phase 分支且语义错位。改用**回合级**落盘（`paused_turns` 一行 = 一个挂起回合），Run 模型零改动。

挂起回合走**混合「实时 + 持久 backstop」**：连着的客户端仍用进程内 `InteractionRegistry` 的 `await`（2a 体验字节不变），同时在 `await` **之前**把整帧（`TurnSuspension`，以 `kind` 判别 `plan_review` / `ask_user`）落到 `paused_turns`；实时 resolve / timeout 后删帧，断连 / 崩溃则帧留存可续。

- **持久缝**：`paused_turns` 一行 = 一个挂起回合（PK = 回合 assistant `message_id`，resume 复用同 id），`frame` JSONB 仅存 **resume 控制元信息**（`tool_call_id` + `base_system_prompt` + plan 带已铸 run_id）；**CEO 窗口/历史、已完成 worker 均不再入帧**（✅ Phase 2 ⑤ `transcript`/`history`、✅ Phase 2 ⑥ `completed` 退出 `to_json`）——窗口源 `journal_entries` 在 `turn_journal` 表、历史在消息库、已完成 worker 由 worker run-final 事实投影，resume 时折出（`plan` 仍入帧，⏳ 待投影）。
- **捕获**：`captain_transcript` / `turn_history` 两个 ContextVar 在回合内发布 live `messages` / 往轮历史，挂起面（delegate checkpoint hook / `AskUserTool`）读它们——`transcript`/`completed` 仅作内存载体（conformance golden 用）+ `history` 供 sidecar 落本机帧；三者均**不再序列化**进云端 `frame`。
- **恢复**：`POST …/messages/{mid}/resume`（原子 claim 杜绝二次恢复）claim 帧 + 从 `turn_journal` 载 `journal_entries` → `seed_journal` 回放整图 → **`window_from_journal` 折 journal + 回灌 history 重建 CEO 窗口**（Phase 2 ④/⑤，不读 `frame.transcript`——已退场；history 由 `service.resume_chat` 自消息库回灌；journal 与内存 transcript 双缺即显式抛错）→ 按 `kind` 分流结算跑尾部（已完成 worker 由 **`completed_from_journal`** re-seed，Phase 2 ⑥，不读 `frame.completed`）→ 续跑 CEO loop 写回复；重开会话经 `GET …/paused` 列待恢复帧渲染卡片。
- **计费**：整回合**一次**计费——实时路完成则正常落账删帧；断连路只在 resume 完成时落账（断连时实时路从未落账）。
- **TTL**：`paused_turns` 7 天 idle 清理（镜像 `session_retention`）。
- **断连兜底**：含**未持久挂起点**（如 approval，transport-only 不 journaled）的回合被取消时，把已完成 worker 产出落成一条 `finish_reason="cancelled"` 的「未完成」消息（零重放）；与 resume 由「挂起事件是否 journaled」天然区分、不重复。
- ⏳ 余项：跨进程 Redis（多 worker 实时分发）+ 治理强制层（见 [`../07-规划/远期规划.md` §2.3](/docs/07-规划/远期规划.md)）。

---

## 十五、工具装配策略 ✅ 已确定

MVP 固定注册 10 内置工具为 worker 全集；CEO 经 `build_ceo_tool_registry()` 只取只读/检索子集；`GRANTABLE` 仅 worker 持有，本地模式经 `ApprovalGate` 拦截。

### 全部 opt-in，无默认档

内置工具全部 opt-in：Agent 在 `tools` 中声明分类或工具名才加载。没有「对所有 Agent 默认发放」的 core 档——能力是刻意声明、不靠隐式继承。（⏳ MVP 暂未按此装配，见上。）

### 分类即工具包

目标态按分类 opt-in；MVP 实际 `ToolCategory` 见上文工具装配。

### 工具来源统一审批

审批门与工具来源无关：内置/市场/连接器工具在执行层只认 `ToolBinding.approval`（`ToolApproval`）一个字段，任意来源的高风险工具接入同一审批流。（✅ 执行层已按此拦截 `GRANTABLE` 工具，见 §十四 `实现现状`；MVP 暂仅内置工具来源，市场/连接器接入后沿用同一门。）

### 时间即上下文

当前日期时间不走工具，由 PromptAssembler 追加到 system prompt 末尾的 `<runtime_context>` 块。省一次工具往返，LLM 永远「知道现在」。

---

## 十六、任务分级与模型选择 ✅ 已确定

⏳ 无 `classify_task`；单 Agent 固定 `chat` profile；多 Agent `model_preference` 仅 fast/strong 两档。

### 档位与轮数预算

`classify_task`（按复杂度自动选档）为纯启发式设计，**⏳ 未实现**；当前 worker 档由编排器 `model_preference` 直接给出（仅 fast/strong 两档）。各 profile 轮数预算见 `llm/config.py` 的 `PROFILES`。

### 模型选择优先级

```
请求级显式模型 > 创作者 pin 的具体模型 > "auto"（按分级） > 角色默认档 > 全局默认
```

- 只有显式 `"auto"` 才走分级，默认尊重创作者 pin
- **角色默认档**：编排者走强档，叶子 Worker 走便宜档（判据：是否还持有委派目标）
- 未配置即 no-op，对存量零影响

---

## 十七、提示词架构 ✅ 已确定

### 两个互不相交的世界

| | World A — Agent 主提示词 | World B — 内部工具型提示词 |
|---|---|---|
| 是什么 | 面向用户的对话 Agent system prompt | 平台内部的窄任务 LLM 调用（标题/分类/摘要…） |
| 触发者 | 每个 Agent、每个 turn | 平台代码按需 |
| 形态 | 身份 + 技能 + 规则 + 上下文，条件装配 | 固定模板 + 少量变量，单次调用 |
| 管理方式 | PromptAssembler（✅ 已落地） | 注册表 + 片段库（⏳ 目标态，当前各自手搓） |

**分离原因**：两者关注点根本不同。强行共用只会互相拖累。

### World A：PromptAssembler 组装顺序

基座 + CEO/worker 条件叠加；无 workspace_ctx 时按 agent_id 实例内缓存。

降级模式：低优先级规则过滤、文档截断、Skills 裁掉；Rules 保留（安全/合规约束越降级越要留）。

> **实现现状（提示词正文，统一中文）**：基座确立 AgentCore 身份与「结论必须基于工具实际返回、绝不编造」原则；CEO 叠加协调者角色与「用户为老板、CEO 对其负责」身份层级及引用规范 hint；worker 叠加「一次性、隔离上下文、不能反问、不能再委派」自我认知段——因 worker 拿不到完整对话、无澄清回路，须明确「信息不足就合理假设并交付」。

### World B：注册表 + 片段库 ⏳ 目标态

**目标态**：内部窄任务提示词收敛到统一的注册表 + 片段库——`PromptTemplate` / `fragments` / `registry` / `catalog`，key 命名 `component.task`，`{{var}}` 渲染，fragments opt-in。

> **实现现状（⏳ 未落地）**：注册表 / 片段库尚未实现，`runtime/prompt.py` 目前只承载 World A（基座 + CEO/worker hint 装配）。当前每个内部 LLM 窄任务调用**各自手搓**：模块内一个 system 常量 + 一个 Python 渲染函数，直接拼成 `LLMMessage`。两处现状：会话标题（`memory/conversation_title.py`：`_TITLE_SYSTEM_PROMPT` + `_render_title_prompt`）、用户记忆抽取（`memory/user_memory.py`：`_EXTRACT_SYSTEM_PROMPT` + `_render_extract_prompt`）。第三处内部调用出现、或手搓样板开始相互拖累时再行收敛——当前两处尚不构成收敛收益。

---

## 十八、运行时核心模型 ✅ 已确定

> **实现现状**：本节为**目标模型**，部分已落地。**Interaction 统一挂起原语已落地**（进程内 `runtime/interaction.py` 的 `InteractionRegistry` ＝ §18.6 ClientRequestBridge，收编审批 / ask_user / 本地工具 / **`plan_review`（2a 波间结构化挂起）** 四 kind，单一 resolve 端点 `POST …/interactions/{id}`）；**`checkpoint_after` 波间挂起（2a）已落地**（`WaveScheduler.on_checkpoint` + delegate 协调器）。**收敛治理 `loop_controller.py` 已落地并接入**。**持久挂起续跑（2b）已落地**（回合级 `paused_turns` 帧 + `POST …/resume`，不加 `RunPhase.PAUSED`，见 §暂停与恢复）。**Turn Journal 执行级溯源已落地**：每回合事实流持久化到 append-only `turn_journal` 表（唯一事实源，§18.3），`messages.runs` 列删除、回放载荷读取时投影；**单一有序日志**（`TurnFactLog`）已接引擎——sink 转发展示事件 + `react_loop` 直记 6 类执行级事实（`turn_started`/`round_boundary`/`llm_call`/`tool_call`/`note`/`message_final`）交织成单流，**持久化切换完成**（fact log 接替 sink 旧 journal 源），展示重载经 `runs_from_entries` 重套 gate 后逐字向后兼容；`Journal` 端口（§18.6）已 Protocol 化。`window_from_journal` 投影器（重建 captain LLM 窗口）已落地、两处投影边界（CEO 引用标注 / 多 Agent note 归属）已清，**resume 已改读该投影（Phase 2 ④）**，**`transcript`/`history` 已退出 `frame` 序列化（Phase 2 ⑤，两端均折 journal 重建窗口；sidecar 无库故本机帧自带 `journal_entries`+`history`）**，**`completed` 已退出 `frame` 序列化（Phase 2 ⑥：补 worker run-final 事实于 executor 收口点 → `completed_from_journal` 投影 re-seed 已完成 worker）**。仍为 Phase 2 余项：删 `frame` 的 `plan` 序列化（待 `run_plan` 投影，含 `steer` 累积）、deltas 退流、跨进程挂起（多 worker 的 Redis BLPOP）。

> 运行时收敛在三个一等模型上。每个模型把一族重复子系统坍缩成单一形状。

### 18.1 三模型总览

| 模型 | 职责 | 解决的问题 |
|------|------|-----------|
| **Interaction** | 唯一挂起原语：server 停下等 client 回值 | 统一审批/ask_user/本地工具/计划评审四套同构 BLPOP |
| **Turn Journal** | 唯一回合事实源：append-only 事实流 | 消除「回合无事实源」导致的历史回放/快照/消息完整性补丁族 |
| **Run** | 唯一执行单元：一棵 Run 树 | 统一委派/DAG/编排/路由四套并存的编排子系统 |

依赖关系：Interaction 最小独立；Journal 是 Run 的地基（事实留痕在 journal）；Run 落在两者之上。

### 18.2 Interaction（统一挂起原语）

> **实现现状（✅ 进程内已落地）**：统一挂起原语 = `runtime/interaction.py` 的 `InteractionRegistry`（§18.6 ClientRequestBridge），收编审批 / `ask_user` / 本地工具 / **`plan_review`** 四 kind 的「挂起-登记-恢复-回值」：单一注册表（`create` / `resolve` / `discard` / `get` / `list_pending`，按 `kind` 标记）+ 单一 resolve 端点 `POST …/interactions/{id}`（按 kind 判别 body）。per-kind 差异留在四个薄面（`ApprovalGate` / `AskUserTool` / `WorkspaceChannel` / delegate `on_checkpoint`）。**`paused` 落盘续跑（2b）已落地**（回合级 `paused_turns` 帧 + `POST …/resume`，不加第三终态，见 §暂停与恢复）；**跨进程等待**（Redis BLPOP，多 worker）仍为目标态。

```
InteractionRequest:
  id            # 请求-响应关联键
  kind          # approval | ask_user | client_tool | plan_review
  conversation_id / turn_id / run_id
  payload       # 各 kind 的请求体
  timeout_s
  scope         # call | turn（「本轮内都允许」语义泛化到原语层）
```

一套机制替换四套：统一等待器、统一挂起登记（`list_pending`）、统一恢复端点、统一回值端点。per-kind 差异降为策略位（durable / 键粒度 / at-rest 脱敏）。

### 18.3 Turn Journal（唯一事实源）

> **实现现状（执行级事实 + 持久化切换 + `window_from_journal` + resume 读投影 + `transcript`/`completed` 退场已落地；删 frame 的 `plan` 仍 Phase 2 余项）**：`turn_journal` 表（append-only，主键 `turn_id + seq`，`turn_id == assistant message_id`）+ `Journal` 端口（§18.6，Postgres 实现 `db/repositories.py::TurnJournalRepository`）+ `runs`↔事实投影（`runtime/journal.py`）已落地：**每回合事实流落库为唯一持久源，`messages.runs` 列已删除**，回放载荷（`MessageDetail.runs`）读取时由 journal 投影（消息列表端点批量 `load_map`，无 N+1）。
>
> **单一有序日志（已落地）**：一回合绑定一个 `runtime/facts.py::TurnFactLog`（`current_fact_log` contextvar，copy 进每个 worker task），`EventSink.emit` 把够格展示事件转发进去、`engine.react_loop` 经 `record_turn_fact` 直接记 **6 类执行级事实**（`turn_started`/`round_boundary`/`llm_call` 只存输出 + `tool_call` 全文 model-facing 结果 + `note`(nudge/finalize/circuit_breaker，带 `run_id`) + `message_final` 全文），二者按发生序交织成单流；`interaction`/`run_event` 仍乘各自展示事件入流，**展示工具卡仍走 `tool_use_start`/`tool_use_end`**（执行 `tool_call` 事实是窗口专用，展示投影跳过它）。回合末 `pipeline` 组合「fact log + process/turn_end 尾」为耐久 entries 存库（`persist_turn_journal(entries=…)`），fresh / regenerate / handoff 三路生效。
>
> **展示零变更（向后兼容）**：`runs_from_entries` 对**执行源 journal**（凭执行事实判别）重套 surface gate（仅 delegate/checkpoint 才出团队图）+ None-gate（纯聊天回合投影为 None），对 legacy / resume 帧 / salvage（无执行事实）仍**纯逆投影**——故重载气泡与切换前逐字一致。**纯聊天回合仍不写 journal**（`runs=None` → 不落库，存储与 None 行为不变）。**暂停的 journal-so-far 现持久化执行级 fact-log 流**（`_save_pause_journal` 落挂起面的 `current_fact_log` 快照 + 挂起卡；无 fact log 时退化为 legacy 展示流），`claim_paused_turn` 另把原始条目载到 `suspension.journal_entries`——**resume 已改读该投影（Phase 2 ④/⑤）**：seed 展示 `journal` 不变，但 CEO 窗口由 `window_from_journal(suspension.journal_entries)` + 回灌 history 重建；**`frame.transcript`/`history` 已退出序列化（Phase 2 ⑤）**——`journal_entries` 与内存 transcript 双缺即显式抛错（不再静默空窗口）。一处刻意的展示外溢：ask_user 挂起重载现也显示其 `tool_use_start` 卡（与实时挂起一致——fact log 不像挂起瞬间 `execution_journal()` 那样把它 gate 掉），由 golden 守护。
>
> **清理走应用级级联**（无 DB 外键）：随宿主消息/会话硬删连带清（`hard_delete` / `delete_after` / `delete_by_id`，后两者按 `conversation_id` 防 IDOR），弃用暂停回合 TTL 扫清——不悬挂、不孤儿。**`window_from_journal` 执行投影已落地**（`runtime/journal.py`，read-only）：`turn_started` 头锚 + 逐轮 fold `llm_call`(assistant，reasoning_content 回灌) + `tool_call`(tool 全文，按 `tool_call_id` 配对) + `note`(注入 user，按事实自带 `run_id` 归属)，按 `run_id` 切 captain 窗口；含单测 + 单元级 conformance（驱真实 `react_loop` 断言 `== 实时 transcript`）。**conformance golden 已立闸**（`tests/test_pause_conformance.py`）：驱真实 `AskUserTool` 走到挂起，断言 `window_from_journal(persisted journal_entries) == frame.transcript`（持久化流 vs `captain_transcript` 快照）+ 展示侧仍出 `checkpoint_required` 卡，覆盖即时挂起 + 完成工具轮后挂起两形。**plan_review/delegate 挂起 golden + 真实 DB 往返已立闸**（`test_pause_conformance` 驱真实 `DelegateTool` `checkpoint_after` 挂起断言投影只留 captain 窗口、排除交织 worker 事实；`tests/integration/test_turn_journal_repo.py` 真库 `record→load` 断言 JSONB 无损 + 两侧折叠一致）。**resume 已改读投影（Phase 2 ④）+ `transcript` 退场（Phase 2 ⑤）**：`pipeline._resumed_captain_window` 折 `suspension.journal_entries` + 回灌 history 重建 captain 窗口、不读 frame `transcript`（云端 `service.resume_chat` 经 `load_chat_context(...)[:-1]` 供史、`claim_paused_turn` 自 `turn_journal` 载 journal；**sidecar 无库故本机帧 `LocalPausedTurnStore` 自带 `journal_entries`+`history`**，`_run_resume` 回灌之；journal 与内存 transcript 双缺即抛错），消费侧由 `tests/test_resume_window.py` 守护、序列化退场由 `tests/test_suspension_serialize.py` / `test_sidecar_paused.py` 守护。**`completed` 退场（Phase 2 ⑥）**：每个终态 worker 在 executor 单一 `execute` 收口点记 **run-final 事实**（payload 即 `state_to_json`，乘 `message_final` kind），`completed_from_journal`（复用 `state_from_json`）投影重建已完成 worker 的 `RunState` map，`_settle_resumed_suspension` 改读它 re-seed、不读 `frame.completed`；**golden 立闸** `test_pause_conformance` 断言 `completed_from_journal(journal-at-pause) == frame.completed`（逐字）。**两处投影边界已清**：CEO 路径引用编号在 emit 后才折进工具消息——已由 `tool_call` 全文事实（在注解之后记录）取代 `tool_use_end` 作窗口工具源；多 Agent captain 中途注入的 note 归属——已由 `note.run_id` 解决（`tests/test_engine_facts.py` / `test_turn_journal.py` 守护）。**仍为 Phase 2 余项**：删 `frame` 的 `plan` 序列化（待 `run_plan` 投影）、deltas 退流。蓝图见 [执行级事件溯源落地设计](/docs/07-规划/执行级事件溯源落地设计.md)。

每个 turn 一条 append-only 条目流（主键 `turn_id + seq`），九类事实：

```
kind ∈ turn_started | round_boundary | llm_call | tool_call | interaction
      | note | run_event | message_final | turn_end
```

**一切皆投影**：

| 消费面 | 来源 | 现状 |
|--------|------|------|
| SSE 流 | 引擎实时 emit（流式增量不入 journal） | ✅ |
| 执行级事实记录 | `react_loop` 经 `record_turn_fact` 直写单一有序日志 | ✅ 6 类（`turn_started`/`round_boundary`/`llm_call`/`tool_call`/`note`/`message_final`） |
| 持久化消息行 | journal 投影器生成 | ✅ `runs_from_entries`（执行源 journal 重套 surface/None gate，legacy 纯逆） |
| 暂停快照 | `round_boundary.fold` | journal-so-far ✅ 落 `turn_journal`（resume seed 投影）；transcript ✅ 由 `window_from_journal` 重建、`completed` ✅ 由 `completed_from_journal`（worker run-final 事实）重建，且二者 `frame` 序列化已删（Phase 2 ⑤/⑥，sidecar 本机帧自带 `journal_entries`+`history`）；`plan` 的 fold ⏳ Phase 2 余项 |
| 跨轮历史回放 | 文本 + interaction 答案投影 | ⏳ 目标（resume 已自消息库回灌 history 拼窗口） |
| LLM 窗口 | `window_from_journal`（correct-by-construction） | 投影器 ✅ 已落地（read-only，折 turn_started→llm_call/tool_call/note，captain 窗口；两处边界已清）；接 resume ✅（`_resumed_captain_window`）+ 真实暂停 golden ✅；`frame.transcript` 已退场 ✅ Phase 2 ⑤ |

### 18.4 Run（唯一执行单元）

执行形状不再是离散「模式」而是数据，由 `depends_on` 边自然落定：

- 单 Agent = 单节点
- `fan_out` = 单波 + captain 汇聚
- `pipeline` = 线性链 + captain 汇聚
- `adaptive` = 空计划 + captain 动态追加
- 单次 delegate = 追加一个节点
- best-of-N 择优 = 某节点 `candidates>1`（`RunPolicy` 策略位，非独立 kind）
- 辩论/审查 = 带 `stance`/`group`/`round` 标记的普通 AGENT DAG（非独立 kind）；真·多轮辩论（Arena）亦是此 DAG 模式（跨轮 `depends_on`，已落地），独立 arena 子系统已否决（见 [`Agent协作模式.md` §7.4](/docs/03-AI核心/Agent协作模式.md)）

> **决策（CEO = 根 Run 节点 ✅）**：CEO chat loop 即本回合 `CAPTAIN` 根 Run；content/reasoning 走气泡级 `content_delta`/`reasoning_delta`（非 run 级）；captain 一次定价、落真实台账行。**否决**旧的 `synthesis` 零成本伪 run。

> **决策（嵌套委派）**：worker 默认叶子；`can_delegate=true` 才获 `delegate`（**默认关、显式开**，见 [`编排器与CEO主Agent.md` §2.4](/docs/03-AI核心/编排器与CEO主Agent.md)）。**硬上限 `depth ≤ 2`**（`MAX_DELEGATION_DEPTH`）：深度 2 永不获 `delegate`，树不可再深。

> **决策（可恢复运行 `RunSession`）✅**：一次 worker run 执行后不丢弃，保留完整 transcript = `RunSpec` / `RunState` 的**可恢复延伸**；CEO 经 `revise` 在保留 transcript 上追加指令、重跑 `react_loop` 即「修订」（同轮 / 跨轮 / 跨进程留人，双 miss 回落重派）。统一收编自动返工 / CEO 热修 / 重派兜底三条接缝。详见 [`多轮编排与队员热修.md`](/docs/03-AI核心/多轮编排与队员热修.md)。

### 18.5 引擎纯化

三条去特判手段：

1. **工具效应模型**：`ToolResult(text, effect)`，`effect ∈ Continue | Handoff | Interact`。引擎只解释效应，`delegate`/`ask_user` 不再是特殊名字
2. **显式 ToolContext**：工具签名携 context（user/conv/turn 等），消灭 `set_runtime_context` 全局大杂烩
3. **去工具名特判**：引擎按能力位（`has_delegation_tool` 等）而非工具名判断行为

### 18.6 端口契约（宿主注入）

引擎面向宿主的 10 项端口（本地/云端同码、宿主注入）；契约在代码里镜像为 `runtime/ports.py`（**ClientRequestBridge / Journal 已落地**、EventSink 已是端口，余项仍为其具体实现、待 Sidecar 取用时再 Protocol 化）：

| 端口 | 定位 |
|------|------|
| Journal ✅ | 引擎唯一持久化出口；`turn_journal` 表 + `TurnJournalRepository`，`runs` 读取时投影自它（§18.3） |
| EventSink ✅ | 渲染流出口（流式增量 + 离散事实实时投影）；`runtime/events.py`，`ports.py` 再导出 |
| InferenceGateway | LLM 调用 |
| ArtifactStore | 归档/寻回 |
| PauseSignal | 暂停信号（Redis flag） |
| ClientRequestBridge ✅ | 统一挂起原语（`runtime/interaction.py`；审批 / ask_user / 本地工具 / **plan_review（2a）**） |
| ConversationStore | 消息持久化（投影写入） |
| BillingSink | 计费事实投影 |
| DelegationTransport | 跨进程子树传输 |
| SnapshotStore | 已被 Journal 吸收 |

### 18.7 一轮一网关（Gateway 传播原则）

当用户自带 LLM 凭据时，整棵多 Agent 委派树必须在**同一把网关**下运行，避免 split-brain（captain 跑用户 key，委派出去的子 Agent 回落平台 key）。

**规则**：在 Turn 最早阶段（工具装配之前）一次性解析 `effective_gateway`，随 Run 上下文贯穿整棵执行树。`InferenceGateway` 端口接收的凭据由此确定——直答、团队编排、恢复各路径读同一把。

此原则与凭据来源开关无关（无论平台供还是用户供），是执行语义的不变量。

---

## 十九、待定

| 议题 | 说明 |
|------|------|
| 超时策略 | 单步骤 / 整体任务超时的具体值 |
| 并行度动态调整 | 是否按系统负载调整 `max_parallel` |
| 联网工具栈国内化 | 搜索引擎选型与熔断（面向大陆用户） |
| 代码执行启用 | 沙箱就绪后的上线时机 |
