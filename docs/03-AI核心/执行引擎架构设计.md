---
status: landed
code: apps/server/agentcore/runtime/
related:
  - docs/03-AI核心/运行时总览.md
  - docs/03-AI核心/编排器与CEO主Agent.md
  - docs/03-AI核心/SSE事件与录制回放.md
  - docs/03-AI核心/运行时三模型与挂起.md
skip_if:
  - 纯样式/文案微调
  - 只改单个 API 路由且无运行时语义
---

# 执行引擎内部架构设计

> **03 执行入口**：DAG / 管线 / ReAct / 工具装配 / 提示词。SSE 与三模型已拆专题。

## 权威归属表

| 主题 | 权威文档 |
|---|---|
| DAG、波循环、晚绑定、失败取消 | **本文** §一 |
| SSE 事件、录制回放 | [SSE 事件与录制回放](/docs/03-AI核心/SSE事件与录制回放.md) |
| 执行管线 Prepare/Execute/Finalize、压缩 | **本文** §三 |
| ReAct、收敛、finish_guard、执行与请求解耦 | **本文** §四 |
| 工具装配、模型选择、提示词 | **本文** §五–七 |
| Interaction / Journal / Run | [运行时三模型与挂起](/docs/03-AI核心/运行时三模型与挂起.md) |

## 核心定位

执行引擎是 CEO 与 worker 之间的调度中枢：接收 `delegate` 子任务（`build_run_plan` 据 `depends_on` 建 DAG），连续依赖调度、在决策边界让出、处理失败/取消，经 SSE 推状态。

---

## 一、DAG 调度与受监督波循环

### 连续依赖调度

计划是 DAG：每 step 声明 `depends_on`。`WaveScheduler` **连续依赖调度**——节点自身依赖就绪且并发有空位（`max_parallel`）即发射，**不等同一拓扑波的其余节点**。步骤 `queued → running → completed`，异常 `failed` / `skipped` / `cancelled`。多 Agent = 多 asyncio Task。

**扇入默认宽松（取消≠失败）**：默认 `require_upstream=false`——≥1 上游成功即跑，缺席上游以「前置缺席」块注入（禁编造）；零成功且存在硬缺席才跳过；`FAILED`+`degrade` 属软缺席仍放行。`require_upstream=true` 恢复严格级联；`force_continue=true` 零成功仍放行。CEO 经 `replaces_run_id` 补派可改写下游 `depends_on` 并复活被级联跳过的节点。

**否决**：把「上游须 SUCCESS」写进 ready 谓词——失败节点永不变 COMPLETED，下游会死等，且与 merge 重派冲突。正确路径：级联跳过 + `replaces_run_id` 改边复活。

级联跳过 / 中途取消均 DURABLE（`run_skipped` / `run_cancelled`），重载图上呈「未执行 / 已停止」而非假排队。

### 检查点（与审批正交）

- **CEO `ask_user`**：唯一发问原语；挂起即收口 → `POST …/resume` 冷路。语义见 [检查点与开工卡](/docs/03-AI核心/检查点与开工卡.md)。
- **DAG `checkpoint_after`**：波间 `plan_review`；仅顶层 `depth==0`；挂起即收口。**协调态例外**：只发 `BOUNDARY_YIELD`，用户拍板改走 `ask_user`。
- **勿与工具审批混淆**：`approval_required` 是 GRANTABLE 闸门，非检查点。

**不引入 `RunPhase.PAUSED`**：暂停的是整回合，用 `paused_turns` 落盘；细节 → [运行时三模型与挂起](/docs/03-AI核心/运行时三模型与挂起.md)。

### 受监督波循环（晚绑定 + 边界让出）

把「CEO 下完计划即缺席」改为「CEO 是波边界的监督者」：一次建全 DAG，节点可晚绑定，调度器只在**决策边界**让出，续跑同一张图。

| 概念 | 取值 / 语义 |
|---|---|
| 晚绑定 | `bind_after_deps`：依赖就绪后 CEO 定稿；未定稿绝不调度 |
| 边界判据 | `CHECKPOINT` / `BIND` / `SCOPE`（checkpoint_after · 待定稿 · 未消费 escalate scope） |
| 边界结果 | `PROCEED` / `YIELD`（软暂停、排空 in-flight、尾部不 skip）/ `ABORT` |
| 双臂 | BIND/SCOPE → CEO（无感 `plan_revised`）；CHECKPOINT → 用户 `plan_review` |

**成本纪律**：无晚绑定 / 无 escalation / 无 checkpoint 时一路跑到 terminal——**CEO 回合数 = 真实决策点数，不是波数**。

**偏离信号**：复用非阻塞 `escalate` + `kind=scope|dep`，同走 SCOPE 边界；**否决**为 scope/dep 单立 `InteractionKind`。

**被否决**：worker 实体化（暂压）；默认阻塞路径的波内抢占 / CEO 波内重入（协调模式除外）；运行期软护栏（边界结构性决策后基本不需）。

协调模式 as-built → [编排器 · 协调模式](/docs/03-AI核心/编排器与CEO主Agent.md)。→ 见代码: `runtime/runs/`

### 失败策略与性能约束

| `on_failure` | 行为 |
|---|---|
| `retry`（默认） | 调度器内退避；用尽 FAILED 后按扇入规则处置下游 |
| `skip` / `degrade` / `abort` | 同款扇入 / 下游照跑 / 排空后停整波 |

| 约束 | 值 |
|---|---|
| 最大并行 | 12（`engine_max_parallel_delegations`） |
| 单次 delegate 最大步骤 | 20（`MAX_DELEGATION_TASKS`，产品口径） |
| 事件传播延迟 | < 50ms（目标） |

树级预算：根 CEO 扇出仍「分而不乘」（父波按可派发宽度切开 ContextVar 份额）。**嵌套** `delegate`（depth≥1）在子团 drive 入口**重装满额** `engine_max_parallel_delegations`，避免兄弟 lead 把单团挤成 `12//N`；安全带为 depth≤3 + 单 lead `MAX_WORKER_SUBDELEGATIONS=4`。超额仍排队（管延迟，不限团规模）。

产物经 `WaveScheduler.completed` 按 `depends_on` 注入下游（无 worker↔worker 直连）。

---

## 二、SSE 事件协议

> 全文 → [SSE 事件与录制回放](/docs/03-AI核心/SSE事件与录制回放.md)。

## 三、执行管线

`conversation/service` → `runtime/pipeline` ReAct → SSE。

**关键决策 / 否决**：

- **历史重建**：最近 N 条 user/assistant 文本进上下文；工具 I/O 不回放；空正文失败轮折叠为系统注记。
- **Finalize**：开局必插 running 占位；Finalize 只结算已有行。**否决**失败再补写/对账自愈。`messages.content` = 最终交付 only。
- **压缩 / 工具清理**：跨回合折叠旧轮（治 context rot + 缓存失效）；回合内 `tool_clear` / `write_args_clear` 只投影发给模型的视图（`write_args_clear` 保留原写工具名、参数压成 landed 短状态，禁 `_write_landed` 作 `function.name` 诱饵 / 非可写 stub / 非 `_landed_summary` 回灌模板），不动 journal。二者正交。

### 长对话压缩

对齐「摘要续跑、禁止长期只砍不压」。与回合内 `tool_clear` **正交**（一管跨轮对话态，一管当轮工具投影）。实现见 `conversation/compaction.py`。

| 决策 | 定案 | 理由 / 边界 |
|---|---|---|
| 策略 | 超出近端 → 滚动结构化摘要（事实 / 决策 / 未决 / 文件标识）+ watermark；近端原文高保真 | 否决纯 FIFO 调 N；Assembler / Rules **不进**可压缩对话态 |
| 近端窗 | `compaction_recency_messages = 12` | Agent 干活档；闲聊略损可接受 |
| 双扳机（任一 due） | ① **消息**：DB 对 watermark 后 batch 做 `_select_fold`，且可 fold ≥ `compaction_message_trigger_min_fold`（默认 **16**）；② **token**：`input_tokens ≥ 32_000` | 禁用 turn `history_len`（含摘要块会误触发）；`min_fold=4` 若兼作扳机会过频重压、打穿 summary 前缀缓存；内部 fold 门仍 `min_fold=4`（空跑不耗 LLM），与消息扳机解耦 |
| 调度时机 | 云 / **本地** finalize 均 `schedule_compaction_if_due`（后台 fire-and-forget） | 与云对称；本地亦有 `input_tokens` |
| 近顶预压 ✅ | 发轮前（send / regenerate / resume）若上一轮 `input_tokens` ≥ 模型窗口 **80%**（无 `context_length` 时绝对阈值 `200_000`）→ **await** `ensure_compaction_before_turn`（最多 3 遍增量），再 `load_chat_context` | 定案⑦A：勿等用户手敲 `/compact`；普通路径仍否决「无差别同步阻塞等压缩」——仅近顶例外；失败冷却对近顶不挡 |
| 失败 / 可用性 | **best-effort**：后台不可用则跳过、不挡主回合；失败不推水位 | 产品诚实：无钥/配额耗尽时仍可能只砍不压；摘要会丢过程细节（硬标识尽量保留），近顶也无法压扁已贴顶的近端原文窗 |
| 失败冷却 ✅ | 失败后进程内冷却（默认 **90s**），期内 **双扳机都不 schedule**；成功写库清除 | 省配额；与 `_inflight_tasks` 同姿态；多 worker 可不完全一致；**近顶预压绕过冷却** |
| 装载 | 有摘要 → 摘要块 + watermark 后尾；无摘要 → 最近 40（安全帽） | 靠提早首压 + 近顶 await 避免长期无摘要硬帽服役；后台异步仍无法让「未 await 的当轮」立刻见摘要 |
| 用户可见 ✅ | REST `context_compacted`；桌面输入区上方轻提示「较早对话已压缩」 | **不**暴露摘要正文；**无**自动压缩 SSE；旗标随 list/get，**无**压缩专用 SSE |

**否决**：只改小 40 无早触发；**无差别**同步阻塞自动路径等压缩（近顶预压除外）；用 `history_len` 当可 fold 代理；消息扳机绑 `min_fold=4`；用户可关自动压缩；做成记忆同款大卡或可编辑摘要；**手动压缩入口**（桌面「压缩较早对话」+ `POST …/compact`）——面向大众 GUI 对齐自动-only，专家向 `/compact` 若将来需要另开需求，不占主输入区；「只改溢出失败文案、不做近顶自动压」（定案⑦ 否决 B）。

---

## 四、ReAct、收敛与执行解耦

### 不变量

轮次上限 `ModelProfile.max_rounds`；治理统一进 `LoopController`（滑窗循环 → NUDGE/FINALIZE、工具失败熔断、无产出早停）；交付前 `finish_guard`；结构化 `RoundOutcome` → `LoopDirective`，主循环穷尽 `match`。

### 收敛治理（决策摘要）

| 机制 | 决策要点 |
|------|----------|
| 重复/交替/同参失败 | 滑窗 3 轮 → NUDGE；再触发 → FINALIZE |
| LLM 重试 | 未提交前透明重试；已提交后不重试（否决「按已吐行数」判可重试） |
| LLM 空响应 · length | 协议字段 `finish_reason=length` 且空正文无工具 → **首轮即 DEGRADED**（不做 Continue；船长协调 listen 豁免也不覆盖）；普通 silent empty 仍按 streak 阶梯。只认协议字段，不扫推理长度启发式。→ `llm/errors.py` · `LoopController.empty_response_action` |
| 硬失败 / 已提交掉线 | ERROR 或 DEGRADED 收口，保留半成品；**否决** profile 级 Flash→Pro fallback |
| 工具熔断 | 按工具累计；`policy_failure` / `contract_failure` 不进累计 |
| Worker token 硬顶 | 统一单值 backstop（否决四档启发式）；到顶 `force_finalize`；瘦身主力是 `tool_clear` |
| 交文件空转·修码 | `files_expected` 且非报告岗：纯调查轮 nudge→工具收窄（卸检索；不 FINALIZE）；与 wind_down 解耦，可复用其白名单 |
| 交文件空转·报告 | 结构化报告岗（`code_audit_gate` / `citation_mode=two_phase` / research·reviews·debate 约定文档 path 声明）：仅 soft nudge 催写报告，**永不**收窄检索；`narrow_rounds=0` |
| 调查空转 | 非交文件 worker：纯调查轮达阈 **仅 soft nudge**（催结论 / handoff / escalate，不催写盘、不收窄）；默认 8 轮 |
| Turn token 硬顶 | 触顶禁新派、在飞跑完不 cancel（策略 A）；交付预留软闸；否决杀在飞 / USD 顶 / 按 playbook 分档 |
| 协作取消 ✅ | **断连 ≠ 取消**。显式 Stop = **取消**（`user_stop` 全清：停协调、关回合）。**否决** Stop→pause 可续（价值薄、同 turn 暂停状态机过重、行业无此惯例）。Interaction 挂起（ask_user 等）仍用 `FinishReason.PAUSED` + 冷 Resume，与 Stop 正交。否决：仅在 cancel 分支塞 harvest 特例 |

**上游 LLM 限流（429）失败可见**（定案）：工程侧短退避（最多 6 次，`Retry-After`>30s 不睡满），**不做** pause 代等。产品态 `LLM_RATE_LIMIT` 中文终态 + 可重试；CEO 回合 `failed` 时若仍 `execution_detached`，失败与「团队后台运行中」**并陈**。与平台 API `RATE_LIMITED` 正交。→ 见代码: `core/errors.LLMRateLimitError` · 桌面 `StatusStrip` / `errors.ts`

**到顶收口**：`is_thrashing()` → DEGRADED + 上浮信号（不自动重分解）；正轨 → `force_finalize`。**顺序分裂已退役**——真并行只在规划层 `delegate`。触顶禁换马甲（须 `continue_from_run_id` 或 `force`）。零写 / prose_idle 中途 warn→FINALIZE+DEGRADED 已退役（仅硬顶 `ceiling_backstop`）。

### finish_guard

模型自报 done 是唯一终止信号；放行前过纯代码轻层守卫（造引用 / 台账 id / 结构完整性 / 交付验收对照），未过回炉。`LoopController` 强制收口**不过此守卫**。回炉发 `content_reset` / `run_output_reset`（transport-only）；前端 chip 进行中为「正在按规则修订…」、完成后为「引用/格式核验后已重写」。回炉清空后、重写尚未流出时若用户 Stop：按行业习惯回灌重置前已流正文再 `message_end(cancelled)`（空壳否决）。→ 见代码: `runtime/verify.py` · `EventSink.interrupt_salvage_content` · `close_user_stop_turn`

**可用性诚实性（定案：甲 + 丙；乙并入姿势 A；档位真源 ✅；全面禁吹二期不做）**：

- **真相（丙）**：验证形工具失败（`browser_navigate` / 有界验证 `test_run` / verify 形 `code_execute`·`terminal` 等）必须压低 `delivery_status` verdict，不得仍为 `delivered`；缺口 `reason=verify_failed`。`test_run` 超预算为验证未完成（`contract_failure`），不进工具熔断。
- **路径验收**：`delivery_status.artifacts` = path + `accepted`|`rejected`（主清单数据源）；`delivered_files` / CEO「已交付」**仅** accepted；draft 不进 artifacts。→ 见代码: `runtime/runs/file_acceptance.py`
- **呈现（甲）**：用户面**否决**「交付验收」大卡与卡上动作。主呈现 = 产物清单（`artifacts`）+ 船长散文；`delivered` / `notes` **静默**（不弹验收）；仅 `partial` / `blocked` 一句轻提示（无缺口明细、无按钮）。可用性短问仍可复用最近一次对账事件（供清单与 finish_guard），不另造第二套。
- **收口诚实性 · 真源 = 对账档位（阶段 2）✅**：`delivery_verdict.state`——`delivered`=正式完成；`partial`/`notes`≈草稿·部分；`blocked`=阻塞。档位 ≠ 正式完成时，正文不得姿势 A（宣称完整交付）。文献证据降档、已声明复核落盘未对齐（`thin_review`）、`verify_failed`、或契约硬失败/节点 FAILED/拒收产物（`node_failed` / `artifact_rejected`）时 `requires_draft_ack`：正文须出现草稿/缺口承认（正向要求，不靠加「综述已完成」「核实落盘」词）。单一闭集探测「是否在说 A」；**禁止**靠案面加完成话术词修案。条数/模块名单与 synthesis「已验收」/ `accepted` artifacts 同源。无对账卡时仅拦同条 A∪C。→ 见代码: `runtime/closing_posture/`（P3-A 子包；旧原子路径经根 shim）
  - **✅ 姿势 A 收窄（已落地）**：保留「对账档位为真源 + 硬回炉」骨架；**已撤回** 20260803 并入姿势 A 的扩面词族（复核/审查/验收通过、已修复、可玩等）；乙（修好/验绿）仍在。书目形态闸**独立保留**（与 closing 正交，见 [工具与能力系统 · 书目形态闸](/docs/03-AI核心/工具与能力系统.md)）。漏拦优先回**档位呈现 / 产物结构**，**禁止再扩大**完成话术正则冒充近零误报（与 `intercept-discipline` 默认否决对齐）。
  - **✅ 零写落盘声称扫词硬回炉已撤（2026-08-09 定案 B）**：不再因正文「已改好 / 已落盘」等闭集 + 无写盘证据而 `finish_guard` 清气泡；检测器可留测试/观测；不恢复【落盘说明】横幅、不加软提醒。解释诚实规则时的禁语举例不得再误伤。
- **产物结构窄闸（非词表主防线）**：`blocked`+空盘不得「已生成/请下载」；有落盘但无 `.pptx` 不得宣称 PPT 可打开。
- **✅ `code_audit` 缺 JSON 归因（写盘不可用）**：`landing_failure_kind=channel_dead|write_failed` 时，缺/读不到配套 `*.audit.json` **不**走结构硬闸；归因进零写 / 队员 soft tip。已读到的 JSON 仍做字段语义校验。→ 见代码: `runtime/runs/contract.py` · `code_audit_gate.py`
- **✅ 系统收口 harvest × 通道死**：团队收口遇本地工作区通道 sticky-dead（已知标志，或收口回合 prepare/salvage 失败）→ 走现有无 LLM `persist_harvest_fallback`（含「本地文件暂时连不上」），**禁止**只落空 `STREAM_ERROR` 助手壳。普通用户回合 prepare 硬闸不变。→ 见代码: `conversation/execution_harvest.py`
- **不做（全面禁吹二期）**：终稿堆正则全面禁「可用」类话术；**不做**「综述已完成 / 站点做好了」等加词修案。裸「已完成 / 已交付 / 弱可用」故意不进姿势 A（修码/建站正常收口不得误伤）。完成态条**不**再标「交付未过关」（失败细节交轻提示/正文）。

→ 见代码: `delegate/delivery_status.py` · `runtime/verify.py` · `runtime/closing_posture/` · 桌面 `DeliveryStatusMount` / `FileArtifactsCard` · 手机 `TeamView` DeliverySection

### 执行与请求解耦

回合执行从 SSE 生命周期解耦：`send` / `regenerate` / `resume` 把 pipeline 作 **detached task** 交 `TurnRunRegistry`；SSE 仅 **attach**；`turn_leases` 持久 RUNNING，崩溃后 sweeper 续跑。

**契约**：

- 客户端断连 ≠ 取消；显式 Stop = **取消**（细则与否决 → 上表「协作取消」）。graceful 关停 = 抢救收口；仅真硬杀标 orphaned。
- 持久化独立于连接；重连 = journal 回放 + live tail。
- 进程内 registry 非真相；真相在 `turn_leases`。

**Resume 身份不变量**：不新开 turn；`message_id` / `execution_id` / journal 游标不变。**否决** resume 伪装新回合、client UUID 当执行槽键。挂起即收口；窄兜底（帧失败退限时挂起）已删。

`regenerate` = 推翻重来。**否决** `retry-failed`（只补失败节点）：产品入口与 API 已退场；部分失败走对 CEO 续聊 / 同人续派，不另开机械补跑旁路。

---

## 五、工具装配原则

- **声明式单源**：工具类自带 `registration`；三 registry / 能力图鉴从名册收集，禁平行清单。
- **全部 opt-in**：无「对所有 Agent 默认发放」的 core 档（MVP 装配现状以代码为准）。
- **审批与来源无关**：只认 `ToolSchema.approval`；`GRANTABLE` 仅 worker、本地经 `ApprovalGate`。
- **时间即上下文**：当前时间进 system prompt `<runtime_context>`，不走工具。

---

## 六、任务分级与模型选择

无 `classify_task`、无旧式 worker 模型档位（`ModelTier` / `model_preference`）。CEO 回合仍解析会话 `turn_model`（主槽）；worker **默认**跟组合 Worker 槽；**节点可显式覆盖**（CEO/`tasks[]`；非法硬失败；确认面不提供人改模）→ [编排器](/docs/03-AI核心/编排器与CEO主Agent.md#per-worker-模型覆盖abc-同一功能)；解析/凭据 → [平台 LLM](/docs/05-平台与运维/平台LLM接入.md)。力度差异仍优先走协作结构。**否决**请求级 `"auto"` 分级树、账号角色→模型矩阵、质量档自动降级。

## 七、提示词架构

World A（Agent 主提示词，`PromptAssembler`）与 World B（内部窄任务）互不相交——关注点不同，禁强行共用。装配顺序权威 → 见代码: `runtime/context/`（`SectionOrder`）；新段只能尾插或按 key 稳定插入（稳定前缀为成本优化，非产品不变量；`cost.prompt_assembled.assembly_hash` 观测漂移）。记忆注入决策 → [记忆系统 §二](/docs/03-AI核心/Agent记忆与知识系统.md)。World B 统一 registry **暂不收敛**（同形样板拖累或第 4 处窄任务再抽薄助手）。

## 八、运行时核心模型

> 全文 → [运行时三模型与挂起](/docs/03-AI核心/运行时三模型与挂起.md)。

## 待定

超时具体值；并行度是否按负载动态调；联网工具国内化选型；云端代码执行默认关闭（见 [安全权限与治理 §三](/docs/05-平台与运维/安全权限与治理.md)）。跨进程 Redis 租约 / 治理强制层：详细提案不在公开仓 / 维护者本地。
