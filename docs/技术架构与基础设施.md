# 技术架构与基础设施

> **状态**：已确定（运行时模型、后端技术栈、数据存储、LLM 接入、部署方案均已确定）

---

## 一、Agent 运行时模型 ✅ 已确定

分层设计：Agent 逻辑和工具执行采用不同的运行时模型。

| 层 | 运行时 | 说明 |
|---|---|---|
| Agent 逻辑 | asyncio 协程 | 纯 I/O 密集型，用协程最轻量 |
| 普通工具 | 同进程异步 | 文件读写、搜索等安全操作 |
| 代码执行 | 受限 subprocess（MVP，`SandboxProvider` 抽象预留） | 当前仅 timeout + tempdir 隔离；Docker/gVisor 升级后置 |

### 沙箱架构原则

- 设计抽象沙箱接口，后端实现可插拔
- 默认禁止出站流量、凭证不进沙箱、文件系统边界严格、全量审计日志

---

## 二、LLM 接入策略 ✅ 已确定

MVP 只接入 DeepSeek V4，用 Flash + Pro 双模型覆盖所有场景：编排器 / 单聊 / worker 按角色分思考档位，标题与记忆维护走非思考。开发期统一 `high` / `max` 两档有效思考强度——worker 的 `fast` / `strong` 同为 `high`，靠回合预算区分，`max` 由 per-agent 覆盖按需解锁（只升不降）。

> **档位与思考强度以 `llm.mdc` §思考强度按角色分档 为权威**；轮数预算与模型映射见 [执行引擎架构设计.md §十六](执行引擎架构设计.md)，运行参数单一真相源是 `apps/server/agentcore/llm/config.py` 的 `PROFILES`。

---

## 三、数据存储 ✅ 已确定

最小组件策略：用尽量少的组件覆盖所有存储需求。

| 组件 | 覆盖的数据 | 说明 |
|------|----------|------|
| PostgreSQL + pgvector | 结构化数据 + 向量检索 | 用户/账户、Agent 配置、会话历史、Agent 记忆 |
| Redis | 实时状态、缓存 | Agent 运行状态、任务进度、热数据缓存 |
| S3 兼容存储 | 大文件 | 代码执行结果、上传文件、工具输出 |

> **实现现状**：开发 compose（`deploy/docker-compose.dev.yml`）起 PostgreSQL 16 + Redis；**pgvector 扩展、S3/MinIO 对象存储、向量检索均未接入**，server 代码当前不读写 Redis / S3（`config.redis_url` 已配置未用）。

---

## 四、技术栈 ✅ 已确定

### 后端

| 决策点 | 选择 |
|--------|------|
| 语言 | Python 3.12+ |
| Web 框架 | FastAPI |
| 异步运行时 | asyncio (uvicorn) |
| 数据模型 | Pydantic v2 |
| 前后端类型同步 | OpenAPI schema → openapi-typescript |

### 前端

| 决策点 | 选择 |
|--------|------|
| 桌面框架 | Electron |
| UI 框架 | React 19 |
| 构建工具 | electron-vite |
| 状态管理 | Zustand 5 |
| 样式 | Tailwind CSS 4 + shadcn/ui |

> 详见 [前端技术选型.md](前端技术选型.md)

---

## 五、部署方案 ✅ 已确定

已有阿里云服务器（`ecs.e-c1m4.large`，深圳），复用现有基础设施部署新版。详见 [`部署与运维.md`](部署与运维.md)。

| 项目 | 选择 |
|------|------|
| 云服务商 | 阿里云 |
| 区域 | cn-shenzhen（深圳） |
| 服务器 | 复用现有 `ecs.e-c1m4.large`（2C8G 共享型） |
| 容器编排 | Docker Compose（单机） |
| 反向代理 | Nginx + Let's Encrypt |
| 域名 | `app.fashitianxia.xyz`（API）、Cloudflare Tunnel 辅助 |
| 镜像仓库 | 阿里云 ACR（华南1） |
| CI/CD | GitHub Actions → ACR → SSH deploy |

---

## 六、成本与配额模型 ✅ 已确定

> **实现现状**：本节为成本 / 配额 / 计费的**目标设计**，MVP 代码**尚未实现**——无 Redis 滑窗限流、无回合配额检查、无 `cost_events` 台账（`config.redis_url` 已配置但运行时未接入）。下文为方向，落地见后续里程碑。

### 6.1 三层防线

三层正交：限流挡「刷得太快」，配额挡「用得太多」，计费只忠实记录、不拦截。

| 层 | 位置 | 管什么 | 失败行为 |
|---|---|---|---|
| **限流** | 中间件（Redis 滑窗） | **速率**——单位时间内的请求次数 | fail-open（Redis 故障放行） |
| **配额** | 业务层（回合开始前） | **总量**——日 token / 月成本 / 日请求数 | 超额拒绝下一回合 |
| **计费** | Finalize（append-only） | **记账**——每次 LLM 调用的真实成本 | 失败只 warning，不中断 |

#### 限流（速率防线）

两个互补的滑窗限流器（Redis ZSET 滑窗）：

- **按用户限流**：限制对话消息发送端点的频率（N 次/60s），挡单账号刷回合。值 ≤0 关闭；未登录请求放行（让路由自身返回 401）
- **按 IP 限认证端点**：限登录/注册/刷新端点，挡撞库

二者均 **fail-open**：Redis 不可达时放行，绝不因基础设施抖动把用户挡在门外。

#### 配额（总量防线）

每回合 prepare 时检查，三维度独立判定：

| 维度 | 窗口 |
|---|---|
| 日 token | 当日 0 点起 |
| 月成本（CNY） | 当月 1 号起 |
| 日请求数 | 当日 0 点起 |

关键决策：

- **0 = 该维度不限制**（fail-safe 宽松默认），全 0 则直接放行
- **回合粒度**：在回合开始前检查，超额拒绝下一个回合（不腰斩进行中回合）
- **每用户覆盖**：支持按用户存覆盖值，管理员可标记 `is_unlimited` 跳过所有检查
- **轻量到可放热路径**：一次检查 = 1 次 quota 表读 + 1 次成本事件聚合，命中既有索引

#### 计费记账（append-only）

每次 LLM 调用在 Finalize 落一行成本事件：tokens（prompt / completion / cache_hit / cache_miss）、四类成本（input / cached / output / total CNY）、rounds、duration。

- **成本口径**：`成本 = tokens × 模型单价`，input / cached / output **三类单价分开**——缓存命中按更低的 cached 价计，如实反映前缀缓存省下的钱
- **角色归属**：通过 trace 标记区分顶层回合（captain）与委派子 Agent（member），支撑按角色看花销
- **多 Agent 分摊**：一次用户交互的成本 = 顶层 + 各委派子 Agent 之和，逐事件记录、可按 conversation / agent / 角色聚合

### 6.2 用量/成本类型不变量

token 与成本在系统中有多种形态（SSE wire、持久化、API 响应、计费台账），但两条不变量保证不会算出两套数：

1. **唯一原始来源**：单次 LLM 调用的原始 Usage（prompt / completion / total + cache_hit / cache_miss + reasoning 六字段），所有用量都从它累加
2. **唯一定价转换**：全系统唯一的 `calculate_cost(model, usage)` 函数把 token 折成钱（input/cached/output 三价分离），任何地方要成本一律调它，绝不另算单价

**真正的钱以 `cost_events` 台账为准**，message/runs 里的成本只是展示快照。新增任何成本展示点 → 复用统一定价函数，勿手算单价。

### 6.3 成本预估 ≠ 实际记账

团队组建时提供启发式预估，与真实记账是两套东西，刻意如此：

- 预估用途是**比较不同团队组合的贵贱**（「三个 premium 模型会很贵」），不是预测真实账单
- 算法基于固定假设（Captain 跑 3 轮、成员各 2 轮，每轮约 3000 输入 + 700 输出 token）
- 假设串始终展示给用户，确保该数字永远不被误当成事实

---

## 七、鉴权与会话 ✅ 已确定

> **实现现状**：本节为鉴权**目标设计**，MVP **未落地**——`auth.py` 路由未在 `main.py` 注册、无 JWT 中间件，对话接口固定 `_MVP_USER_ID` 单用户；`config.py` 有 `jwt_*` 配置但未使用。下文为上线前需实现的方向。

### 7.1 双令牌 httpOnly Cookie 模型

| 令牌 | 生命周期 | 用途 |
|---|---|---|
| access_token | 短期（默认 30min） | 所有 API 请求 |
| refresh_token | 长期（默认 7d；remember_me 30d） | 仅用于换新 access_token |

两者均为 httpOnly Cookie，前端拿不到令牌明文，靠 `credentials: 'include'` 自动携带。

### 7.2 静默刷新（Reactive）

访问令牌过期后由 401 触发刷新 → 换新 → 重试原请求，对用户无感；刷新令牌过期才真正跳登录。

**关键约束——流式通道必须独立接入刷新**：

聊天 SSE（发消息、恢复、异步委派流）通常绕过统一的 HTTP 请求封装直接 fetch。这些通道**必须各自包一层 401→刷新→重发**，否则「读了半小时再发消息」会因访问令牌过期而直接掉登录，而非无感续期。新增任何绕过统一请求层的 fetch/XHR 通道时，都需接入此机制。

重发安全保证：401 在认证依赖层抛出、路由 handler 尚未执行，请求无副作用，可安全重发。

### 7.3 刷新令牌轮换 + 并发宽限窗口

刷新令牌单次使用：每次 refresh 把旧令牌标记已轮换，并在同一 token family 内签发后继（仅 login/register 起新 family）。

**多标签页互踢解法——宽限窗口**（默认 10s）：

| 场景 | 行为 |
|---|---|
| 令牌首次交换 | 标记已轮换、同 family 签发后继 |
| 已轮换令牌在窗口内再次出现 | 判为善意并发（多标签/并发重试），重新签发、不杀会话 |
| 已轮换令牌在窗口外再次出现 | 判为泄露/重放 → 吊销整族，强制重登 |

### 7.4 Cookie Path 约束（生产专属坑）

当 web 与 api 同源部署（反向代理把 `/api/` 反代到后端）时，refresh Cookie 的 `path` 必须是 `/`，**不能**写后端真实路由。否则按 RFC 6265，浏览器实际请求路径不以 Cookie path 为前缀 → Cookie 永不回传 → 静默刷新 100% 失败。

此坑 dev 环境测不出（直连后端，路径恰好命中），只在生产触发。会话隔离靠 Cookie **名字**区分，不靠 path。

### 7.5 易踩坑速查

- 动 refresh Cookie 的 `path` → 只能用 `/`，否则生产必掉登录
- 新增绕过统一请求层的 fetch/XHR（尤其 SSE）→ 必须自行接 401→刷新→重发
- "总是掉登录"先查刷新链路（Cookie path / SSE 是否接入刷新 / 轮换是否互踢），调长访问令牌过期时间只是掩盖、非根治

---

## 八、后端架构模式 ✅ 已确定

> **与项目结构的关系**：本节描述的是完整目标架构的模块体系。MVP 阶段的实际目录结构见 [`项目结构.md`](项目结构.md)，仅包含标记为 ✅ MVP 的模块。

### 8.1 五层分层

每个业务模块遵循 `models → schemas → repository → service → router`，调用方向严格单向、不可反转：

| 层 | 职责 | 约束 |
|----|------|------|
| **models** | ORM 定义 | 无 ForeignKey（应用层维护引用；MVP 阶段可使用 FK 简化开发，上线前评估是否需要去除）；主键用 UUID |
| **schemas** | Pydantic request/response | 命名规范 `CreateXxxRequest`、`XxxDetail`、`XxxListItem` |
| **repository** | 纯数据访问 | 接收 AsyncSession；不含业务逻辑 |
| **service** | 业务逻辑 | 构造注入 AsyncSession，内部实例化 repository，组合多 repo |
| **router** | HTTP 端点 | 薄层委托 service，用 DI 别名，不直接操作 DB |

### 8.2 模块组织

后端按职责分三区：

**平台核心**（基础设施 + 平台级通用能力）：

| 模块 | 定位 | MVP 阶段 |
|------|------|----------|
| common | DB / Redis / 消息队列 / 对象存储 / 错误 / 日志 基础设施 | ✅ MVP（精简为 db + core） |
| middleware | JWT 认证 + 请求日志 + 限流 + 服务间内部认证 | ⏳ 占位（`auth.py`/`rate_limit.py` 仅 docstring，未挂载） |
| user | 注册/登录/JWT 刷新/用户偏好 | ⏳ 部分（`users`/`refresh_tokens` 表 + `UserRepository.get_by_id`；注册/登录未实现，`auth.py` 未注册） |
| agent | Agent & Team CRUD + 版本管理 + Fork + 语义发现路由 | ⏳ Post-MVP（MVP 用编排器动态分配） |
| conversation | 对话管理 + SSE 流式聊天 + Turn 聚合层 | ✅ MVP |
| runtime | ReAct 引擎 + Prompt 组装 + 工具注册 + Agent 委派 + 编排 + 审批 + 检查点 | ✅ MVP |
| llm | LLM Gateway — 多 Provider 统一调用 | ✅ MVP |
| capability | Tool / Skill / Rule 三类能力实体的定义/绑定/版本（能力地基层） | ⏳ Post-MVP（MVP 内置工具直接注册） |
| file | 文件上传/下载 + 在线文档 + 工作区上下文 + RAG 检索 | ⏳ Day 2 |
| billing | LLM 调用的 token 用量 + 成本记录 | ⏳ Post-MVP |
| task | 异步任务引擎 — 消息队列 + Worker 后台执行 | ⏳ Post-MVP |

**平台业务**（面向用户的业务模块）：

| 模块 | 定位 | MVP 阶段 |
|------|------|----------|
| webhook | Agent 触发器 + headless Run 执行（事件驱动） | ⏳ Post-MVP |
| scheduler | 提醒与日程 | ⏳ Post-MVP |
| attention | 跨会话「待办流」：聚合可恢复的暂停 Run + 待审批工具调用 | ⏳ Post-MVP |
| share | 统一分享（文件/对话，share_token + 过期） | ⏳ Post-MVP |
| table | 多维表格 — 结构化数据记录 CRUD | ⏳ Post-MVP |
| admin | 管理后台：管理员鉴权 + 操作审计 + 用户运营 | ⏳ Post-MVP |
| desktop | 桌面自动更新策略（版本政策 + 远程熔断 + 下载入口） | ⏳ Post-MVP |
| flags | 特性开关 / 远程配置（灰度百分比 + 最低客户端版本门控） | ⏳ Post-MVP |

### 8.3 runtime 子包结构

> **实现现状**：`runtime/` 当前为**扁平模块**（非子包）：`pipeline` / `planner` / `plan` / `engine` / `runs` / `workspace` / `interactions` / `events` / `prompt` 已实现；`suspend` / `journal` / `loop_controller` / `resolve` 为空壳；无 `host` / `delegation` / `memory` 模块。下表为目标子包划分。

`runtime/` 是执行核心，按运行时关注点分子包：

| 子包 | 关注点 |
|------|--------|
| engine | ReAct 主循环（LLM 调用 / 工具执行 / 轮次控制 / 上下文装配） |
| runs | **Run 模型**：统一执行单元——RunPlan + WaveScheduler + 执行器 |
| journal | **Turn Journal**：每 turn append-only 事实流 |
| interactions | **统一交互原语**：server→client 等待的单一类型 |
| delegation | Agent 委派执行：派发 + 契约 + 熔断 + 并发预算 + 预审 |
| prompt | system prompt 组装（skills / rules / workspace 注入） |
| events | SSE 事件类型 + 事件总线 |
| host | 运行时宿主端口（各接缝 Protocol） |
| resolve | 回合准备：agent→prompt→tools 解析 + 任务复杂度分级 |
| memory | 工作记忆：工件存储 + TWM 注入 |
| suspend | 持久暂停/恢复 + checkpoint |
| sandbox | 代码执行沙箱后端 |

### 8.4 垂直插件机制

平台核心行业无关，垂直领域作为可插拔插件接入。核心可在不加载任何垂直时独立启动、测试、发布。

#### 契约（6 个贡献点）

| 贡献点 | 用途 |
|--------|------|
| `builtin_modules()` | 无状态内置工具 |
| `tool_providers()` | 有上下文的工具提供器 |
| `routers()` | FastAPI 路由 |
| `task_handlers()` | 异步任务类型处理器 |
| `on_startup()` | 垂直专属设施启动 |
| `on_shutdown()` | 垂直专属设施关闭 |

#### 发现机制

约定式扫描垂直子包目录，环境变量白名单控制启用哪些垂直（空 = 纯净核心）。

#### 反向依赖消除

垂直可依赖核心，核心不得依赖任何垂直。验证指标：白名单为空时平台核心独立运行。

---

## 九、评估体系 ✅ 已确定

> **实现现状**：本节为评估体系**目标设计**，MVP **未落地**——CI（`.github/workflows/ci.yml`）仅跑后端 ruff + pytest（`runtime/` 暂无单测）+ 前端 lint/test；无种子 lint、无执行轨迹断言、无 Layer 2 LLM 评判。

### 9.1 两层模型

| 层 | 性质 | 依赖 | 门禁 | 触发 |
|---|---|---|---|---|
| **Layer 1** 离线确定性 | 静态/机制检查 | 零 LLM、零 DB、毫秒级 | **PR CI 硬门禁** | 每次 PR |
| **Layer 2** 在线 LLM-in-the-loop | 真实 Agent 执行 + 评判 | 真 LLM + 已 seed 的 DB | **软门禁**（仅告警不阻断） | 定时/手动 |

**为何这样分**：质量评测天然要调真 LLM（语义判断无法 mock），但真 LLM 慢、要钱、非确定——进 PR 硬门禁会 flaky 且昂贵。把「给定事件流即可裁定」的确定性部分下沉 Layer 1 硬门禁，把「必须真 LLM」的留 Layer 2 软门禁。

### 9.2 Layer 1 维度

| 维度 | 说明 |
|---|---|
| **种子完整性 lint** | 种子 JSON 的静默失败检测（委派目标不存在、团队成员缺失、非法分类等） |
| **执行轨迹断言** | 从运行时事件流还原结构化轨迹，按声明式期望裁定过程正确性 |

### 9.3 Layer 2 维度

| 维度 | 评什么 | 方法 |
|---|---|---|
| **路由准确率** | Pilot 的路由决策质量 | 真实 LLM 单轮，截获 `delegate` 目标与 golden 比对。固定 temperature=0 |
| **输出质量** | 最终回答是否答到点 | 端到端真实 Agent 执行 + LLM 裁判按 rubric 打分（1-5） |

**判定口径**：规则断言全过 **且** 裁判分 ≥ 3 才算通过。
